# Superseded Feedback: Claude Code Real Subagents

> Status: superseded by `docs/feedback/final_real-subagent.md`
> Date: 2026-06-16

This earlier feedback file is intentionally reduced to an index note so it does not compete with the final architecture.

Use the final decision record and the active OpenSpec change as the only guidance:

- `docs/feedback/final_real-subagent.md`
- `openspec/changes/prototype-subagent/`

The accepted path is native Claude Code / Codex LLM subagents, stable project-level role agents, dynamic `task.md` and `result.schema.json`, Parent Relay, validated `result.json`, and Engine-owned dispatch/collect/merge/gate semantics.
