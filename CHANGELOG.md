# Changelog

## 0.2.1

- Wave0 source intake now rejects concurrent same-target writers before queue or work-unit mutation while preserving distinct-target batching and serial supplements.

## 0.2.0

- 版本体系重置：采用 `MAJOR.MINOR.BUILD` 双小数点规则，自 `0.2.x` 起推进；历史细碎 `v0.x` 条目废弃（可在 git history 查阅）。
