# Agentic Queue Nodes

This prototype is API-first. The command experiments copy this directory into disposable bundles to preserve the standard experiment shape, but queue manager behavior is exercised through `agentic-queue.mjs` and `agentic-queue-cli.mjs`.
