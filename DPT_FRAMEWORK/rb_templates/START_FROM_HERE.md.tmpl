# Start from here — {{name}}

本目录是一个 Deep Research Runtime Bundle。
框架位于 `../DPT_FRAMEWORK/`（共享 reusable framework assets，运行时只读）。

## Active Runtime Coordinates

- repo_command_root: 包含 `DPT_FRAMEWORK/` 和 `dpt_rb_{{name}}/` 的 repo root；通常也是 shell cwd。
- bundle_root: 本文件所在目录；从 repo root 调用时是 `dpt_rb_{{name}}`，从 bundle 内看是 `.`。
- bundle_name: `{{name}}`
- framework_root: `../DPT_FRAMEWORK/`；从 repo root 执行命令时使用 `DPT_FRAMEWORK/...`。
- 所有 `<bundle>`、bundle-relative path（如 `artifacts/...`、`_cache/...`、`_work_units/...`）都以 `bundle_root` 为根。
- 不要把 repo cwd 当成 bundle root；每次执行 gate、queue、work-unit、inspect 或 log 相关命令，都显式传入 active bundle path。

## 控制文件
- rb_plan.md       — 话题设计 (Markdown + JSON frontmatter)
- rb_profile.yaml  — 用户意图 (YAML)
- rb_status.json   — 运行时状态 (JSON)
- rb_queue.json    — 执行队列 (JSON)
- rb_trace.jsonl   — runtime audit trace (JSON Lines, append-only)
- rb_output_declarations.jsonl — submitted work-unit output declaration ledger（由 `operate-work-unit submit` 写入，gate 读取 delegated Agent 产物的唯一发现面）

## 审计与诊断
- rb_trace.jsonl   — runtime audit trace，pass/fail 和 gate/check evidence 从这里审计
- _logs/run.log    — diagnostic log（如果存在，用于排障；不是 verdict authority）

## 数据目录

- **seed_topics/** — 种子话题文件（01_<slug>.md ... 0N_<slug>.md）
- **reference/** — 扁平 evidence 目录（无子目录）
  - `00-shared-<slug>.md` — Wave 0 共享基础 reference（rich MD，一个 source 一个文件）
  - `{slug}-<qualifier>.md` — Wave 1 topic 专属 reference（slug 已含 `NN_` 编号前缀，qualifier 为 source 短标识）
  - `00-cross-<slug>.md` — Wave 2 跨 topic 发现 reference（可选）
  - `_INDEX.md` — Canonical reference inventory table（8 列：ref_file, source_type, trust_level, tier, related_topic, source_layer, acceptance_status, date_landed）
  - `README.md` — 人类导航
  - Engine 不通过扫描本目录发现 Agent 产物；gate 只读取 `rb_output_declarations.jsonl` 中已完成声明的 reference 条目
- **artifacts/** — 阶段产物，按 wave 组织
  - `wave0/<topic>/source.yaml` — thin YAML source 列表
  - `wave1/<topic>/` — evidence-summary.md + question-list.md
  - `wave2/` — synthesis.md + cross-topic-ledger.md + finding-index.yaml
- **_cache/** — 可重建缓存，不是 authority
- **final/** — 最终报告

## 规则
- 当前 run 的 truth 只来自本 bundle 的控制文件、declaration ledger、数据目录和 trace。
- 续跑口诀: 先读 `START_FROM_HERE.md`、`rb_status.json`、`rb_queue.json`、`rb_trace.jsonl`；用 repo root 执行框架命令，用 bundle path 定位 runtime truth；`_logs/run.log` 只用于排障，不能靠聊天记忆或 log 推断当前状态。
- Delegated Agent 产物进入 gate 前必须经过 work-unit result + runtime receipt → `operate-work-unit submit` → `rb_output_declarations.jsonl`；未提交声明文件不能帮助 gate pass。
- 不要把 HITL answer、gate result、repair state、trace、artifact 或 final output 写回 `../DPT_FRAMEWORK/`。
- 停止授权: 仅 final_delivery / decision_blocker / empty_queue_after_refill
- Gate 过渡前 reload 所有控制文件
