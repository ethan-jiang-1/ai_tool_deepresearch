---
{
  "requires": [],
  "req": "WDM-001"
}
---

# No-op Step

This file has no JS code block — it loads normally. The Engine emits `file_loaded` and writes executionOrder/counters.

## Role in Experiment
验证无代码块的 MD 文件正常加载，不抛错。这是 Agent-readable MD 的预期常态。
