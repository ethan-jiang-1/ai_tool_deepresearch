## 1. Plan Review and Preconditions

- [ ] 1.1 `openspec-feedback:plan-review` Review proposal, delta specs, design, tasks, and verification plan for FDB-001/FDB-002/ARP-004/CDP-006 authority, reader/writer boundaries, Final terminal compatibility, and proof coherence before the first target edit; record each actionable finding below as an ordinary unchecked task with an owner, smallest repair, and observable done condition.
- [ ] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change traceable-final-delivery-backing --mode plan`; done only when the selected classes and planned test assets pass route validation before any target edit.
- [ ] 1.3 Recheck the current submitted-declaration reader and `classifyReferenceAuthority()` contract before target edits, including whether C2 or another active provenance change altered a required interface; update this change rather than duplicate an unsettled rule. Done when the chosen direct-output roles and reference classifications still match FDB-002.

## 2. Bounded Final-backing Evaluator

- [ ] 2.1 Implement FDB-001 and FDB-002 as one Engine-owned, pure Final Markdown Evidence Map parser/evaluator at the existing helper boundary. It SHALL reuse `markdownSemanticSectionEntries()` for tolerant heading parsing plus duplicate-section detection, parse only the designated table, resolve links from the intended Final target, reuse normalized submitted declarations plus `classifyReferenceAuthority()`, and emit structured check/inspect/advice without new state, ledger, Gate, trace, or semantic-quality verdict. Done when zero/malformed/ambiguous map facts and unsafe/missing/unsubmitted links fail before the persistence caller can create a workspace.
- [ ] 2.2 Add focused FDB-001/FDB-002 unit coverage in `tests/engine/helpers/final-delivery-backing.test.mjs`: absent/empty/malformed map, repeated-finding consistency, target-relative resolution plus absolute/traversal/symlink rejection, direct submitted `source_yaml`/`evidence_summary`, submitted-backed `reference/`, unsubmitted/failed/cache/final/index rejection, and a structurally valid but semantically unjudged declaration. Done when the test uses real Engine readers/classification rather than reimplementing provenance in fixtures.

## 3. Final Persistence Admission

- [ ] 3.1 Implement ARP-004 in `operate-artifact-persistence.mjs` and its existing persistence boundary: add `persist-final-report`, restrict it to safe Markdown targets under `final/` (including ASCII case variants of `.md`), preserve existing invalid-target/configuration precedence before Final-target routing, validate retained staging before workspace creation, delegate a pass once to the existing CAS/atomic persistence path, and reject generic `persist` for safe Final Markdown reports with one direct rerun operation named through its existing result schema. Reuse that same evaluator in `sweep` before it finalizes a prepared Final Markdown payload, so an old/crash-recovery workspace cannot bypass admission. Add a separate Zod operation-result schema whose top-level `check`/`inspect`/`advice` are Final-backing admission feedback while ordinary persistence fields retain the durability verdict; do not widen existing `persist`/`sweep` result schemas. Done when invalid backing creates no workspace or target write, unsafe Final-looking targets retain exit-2 configuration behavior, invalid prepared Final payload blocks recovery without target mutation, and normal non-Final persistence/sweep behavior remains intact.
- [ ] 3.2 Extend `tests/integration/cli/artifact-persistence.test.mjs` for ARP-004: accepted Final report commits exact bytes through the existing durability path; zero/malformed/unsafe/missing/unsubmitted backing returns a structured exit-1 admission rejection with no workspace/write; generic safe Final Markdown `persist` cannot bypass and its existing-schema reason names `persist-final-report`; unsafe Final-looking targets and invalid invocation remain exit 2; and a controlled real prepared-workspace crash followed by `sweep` reruns the same backing evaluator before finalization. Migrate the pre-existing generic Final CAS fixture to a non-Final target so it continues to prove ordinary persistence rather than asserting the deprecated bypass. Done when tests execute production CLI/helper paths over isolated temporary bundles and create submitted evidence through real work-unit submit paths rather than hand-written ledger/workspace authority.

## 4. Agent-facing Delivery and Vocabulary

- [ ] 4.1 Implement CDP-006 in `phase-final.md`, `command_playbook/persist-artifact.md`, and `COMMANDS.md`: teach the Agent to write an Evidence Map in retained Final Markdown staging, invoke `persist-final-report`, repair the named direct fact, and rerun the same operation. Preserve `gate: null`, no Final trace/event/loop/prompt, and retain generic persistence guidance for non-Final targets. Done when `tests/integration/md/artifact-persistence-contract.test.mjs` statically proves the one legal Final Markdown command and preserved terminal boundary.
- [ ] 4.2 Add the canonical glossary entries from the design to `CONTEXT.md`: Final key-finding declaration, Final Evidence Map, and Final backing. Done when they distinguish reader declaration, submitted authority, and projection without implementation details or overloaded `submitted_work` terminology.
- [ ] 4.3 Release the implemented framework as v0.70 by updating `CHANGELOG.md` and the `DPT_FRAMEWORK/RUN.md` banner/current-release summary. Done when both describe the same bounded Final-backing admission behavior and do not claim semantic validation or a Final Gate.
- [ ] 4.4 After implementation evidence exists, update BUG-199 and the C3 rows/checklist in `_backlog/plans/bug-187-199-systemic-remediation-plan.md` with the actual change name, selected evidence, and residual Agent/human semantic-review boundary. Done when neither backlog surface claims a delivered fix before its focused verification passes.

## 5. Verification, Closeout, and Archive Preparation

- [ ] 5.1 Run the selected FDB-001/FDB-002 unit and ARP-004/CDP-006 integration tests, then `node openspec/governance/check-verification-routing.mjs --change traceable-final-delivery-backing --mode assets`; record exact commands/results in the completed tasks. Done when all selected deterministic proof assets pass and no real-Agent behavior is claimed.
- [ ] 5.2 Run `openspec validate traceable-final-delivery-backing --strict`, `node openspec/governance/check-project-reqs.mjs`, `node openspec/governance/check-project-specs.mjs`, and `git diff --check`; resolve any change-scoped finding. Done only when the checks pass with no unregistered requirement, malformed main spec, or whitespace error.
- [ ] 5.3 Sync the approved delta specs into their accepted main-spec locations using the supported OpenSpec spec-sync operation, then rerun requirement/spec governance checks. Done when the accepted specs contain FDB-001/FDB-002/ARP-004/CDP-006 exactly once and the active delta/main comparison is coherent.
- [ ] 5.4 `openspec-feedback:closeout-review` Review the scoped implemented diff, synced specs, verification evidence, release/glossary/backlog updates, and every ordinary task. Complete only with no open C3 finding; convert any actionable closeout finding into an unchecked task and repair it before archive.

### 5.5 Governed archive transition

After every checkbox above is complete, invoke only `node openspec/governance/finalize-change-archive.mjs --change traceable-final-delivery-backing` and use its structured result as archive evidence. This is intentionally an operation instruction, not a checkbox: the governed finalizer requires every ordinary task to be complete before it can perform the archive, so a checkbox whose done condition is the archive result would be self-blocking.
