## 0. Apply Entry Review

- [ ] 0.1 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 `openspec-feedback:plan-review` - Before target edits, read the current change-feedback operation guidance and review the approved proposal/design/deltas against the actual ProfileSchema, HITL1 Gate, adapter resolver, shared schema readers, fixtures, and C4a card. Record every actionable scope or reader finding as an ordinary unchecked task with owner, smallest repair, and observable done condition. Done when exactly one plan-review marker is complete and no unrecorded actionable finding remains.

## 1. Current Contract And Resolver Removal

- [ ] 1.1 @impl SCO-002, SCO-008 - Remove legacy research-access envelope schemas, enums, refinements, and union acceptance from `schema/contracts/profile.mjs`; retain optional absence, strict `unprobed`, and complete current direct-sample observations. Done when every URL/fetch/search/candidate/source-class/access-boundary input fails ProfileSchema without defaulting, conversion, or a new discriminator, while each current legal state remains valid.
- [ ] 1.2 @impl PRG-002, PRG-010 - Remove HITL1's legacy boundary import, resolver branch, field reads, and legacy wording; retain the existing `profile_schema_valid` / `missing_contract` root for schema-invalid profile data and the existing absent/unprobed direct-probe root. Done when one retired envelope gives only the schema root, masks dependent rules, and does not produce adapter, provider, external-action, or inferred repair feedback.
- [ ] 1.3 @impl REA-001, REA-002, REA-003 - Delete the adapter boundary-location owner map/export and its legacy-only comments without changing selected adapter contract parsing, launcher invocation, or caller permission-bypass protection. Done when no current Harness consumer imports a historical boundary resolver and current canary tests still prove the retained adapter contract.
- [ ] 1.4 @impl SCO-002, PRG-002, PRG-010, REA-001 - Rewrite `gate-hitl1-recorded.definition.json`, `phase-hitl1.md`, and `shared-profile.md` to describe only current direct observations, generic schema rejection, and the existing HITL1 loop. Done when no target guidance or definition presents legacy URL/fetch/access-boundary fields, owner hints, migration, or compatibility as an operational path.

## 2. Fixtures And Behavioral Coverage

- [ ] 2.1 @impl SCO-002, SCO-008 - Replace schema tests that assert legacy envelope acceptance with one compact explicit rejection table; retain absent, `unprobed`, available, unavailable, complete-sample, no-request, and current forbidden-field coverage. Done when `tests/schema/contracts/profile.test.mjs` proves all retired field families fail and all protected current states pass.
- [ ] 2.2 @impl PRG-002, PRG-010, REA-001, REA-002, REA-003 - Replace HITL1/adapter positive legacy fixture cases with one schema-root rejection case and current direct-sample success/no-provider-diagnosis cases. Done when Gate output has exactly one `profile_schema_valid` `missing_contract` root for a legacy envelope, no `research_access_available` or adapter owner hint, and no adapter test imports/uses the removed resolver.
- [ ] 2.3 @impl SCO-002, PRG-003 - Convert all selected Setup, rerun-ready, lifecycle, style, and deterministic E2E fixture profiles from retired envelope shape to valid current direct-sample shape; add scoped shared-reader rejection assertions where their public contract already exposes profile-schema failure. Done when no current-success fixture relies on retired fields and shared readers fail closed rather than accepting or translating them.
- [ ] 2.4 @impl RES-001, RES-002 - Keep `apply-research-style` preservation coverage only for schema-valid current available/unavailable observations; ensure an invalid legacy envelope does not become a successful style-write preservation case. Done when style recomputation retains valid direct observations unchanged and test scope contains no legacy positive preservation assertion.
- [ ] 2.5 @impl SCO-002, PRG-002, PRG-010, REA-001 - Run and repair the focused schema/HITL1/adapter/style/shared-reader suites. Done when the selected tests pass with no skipped/cancelled test treated as proof and no positive legacy envelope fixture remains.

## 3. Specification And Governance Synchronization

- [ ] 3.1 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 - Sync all three delta requirements into accepted `engine/schema-core`, `research/pre-research-gate-implementation`, and `research/research-access-adapter` specs. Done when main specs describe optional current-only access, the schema-root Gate failure, and no adapter boundary mapping.
- [ ] 3.2 @impl SCO-002, PRG-010 - Update `openspec/governance/semantic-fact-families.yaml` for `research.host-access-envelope` so its bounded question covers only current direct-sample facts and does not claim legacy boundary ownership. Update this change's semantic closure to name the actual current resolver and verdict consumers. Done when the catalog, closure record, code, and accepted specs agree on the current fact boundary.
- [ ] 3.3 @impl SCO-002, PRG-002, PRG-010, REA-001 - Re-run the scoped current-surface scan across ProfileSchema/HITL1/adapter/guidance/tests. Done when each remaining non-archive legacy token is a negative rejection assertion, an ordinary pending task, or an unrelated protected surface; no selected surface promises legacy acceptance, boundary-owner projection, migration, or upgrade.

## 4. Verification And Closeout

- [ ] 4.1 @impl VER-001, VER-002, VER-003 - Execute the selected `verification-plan.yaml` unit, integration, and deterministic E2E assets plus workflow package validation. Done when all selected assets pass and results are recorded in this change without claiming unrun Agent behavior.
- [ ] 4.2 @impl VER-002, VER-003 - Run `git diff --check`, strict OpenSpec validation, plan/asset verification-routing checks, capability discovery, and semantic-closure checks. Done when every command exits `0` and the structural result is not presented as runtime proof.
- [ ] 4.3 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 `openspec-feedback:closeout-review` - Review the scoped actual diff, schema contract, former resolver removal, current fixture conversions, semantic catalog/closure, accepted-spec sync, and selected evidence. Record any actionable finding as an ordinary unchecked task with requirement/reader question, owner, smallest repair, and observable done condition. Done when exactly one closeout-review marker is complete and no actionable finding remains.

## 5. Archive Preconditions

- [ ] 5.1 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 - Compare every modified delta requirement/scenario with its accepted main-spec block and run `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-legacy-research-access-envelope`. Done when modified blocks match and the check reports zero duplicate, orphan, unregistered, or reused-retired IDs.
- [ ] 5.2 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 - Run `node openspec/governance/check-project-specs.mjs`. Done when it reports zero delta-header-in-main, missing-purpose, missing-requirements, and missing-requirement-header findings.
- [ ] 5.3 @impl VER-002, VER-003 - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-legacy-research-access-envelope`. If it fails, restore this task to unchecked and repair its named root. Done when the governed finalizer reports an archive location and all supported mechanical closeout checks pass.
