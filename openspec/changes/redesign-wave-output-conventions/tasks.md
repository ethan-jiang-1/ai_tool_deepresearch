## 1. New Shared Node — Reference Template

- [x] 1.1 Create `workflows/nodes/shared/shared-reference-template.md` with rich MD reference file format specification. Implement REF-002. The file's own YAML frontmatter must follow WNC-002 (node_type: shared, id: shared-reference-template, shared_scope: template, authority: guidance-only, requires: [], suggested_context: []). Body SHALL describe: (a) metadata block fields (source_url, acceptance_status, source_type, tier, evidence_role, trust_level, why_it_matters, etc.) with colon-separated key-value format; (b) five standard sections (## Key Facts, ## Core Content Capture, ## Relevance To This Research, ## Quotable Terms / Concepts, ## Risks And Limitations); (c) naming convention (00-shared-* / 00-cross-* / 0N-*).

## 2. Phase Node Updates

- [x] 2.1 Update `phase-wave0.md` throughout to reflect new output conventions. Also update `phase-wave0-subagent.md` section 2 title and step 6: change `reference/{topic.slug}/source.yaml` to `artifacts/wave0/{topic.slug}/source.yaml`. Add pre-gate structural lint step to §3.3: Agent SHALL run `node DPT_FRAMEWORK/cli/inspect-wave0-output.mjs --bundle <path>` before gate, read inspect/advice, fix structure issues, rerun until clean, then run gate. Implement RWP-009, REF-001, WAD-002. Changes across ALL sections of phase-wave0.md: — change from `reference/<topic>/source.yaml` to `reference/00-shared-<slug>.md` (rich MD) + `artifacts/wave0/<topic>/source.yaml` (thin YAML) + `reference/_INDEX.md` + `reference/README.md`; (b) Required Inputs — add `shared-reference-template.md`; (c) Allowed Actions §3.1 task card JSON template — change `writes_to` and `required_receipts` from `reference/{topic.slug}/source.yaml` to `artifacts/wave0/{topic.slug}/source.yaml`, update `action` field to include rich MD creation; (d) Allowed Actions §3.2 — change Sub-agent output target from `reference/<topic>/source.yaml` to `artifacts/wave0/<topic>/source.yaml` + `reference/00-shared-<slug>.md`; (e) Allowed Actions §3.3 — change `reference/index.md` to `reference/_INDEX.md`, add `_INDEX.md` update step; (f) Expected Artifacts — full rewrite (see spec RWP-001); (g) On Gate Fail table — update paths in missing-artifact scenarios; (h) Rerun-Aware Behavior — update artifact paths; (i) Anti-Cheating Rules — update prohibitions. Note: this is the single largest file change — the current phase body's entire workflow is built around per-topic `reference/<topic>/source.yaml` and every section references it.

- [x] 2.2 Update `phase-wave1.md` to add `reference/0N-<slug>.md` (rich MD per topic source) as a new required artifact alongside existing `evidence-summary.md` and `question-list.md`. Implement RWP-010. Update: (a) Stage Goal — add rich MD file creation per topic source; (b) Required Inputs — add `shared-reference-template.md` and `reference/_INDEX.md`; (c) Allowed Actions §3 task card JSON — add third writes_to / required_receipts entry for `reference/{topic.slug}-<slug>.md`; (d) Expected Artifacts — add `reference/0N-<slug>.md` (count_floor ≥ 1 per topic); (e) On Gate Fail — add missing reference file scenario; (f) Anti-Cheating Rules — add prohibition against `00-shared-*` / `00-cross-*` prefixes; (g) Add pre-gate structural lint step: Agent SHALL run `node DPT_FRAMEWORK/cli/inspect-wave1-output.mjs --bundle <path>` before gate, fix issues, rerun until clean. The rich MD file is produced by the Phase Agent after Sub-agent completes.

- [x] 2.3 Update `phase-wave2.md` throughout to replace `reference/00_shared/source.yaml` with optional `reference/00-cross-<slug>.md`. Implement RWP-011. Changes: (a) Allowed Actions §3.2 step 6 — change scout promotion target from `reference/00_shared/source.yaml` to `reference/00-cross-<slug>.md` (rich MD format per shared-reference-template.md); (b) Required Inputs — update `reference/<topic>/source.yaml` to `artifacts/wave0/<topic>/source.yaml`; (c) Expected Artifacts — replace `reference/00_shared/source.yaml` (optional) with `reference/00-cross-<slug>.md` (optional); (d) add `_INDEX.md` update step when cross files created; (e) §3.3 — add pre-gate structural lint: Agent SHALL run `node DPT_FRAMEWORK/cli/inspect-wave2-output.mjs --bundle <path>` before gate, fix issues, rerun until clean. Synthesis artifacts stay in `artifacts/wave2/`.

- [x] 2.4 Verify `phase-wave2-subagent.md` — confirm no instruction prescribes output to `reference/00_shared/` (current file does not reference it; sub-agent outputs JSON to slot directory). No changes needed. If desired, add a note that the Phase Agent may promote sub-agent results to `reference/00-cross-<slug>.md` but the sub-agent itself continues to output JSON per relay slot contract.

- [x] 2.5 Update `phase-wave1.md` to remove Future Expansion Guidance section (the 6 expansion tracks: topic-specific deepening, subagent dispatch, candidate intake, repair/backfill, fan-in review, topic artifact quality gates). Implement RWP-004 and RWP-005 REMOVED. Also verify: the current Expected Artifacts do not mention `skeleton.md` or `foundation-placeholder` (already removed in prior change). Remove Anti-Cheating Rule 8 ("禁止声称 Sub-agent fan-in/fan-out complete") if still present. If task 2.2 has already been completed, verify no skeleton/foundation-placeholder references were accidentally re-introduced.

- [x] 2.6 Update synthesis reference path examples for flat reference/ layout. Implement RWP-006 MODIFIED. The primary target is `shared-schemas.md` which contains the authoritative Reference Layer documentation and link path examples. Changes: (a) Update `shared-schemas.md` Reference Layer section — document flat `reference/` with `00-shared-*.md` / `0N-*.md` / `00-cross-*.md`, `_INDEX.md` as canonical inventory, `README.md`; document `artifacts/wave0/<topic>/source.yaml` for thin YAML; (b) Update the Wave2 synthesis link path example from `../../reference/<topic>/source.yaml` to `../../reference/0N-<slug>.md` and `../../reference/00-shared-<slug>.md` (two levels up from `artifacts/wave2/` to bundle root); (c) In `phase-wave2.md` Required Inputs, update `reference/<topic>/source.yaml` to `artifacts/wave0/<topic>/source.yaml`.

- [x] 2.7 Update `shared-schemas.md` comprehensively for all new conventions (in addition to the path example changes in 2.6). Update: (a) the file-level comment about `reference/<topic>/source.yaml` to `artifacts/wave0/<topic>/source.yaml`; (b) the Reference Layer section to document flat reference/ directory structure; (c) any other section that describes the old nested reference/ or source.yaml conventions. This is critical — Agents read shared-schemas.md to understand where to read/write artifacts. If it stays outdated, Agents will follow old conventions.

## 3. Gate Definition Updates

Note for all gate tasks: The current gate engine's `count_floor` check type counts YAML array entries within a single file. Using `count_floor` to count files matching a glob pattern (e.g., `reference/00-shared-*.md`) is a NEW usage pattern. Tasks 3.1 and 3.2 may require extending the gate checker CLI to support glob-based file counting. If this is beyond the change scope, use `file_exists` with `{topic}` expansion as an alternative (verified existence per topic, though not an exact count).

- [x] 3.1 Update `gate-wave0-complete.definition.json`. Implement RWG-011. Changes: (a) Change rule targets from `reference/{topic}/source.yaml` to `artifacts/wave0/{topic}/source.yaml` (rules: per_topic_source_yaml_exists, per_topic_reference_schema_valid, per_topic_count_floor); (b) Change rule reference_index_exists target from `reference/index.md` to `reference/_INDEX.md`; (c) Add new rules: `file_exists` + `field_non_empty` for `reference/README.md`; (d) Add `count_floor` for `reference/00-shared-*.md` with threshold 1 (requires glob-based file counting — if unsupported by gate engine, implement as a `pattern_match` or `file_exists` alternative); (e) Optionally remove or keep the `dir_exists: reference` rule. Also update `check-gate-wave0-complete.mjs`: add `field_non_empty` handler (currently missing; port from wave2 checker which strips frontmatter and checks body non-empty). Add glob-based count_floor support if needed.

- [x] 3.2 Update `gate-wave1-complete.definition.json`. Implement RWG-012. Changes: (a) Verify current definition has no skeleton/foundation-placeholder rules to remove (it doesn't); (b) Retain existing `dir_exists: artifacts/wave1/`, `trace_event_present: wave1_completion`, `status_value` checks; (c) Add `count_floor` for `reference/0N-*.md` per topic using `{topic}` expansion (target: `reference/{topic}-*.md`, threshold 1 per topic). If glob-based count_floor is unsupported, add `file_exists` with `{topic}` expansion or `pattern_match` per topic; (d) Existing evidence-summary.md and question-list.md checks should already be present — verify. Also update `check-gate-wave1-complete.mjs`: add `count_floor` handler if not already present.

- [x] 3.3 Verify `gate-wave2-complete.definition.json` has no rules targeting `reference/00_shared/` or `reference/00_shared/source.yaml` (confirmed: current definition is already clean). Implement RWG-013. No rule removal needed. Add NO `count_floor` for `00-cross-*.md` (optional artifact, must not block gate). Keep all existing `artifacts/wave2/` checks unchanged.

## 4. Schema Updates

- [x] 4.1 Update `schema/contracts/reference.mjs`. Implement SCO-010. Changes: (a) Update file-level comment from "for reference/<topic>/source.yaml" to "for artifacts/wave0/<topic>/source.yaml"; (b) ReferenceMetadataSchema unchanged — continues to validate thin YAML entries (url, title, retrieved_date, topic_tag, notes); (c) Add a new exported function `validateIndexMD(content: string)` that parses the Markdown table from `_INDEX.md`, extracts headers and rows, and validates: all 8 required columns present (ref_file, source_type, trust_level, tier, related_topic, source_layer, acceptance_status, date_landed), at least 1 data row. Returns `{ valid: boolean, errors: string[] }`. Note: this is a function export, not a Zod schema, since the input is Markdown text — the Markdown table must be parsed before validation.

- [x] 4.2 Update `schema/index.mjs` barrel export to add the new `validateIndexMD` export from `reference.mjs`.

## 5. CLI Updates

- [x] 5.1 Update `cli/inspect-bundle.mjs`. Implement WAD-001, WDC-009. Changes: (a) Add `'artifacts/wave0/'` to the REQUIRED paths array; (b) Add `'reference/_INDEX.md'` and `'reference/README.md'` as explicit file entries in the REQUIRED array. The existing `existsSync` loop handles both directory and file entries. Note: this depends on Task 5.2 — newly instantiated bundles must include `artifacts/wave0/` for inspect to pass.

- [x] 5.2 Update `cli/instantiate-run-bundle.mjs`. Implement WAD-003. Changes: (a) Add `'artifacts/wave0'` to the `dirs` array (alongside existing `artifacts/wave1`, `artifacts/wave2`); (b) Update the console output message that prints "6 data directories" — either change to "7 data directories" or remove the count and list directories explicitly.

- [x] 5.3 Create `cli/inspect-wave0-output.mjs` — wave 0 structural lint. Implement IOC-001. Reads `topic_registry` from `rb_plan.md` frontmatter（复用 `readBundlePlan()` from `DPT_FRAMEWORK/engine/helpers/gate-helpers.mjs` 或等价方式）。Checks: flat reference/ directory (no subdirectories), file naming (00-shared-* only), metadata block completeness (9 required keys: source_url, acceptance_status, source_type, tier, evidence_role, trust_level, why_it_matters, accessed_at, related_topic), 5 standard section headers, _INDEX.md header completeness (8 columns) + >=1 data row, README.md exists and non-empty, artifacts/wave0/<topic>/source.yaml per topic_registry with schema_valid. Output `{ check, inspect, advice }` JSON, no routing, no trace write. Pure Node.js built-ins + yaml + gate-helpers import.

- [x] 5.4 Create `cli/inspect-wave1-output.mjs` — wave 1 structural lint. Implement IOC-002. Checks: per-topic reference/0N-*.md existence (by topic_registry id), each 0N-*.md metadata block + 5 sections, artifacts/wave1/<topic>/evidence-summary.md + question-list.md per topic, _INDEX.md has source_layer: wave1_topic entries. Does NOT repeat wave 0 checks (use inspect-wave0-output.mjs for those).

- [x] 5.5 Create `cli/inspect-wave2-output.mjs` — wave 2 structural lint. Implement IOC-003. Checks: no reference/00_shared/ subdirectory, any 00-cross-*.md files have metadata + sections + _INDEX.md entries with source_layer: wave2_cross, artifacts/wave2/ triple (synthesis.md, cross-topic-ledger.md with 6 sections, finding-index.yaml parseable), no __BACKFILL_WAVE2_JUDGMENT__ / __BACKFILL_PENDING_QUESTIONS__ tokens in seed_topics/*.md. Does NOT repeat wave 0 or wave 1 checks.

## 6. Template Updates

- [x] 6.1 Update `rb_templates/START_FROM_HERE.md.tmpl`. Implement BUS-002. Expand the data directory map section to document: (a) `reference/` — flat directory, three file prefixes (00-shared-*.md, 0N-*.md, 00-cross-*.md), `_INDEX.md` as canonical inventory, `README.md` as human navigation; (b) `artifacts/` — organized by wave (wave0/ thin YAML, wave1/ topic synthesis, wave2/ cross-topic synthesis); (c) `seed_topics/` and `final/` — unchanged. Remove any references to `reference/<topic>/` subdirectories or `reference/00_shared/source.yaml`.

- [x] 6.2 Create `rb_templates/artifacts/README.md.tmpl` and wire into `instantiate-run-bundle.mjs`. Implement WAD-004. Create a README for the `artifacts/` directory that documents the wave-organized output structure for AI/coding agents (not human readers). Content: directory map table (wave0/, wave1/, wave2/, hitl2/), per-wave file listing with format descriptions (wave0: thin YAML source.yaml per topic; wave1: evidence-summary.md + question-list.md per topic; wave2: synthesis.md + cross-topic-ledger.md + finding-index.yaml; hitl2: decision-brief.md), and note that hitl1/ has no directory (HITL1 outputs to rb_plan.md + rb_profile.yaml). Add `{ tmpl: 'artifacts/README.md.tmpl', dest: 'artifacts/README.md', parse: null, schema: null }` to the templates array in `instantiate-run-bundle.mjs`. Also update the console output message to mention reference + artifacts scaffolds.

## 7. Verification

- [x] 7.1 Run-package.mjs` — SHALL pass (manifest ↔ updated gate definitions ↔ transition chain consistency verified).

## 8. Experiment Playbooks — Verify Inspect CLIs

> 所有实验遵循 `guidelines/command-experiments.md` 的 playbook 规范：`schema: command-experiment/v1`、`weight: light`、`execution: real-bundle`、`verdict: trace-jsonl`。使用 `new-disposable-bundle.mjs` 创建 bundle，`extract-field.mjs` 解析 CLI JSON 输出，`wff-playbook-utils.mjs` 的 `recordCheck()` + `verdict()` 做裁决。禁止 mock、禁止手写 trace、禁止 console.log 裁决。PASS 才 cleanup，FAIL 保留现场。

- [x] 8.1 Create/case-401-light-happy-and-fail.md`. Test `inspect-wave0-output.mjs` with two sub-cases: (a) Happy path — create valid `reference/00-shared-<slug>.md` with complete metadata + 5 sections, valid `_INDEX.md`, `README.md`, flat directory, per-topic thin YAML → CLI passes all checks; (b) Fail path — introduce 3 deliberate errors: subdirectory under reference/, metadata missing `trust_level`, missing `## Risks And Limitations` section → CLI reports all 3 in inspect. Verifies IOC-001.

- [x] 8.2 Create/case-401-light-happy-and-fail.md`. Test `inspect-wave1-output.mjs` with two sub-cases: (a) Happy path — create valid `reference/0N-<slug>.md` per topic with complete metadata + sections, valid evidence-summary + question-list per topic, `_INDEX.md` with `wave1_topic` entries → CLI passes; (b) Fail path — missing per-topic 0N-*.md, metadata missing `why_it_matters`, `_INDEX.md` missing `source_layer: wave1_topic` entry → CLI reports all 3. Verifies IOC-002.

- [x] 8.3 Create/case-401-light-happy-and-fail.md`. Test `inspect-wave2-output.mjs` with two sub-cases: (a) Happy path — valid `artifacts/wave2/` triple (synthesis.md with W2F-xxx references, cross-topic-ledger.md with 6 sections, finding-index.yaml parseable), optional `reference/00-cross-<slug>.md` with correct format, no `00_shared/` subdirectory, no backfill tokens → CLI passes; (b) Fail path — `reference/00_shared/` subdirectory present, synthesis.md empty, seed_topics has residual `__BACKFILL_WAVE2_JUDGMENT__` → CLI reports all 3. Verifies IOC-003.

## 9. Governance Checks

- [x] 9.1 Run.mjs` — MUST pass (0 duplicate, 0 orphan, 0 unregistered, 0 reusedRetired).

- [x] 9.2 Run.mjs` — MUST pass (0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, 0 missingReqHeader).

## 10. Existing Bundle Migration（最后执行）

> ⚠️ 此 task 在所有其他 framework 改动完成并验证通过后再执行。先改 framework，再迁移 bundle。这是纯数据迁移——不修改任何 framework 文件。

- [x] 10.1 Migrate `dpt_rb_china-reaction-world-cup-2026/` to new conventions. Steps: (a) Create `artifacts/wave0/` directory (if not exists); (b) Move each `reference/<topic>/source.yaml` to `artifacts/wave0/<topic>/source.yaml`; (c) Move `reference/00_shared/source.yaml` to `artifacts/wave2/00_cross_topic_sources.yaml` (this is wave2 content, not wave0); (d) Create `reference/_INDEX.md` — convert `reference/index.md` into the 8-column Markdown table format (ref_file, source_type, trust_level, tier, related_topic, source_layer, acceptance_status, date_landed). For existing sources, derive: source_type=primary, tier=infer from source quality, source_layer=wave0_foundation for per-topic sources or wave2_cross for shared sources, acceptance_status=accepted. Also update any `wave2_cross_topic` references in `artifacts/wave2/` to `wave2_cross` (canonical value per REF-003). Document assumptions in the file; (e) Create `reference/README.md` — brief description of flat reference directory (three naming prefixes, _INDEX.md as canonical inventory, files follow shared-reference-template.md format); (f) Remove empty `reference/<topic>/` and `reference/00_shared/` subdirectories.

- [x] 10.2 Run `node DPT_FRAMEWORK/cli/validate-bundle.mjs dpt_rb_china-reaction-world-cup-2026` — validated (2 pre-existing enum issues in bundle, unrelated to migration).

- [x] 10.3 Run `node DPT_FRAMEWORK/cli/inspect-bundle.mjs dpt_rb_china-reaction-world-cup-2026` — PASS, directory structure complete.
