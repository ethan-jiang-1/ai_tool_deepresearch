## 1. Apply Entry And Boundary Review

- [x] 1.1 `openspec-feedback:plan-review` — Before the first target edit, obtain current feedback-loop Apply guidance and review the proposal, four delta specs, design, `semantic-closure.yaml`, verification plan, accepted ERS-001/ERS-002, EXA-009, EXO-007, and PLR-004 owners, current retained reader, regression admission, docs, and focused tests. Record every actionable finding as an ordinary unchecked task with its affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition; complete this marker only when no finding remains. Completed 2026-08-14: the proposed current-v2 input boundary, semantic-closure resolver/established-by/consumers, preserved v2 qualification path, focused fixtures, and two README projections are coherent. `readRetainedExperimentObservations`, `ExperimentSelectionObservationSchema`, `planExperimentRun`, and `runSupervisor` are actual symbols; no unplanned current consumer, missing direct Source of Record, or false closure role remains.
- [x] 1.2 Before the first target edit, run strict change validation, `node openspec/governance/check-project-reqs.mjs --mode plan`, project-spec governance, capability taxonomy/discovery, verification-routing plan validation, semantic-closure plan validation, and `git diff --check`. Done when all commands exit zero and their results are recorded as structural planning evidence only. Completed 2026-08-14: strict validation, plan requirement governance, project-spec, capability taxonomy/discovery, verification-routing plan, semantic-closure plan, and diff checks all passed.
- [x] 1.3 Re-run scoped searches across current Harness source, tests, accepted specs, active guidance, and active changes for `AgentExperimentBatchReportV1Schema`, `AgentExperimentAuditEventV1Schema`, `ExperimentSelectionObservationV1Schema`, retained-reader imports, and historical qualification wording. Do not inspect `.exp-bundles/`. Done when every current consumer is either in this task list or recorded as an ordinary repair finding, and unrelated current v1 discriminator owners are explicitly protected. Completed 2026-08-14: the retired schema definitions occur only in `agent-experiment-contract.mjs`; their production read path is only `readRetainedExperimentObservations`, and current direct-test callers are the selected strategy/Supervisor fixtures. Historical-qualification wording is limited to those selected tests, the two selected READMEs, accepted owners, and this active proposal. Completion, run-context, health, and manifest v1 contracts retain separate active source/test owners and are excluded.

## 2. Current-V2 Retained Input Boundary

- [x] 2.1 Implement EXO-007 and ERS-001 in `DEEP_RESEARCH_HARNESS/host_tools/lib/agent-experiment-contract.mjs`: remove only the retained v1 report, audit-event, and selection-observation current-input schemas/union branches; require v2 selection observations directly inside current v2 report/audit contracts. Do not alter completion, health, run-context, manifest-marker, or other current v1 discriminator contracts. Done when no retired schema/export or v1 selection union can establish a current retained strategy fact. Completed 2026-08-14: current retained report, audit-event, and selection-observation schemas are direct v2 schemas; unrelated v1 discriminator contracts remain present.
- [x] 2.2 Implement ERS-001 and EXO-007 in `DEEP_RESEARCH_HARNESS/host_tools/lib/experiment-run-strategy.mjs`: parse only complete v2 retained report/audit observations, route v1, v2-with-v1-selection, malformed, and unreadable retained material through existing non-fatal diagnostics, and produce no normalized observation from it. Do not add a version router, converter, fallback, migration, compatibility alias, or new diagnostic taxonomy. Done when current v2 observations remain readable and every rejected record is incapable of contributing duration, cost, outcome, source relation, prediction, rotation, admission, qualification, group-gap, selection, or launch input. Completed 2026-08-14: strict current schemas route all retained non-v2 material through the existing invalid-record diagnostics; the focused unit fixture retains only its complete v2 observation.
- [x] 2.3 Implement ERS-002 in `DEEP_RESEARCH_HARNESS/host_tools/lib/experiment-run-strategy.mjs`: make `needs_qualification` require one latest fast PASS+CLEAN current-v2 result with matching source and a comparable stale execution surface. Retain the explicit qualification intent for that v2 drift case; make missing observations, retained v1, source drift, invalid outcome/health/SLO facts, and incomplete v2 facts `ineligible`. Done when normal regression never selects either a qualification or historical candidate, and explicit qualification can select only the retained current-v2 drift candidate under the existing envelope. Completed 2026-08-14: only a complete current-v2 stale execution-surface relation reaches `needs_qualification`; missing execution surface is ineligible.
- [x] 2.4 Add `verification.autorun-current-retained-observation` to `openspec/governance/semantic-fact-families.yaml` exactly as declared by `semantic-closure.yaml`. Done when the catalog answers the same bounded retained-input question without introducing a second fact family or new runtime authority. Completed 2026-08-14: catalog entry exactly matches the closure bounded question.

## 3. Current Guidance And Deterministic Evidence

- [x] 3.1 Update ERS-001/ERS-002/EXO-007 unit coverage in `tests/host_tools/experiment-run-strategy.test.mjs`. Prove v1 report/audit and a v2 envelope carrying v1 selection metadata create diagnostics but no current observation/prediction/admission; retain proof that a valid current-v2 execution-surface drift is `needs_qualification` only under explicit intent. Use isolated temporary fixtures only. Done when the retired v1 qualification expectation and v1 parser assertion are absent, no facts are stitched across records, and the current-v2 qualification case still passes. Completed 2026-08-14: the isolated fixture rejects v1 report/audit and a v2 envelope carrying v1 selection metadata, retains only the valid v2 record, and the current-v2 drift test passes.
- [x] 3.2 Update EXA-009/ERS-002/EXO-007 integration coverage in `tests/integration/host_tools/run-agent-experiment.test.mjs`. Prove a v1-only retained fixture produces a no-current-observation/ineligible dry-run plus a non-fatal diagnostic, and that normal regression and `--regression-qualification` create no run root from it. Keep separate current-v2 qualification coverage. Done when the integration fixture proves deterministic Supervisor mechanics only and creates no real Agent-flow claim. Completed 2026-08-14: the deterministic Supervisor fixture reports the v1-only group as ineligible with its retained-record diagnostic, and both normal and explicit-qualification calls create no run root.
- [x] 3.3 Update PLR-004 normal Autorun guidance in `DEEP_RESEARCH_HARNESS/host_tools/README.md` and `experiments_playbook/README.md`, and update `tests/integration/md/agent-experiment-autorun-terminology.test.mjs` with the same current-only rule. Done when guidance calls retained v1 history human-readable/diagnostic-only, reserves qualification for current-v2 execution-surface drift, retains the explicit command/envelope, and no surface teaches historical v1 qualification. Completed 2026-08-14: both normal guidance surfaces and their terminology test describe v1 as diagnostic-only and reserve qualification for stale current-v2 execution surfaces.

## 4. Verification, Spec Sync, And Scoped Closeout

- [x] 4.1 Run every selected unit and integration asset in `verification-plan.yaml`: `tests/host_tools/experiment-run-strategy.test.mjs`, `tests/integration/host_tools/run-agent-experiment.test.mjs`, and `tests/integration/md/agent-experiment-autorun-terminology.test.mjs`. Done when all pass; record only deterministic unit/integration evidence and do not inspect real retained history or claim `agent_flow_e2e` evidence. Completed 2026-08-14: all three selected `node --test` assets passed; this is deterministic unit/integration evidence only.
- [ ] 4.2 Perform scoped absence/protected-surface review. Prove the retired v1 retained-input schemas and historical-qualification path are absent from current source/tests/guidance, while valid current-v2 observation/qualification behavior and unrelated completion/health/run-context/manifest v1 owners remain unchanged. Done when every unexpected reference or protected-surface change is repaired or recorded as an ordinary task.
- [ ] 4.3 Sync the four applied delta specs into the accepted ERS-001/ERS-002, EXA-009, EXO-007, and PLR-004 main-spec owners, then re-read the synchronized blocks against the delta specs. Done when each accepted requirement expresses the approved current-v2-only policy without carrying the retired v1 qualification path.
- [ ] 4.4 Run `openspec validate decide-retained-experiment-history-policy --strict`, `node openspec/governance/check-project-reqs.mjs --mode archive --change decide-retained-experiment-history-policy`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, `node openspec/governance/check-verification-routing.mjs --change decide-retained-experiment-history-policy --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change decide-retained-experiment-history-policy --mode assets`, and `git diff --check`. Done when all pass; record structural/governance evidence separately from selected deterministic tests.

## 5. Archive Readiness

- [ ] 5.1 `openspec-feedback:closeout-review` — Before archive, obtain current feedback-loop Archive guidance and review the change-scoped actual diff, synchronized main specs, selected verification results, semantic-closure record, catalog addition, and protected v1 owners. Record every actionable finding as an ordinary unchecked task with owner, smallest repair, and independently observable done condition; complete this marker only when no finding remains.
- [ ] 5.2 After all ordinary tasks and both feedback reviews are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change decide-retained-experiment-history-policy`. If finalization fails, restore this task to unchecked and repair only its named root.
