## 1. Review and Preconditions

- [x] 1.1 `openspec-feedback:plan-review` Review proposal, delta specs, design, tasks, and verification plan for DEW-025/REF-007/REF-009/WPG-017/RWG-022/CTS-009/RWP-001 authority, writer/reader, compatibility, and proof coherence before the first target edit; record every actionable finding below as an ordinary task with owner, smallest repair, and observable done condition.
  Review record (2026-08-04): corrected the existing `submitted_work` wire-discriminator ambiguity, named the existing artifact-persistence writer, corrected unit verification routing, and added a version-selected v1/v2 assignment compatibility decision through task 1.4. Polish follow-up: recorded markerless legacy compatibility, made `source_identity.work_id` plus `entry_id` the exact Wave0 identity representation, fixed the deferred writer's existing limitation values, redirected DEW-025 proof to its actual unit asset, and made downstream v2 fixture migration explicit in task 4.4. The pre-existing Wave0 repair-kind expectation remains task 4.5 with its direct provenance-recovery owner to be adjudicated.
- [x] 1.2 Register DEW-025, REF-009, WPG-017, RWG-022, and CTS-009 in `openspec/governance/req-registry.yaml` under their existing capability groups; done when the active deltas contain no unregistered C1 IDs.
- [x] 1.3 Validate `verification-plan.yaml` with `check-verification-routing.mjs --change materialize-wave0-submitted-references-and-batch-projections --mode plan`; done when its selected test classes and assets pass the plan schema/checker.
- [x] 1.4 Plan-review finding for DEW-025: the assignment-contract version owner must distinguish immutable v1 Wave0 reference-capable manifests from v2 source-only new claims. Amend the C1 design/task evidence to name that version-selected compatibility path; done when no reader reconstructs a v1 manifest through v2 semantics.

## 2. Current Wave0 Producer Contract

- [x] 2.1 Implement DEW-025 in the Wave0 assignment resolver and generated work-unit contract projections: new v2 `wave0_source_intake` claims expose only the assigned source YAML plus existing cache/result/receipt obligations and reject a delegated rich-reference completion route; version-selected v1 readers preserve bound legacy attempts without reconstructing them through v2 semantics. Evidence: `node --test tests/engine/work-unit-assignment-contract.test.mjs tests/engine/work-unit-actor-submit.test.mjs tests/engine/work-unit-lifecycle.test.mjs tests/schema/contracts/work-unit.test.mjs tests/engine/work-unit-submit.test.mjs tests/integration/cli/operate-work-unit.test.mjs` passed after explicit current-v2, marked-v1, and marker-absent fixtures were separated.
- [ ] 2.2 Implement REF-007 and RWP-001 in the Wave0 Phase and source-intake Markdown delivery chain: source-intake guidance produces submitted source/cache facts only; post-submit guidance uses the existing `operate-artifact-persistence` boundary for exact-backing materialization or the contribution-scoped deferred intent followed by the same inspect.
- [ ] 2.3 Add focused producer-contract and Markdown-delivery regression coverage for DEW-025, REF-007, and RWP-001, including marked-v1 and markerless legacy task/ledger compatibility, plus the absence of a current actor reference-output route.

## 3. Submitted Backing and Reference Authority

- [ ] 3.1 Implement REF-009's one bounded Wave0 submitted-backing reader at the existing work-unit projection/direct-output seam; it resolves exact current `<work_id>/<ordinal>` source URL, source YAML, cache, result, and work-unit facts without exposing a second source catalog or reparsing source facts in each consumer.
- [ ] 3.2 Implement WPG-017 in reference authority classification and numeric eligibility: retain submitted legacy Wave0 reference coverage, accept exact submitted-backed Phase-owned shared projections, and fail closed for URL-only, unsubmitted, ambiguous, index-only, and filesystem-only inputs.
- [ ] 3.3 Add unit regression coverage for REF-009/WPG-017: exact identity binding, duplicate URL isolation, legacy reference compatibility, backed Phase-owned projection acceptance, and unbacked rejection.

## 4. Wave0 Convergence and Deferred Projection

- [ ] 4.1 Implement RWG-022 as one pure Wave0 submitted-reference convergence evaluator, then route `evaluateWave0Contract`, Wave0 inspect, and formal Gate through its ordered result without adding state, a controller, or a Gate mutation path.
- [ ] 4.2 Integrate convergence diagnostics with existing reference/index/count helpers so materializable backing appears before only its dependent floor outcome, while independent malformed/provenance/queue/receipt/cache/navigation roots remain visible.
- [ ] 4.3 Implement CTS-009's strict Wave0 contribution-scoped deferred packet branch in the existing topic-state schema/writer; preserve the existing `source_identity.kind: submitted_work` wire discriminator while atomically expanding current unprojected identities, preserving explicit entries, rejecting collisions/unsubmitted input, and retaining idempotent replay/recovery semantics.
- [ ] 4.4 First migrate the selected Wave0 integration fixtures to current v2 source/cache-only submitted results, retaining direct actor-owned reference outputs only in explicit marked-v1 legacy fixtures. Then add RWG-022/CTS-009 regression coverage for inspect/Gate parity, materialization-before-floor, true deficit, independent invalid backing, `1..19` versus later `/20`, collision rejection, idempotent replay, and explicit-entry compatibility. Done when `check-gate-wave0-complete` and `operate-topic-state-projection` fail only on their asserted C1 behavior rather than pre-submit v2 output-contract rejection.
- [ ] 4.5 Adjudicate the pre-existing `wave0_work_unit_submission_presence.repair_kind` regression against its direct provenance-recovery owner. If C1 changes that owner or surface, implement the narrow repair and regression assertion; otherwise record the isolated owner and retain the C1 expectation without weakening it. Done when the focused C1 test command has an evidence-backed classification for the assertion.

## 5. Documentation and Release

- [ ] 5.1 Update `CONTEXT.md` with the C1 glossary only: work unit/attempt, submitted ledger row, submitted Wave0 contribution, source identity, reference, consumer projection, seed projection, deferred disposition, and the existing `submitted_work` wire discriminator; do not add implementation rules or broaden it into a runtime alias.
- [ ] 5.2 Complete the C1 contract-coherence matrix as change verification evidence, linking each assignment, task-delivery, submit/provenance, materialization, topic-state packet/result, inspect/Gate, and feedback row to its `verification-plan.yaml` claim. Extend `tests/integration/md/wave-producer-contract-guidance.test.mjs` as the static delivery guard so it rejects both a current actor reference-output route and missing Phase-side submitted-backing/deferred rerun guidance. Done when every matrix row has a direct proof claim and a changed delivery surface cannot silently reintroduce conflicting terminology or authority claims.
- [ ] 5.3 Release the implemented framework as v0.69: update `CHANGELOG.md` and synchronize the `DPT_FRAMEWORK/RUN.md` version banner/entry with the same concise behavior statement.
- [ ] 5.4 Update BUG-189 and BUG-191 plus the progressive systemic remediation plan only after implementation evidence exists; record the concrete C1 behavior, verification, residual real-Actor boundary, and truthful stage/status transition.

## 6. Verification, Sync, and Archive

- [ ] 6.1 Run the selected unit/integration assets from `verification-plan.yaml` and the focused C1 command set; fix C1 failures or record a direct, out-of-scope owner without silently changing coverage claims.
- [ ] 6.2 Run the repository-appropriate broader deterministic regression suite and lint/format checks required by changed surfaces; record commands and results in C1 evidence/task notes.
- [ ] 6.3 Run `check-verification-routing.mjs --change materialize-wave0-submitted-references-and-batch-projections --mode assets`; done when every selected verification asset exists and passes routing validation.
- [ ] 6.4 Sync all six C1 delta specs into their accepted main specs, then semantically compare each synced requirement with its delta and implementation evidence.
- [ ] 6.5 Run `node openspec/governance/check-project-reqs.mjs` and `node openspec/governance/check-project-specs.mjs`; done only when both report zero required violations.
- [ ] 6.6 Run strict OpenSpec validation for the change and re-check the complete task list, release docs, glossary, backlog evidence, and active-change diff against the C1 boundary.
- [ ] 6.7 `openspec-feedback:closeout-review` Review the scoped implemented C1 diff, synced specs, verification evidence, and outstanding findings; complete only with no open C1 finding and all ordinary repair work complete.
- [ ] 6.8 Archive the change exclusively with `node openspec/governance/finalize-change-archive.mjs --change materialize-wave0-submitted-references-and-batch-projections`; done when the governed finalizer reports the archive transition succeeded.
