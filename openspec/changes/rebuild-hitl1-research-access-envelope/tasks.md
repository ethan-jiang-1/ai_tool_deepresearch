## 1. Apply Preconditions

- [ ] 1.1 `openspec-feedback:plan-review` Review SCO-002, REA-001/REA-002, PRG-002/PRG-010, PRP-002/PRP-005/PRP-015, HIU-002, `design.md`, `semantic-closure.yaml`, and `verification-plan.yaml` before the first target edit. Confirm the statically bounded envelope boundary, the current-writer complete-ladder rule, the two-axis pairing, owner→repair_kind derivation as the single derivation point, the Gate rule set staying unchanged in count, the exact partial-reachability disclosure text, and the v0.85 release target. Record every actionable finding as an ordinary unchecked task naming its requirement/reader question, authoritative owner, smallest repair, and observable done condition.
- [ ] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change rebuild-hitl1-research-access-envelope --mode plan` and `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-research-access-envelope --mode plan`. Done when both pass and their selected proof classes match the plan (`agent_flow_e2e` remains `not_applicable`).
- [ ] 1.3 Before the first SCO-002-dependent target edit, add `research.host-access-envelope` with the bounded question from this change's `semantic-closure.yaml` to `openspec/governance/semantic-fact-families.yaml`. Done when the global catalog contains the exact family ID and bounded question, and rerunning `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-research-access-envelope --mode plan` passes without a catalog addition.

## 2. Independent Probe Controller

- [ ] 2.1 Implement SCO-001 in `DEEP_RESEARCH_HARNESS/schema/enums.mjs`: add closed `SourceClass`, `SourceClassReachability`, `ResearchAccessBoundaryLocation`, and `ResearchAccessBoundaryExtent` enums; export them from `DEEP_RESEARCH_HARNESS/schema/index.mjs`. `SourceClass` order is the declared probe order (`encyclopedia`, `code_host`, `general_web`). Done when each enum is importable, closed, and no consumer can extend it at runtime.
- [ ] 2.2 Implement PRP-002 in new `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-hitl1-research-access-envelope.md`: use the existing independent shared-node shape (`node_type: shared`, `shared_scope: hitl1-research-access-envelope`, `authority: guidance-only`, `actor_delivery: required`) and declare the source classes with their exact neutral queries (`encyclopedia` → `site:wikipedia.org "Internet protocol suite"`, `code_host` → `site:github.com "Hello World"`, `general_web` → `"Internet protocol suite"`), both closed classification axes, and its no-runtime-authority boundary. Done when the controller is physically separate from `research-access-adapter.md`, `phase-hitl1.md`, and `shared-hitl1-capability-probe.md`; it contains no run-bundle path or Gate authority; and its declared class ordering equals `SourceClass`.
- [ ] 2.3 Implement PRP-002 in that controller body: own the complete ordered first-success traversal, one-search/three-candidate/native-first/same-URL-fallback bounds, the branch-complete compact return map, paired `access_boundary` vocabulary, honest-unclassified rule, and no-evidence/no-retry constraints. The current return map SHALL include one reachability entry for every declared class, marking classes after an available first-success as `not_attempted`. Done when each unavailable branch is classified or explicitly unclassified, no branch uses a prose prefix, and no controller rule can add a fallback, permission, lifecycle state, or user decision.
- [ ] 2.4 Implement REA-001 in `DEEP_RESEARCH_HARNESS/host_tools/research-access-adapter.md`: remove `unavailable_roots`, source classes, query, return-map, and classification controller prose; retain only selected-host/launcher/operation facts and a reference to the separate controller. Done when neither adapter frontmatter nor body can be read as the source-class or probe-control Source of Record.

## 3. Schema Contract

- [ ] 3.1 Implement SCO-002 in `DEEP_RESEARCH_HARNESS/schema/contracts/profile.mjs`: add the optional statically bounded `source_class_reachability` array to the `available` and `unavailable` branches per design D2 — at most one entry per declared class, `.max()` bounded to the declared width, no URL/HTTP-status/query/attempt fields on an entry. Use the exported source-class enum rather than duplicate string literals. Done when the array validates only declared classes and cannot express a per-attempt matrix while earlier envelopes remain readable after a future class addition.
- [ ] 3.2 Implement SCO-002 envelope cross-field rules via `.superRefine()`: reject duplicate class entries, reject an `available` observation with no `reachable` entry, and reject an `unavailable` observation with any `reachable` entry. Done when each rejection has a focused failing case and legacy observations without the field still validate.
- [ ] 3.3 Implement SCO-002 in `profile.mjs`: add the optional nested `access_boundary` object (`location` + `extent`, both imported closed enums and `.strict()`) to the `available` and `unavailable` branches, per design D3. Enforce `available` + boundary as `class_scoped` with both reachable and unreachable classes, and `unavailable` + boundary as `universal`; leave either branch unclassified when the object is absent. Done when a half-populated pair is structurally inexpressible, the whole object is omittable, and the field is rejected on `unprobed`.
- [ ] 3.4 Implement SCO-002 in `profile.mjs`: extend the `unprobed` branch's contradiction rejection to cover the new envelope and `access_boundary` fields. Done when an `unprobed` observation carrying either new field fails validation.
- [ ] 3.5 Implement SCO-002 in `tests/schema/contracts/profile.test.mjs`: cover the `envelope-shape-statically-bounded` claim — bounded-envelope acceptance, each envelope rejection, the paired/closed access-boundary truth table (`available/class_scoped`, `unavailable/universal`, and invalid cross-pairs), the unclassified-remains-valid case, the legacy-readable case, and the `unprobed` contradiction cases. Done when the file passes under `node --test` and asserts no defaulting or normalization of an absent boundary.

## 4. Resolver And Gate

- [ ] 4.1 Implement REA-001 in `DEEP_RESEARCH_HARNESS/host_tools/lib/research-access-adapter.mjs`: rebuild `UNAVAILABLE_FACTS` as a boundary-location-keyed owner table declaring `owner` and `actor` (`external` | `agent`) and no `repair_kind` field, and export `selectedAdapterBoundaryFact(location)` deriving `repair_kind` from `actor` per design D4. Done when `probe_relay` yields `agent_action`, the three external locations yield `external_action`, and the derivation exists at exactly one code point.
- [ ] 4.2 Delete `selectedAdapterUnavailableRoot()` and its prose-prefix parsing from `research-access-adapter.mjs` with no deprecated wrapper, per design D4. Done when no repository code splits a `reason` string to determine identity, owner, or repair lineage.
- [ ] 4.3 Implement REA-001 in `tests/host_tools/research-access-adapter.test.mjs`: cover the `two-axis-classification-and-repair-derivation` claim's resolver portion — the four unavailable-boundary location owner/repair truth table, the explicit unclassified conclusion, and the absence of any prose-derived route. The boundary extent truth table stays in task 3.5; the resolver never derives or reports extent. Done when the file passes under `node --test` and no assertion depends on reason text.
- [ ] 4.4 Implement PRG-002 in `DEEP_RESEARCH_HARNESS/cli/gates/check-gate-hitl1-recorded.mjs`: keep the rule set unchanged in count and keep admission on `research_access.status`, relying on ProfileSchema for envelope consistency per design D6. Done when no research-access check type, cross-field validator, degraded pass, or per-class rule is added.
- [ ] 4.5 Implement PRG-010 in `check-gate-hitl1-recorded.mjs`: replace `researchAccessFinding()`'s flat `external_action` with the three blocking branches from design D5 — unavailable with a classified `access_boundary` (owner and derived repair kind from `selectedAdapterBoundaryFact`, universal extent reflected in the feedback), unavailable without classification (explicit "no routeable boundary" direct fact plus the recorded reason, `agent_action`), and the unchanged unprobed/missing path. An available `class_scoped` access boundary is disclosure-only and SHALL NOT enter this function. Done when line-179's unconditional `external_action` is gone and no branch degrades to a generic retry message.
- [ ] 4.6 Implement PRG-002/PRG-010 in `tests/integration/cli/check-gate-hitl1-recorded.test.mjs`: cover the `gate-admission-and-feedback-projection` claim — partial-reachability admission, zero-reachable blocking, one direct finding per boundary location, the `probe_relay` agent route, and the explicit unclassified finding. Done when the file passes under `node --test` against temporary bundles with no network call.
- [ ] 4.7 Implement REA-001/REA-002 in `tests/integration/cli/hitl1-research-access-adapter.test.mjs`: cover the `adapter-contract-declaration-integrity` claim — retained identity/launcher/permission/operation bindings and the absence of source-class, query, taxonomy, or return-map controller content from the adapter. Done when the file passes under `node --test`.
- [ ] 4.8 Fix the pre-existing static-string drift at `tests/integration/cli/hitl1-research-access-adapter.test.mjs:123`. Reader question: does the assertion match the current `phase-hitl1.md` text? Authoritative owner: `phase-hitl1.md` as the static guidance surface. Smallest repair: align the assertion with the current sentence ("不要求用户重复回答 HITL1 choices、运行 `curl` 或手改 profile") after task 5.2 settles that text. Done when the previously failing case passes without weakening what it asserts.

## 5. Agent-Facing Guidance

- [ ] 5.1 Implement REA-002 and PRP-002 in `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-hitl1-capability-probe.md`: retain only generic isolated-probe safety and authority boundaries; require the separately delivered `shared-hitl1-research-access-envelope.md` controller for all class order, query, candidate eligibility, fetch sequence, fallback, classification, and return-map control. Done when this guide does not hardcode a class, query, candidate/fetch/traversal bound, classification axis, or return branch and cannot instruct a second search within a class, a new fallback tier, or a retry.
- [ ] 5.2 Implement PRP-002 in `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-hitl1.md`: add the independent controller to the Phase's required shared nodes and deliver it with the general guide to exactly one isolated probe. Done when the Phase remains the sole `rb_profile.yaml` writer, the probe reads no framework file, the controller is not persisted as evidence or a second source of record, and no HITL checkpoint is added.
- [ ] 5.3 Implement PRP-002/PRP-005 in `phase-hitl1.md`: update the §3d write obligation to relay the controller-governed envelope and `access_boundary` pair unchanged, map spawn/invalid-return to the `probe_relay` boundary location with universal extent and an all-declared-class `unreachable` envelope, add the partial-reachability disclosure trigger, and extend the payload checklist with the envelope and the present-together-or-absent-together pair. Done when the checklist shows an absent pair as explicitly unclassified and the Phase does not restate controller query/class/return-map content.
- [ ] 5.4 Implement HIU-002 in `DEEP_RESEARCH_HARNESS/workflows/nodes/brief/hitl1.md`: add the exact partial-reachability disclosure rendered only alongside the available result when the recorded observation reports unreachable classes, filling the bracketed segment from the recorded classes only. Done when the message offers no options, requests no instructions, promises no restoration, and is absent when no recorded source class is `unreachable`.
- [ ] 5.5 Implement PRP-015 in `phase-hitl1.md`: actor-deliver selected-host operations from the adapter and all class ladder/two-axis/return-map control only from the separate controller to the isolated probe; the Phase itself never invokes search or fetch. Done when a controller revision requires no second edit in the phase body, adapter contract, or general probe guide to stay consistent.
- [ ] 5.6 Implement PRP-002/PRP-005/HIU-002 in `tests/integration/md/phase-hitl1-research-access.test.mjs`: cover the `static-guidance-branch-coverage` claim — controller frontmatter equals the source-class and boundary enums and contains the three exact initial neutral queries, Phase requires and actor-delivers the controller, existing files do not duplicate controller content, the exact disclosure text and its available-only trigger, the prohibition on persisting probe material to research-evidence surfaces, and the absence of a second decision point. Done when the file passes under `node --test` reading only framework artifacts.

## 6. End-To-End Loop

- [ ] 6.1 Implement the `partial-reachability-admission-loop` claim in `tests/e2e/hitl1-research-access-adapter.test.mjs`: a test-controlled temporary bundle whose recorded envelope mixes reachable and unreachable classes passes the existing HITL1 Gate with its classification retained; a test-controlled bundle with no reachable class stays blocked with HITL1 choices preserved. Done when the file passes under `node --test` against temporary bundles with no network call and does not claim that a Phase Agent or probe ran.
- [ ] 6.2 Assert in the same e2e file that evaluating the Gate on those test-controlled profiles does not add probe material to reference, cache, artifacts, receipts, ledger, or coverage floors. Done when the assertions fail if the Gate invocation writes any test-sentinel probe URL, page content, or tool output into those surfaces.

## 7. Release And Specification Sync

- [ ] 7.1 Release v0.85 for SCO-002, REA-001/REA-002, PRG-002/PRG-010, PRP-002/PRP-005/PRP-015, and HIU-002. Update `CHANGELOG.md` with the access-envelope rebuild and `DEEP_RESEARCH_HARNESS/RUN.md` banner/current-release summary with the same bounded claim. Done when both surfaces name v0.85 and neither claims provider availability nor coverage guarantees.
- [ ] 7.2 Sync the five delta specs into their accepted main specs and recompare SCO-002, REA-001/REA-002, PRG-002/PRG-010, PRP-002/PRP-005/PRP-015, and HIU-002. Done when each modified requirement remains once in its owning capability and no new requirement identity or main-spec delta header is introduced.

## 8. Verification And Governance

- [ ] 8.1 Run the selected unit, integration, and deterministic_e2e commands from `verification-plan.yaml`, plus `openspec validate rebuild-hitl1-research-access-envelope --strict`, `node openspec/governance/check-verification-routing.mjs --change rebuild-hitl1-research-access-envelope --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-research-access-envelope --mode assets`, and `git diff --check`. Done when every deterministic claim passes.
- [ ] 8.2 Run the full `node --test` suite and confirm no unrelated regression. Done when the only changed results are the six planned files plus task 4.8's previously failing assertion.
- [ ] 8.3 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change rebuild-hitl1-research-access-envelope`. Done when it passes with 0 duplicate, orphan, unregistered, and reusedRetired IDs.
- [ ] 8.4 Run `node openspec/governance/check-project-specs.mjs`. Done when it passes with 0 deltaHeaderInMain, missingPurpose, missingRequirements, and missingReqHeader findings.

## 9. Closeout

- [ ] 9.1 `openspec-feedback:closeout-review` Review the change-scoped implementation diff, synced main specs, v0.85 release text, semantic closure, verification plan, and deterministic evidence. Confirm no prose-prefix route survives anywhere, the derivation point is unique, and the disclosure adds no decision point. Complete only with no open finding; add each finding as an ordinary task with its owner, smallest repair, and observable done condition.

### 9.2 Governed Archive Transition

After every checkbox above is complete, invoke only
`node openspec/governance/finalize-change-archive.mjs --change
rebuild-hitl1-research-access-envelope` and use its structured result as archive
evidence. This is an operation instruction rather than a checkbox because the
governed finalizer requires every ordinary task to be complete before it can
perform the archive transition.
