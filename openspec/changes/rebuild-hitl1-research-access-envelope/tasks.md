## 1. Apply Preconditions

- [ ] 1.1 `openspec-feedback:plan-review` Review SCO-002, REA-001/REA-002, PRG-002/PRG-010, PRP-002/PRP-005/PRP-015, HIU-002, `design.md`, `semantic-closure.yaml`, and `verification-plan.yaml` before the first target edit. Confirm the fixed-width envelope boundary, the two-axis pairing, owner→repair_kind derivation as the single derivation point, the Gate rule set staying unchanged in count, the exact partial-reachability disclosure text, and the v0.85 release target. Record every actionable finding as an ordinary unchecked task naming its requirement/reader question, authoritative owner, smallest repair, and observable done condition.
- [ ] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change rebuild-hitl1-research-access-envelope --mode plan` and `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-research-access-envelope --mode plan`. Done when both pass and their selected proof classes match the plan (`agent_flow_e2e` remains `not_applicable`).

## 2. Declaration Assets

- [ ] 2.1 Implement SCO-001 in `DEEP_RESEARCH_HARNESS/schema/enums.mjs`: add the `SourceClass` closed enum whose value order is the declared probe ladder (`encyclopedia`, `code_host`, `general_web`), export it from `DEEP_RESEARCH_HARNESS/schema/index.mjs`, and add the `SourceClassReachability` enum (`reachable`, `unreachable`, `not_attempted`) per design D1/D2. Done when both enums are importable, closed, and no consumer can extend them at runtime.
- [ ] 2.2 Implement REA-001 in `DEEP_RESEARCH_HARNESS/host_tools/research-access-adapter.md` frontmatter: replace `unavailable_roots` with the two declared axes (`boundary_locations: [host_surface, host_policy, network_path, probe_relay]`, `boundary_extents: [universal, class_scoped]`) and add `source_classes:` carrying each declared class with its neutral query. Done when the frontmatter declares both axes and the class ladder, and no prefix vocabulary remains in it.
- [ ] 2.3 Implement REA-001 in `DEEP_RESEARCH_HARNESS/host_tools/research-access-adapter.md` body: replace the `surface_absent:` / `permission_required:` prefix prose with the two-axis vocabulary, the owner per boundary location, the class ladder and its any-success rule, and the explicitly-unclassified allowance. Done when the body cannot be read as requiring a textual reason prefix.
- [ ] 2.4 Extend the drift-check in `readSelectedResearchAccessAdapterContract()` (`DEEP_RESEARCH_HARNESS/host_tools/lib/research-access-adapter.mjs`) to assert the frontmatter's `source_classes` list equals the `SourceClass` enum exactly and in order, and that both declared axes equal the resolver's enumerations, throwing on any mismatch per design D1. Done when a deliberate one-value drift in either surface makes the read throw rather than silently disabling classification.

## 3. Schema Contract

- [ ] 3.1 Implement SCO-002 in `DEEP_RESEARCH_HARNESS/schema/contracts/profile.mjs`: add the optional fixed-width `source_class_reachability` array to the `available` and `unavailable` branches per design D2 — one entry per declared class, `.max()` bounded to the declared width, no URL/HTTP-status/query/attempt fields on an entry. Done when the array validates only declared classes and cannot express a per-attempt matrix.
- [ ] 3.2 Implement SCO-002 envelope cross-field rules via `.superRefine()`: reject duplicate class entries, reject an `available` observation with no `reachable` entry, and reject an `unavailable` observation with any `reachable` entry. Done when each rejection has a focused failing case and legacy observations without the field still validate.
- [ ] 3.3 Implement SCO-002 in `profile.mjs`: add the optional nested `unavailable_boundary` object (`location` + `extent`, both closed, `.strict()`) to the `unavailable` branch only, per design D3. Done when a half-populated pair is structurally inexpressible, the whole object is omittable, and the field is rejected on `available` and `unprobed`.
- [ ] 3.4 Implement SCO-002 in `profile.mjs`: extend the `unprobed` branch's contradiction rejection to cover the new envelope and boundary fields. Done when an `unprobed` observation carrying either new field fails validation.
- [ ] 3.5 Implement SCO-002 in `tests/schema/contracts/profile.test.mjs`: cover the `envelope-shape-fixed-width` claim — fixed-width acceptance, each envelope rejection, the paired/closed boundary truth table, the unclassified-remains-valid case, the legacy-readable case, and the `unprobed` contradiction cases. Done when the file passes under `node --test` and asserts no defaulting or normalization of an absent boundary.

## 4. Resolver And Gate

- [ ] 4.1 Implement REA-001 in `DEEP_RESEARCH_HARNESS/host_tools/lib/research-access-adapter.mjs`: rebuild `UNAVAILABLE_FACTS` as a boundary-location-keyed owner table declaring `owner` and `actor` (`external` | `agent`) and no `repair_kind` field, and export `selectedAdapterBoundaryFact(location)` deriving `repair_kind` from `actor` per design D4. Done when `probe_relay` yields `agent_action`, the three external locations yield `external_action`, and the derivation exists at exactly one code point.
- [ ] 4.2 Delete `selectedAdapterUnavailableRoot()` and its prose-prefix parsing from `research-access-adapter.mjs` with no deprecated wrapper, per design D4. Done when no repository code splits a `reason` string to determine identity, owner, or repair lineage.
- [ ] 4.3 Implement REA-001 in `tests/host_tools/research-access-adapter.test.mjs`: cover the `two-axis-classification-and-repair-derivation` claim — the four-location owner/repair truth table, both extent values, the explicit unclassified conclusion, and the absence of any prose-derived route. Done when the file passes under `node --test` and no assertion depends on reason text.
- [ ] 4.4 Implement PRG-002 in `DEEP_RESEARCH_HARNESS/cli/gates/check-gate-hitl1-recorded.mjs`: keep the rule set unchanged in count and keep admission on `research_access.status`, relying on ProfileSchema for envelope consistency per design D6. Done when no research-access check type, cross-field validator, degraded pass, or per-class rule is added.
- [ ] 4.5 Implement PRG-010 in `check-gate-hitl1-recorded.mjs`: replace `researchAccessFinding()`'s flat `external_action` with the three branches from design D5 — classified (owner and derived repair kind from `selectedAdapterBoundaryFact`, extent reflected in the feedback), unavailable-without-classification (explicit "no routeable boundary" direct fact plus the recorded reason, `agent_action`), and the unchanged unprobed/missing path. Done when line-179's unconditional `external_action` is gone and no branch degrades to a generic retry message.
- [ ] 4.6 Implement PRG-002/PRG-010 in `tests/integration/cli/check-gate-hitl1-recorded.test.mjs`: cover the `gate-admission-and-feedback-projection` claim — partial-reachability admission, zero-reachable blocking, one direct finding per boundary location, the `probe_relay` agent route, and the explicit unclassified finding. Done when the file passes under `node --test` against temporary bundles with no network call.
- [ ] 4.7 Implement REA-001/REA-002 in `tests/integration/cli/hitl1-research-access-adapter.test.mjs`: cover the `adapter-contract-declaration-integrity` claim — both declared axes, the class ladder equality/order check, and the retained identity/launcher/permission/operation bindings failing loudly on drift. Done when the file passes under `node --test`.
- [ ] 4.8 Fix the pre-existing static-string drift at `tests/integration/cli/hitl1-research-access-adapter.test.mjs:123`. Reader question: does the assertion match the current `phase-hitl1.md` text? Authoritative owner: `phase-hitl1.md` as the static guidance surface. Smallest repair: align the assertion with the current sentence ("不要求用户重复回答 HITL1 choices、运行 `curl` 或手改 profile") after task 5.2 settles that text. Done when the previously failing case passes without weakening what it asserts.

## 5. Agent-Facing Guidance

- [ ] 5.1 Implement REA-002 and PRP-002 in `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-hitl1-capability-probe.md`: present the declared source-class ladder in declared order with first-success short-circuit, per-class bounds unchanged (one search, ≤3 eligible candidates, native-first, one permitted same-URL fallback), and the per-class reachability result to return. Done when the guide cannot instruct a second search within a class, a new fallback tier, or a retry, and unattempted classes are returned as not attempted rather than unreachable.
- [ ] 5.2 Implement PRP-002 in `shared-hitl1-capability-probe.md`: require the same boundary-location/boundary-extent vocabulary on **every** unavailable branch, including the positive-candidate fetch-side branch that previously required only "one direct non-empty reason", and state explicitly that an observation establishing no owning boundary omits the pair and stays honest. Done when no unavailable branch in the guide is satisfiable by a free-text reason alone and no branch instructs a textual prefix.
- [ ] 5.3 Implement PRP-002/PRP-005 in `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-hitl1.md`: update the §3d write obligation to relay the envelope and boundary pair unchanged, map spawn/invalid-return to the `probe_relay` boundary location, add the partial-reachability disclosure trigger, and extend the payload checklist with the envelope and the present-together-or-absent-together pair. Done when the Phase Agent remains the sole `rb_profile.yaml` writer, no HITL checkpoint is added, and the checklist shows an absent pair as explicitly unclassified.
- [ ] 5.4 Implement HIU-002 in `DEEP_RESEARCH_HARNESS/workflows/nodes/brief/hitl1.md`: add the exact partial-reachability disclosure rendered only alongside the available result when the recorded observation reports unreachable classes, filling the bracketed segment from the recorded classes only. Done when the message offers no options, requests no instructions, promises no restoration, and is absent when every attempted class was reachable.
- [ ] 5.5 Implement PRP-015 in `phase-hitl1.md`: take the class ladder and both axes from the adapter contract's declarations rather than restating them as independent phase prose. Done when a declaration revision requires no second edit in the phase body to stay consistent.
- [ ] 5.6 Implement PRP-002/PRP-005/HIU-002 in `tests/integration/md/phase-hitl1-research-access.test.mjs`: cover the `static-guidance-branch-coverage` claim — classification vocabulary on every unavailable branch, the declared ladder presence, the exact disclosure text and its available-only trigger, and the absence of a second decision point. Done when the file passes under `node --test` reading only framework artifacts.

## 6. End-To-End Loop

- [ ] 6.1 Implement the `partial-reachability-admission-loop` claim in `tests/e2e/hitl1-research-access-adapter.test.mjs`: a bundle whose envelope mixes reachable and unreachable classes advances past HITL1 with its classification retained; a bundle with no reachable class stays blocked with HITL1 choices preserved. Done when the file passes under `node --test` against temporary bundles with no network call.
- [ ] 6.2 Assert in the same e2e file that neither path writes probe material into reference, cache, artifacts, receipts, ledger, or coverage floors. Done when the assertions fail if any probe URL, page content, or tool output leaks into those surfaces.

## 7. Release And Specification Sync

- [ ] 7.1 Release v0.85 for SCO-002, REA-001/REA-002, PRG-002/PRG-010, PRP-002/PRP-005/PRP-015, and HIU-002. Update `CHANGELOG.md` with the access-envelope rebuild and `DEEP_RESEARCH_HARNESS/RUN.md` banner/current-release summary with the same bounded claim. Done when both surfaces name v0.85 and neither claims provider availability nor coverage guarantees.
- [ ] 7.2 Sync the five delta specs into their accepted main specs and recompare SCO-002, REA-001/REA-002, PRG-002/PRG-010, PRP-002/PRP-005/PRP-015, and HIU-002. Done when each modified requirement remains once in its owning capability and no new requirement identity or main-spec delta header is introduced.

## 8. Verification And Governance

- [ ] 8.1 Run the selected unit, integration, and deterministic_e2e commands from `verification-plan.yaml`, plus `openspec validate rebuild-hitl1-research-access-envelope --strict`, `node openspec/governance/check-verification-routing.mjs --change rebuild-hitl1-research-access-envelope --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-research-access-envelope --mode assets`, and `git diff --check`. Done when every deterministic claim passes.
- [ ] 8.2 Run the full `node --test` suite and confirm no unrelated regression. Done when the only changed results are the six planned files plus task 4.8's previously failing assertion.
- [ ] 8.3 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change rebuild-hitl1-research-access-envelope`. Done when it passes with 0 duplicate, orphan, unregistered, and reusedRetired IDs.
- [ ] 8.4 Run `node openspec/governance/check-project-specs.mjs`. Done when it passes with 0 deltaHeaderInMain, missingPurpose, missingRequirements, and missingReqHeader findings.

## 9. Closeout

- [ ] 9.1 `openspec-feedback:closeout-review` Review the change-scoped implementation diff, synced main specs, v0.85 release text, semantic closure, verification plan, and deterministic evidence. Confirm no prose-prefix route survives anywhere, the derivation point is unique, and the disclosure adds no decision point. Complete only with no open finding; add each finding as an ordinary task with its owner, smallest repair, and observable done condition.

### 9.2 Governed Archive Transition

After every checkbox above is complete, invoke only
`node openspec/governance/finalize-change-archive.mjs --change
rebuild-hitl1-research-access-envelope` and use its structured result as archive
evidence. This is an operation instruction rather than a checkbox because the
governed finalizer requires every ordinary task to be complete before it can
perform the archive transition.
