## 0. Review And Bootstrap Boundary

- [x] 0.1 Complete the scoped plan review (`openspec-feedback:plan-review`) for SEF-001..004 and CHF-002..003; before the first target edit, inspect the proposal, deltas, design, records, and verification plan, including the boundary that this governance-only `not_applicable` bootstrap does not classify OpenSpec lifecycle verdicts as Harness runtime families, and SEF-003's future affected-record authority/consumer review boundary rather than treating parser validity as proof; turn any finding into an ordinary unchecked task with owner, smallest repair, and observable done condition.
- [x] 0.2 Before the first target edit, run `node openspec/governance/check-verification-routing.mjs --change establish-semantic-fact-closure-governance --mode plan` and confirm the bootstrap records for SEF-002 and VER-002: keep this change's `semantic-closure.yaml` as a truthful `not_applicable` governance-only declaration and its `verification-plan.yaml` as the selected unit/integration proof route; done when the routing plan check passes before bootstrap edits. The semantic-closure command is unavailable until tasks 1.1 through 1.8, and task 1.9 owns its required plan-mode dogfood.

## 1. Semantic Closure Core

- [ ] 1.1 Implement SEF-001: add `openspec/governance/semantic-fact-families.yaml` with the exact thirteen initial bounded family ID/question pairs and no `other` fallback; done when the catalog is strict-parseable and each initial family is independently identifiable.
- [ ] 1.2 Implement SEF-002 catalog and base-record parsing in `openspec/governance/semantic-fact-closure-contract.mjs`: strict `semantic-fact-families/v1` (`schema_version`, `families[].id`, `families[].bounded_question` only), safe change identity/coordinate helpers, and closed status branches (`not_applicable` has only `reason`; `affected` has explicit `catalog_additions` plus a non-empty affected sequence); done when valid/invalid catalog and governance-only record objects produce deterministic parser results.
- [ ] 1.3 Implement SEF-002 affected-entry parsing in the same contract: one resolver, non-empty `established_by`/consumer declarations, and non-empty structural overlap (`authoritative|derived|retired` with a coordinate, or a sole explanatory `none`); done when each required declaration and real/no/mixed overlap form has deterministic parser results without claiming SEF-003 consumer completeness.
- [ ] 1.4 Implement the remaining SEF-002/SEF-004 cross-record refinements: unique affected families, no `reason` in the affected branch, same-change catalog additions used exactly once, and two distinct verification coordinates; done when the contract exposes these direct structural failures without scanning implementation bodies.
- [ ] 1.5 Implement SEF-004 plan mode in `openspec/governance/check-semantic-closure.mjs`: resolve and strict-parse the selected change/record/global catalog in either status branch, require each same-change catalog addition to be absent and every affected family to be cataloged or added by that record, produce root-first `family`/`missing_fact`/`owner`/`write_to`/rerun feedback, use `parseVerificationRoutingPlan` from `verification-routing-contract.mjs` rather than duplicate its schema or route rules, reject a parsed plan whose `change` differs from the selected change, and confirm declared verification coordinates are selected assets; done when it makes no writes, executes no tests, and cannot claim semantic or runtime PASS.
- [ ] 1.6 Extend SEF-004 checker assets mode: repeat common structural validation but replace the plan-only catalog-addition absence predicate with a requirement that every declared addition is present with the same bounded question, then require the catalog plus declared authority/`established_by`/consumer/overlap/verification coordinates to be regular files whose realpaths remain inside the repository root; done when lexical and symlink escapes, missing paths, non-files, missing additions, and mismatched addition questions return the nearest structural root without reinterpreting verification-routing boundaries.
- [ ] 1.7 Add focused SEF-001/SEF-002 unit proof at `tests/governance/semantic-fact-closure-contract.test.mjs`; cover strict catalog shape and exact initial ID/question pairs (including `work-unit.assignment-output-obligation`), both record branches, unknown/new families, unused catalog additions, real/no/mixed/unsafe overlaps, duplicate/unsafe coordinates, and distinct verification coordinates; done when the parser tests pass without presenting SEF-003 semantic completeness as a unit-test claim.
- [ ] 1.8 Add focused SEF-004 integration proof at `tests/integration/governance/check-semantic-closure.test.mjs`; cover canonical selected-verification references including plan/change identity mismatch, plan-only rejection of a re-added family, assets-mode acceptance after its matching catalog write, missing/mismatched additions, plan-versus-assets separation, missing/malformed catalog for either status branch, missing/non-file/realpath-symlink failures, root-first diagnostics, and read-only behavior; done when the production CLI tests pass against isolated changes.
- [ ] 1.9 Run `node --test tests/governance/semantic-fact-closure-contract.test.mjs tests/integration/governance/check-semantic-closure.test.mjs` and then `node openspec/governance/check-semantic-closure.mjs --change establish-semantic-fact-closure-governance --mode plan` after the bootstrap assets exist; done when both focused proof commands and this change's governance-only record pass, and no registry/configuration/lifecycle/finalizer target is edited before that evidence exists.

## 2. Lifecycle Integration

- [ ] 2.1 Implement SEF-001 and RET-001..004 registration: add `SEF: governance/semantic-fact-closure`, SEF-001..004, and the live requirement group in `openspec/governance/req-registry.yaml`, plus the `governance/semantic-fact-closure` row with correct control boundaries in `openspec/specs/README.md`; done when the required registry/catalog records are present in their canonical positions and the post-sync governance checks can resolve the new capability without duplicate or orphan records.
- [ ] 2.2 Implement SEF-002 and CHF-002: update `openspec/config.yaml` proposal/apply guidance so proposal instructions identify `openspec/governance/semantic-fact-families.yaml` as the only family vocabulary, require a change-root `semantic-closure.yaml`, present distinct `not_applicable`/`affected` starters without choosing a branch, and direct a catalog addition to be written before the first target edit that relies on it; every selected change then runs verification-routing plan mode followed by semantic-closure plan mode before target edits, feedback-lifecycle changes do so after review, and missing records have one repair/rerun boundary; done when fresh proposal and apply instruction JSON expose that route without treating guidance as a verdict.
- [ ] 2.3 Implement SEF-003 and CHF-002 review delivery in `guidelines/change-feedback-loop.md`: for an `affected` record, prompt both plan and closeout review to assess bounded fact/resolver/establishing surfaces/consumers/overlap against the relevant planned or actual change; for `not_applicable`, prompt review to assess the reason against touched surfaces; preserve the ordinary finding-task route and state that checker structure PASS is not semantic completeness. Done when the guideline remains advisory, names no new lifecycle state or archive authority, and gives the Agent a bounded review question.
- [ ] 2.4 Implement CHF-002's single entry inventory in `openspec/governance/finalize-change-archive.mjs`: replace the deleted `.codex` paths in `SUPPORTED_ENTRY_SURFACES` with the four current apply paths (`.agents/skills/openspec-apply-change/SKILL.md`, `.agents/skills/source-command-opsx-apply/SKILL.md`, `.claude/skills/openspec-apply-change/SKILL.md`, `.claude/commands/opsx/apply.md`) and four matching archive paths (`.agents/skills/openspec-archive-change/SKILL.md`, `.agents/skills/source-command-opsx-archive/SKILL.md`, `.claude/skills/openspec-archive-change/SKILL.md`, `.claude/commands/opsx/archive.md`); done when every declared target exists and no deleted `.codex` path remains in the exported inventory.
- [ ] 2.5 Implement CHF-002 across every apply entry declared by the reconciled `SUPPORTED_ENTRY_SURFACES`; done when each preserves plan review, runs verification-routing then semantic-closure plan checks in order for every selected change even without feedback markers, has no missing-command fallback after these entries land, and stops before other target edits on failure.
- [ ] 2.6 Implement CHF-002 across every archive entry declared by the reconciled `SUPPORTED_ENTRY_SURFACES`; done when each blocks an unmarked selected change before finalization, directs it to add the existing review markers and resume apply, and has no raw/native alternate archive success path.
- [ ] 2.7 Implement CHF-003: extend `openspec/governance/finalize-change-archive.mjs` to run semantic-closure assets mode after verification-routing assets and before native archive, with one `semantic_closure_failed` root, one `semantic_closure` passed-check entry in the strict result schema, and the existing finalizer rerun coordinate; done when it preserves first-failure ordering and does not add semantic review, spec sync, test-running, or archive alternatives.

## 3. Verification And Closeout

- [ ] 3.1 Update `tests/governance/change-feedback-finalizer.test.mjs` for CHF-003; done when the unit contract proves the new root/check result-schema entries, semantic-closure ordering, short-circuit behavior, and no native archive after the new failure root.
- [ ] 3.2 Update `tests/integration/governance/change-feedback-finalizer.test.mjs` for SEF-002..003 proposal/config/guideline integration; done when isolated OpenSpec instruction/config fixtures prove proposal guidance exposes the global catalog, both closure-record branches, the rule that a catalog addition is absent in plan but written before its first dependent target edit and required to match in assets mode, and the review prompt that preserves Agent-owned semantic completeness without treating guidance as a verdict.
- [ ] 3.3 Update `tests/integration/governance/change-feedback-finalizer.test.mjs` for CHF-002 apply-entry integration; done when it reads the reconciled finalizer-owned entry inventory, every declared current apply entry demonstrates verification-routing followed by semantic-closure plan checks for marked and unmarked selected changes, then delivers the current semantic-closure review prompt with no missing-command fallback after the capability lands.
- [ ] 3.4 Update `tests/integration/governance/change-feedback-finalizer.test.mjs` for CHF-002..003 archive integration; done when it reads the reconciled finalizer-owned entry inventory, every declared current archive entry blocks an unmarked change without a native bypass, delivers the current semantic-closure closeout prompt, and the archive finalizer exposes the new semantic-closure checker boundary.
- [ ] 3.5 Run `node --test tests/governance/semantic-fact-closure-contract.test.mjs tests/governance/change-feedback-finalizer.test.mjs tests/integration/governance/check-semantic-closure.test.mjs tests/integration/governance/change-feedback-finalizer.test.mjs` and `node openspec/governance/check-verification-routing.mjs --change establish-semantic-fact-closure-governance --mode plan`; done when all selected unit/integration commands pass and the route checker reports only route validity.
- [ ] 3.6 Perform Agent-owned delta/main spec sync and re-comparison for SEF-001..004 and CHF-002..003; done when accepted main specs, the requirement registry, and the catalog have one coherent meaning and no unresolved semantic-delta finding remains.
- [ ] 3.7 Run `node openspec/governance/check-project-reqs.mjs` and `node openspec/governance/check-project-specs.mjs`; done when both pass with zero reported governance defects.
- [ ] 3.8 Run `node openspec/governance/check-capability-taxonomy.mjs`, `node openspec/governance/check-capability-discovery.mjs --change establish-semantic-fact-closure-governance`, `node openspec/governance/check-verification-routing.mjs --change establish-semantic-fact-closure-governance --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change establish-semantic-fact-closure-governance --mode assets`; done when all four checks pass with the real assets present.
- [ ] 3.9 Complete the scoped closeout review (`openspec-feedback:closeout-review`) for the change-scoped diff, delta/main comparison, and selected evidence; any finding remains an ordinary unchecked repair task, and done means no open finding remains before the governed finalizer is invoked.
