## 1. Pre-Target Evidence And Contract Tests

- [ ] 1.1 @impl DEW-021: Create `implementation-evidence.md`; record the v0.47 worktree/baseline and reproduce claim invalid-tuple, envelope completion, and mixed-root dry-submit behavior on fresh disposable bundles without editing diagnostic production state; run `node openspec/governance/check-verification-routing.mjs --change make-delegated-work-contracts-constructible --mode plan` before target edits. Done when every planned claim is classified as deterministic evidence, real-actor evidence, or an honest external boundary.
- [ ] 1.2 @impl DEW-021: Add focused red unit coverage in `tests/engine/work-unit-contract-constructibility.test.mjs` for the four legal observation case shapes, invalid tuple vocabulary/no synthetic outcome, and candidate primary selection parity between recommended action and all public primary repair fields. Done when the tests fail on the current opaque parse/`violations[0]` split without a bundle, queue mutation, or external call.
- [ ] 1.3 @impl DEW-021: Add focused red integration coverage in `tests/integration/engine/work-unit-contract-constructibility.test.mjs` for Wave0 plus primary/supplementary Wave1 claim/envelope projection parity, task/spawn first-entry reference, no pre-created result/receipt/cache/output/ledger authority files, old-envelope readability, and prerequisite masking. Extend `tests/integration/cli/operate-work-unit.test.mjs` for structured CLI invalid-tuple/no-mutation and dry/formal/timeout primary-root consistency. Done when tests distinguish independent roots from derived symptoms and use real temporary bundle contracts only.

## 2. One Contract Lineage At Claim And Actor Entry

- [ ] 2.1 @impl DEW-021: Refactor `DPT_FRAMEWORK/schema/contracts/work-unit.mjs` and `DPT_FRAMEWORK/engine/work-unit-actor.mjs` so one closed immutable observation-case definition supplies both Zod validation and the output-only role-bound vocabulary projection. Done when legal tuple/fallback/no-claim semantics remain unchanged, invalid input names the conflicting fact and valid cases, no generic HITL/trace conversion exists, and code comments carry `@impl DEW-021`.
- [ ] 2.2 @impl DEW-021: Update `DPT_FRAMEWORK/engine/work-unit-lifecycle.mjs` and `DPT_FRAMEWORK/cli/operate-work-unit.mjs` to return the structured invalid/no-claim projection through the existing claim decision path before trace or allocation mutation. Done when no concrete observation result is fabricated, existing valid claim/fallback behavior and exit semantics remain compatible, and the same claim checkpoint is the sole repair path.
- [ ] 2.3 @impl DEW-021: Update `DPT_FRAMEWORK/engine/work-unit-envelope.mjs` to generate the non-authoritative `completion-contract.md` from existing manifest/beacon/result-schema/direct-output/cache/source/receipt owners, then make task and spawn identify it as the first authoring entry while removing task-level duplicated completion fragments. Done when projection parity holds for Wave0, primary Wave1 and supplementary Wave1, the task still retains role/lifecycle context, and no manifest/beacon/result/receipt/cache/output/ledger schema or bytes are added merely by claim.

## 3. One Primary Root Across Existing Checkpoints

- [ ] 3.1 @impl DEW-021: Refactor `DPT_FRAMEWORK/engine/work-unit-candidate-projection.mjs` to expose one pure selected public root together with its existing action/code, preserving all independently evaluated violations and their coordinates. Done when scope precedence and phase ordering remain deterministic, selection adds no persistent state or validator, and no consumer independently selects `violations[0]`.
- [ ] 3.2 @impl DEW-021: Update `DPT_FRAMEWORK/engine/work-unit-submit.mjs` and `DPT_FRAMEWORK/engine/work-unit-timeout-preflight.mjs` to consume that selected root for dry-submit, formal rejection and timeout advice, and to short-circuit only checks that lack their prerequisite authority. Done when primary action/code/repair fields agree, same-check repair remains the only legal mechanical loop, and ledger/queue/provenance/Gate/terminal-replacement semantics do not change.

## 4. Real-Actor Boundary And Regression

- [ ] 4.1 @impl DEW-021: Update the existing `experiments_playbook/exp_wfn_wave1/case-221-heavy-batch-subagent.md` evidence contract so a real actor's supplied task/projection proves that `completion-contract.md` was the authoring entry before its first returned work, without parent-authored output, invented receipt/trace facts, a new verdict check, or a claim of research quality. Done when the case retains its existing durable evidence roles and native submit/inspect/Gate authority.
- [ ] 4.2 @impl DEW-021: Run the selected unit/integration suites, `validate-work-unit-hygiene`, `validate-workflow-package`, and the existing case-221 Agent-flow path when authenticated real actor runtime/budget are available. Done when deterministic tests pass and the real path records native PASS/FAIL/NOT_RUN with exact evidence; fixture, mock, or manually authored artifacts never substitute for actor behavior.

## 5. Release, Governance And Closure

- [ ] 5.1 @impl DEW-021: Update root `CHANGELOG.md` for `v0.48` and synchronize `DPT_FRAMEWORK/RUN.md` banner/current-release summary. Done when both describe generated constructible contract entry and unified primary feedback without claiming a real actor behavior result not observed.
- [ ] 5.2 @impl DEW-021: Complete `implementation-evidence.md` and audit the final surface list. Done when it identifies the three added output-only projections, the removed duplicate task/primary-selection paths, no new state/validator/controller/retry/success authority, and BUG-114/115 closure only at their stated evidence boundaries.
- [ ] 5.3 @impl DEW-021: Run `node openspec/governance/check-verification-routing.mjs --change make-delegated-work-contracts-constructible --mode assets`. Done when selected assets exist at canonical paths and the actor/runtime/external-call/verdict boundaries remain declared.
- [ ] 5.4 @impl DEW-021: Run `node openspec/governance/check-project-reqs.mjs`. Done with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired violations, including registered `DEW-021`.
- [ ] 5.5 @impl DEW-021: Run `node openspec/governance/check-project-specs.mjs`. Done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [ ] 5.6 @impl DEW-021: Run `openspec validate make-delegated-work-contracts-constructible --strict`, `git diff --check`, and focused compatibility review. Done when proposal/spec/design/tasks agree on no generic HITL proof, no pre-created authority, one primary selector, v0.48 release metadata, and unchanged queue/Gate/ledger/replacement authority.
