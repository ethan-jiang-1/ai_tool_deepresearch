## 1. Apply Entry And Verification Governance

- [ ] 1.1 Create `implementation-evidence.md` before target edits. Done when it records the approved route plan, apply target manifest, explicit `tests/e2e/` simulation boundary, deferred per-row authority scenario, commands to run, native verdict locations, and residual-risk sections without copying PASS/FAIL into `verification-plan.yaml`. (VER-002, VER-003, VER-004)
- [ ] 1.2 At apply entry, confirm that `VER` and `VER-001` through `VER-005` exist under `# verification-routing` in `openspec/governance/req-registry.yaml`, and record their stale three-method/fourth-layer descriptions for post-bootstrap correction. Done when no registry edit occurs before plan-mode dogfood and the exact correction remains tracked by task 1.8. (VER-001, VER-002, VER-003, VER-004, VER-005)
- [ ] 1.3 Implement `openspec/governance/verification-routing-contract.mjs` as the single strict Zod parser and `check-verification-routing.mjs --change <name> --mode plan|assets` as its read-only CLI consumer. Done when `.strict()`/`.superRefine()` enforce exactly four test classes, `selected|not_applicable` status, unique claim id plus non-empty statement, exact test-class/proof-subject/asset/verdict tuples, change identity, the closed `execution_profile`, and no result-bearing or redundant method fields. (VER-002, VER-003)
- [ ] 1.4 Implement safe path and asset-registration checks. Done when plan mode rejects absolute/traversal/normalized escape and wrong ownership roots, while assets mode additionally rejects missing/non-regular/realpath-escape assets, duplicate `agent_flow_e2e` case identity, filename/frontmatter mismatch, failed canonical playbook validation, and missing or duplicate `RUN_EXPS.md` registration. (VER-003)
- [ ] 1.5 Add `tests/governance/verification-routing-contract.test.mjs` as focused `unit` coverage for strict parsing, missing/extra test classes, every invalid `selected|not_applicable` combination, selected-class claim cardinality, compatible versus conflicting shared asset paths, all four test-class/proof-subject tuples, deterministic/real-human/AI-judge provenance, rejected redundant route fields, and execution-profile cross-field rules. Done when it imports the shared contract directly, uses no subprocess or repository asset lookup, and each schema failure identifies the nearest invalid field. (VER-002, VER-003)
- [ ] 1.6 Add `tests/integration/governance/check-verification-routing.test.mjs` as `integration` coverage for the real checker process, lexical and realpath safety, planned versus existing assets, filename/frontmatter/case uniqueness, runner registration, same-mode rerun diagnostics, and read-only repository behavior. Done when valid and invalid fixture repositories prove both checker modes without executing tests/playbooks or mutating the fixture tree. (VER-002, VER-003)
- [ ] 1.7 Dogfood plan mode immediately after tasks 1.3-1.6 and before every non-bootstrap target edit. Done when all seven claims and all four selected test classes pass route validation without emitting an execution verdict; registry, rerun proof, `tests/e2e/`, playbook, config, guidance, and documentation surfaces remain untouched until then. (VER-002, VER-003, VER-004, VER-005)
- [ ] 1.8 After plan mode passes, update `openspec/governance/req-registry.yaml`. Done when VER-001/002/004/005 describe the four canonical test classes and claim-to-`test_class` routing without the retired three-method/fourth-layer model and registry ordering remains valid. (VER-001, VER-002, VER-003, VER-004, VER-005)

## 2. Focused Unit And Integration Proof

- [ ] 2.1 Preserve `tests/engine/helpers/wave-contract-evaluators-direction.test.mjs` coverage for matching, stale, future/crash-window, legacy-unbound, and invalid states. Done when the focused helper test passes and remains scoped to deterministic resolver semantics. (VER-001, VER-004)
- [ ] 2.2 Replace test-local eligible-row filtering in `tests/integration/cli/rerun-round-continuity.test.mjs` with `spawnSync('node', [...])` calls to `operate-work-unit.mjs inspect --eligible-rows`. Done when structured output proves current-round inclusion, prior-round exclusion, legacy warnings, inconsistent-authority fail-closed behavior, and no shell interpolation or Engine-internal filtering import. (VER-001, VER-004)
- [ ] 2.3 Record the backlog per-row projection-authority scenario as deferred behavior in implementation evidence. Done when no `node_test` implements an alternate eligible-work-id-to-projection matcher and no existing return-map check is overclaimed as that missing contract. (VER-004)

## 3. Deterministic E2E Under tests/e2e

- [ ] 3.1 Add `tests/e2e/README.md` defining `tests/e2e/` as the `deterministic_e2e` class boundary. Done when it states: JS may simulate explicitly labeled Agent/human-owned Markdown/YAML/artifact fixture inputs and actor candidate result/receipt/output files; candidates remain non-authoritative until real submit acceptance; accepted status, declaration ledger rows, gate attempts, transitions, trace, and verdicts must come from production paths; the class proves deterministic chain behavior, not real human or Agent intelligence. (VER-001, VER-004, VER-005)
- [ ] 3.2 Build reusable harness primitives for `tests/e2e/rerun-round-continuity.test.mjs`, extracting only genuinely reused operations to `tests/e2e/helpers/`. Done when argument-array `spawnSync`, tmp-root allocation, byte snapshots, runtime-generated baseline cloning, and `after()` cleanup are available; helpers do not calculate verdicts/transitions, append trace, or write accepted Engine authority. (VER-001, VER-004)
- [ ] 3.3 Build one immutable legal HITL2 baseline per test run. Done when it begins with `instantiate-run-bundle.mjs --target-dir <os.tmpdir()>`, reaches HITL2 through real predecessor gates/handoffs/status synchronization with no `writeGateAttempt()` shortcut, remains under the tmp root, is never committed/cached across runs, and is cloned before every mutating scenario. (VER-001, VER-004)
- [ ] 3.4 Implement the happy deterministic rerun chain HITL2 -> phase-rerun -> seed-topics -> wave0 -> wave1 -> wave2 -> HITL2 from an isolated baseline clone. Done when JS supplies only minimum fixture-labeled non-Engine inputs and actor candidate files, real work-unit submit accepts candidate bindings where required, and real gate/enter-phase/advance-status/trace paths produce every deterministic transition and authority mutation asserted by the test. (VER-001, VER-004)
- [ ] 3.5 Add missing and malformed profile, required phase artifact, and structured-authority input cases at their real owning checkpoints. Done when each expected checkpoint fails nonzero with structured root-cause feedback, downstream CLI steps are not invoked, and Engine-owned authority snapshots remain unchanged except for real failed-attempt diagnostic writes. (VER-001, VER-004)
- [ ] 3.6 Add stale, invalid, and future/crash-window direction cases at the real Wave2 direction consumer. Done when stale/invalid directions do not activate `action:add`, future direction preserves current crash-window semantics, and a fixture-labeled Markdown recovery brings profile count into match without rewriting direction before rerunning the same real consumer. Done condition SHALL state that profile increment/reuse is simulated Markdown behavior, not an Engine recovery verdict. (VER-001, VER-004)
- [ ] 3.7 Add partial-phase and gate-failure recovery cases. Done when a failed wave checkpoint preserves the latest legal node/gate/status, a fixture-labeled Agent-owned repair is applied, the same gate is rerun, and the chain continues without a second transition or hidden fallback path. (VER-001, VER-004)
- [ ] 3.8 Add current-round, prior-round, legacy, and inconsistent work-unit cases within the long chain using real work-unit claim/submit/inspect operations. Done when fixture candidate result/receipt/output files are explicitly identified as non-authoritative inputs, and `eligible_rows`, accepted receipt bindings, submitted status, declaration ledger rows, warnings, and fail-closed output come only from Engine/CLI acceptance. (VER-001, VER-004)
- [ ] 3.9 Add authority-snapshot and anti-fabrication assertions across expected failures. Done when status/queue/work-unit index/ledger/trace changes are either absent or exactly attributable to the invoked real CLI, and the test never hand-writes a passing gate attempt, accepted/submitted state, declaration ledger row, transition witness, trace verdict, or status transition. (VER-001, VER-004)
- [ ] 3.10 Verify canonical discovery, runtime budget, and scope hygiene. Done when `npm test` discovers `tests/e2e/**/*.test.mjs`, the focused E2E file completes within 60 seconds locally with zero external calls, no repo-top-level `tests_e2e/` exists, no mixed test/playbook runner is added, and helpers are reused without reimplementing Engine logic. (VER-001, VER-004)
- [ ] 3.11 Handle any real framework defect exposed by the long chain through OpenSpec feedback, not test weakening. Done when a failing production boundary is recorded in implementation evidence and implementation pauses to update proposal/spec/design/tasks before any newly required `DPT_FRAMEWORK/` behavior or version scope is changed. (VER-004)

## 4. Agent Flow E2E Real-Agent Canary

- [ ] 4.1 Create `experiments_playbook/exp_wfn_rerun/case-318-heavy-rerun-direction-recovery.md`. Done when it uses a fresh disposable bundle, declares a real subject-Agent dependency, requires that subject Agent to write the current rerun direction and recover the controlled direction/profile interruption, records verdict-affecting facts as trace checks, and derives verdict from bundle-root `rb_trace.jsonl`. (VER-001, VER-004)
- [ ] 4.2 Add the Reality Distance Ledger and anti-fabrication boundary. Done when the playbook and route declaration agree on `fixture: setup_only`, `subject_execution: real_agent`, `runtime: real_disposable_bundle`, and `external_calls: none`; distinguish the coding-Agent runner from the subject Agent; identify the exact legal pre-interruption fixture boundary; and forbid inline JS/shell from producing any subject-Agent-attributed direction/recovery pass fact or manufacturing accepted Engine authority. (VER-001, VER-004)
- [ ] 4.3 Register the exact case-318 path once in active `experiments_playbook/RUN_EXPS.md` and update only adjacent experiment navigation needed to expose it. Done when case identity is globally unique, filename/frontmatter match, weight/cost are consistent, and the runner describes the precise proof and non-proof boundaries. (VER-003, VER-004)

## 5. Knowledge-Surface Convergence

- [ ] 5.1 Rewrite the verification section of `openspec/config.yaml` to name the single four-class taxonomy and ownership: `unit`, `integration`, and `deterministic_e2e` are JS-led under `tests/` (`tests/integration/` and `tests/e2e/` for the latter two), while `agent_flow_e2e` is coding-Agent/Markdown-led under `experiments_playbook/` over real disposable bundles. Done when config requires `verification-plan.yaml`, plan-mode checking before target edits, and asset-mode checking before archive; identifies that wiring as repo lifecycle discipline rather than an OpenSpec-native artifact gate; points to the accepted `verification-routing` spec without copying its full schema; and does not present `regression`, controlled-E2E prose, cost, actor, or a runtime bundle type as a peer class. (VER-001, VER-002, VER-003, VER-005)
- [ ] 5.2 Update `AGENTS.md` and `CLAUDE.md` with the same concise asset-placement hard rule: `unit`, `integration`, and `deterministic_e2e` live under `tests/`; `agent_flow_e2e` lives under `experiments_playbook/`; routing semantics live in `verification-routing`. (VER-005)
- [ ] 5.3 Align `guidelines/project-charter.md`, root `README.md`, `tests/README.md`, `tests/integration/README.md`, `experiments_playbook/README.md`, and the cost summaries in `experiments_playbook/RUN_EXPS.md` to owned boundary facts and pointers. Done when `tests/e2e/` is discoverable; `experiments_playbook/README.md` no longer says `tests/` contains only unit+integration; both experiment entry surfaces agree with `guidelines/command-experiments.md` that real Agent/sub-agent, external-call, long-chain, or other slow work may be `heavy`; ordinal layer identifiers are not used as class names; and no document claims JS simulation proves subject-Agent behavior. (VER-001, VER-004, VER-005)
- [ ] 5.4 Add a concise pointer in `guidelines/command-experiments.md` explaining when deterministic long-chain proof belongs in `tests/e2e/` and when a real-Agent claim still requires a controlled playbook. Done when the full routing policy is not duplicated. (VER-001, VER-002, VER-005)
- [ ] 5.5 Converge all five existing subprocess-bearing test files found outside `tests/integration/`: move `tests/governance/check-project.test.mjs` to `tests/integration/governance/`; move `tests/helpers/run-gate-with-monitor.test.mjs` and `tests/schema/verify-bundle-health.test.mjs` to `tests/integration/experiments_env/`; split only the production-CLI scenario from `tests/engine/queue-manager-receipts-cli-render.test.mjs` into `tests/integration/cli/operate-queue-receipts-render.test.mjs`; and split the `apply-research-style.mjs CLI integration` block from `tests/schema/research-styles-computation.test.mjs` into `tests/integration/cli/apply-research-style.test.mjs`. Done when focused in-process queue/schema/style cases remain at their unit-owned paths, behavior and existing `@impl` coverage are unchanged, file-local path constants/comments and inventory references such as `tests/integration/cli/exit-code-convention.test.mjs` are updated, local README/navigation is updated, `npm test` discovers every case exactly once, and `rg` finds no remaining production CLI/subprocess test outside `tests/integration/` or `tests/e2e/`. (VER-001, VER-005)
- [ ] 5.6 Add `tests/integration/md/verification-routing-knowledge-surfaces.test.mjs` as the narrow executable guard for VER-005. Done when it reads only the high-frequency entry and governance surfaces named in this change, including `openspec/governance/req-registry.yaml`; confirms canonical class identifiers plus owned-directory/spec pointers; rejects ordinal or regression-only/controlled/real-environment peer taxonomies; confirms experiment cost summaries allow real Agent/sub-agent, external-call, long-chain, or other slow work to be `heavy`; and does not duplicate the route matrix, plan schema, or checker semantics. (VER-005)
- [ ] 5.7 Sync the `workflow-directory-contract` WDC-005 delta through archive. Done when the accepted requirement teaches the same four `test_class` values, includes `tests/e2e/` and integration ownership, and explicitly permits test-owned temporary fixture I/O inside focused in-process `unit` tests. (VER-001, VER-005)

## 6. Native Evidence And Source Reconciliation

- [ ] 6.1 Run the route parser/checker focused tests, knowledge-surface contract, resolver test, focused rerun integration test, deterministic `tests/e2e/` suite, and full `npm test`. Done when commands and native `node:test` outcomes are recorded in implementation evidence, with no claim broader than `proof_subject: deterministic_contract`. (VER-003, VER-004, VER-005)
- [ ] 6.2 Run the canonical playbook validator and execute case-318 with a real subject Agent. Done when native trace verdict and health/cleanup outcome are recorded; NOT RUN or missing subject-Agent execution leaves the `proof_subject: agent_behavior` claim incomplete rather than converted into PASS. (VER-003, VER-004)
- [ ] 6.3 Audit runtime and evidence labels after execution. Done when the case remains `test_class: agent_flow_e2e` despite using a real disposable bundle, its PASS/FAIL and cleanup/preservation outcome comes from native runner/trace evidence, and no fixture or disposable result is reported as separately selected live-production evidence. (VER-001, VER-002, VER-004)
- [ ] 6.4 After deterministic and real-Agent proof complete, annotate only the metadata/header of `_backlog/plans/tests-e2e-layer.md` as superseded by this change. Done when the body remains historical input and the pointer explains that its deterministic intent moved to the `deterministic_e2e` class under `tests/e2e/`, not a parallel taxonomy. (VER-005)
- [ ] 6.5 Reconcile `seed-backfill-round-continuity` task 10.7 only from the newly executed case-318 native trace evidence. Done when the old checked task is corrected or annotated honestly if the real-Agent execution did not complete. (VER-004)

## 7. Completion Checks

- [ ] 7.1 Run `node openspec/governance/check-verification-routing.mjs --change formalize-verification-routing --mode assets`. Done when all selected class assets exist at their declared boundaries and case-318 has one active registration. (VER-003)
- [ ] 7.2 Run `openspec validate formalize-verification-routing --strict`. Done when proposal, delta spec, design, and tasks validate; completion wording does not imply OpenSpec natively validated the custom plan. (VER-001, VER-002, VER-003, VER-004, VER-005)
- [ ] 7.3 Run `node openspec/governance/check-project-reqs.mjs`. Done when it reports 0 duplicate, 0 unregistered, 0 orphan, and 0 reusedRetired requirement IDs. (VER-001, VER-002, VER-003, VER-004, VER-005)
- [ ] 7.4 Run `node openspec/governance/check-project-specs.mjs`. Done when it reports 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader violations. (VER-001, VER-002, VER-003, VER-004, VER-005)
- [ ] 7.5 Perform the final scope audit against the apply target manifest. Done when there is one route parser, one route checker, one deterministic `tests/e2e/` chain, one real-Agent case, no `DPT_FRAMEWORK/` behavior change, no top-level `tests_e2e/`, no mixed runner, and no fabricated Engine authority. (VER-001, VER-002, VER-003, VER-004, VER-005)
