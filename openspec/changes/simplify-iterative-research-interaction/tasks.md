## 1. Apply preflight and failing contract baselines

- [ ] 1.1 @impl VER-001, VER-002, VER-003: Run `node openspec/governance/check-verification-routing.mjs --change simplify-iterative-research-interaction --mode plan` before any target edit; done when all seven claims validate and the apply target manifest in `design.md` confirms no new state, Gate, CLI, transition, message transport or persistent interaction surface.
- [ ] 1.2 @impl SWE-001, SWE-004, SWE-005, SWE-006, CPT-001, CPT-003, DEW-003, DEW-013, WNC-008, GSK-002, GSK-006, CHI-001, REI-003, STM-003, RWG-018, RES-002, CDP-004: Update/add focused cue, autonomous/terminal header, fatigue-advice and CLI regression assertions so current `interaction: prohibited`, absolute no-reply/surfacing wording, all stop:no Gate/entry/status/claim projections, and representative rerun/seed-topics/queue/topic-state/generated-task/Wave1/Wave2 action-bearing advice that says ask/return/enter/surface/involve-user fail against the target direction-aware contract; done when tests preserve every existing fatigue threshold/verdict, route, true `repair_kind`/`missing_fact`/`write_to`/`rerun`, `next_action`, locator, `required` HITL and `terminal_delivery` Final expectation, correct recorded-profile style failures to the existing `apply-research-style.mjs` mechanical owner, and expose only the intended token/advice/owner drift.
- [ ] 1.3 @impl HIU-001, HIU-002, HIU-003, HIU-005, HIU-006, PRP-002, CDP-001, CDP-004, ACS-001, RUE-005, CHI-001, RWP-002, RWP-018, RWP-019, DEW-013: Add `tests/integration/md/iterative-interaction-contract.test.mjs` characterization/target assertions for enumerated owner surfaces that declare recommendation-first HITL1/HITL2, permit natural-language acceptance/correction, remove blanket second confirmation/fixed-round nudges/text-sentinel uncertainty, require a reply to user-initiated turns without new authority, treat `repair_kind`/timeout `block` as Agent-facing responsibility rather than interaction timing at every non-terminal stop:no consumer, expose only two framework-initiated waits, and scan `phase-final.md` plus `TERMINAL_DELIVERY_HEADER` for factual reply without a Final loop or terminal-cue mutation. The same test SHALL require `phase-hitl2.md` to own one repo-root read-only Node ESM import of existing `loadGateDefinition`, exact gate/rule/check/target/operator/value/count guards, fail-closed unavailable wording and the pre-increment `current_count = limit - 1 -> next_count = limit -> rerun unavailable` case, while `brief/hitl2.md` contains no duplicate invocation; done when old enum-first/absolute-silence/micro-control/automatic-escalation/ambiguous-loader surfaces fail without using presentation trivia as a blocking proxy for real Agent behavior.
- [ ] 1.4 @impl REI-003, REI-005: Rewrite the rerun-ready Gate boundary test setup to read the production-parsed active `rerun_count` rule/operator/value and assert post-increment below/at/above-boundary behavior; done when the Gate accepts `limit - 1`, rejects `limit` and above, the current active definition is unchanged, the two stale expectations fail before implementation cleanup, and no new limit constant/helper is introduced.

## 2. Recommendation-first HITL interaction

- [ ] 2.1 @impl HIU-001, HIU-002, HIU-005, HIU-006, PRP-002: Rewrite `brief/hitl1.md`, `shared-agent-ux-guidance.md` and `phase-hitl1.md` so Agent first presents one grounded target/must-answer/topic/profile recommendation, accepts clear natural-language acceptance/correction or optional A/B/C shortcuts, treats an unambiguous answer as confirmation and asks only the smallest ambiguity/cost/permission/irreversible-risk question; replace fixed round/sub-loop counters with semantic-progress judgment and replace `gap_queue_backed` text sentinels with concrete Agent-proposed must-answer questions accepted/corrected into the existing owner; done when no counter, parser, sentinel, intake field or schema expansion is added and profile/topic/style/probe/Gate plus `stop: yes` remain unchanged.
- [ ] 2.2 @impl HIU-003, CDP-001, ACS-001, REI-003, REI-005, GSK-002: Rewrite `brief/hitl2.md`, `phase-hitl2.md` and shared UX wording so the decision brief exposes current answers, limits/disputes and exactly one available Agent recommendation with reason/expected effect, discloses foreseeable material effort/cost for rerun-like work from current facts, accepts natural-language delivery/view/rerun/repair/stop intent or optional shortcuts and records only the existing five decision enums at the accepted HITL2 boundary. Before recommending/recording rerun, `phase-hitl2.md` SHALL use one repo-root read-only inline Node ESM invocation importing existing `loadGateDefinition('rerun-ready')` from `engine/helpers/gate-helpers.mjs`, validate the exact supported gate/rule/check/target/less-than/positive-integer shape, parse current count as absent=>0 or a nonnegative integer, and require `current_count + 1 < value`; done when acceptance confirms disclosed impact, only expansion beyond disclosure/current permission asks again, `current_count = value - 1` and every unreadable/unsupported direct fact become only an honest new-bundle/unavailable boundary, `proceed_to_readiness`/legal `rerun` retain existing Gate/chain handoffs, the invocation writes no file/trace/state and is not duplicated in the brief, and no raw definition parse, `failure_message` interpretation, wrapper/CLI/helper/Gate/state/numeric copy is added.
- [ ] 2.3 @impl HIU-001, HIU-003, CDP-001: Preserve the honest context-dependent branch boundary: `request_view_revision`, `repair` and `stop_blocked` execute only when an existing legal path supports the concrete action, never gain fixed transitions or default readiness, and are not recommended as immediately executable when known unavailable; done when missing-path behavior reports one honest boundary and no recommendation field, new enum, Gate, controller, route or hand-written status is added.
- [ ] 2.4 @impl CDP-004, RUE-005, ACS-001: Align HITL exit wording, `phase-final.md`, shared silent Final exception and `TERMINAL_DELIVERY_HEADER` so the next framework-initiated wait after HITL1 is HITL2 and Final is terminal delivery, not a third recommendation/confirmation/repair loop; a current user turn receives verified facts/smallest boundary, pre-artifact reply does not claim delivery/progress/partial output, and `terminal_delivery`/`deliver_final_artifacts` remains unchanged; done when ordinary factual replies do not require recovery, only explicit post-final rerun uses the accepted audited operation, and prompt-side HITL2 mapping is not generalized to Final.

## 3. Silent means do not initiate

- [ ] 3.1 @impl SWE-001, SWE-004, WNC-008, GSK-006, CHI-001: Rewrite `shared-silent-execution.md`, `shared-repair-guidance.md`, `shared-anti-cheating-rules.md`, `AUTONOMOUS_MODE_HEADER` and Gate fatigue advice as one direction-aware baseline: no framework-initiated question/progress/partial-delivery/acknowledgement/idle/confirmation, required normal response to an already received user-initiated conversation turn, diagnostic escalation that still obeys current `stop`, then unchanged current node/status/next action absent an accepted task change; done when “user is unavailable”, absolute no-reply/defer-without-response/`ABSOLUTE PROHIBITION`, generic `escalation->block` and the silent override table are removed while repair/change-strategy/legal-handoff/silent-hold and the existing fatigue threshold/verdict remain.
- [ ] 3.2 @impl SWE-004, ACS-001, RUE-005, CDP-004: Make the response boundary explicit across Agent-facing command/entry guidance: only HITL1/HITL2 are framework-initiated semantic waits; the RUN trigger is user-initiated and adds no route-clarification wait; a user message in the silent middle or active Final receives a response but is not a third HITL, permission, mutation/reentry authority, pause/interrupt lifecycle or guaranteed durable intent; done when an existing legal path returns mechanics to Agent, a missing path returns one honest boundary without hand-written authority, and the response preserves the applicable autonomous-continuation or terminal-delivery obligation without claiming observed post-answer execution.
- [ ] 3.3 @impl SWE-001, SWE-005: Narrow `surfacing_intent` guidance and static tests to Agent/framework-initiated prohibited output only; done when a reply to a user-initiated turn is not logged as would-have-surfaced solely because the active node is `stop:no`, while self-initiated progress/question/partial-delivery remains aborted and diagnostic-only.
- [ ] 3.4 @impl SWE-006, DEW-003: Preserve delegated active polling semantics under the new wording: claim/poll/submit/repair/terminalize continues without user acknowledgement or task notification; done when no user-message queue, chat classifier or polling controller is added.
- [ ] 3.5 @impl CHI-001, RWP-002, RWP-018, RWP-019, DEW-013, SWE-001, SWE-004, ACS-001: Align structured-hint/timeout-block consumption in every non-terminal stop:no phase (`phase-instantiation.md`, `phase-setup.md`, `phase-seed-topics.md`, `phase-wave0.md`, `phase-wave1.md`, `phase-wave2.md`, `phase-rerun.md`, `phase-readiness.md`) plus `shared-subagent-protocol.md`, `command_playbook/work-unit-actor-decision.md` and `command_playbook/persist-artifact.md`: classification identifies action responsibility/the smallest honest Agent-facing boundary, while current `stop` alone controls framework-initiated interaction; done when no boundary classification or timeout `block` by itself initiates a question, approval, status/partial-delivery output or acknowledgement wait, the Agent continues other legal repair/strategy/degradation/handoff or silently holds, a current relevant user-initiated turn is answered from direct facts without new authority, satisfied prerequisites return to the same checkpoint, and a scoped active-source scan finds no stop:no consumer that treats `user_decision|external_action|missing_contract`, timeout `block`, or a host-policy blocker as automatic user escalation.
- [ ] 3.6 @impl GSK-002, CHI-001, REI-003, STM-003, RWG-018, RES-002, DEW-013, QIV-006: Make stop:no action-bearing producers placement-neutral in `check-gate-rerun-ready.mjs`, `check-gate-seed-topics-ready.mjs`, `wave-depth-contracts.mjs`, `canonical-topic-state.mjs`, `operate-queue.mjs` and generated `work-unit-envelope.mjs` task guidance: preserve direct fact/true owner/same-check rerun, replace immediate ask-user, return/enter-HITL/rerun, surface-blocker and involve-user commands with boundary wording, and when a recorded profile already makes missing `research_style_params` mechanically derivable classify it as existing `apply-research-style.mjs` `engine_operation` rather than `user_decision`; done when representative real CLI/inspect/generated-task output preserves pass/fail, `no_transition`, true hint coordinates/classification, does not tell a stop:no Controller to initiate interaction or jump lifecycle, and `gate-rerun-ready.definition.json` remains byte-identical so its operator/value and post-final digest do not change.

## 4. Stateless continuation projection migration

- [ ] 4.1 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003: Change the pure continuation helper's non-terminal stop:no enum from `prohibited` to `do_not_initiate` for loaded nodes, Gate pass/fail and successful work-unit claim; done when `required`, `terminal_delivery`, every `next_action`, locator, omission/fail-closed case and absence of persistent/chat state remain unchanged.
- [ ] 4.2 @impl CPT-001, CPT-003: Update Gate result, `enter-phase` and covered/bootstrap-compatible `advance-status` projections and rendered marker blocks atomically; done when real CLI integration output uses `do_not_initiate`, HITL/Final output is unchanged and no command chooses routes or mutates extra state.
- [ ] 4.3 @impl DEW-003, SWE-006: Update successful claim output, shared Sub-agent polling guidance and case-606 continuation assertions to `do_not_initiate`; done when `work_ids == claimed_work_ids`, empty/failed claim behavior and immediate inspect/poll obligation remain unchanged.
- [ ] 4.4 @impl SWE-001, CPT-001, CPT-003, DEW-003: Run the unit and integration cue claims from `verification-plan.yaml`; done when `tests/engine/helpers/continuation-cue.test.mjs` and `tests/integration/cli/continuation-initiation-contract.test.mjs` pass and repository search finds no active stop:no `interaction: prohibited` consumer outside archives/historical evidence.

## 5. Positioning and one rerun-limit truth path

- [ ] 5.1 @impl RUE-005: Add one short iterative-research posture to `guidelines/project-charter.md`; done when it states HITL1 -> silent autonomy -> HITL2 -> Final delivery and history retention != permanent current validity, while deferring concrete behavior to accepted specs and adding no guideline or duplicate Source of Record.
- [ ] 5.2 @impl RUE-005, ACS-001: Align `DPT_FRAMEWORK/RUN.md`, `DPT_FRAMEWORK/README.md`, `DPT_FRAMEWORK/COMMANDS.md`, `DPT_FRAMEWORK/command_playbook/start-research.md`, `DPT_FRAMEWORK/AGENTS.md` and `DPT_FRAMEWORK/CLAUDE.md` only where each owns the same high-frequency boundary; done when all expose the two-HITL rhythm, user-initiated entry/message semantics and Final delivery without expanding root README or creating a third wait, and the framework-local AGENTS/CLAUDE synchronized pair remains byte-equivalent apart from platform names.
- [ ] 5.3 @impl REI-003, REI-005: Remove only stale concrete rerun-count `<3`/`>=3` wording from `phase-rerun.md` and shared silent guidance, update REI-003/REI-005 registry descriptions to stable active-rule semantics, and preserve unrelated `N >= 3` fatigue-threshold semantics/tests; done when active Gate definition bytes/operator/value and fatigue threshold are unchanged.
- [ ] 5.4 @impl REI-003, REI-005: Update `tests/integration/cli/check-gate-rerun-ready.test.mjs`, `experiments_playbook/exp_wfn_rerun/case-304-light-gate-fail-max-count.md` and `experiments_playbook/RUN_EXPS.md` so boundary inputs/labels derive from the production-parsed active definition and the index carries no concrete current count; done when below/at/above behavior passes, case-304 real Gate failure uses the active boundary, and no copied max-rerun constant remains in active docs/registry/tests/experiments.
- [ ] 5.5 @impl HIU-002, HIU-003, HIU-005, HIU-006, PRP-002: Remove the nonexistent `shared-hitl-prompt-templates.md`, retired search-preference prompt plus `search_preference`/`not_specified_use_profile_defaults`, fixed-round nudge descriptions and `gap_queue_backed` text-sentinel contracts from `brief/hitl1.md`, `phase-hitl1.md`, `phase-wave0.md`, `phase-seed-topics.md`, `shared-agent-ux-guidance.md`, `openspec/governance/req-registry.yaml` and focused tests; update HIU-002/003/005/006 registry descriptions to stable replacement semantics; done when `brief/hitl1.md` is the sole prompt owner, a scoped active-source scan excluding archives/history finds none of the retired prompts/tokens/contracts, and no profile/schema expansion is made.
- [ ] 5.6 @impl RUE-001, VEM-004: Remove accepted historical current-version pins (`v0.6`/`v0.7`) by retaining generic CHANGELOG/banner equality and proposal-declared version semantics; done when accepted specs no longer preserve any change-specific version as permanently current, version tests remain authority-based, and this change's concrete `v0.32` stays in proposal/tasks/CHANGELOG rather than the stable accepted VEM-004 requirement.

## 6. Real Agent behavior proof

- [ ] 6.1 @impl VER-001, VER-003, PRP-002, CDP-001, SWE-004: Create setup-only helpers for cases 711-713; done when 711 ends at legal HITL1 with `research_access.status: unprobed` and no accepted decision/recommendation/topic/profile/Gate/continuation subject facts, 712 ends at legal HITL2 before decision-brief/recommendation/user decision, and 713 ends at a legal non-HITL node with two seeded sentinel facts plus saved authority hashes/current continuation cue; runner/setup SHALL NOT write any subject-attributed behavior or PASS fact.
- [ ] 6.2 @impl VER-001, VER-003, HIU-001, HIU-002, PRP-002: Author and validate `experiments_playbook/exp_iterative_interaction/case-711-heavy-hitl1-natural-acceptance.md`; done when a distinct real subject Agent first reads the production phase/dependency closure, generates the recommendation, then receives only “按这个开始”, performs the bounded real search/fetch probe from `unprobed`, writes existing owners, runs the real Gate and enters at least one bounded stop:no segment without second confirmation or framework-initiated progress/question/confirmation; structured transcript plus trace/filesystem facts SHALL prove each behavior, and setup/runner SHALL NOT prewrite them.
- [ ] 6.3 @impl VER-001, VER-003, HIU-003, CDP-001: Author and validate `case-712-heavy-hitl2-natural-rerun.md`; done when a distinct real subject Agent first generates the research review/one recommendation from the production HITL2 surface, then receives only “资本约束这部分还不够，再补一下”, writes existing rerun/rationale, runs the real Gate and consumes the witnessed handoff with no second user response; saved structured transcript SHALL check only user-facing assistant blocks for redundant confirmation/canonical enum exposure, while trace/filesystem prove decision and mechanics.
- [ ] 6.4 @impl VER-001, VER-003, SWE-004, ACS-001: Author and validate `case-713-heavy-user-initiated-turn.md` at the legal setup boundary using one normal user turn that asks for both seeded sentinel facts, not async interruption transport; done when structured transcript proves the real subject Agent answered both facts, transcript hash is retained, before/after profile/status/trace-permission hashes and current node/status are unchanged, no pause/request state appears, and the projected autonomous `next_action` is unchanged without claiming post-answer execution.
- [ ] 6.5 @impl VER-001, VER-003: For all three playbooks, constrain the subject instruction to the exact bundle, real loaded production surface and user utterance only; done when it does not restate the expected enum, no-confirmation/no-mutation rule, command sequence, expected next action or verdict, so subject behavior must come from production guidance. Missing independent real subject execution SHALL be `NOT RUN`, never fixture/scripted PASS.
- [ ] 6.6 @impl VER-001, VER-003: Register cases 711-713 in `experiments_playbook/RUN_EXPS.md` with explicit Reality Distance Ledgers, real-Agent/no-substitute rules, transcript requirements and PASS-cleanup/FAIL-preserve behavior; run verification routing in assets mode after registration.
- [ ] 6.7 @impl VER-001, VER-003: Execute case 711 end to end and require native `rb_trace.jsonl` PASS plus clean health; done only after transcript/trace/filesystem verdict passes, otherwise preserve the bundle as FAIL/NOT RUN evidence.
- [ ] 6.8 @impl VER-001, VER-003: Execute case 712 end to end and require native `rb_trace.jsonl` PASS plus clean health; done only after transcript/trace/filesystem verdict passes, otherwise preserve the bundle as FAIL/NOT RUN evidence.
- [ ] 6.9 @impl VER-001, VER-003: Execute case 713 end to end and require native `rb_trace.jsonl` PASS plus clean health; done only after transcript/hash/authority-diff verdict passes, otherwise preserve the bundle as FAIL/NOT RUN evidence.

## 7. Focused regression and package verification

- [ ] 7.1 @impl HIU-001, HIU-002, HIU-003, HIU-005, HIU-006, PRP-002, CDP-001, CDP-004, SWE-001, SWE-004, SWE-005, WNC-008, ACS-001, RUE-005, CHI-001, RWP-002, RWP-018, RWP-019, DEW-013: Run the Markdown contract claim plus affected user-facing-language, command-contract, HITL gate/phase and workflow-chain suites; done when recommendation/natural-language/confirmation/semantic-progress/two-HITL/user-reply/Final boundaries pass, HITL2's explicit loader invocation rejects identity/target/operator/value/count drift and suppresses rerun at the exact pre-increment exhausted boundary, unreadable facts fail to one honest boundary, Final answers verified facts without opening a loop or changing terminal cue, all enumerated stop:no hint/timeout-block consumers obey lifecycle-owned interaction timing, and no brittle formatting-only blocker substitutes for behavior.
- [ ] 7.2 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003, DEW-013, GSK-002, GSK-006, CHI-001, REI-003, STM-003, RWG-018, RES-002, QIV-006: Run all cue/Gate/entry/status/queue/topic-state/work-unit/generated-task and producer-feedback unit/integration suites affected by the enum/wording migration; done when deterministic authority, true hint enum/coordinates, Gate verdict/routes, exit codes, handoff witnessing, dry-submit validation/no-side-effect behavior, work-unit state and rerun-definition bytes are unchanged while stop:no action-bearing advice is placement-neutral and recorded-profile style repair uses its existing Engine owner.
- [ ] 7.3 @impl WNC-008, SWE-001, CHI-001, RWP-002, RWP-018, RWP-019, DEW-013: Run `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`, `node DPT_FRAMEWORK/cli/validate-work-unit-hygiene.mjs` and relevant static regression tests; done when dependency/header injection is consistent, every non-terminal stop:no phase and delegated guidance surface uses lifecycle-owned interaction timing, and no retired interaction or delegated authority surface remains.
- [ ] 7.4 @impl VER-001, VER-003: Run `node openspec/governance/check-verification-routing.mjs --change simplify-iterative-research-interaction --mode assets`; done when every declared proof asset exists at its canonical route with no class/profile mismatch.

## 8. Versioning and lifecycle closeout

- [ ] 8.1 @impl VEM-001, VEM-002, VEM-004: Add concise `CHANGELOG.md` entry `v0.32` summarizing recommendation-first two-HITL interaction, direction-aware user reply including Final, `do_not_initiate` cue migration and single-source rerun availability/limit cleanup; done when it contains no file/requirement inventory or unsupported durable-intervention claim.
- [ ] 8.2 @impl VEM-003, RUE-001: Update `DPT_FRAMEWORK/RUN.md` banner/current release to `v0.32` and run version-management tests; done when latest changelog and entry banner match exactly.
- [ ] 8.3 @impl RET-006: Run `openspec validate simplify-iterative-research-interaction --strict`; done when proposal, design, all delta specs and tasks validate with no warning/error.
- [ ] 8.4 @impl RET-006: Run `node openspec/governance/check-project-reqs.mjs`; done only with 0 duplicate, orphan, unregistered and reused-retired requirements.
- [ ] 8.5 @impl RET-006: Run `node openspec/governance/check-project-specs.mjs`; done only with 0 deltaHeaderInMain, missingPurpose, missingRequirements and missingReqHeader violations.
- [ ] 8.6 @impl RET-006: Run a read-only OpenSpec archive/spec-sync preflight using the installed `findSpecUpdates()` + `buildUpdatedSpec()` over every delta; done when every capability builds successfully with zero missing-current-scenario, rename, cross-section or target-structure error before apply is declared complete.
