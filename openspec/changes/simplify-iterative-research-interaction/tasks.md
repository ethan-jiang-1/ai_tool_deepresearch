## 1. Apply preflight and failing baselines

- [ ] 1.1 @impl VER-001, VER-002, VER-003: Run `node openspec/governance/check-verification-routing.mjs --change simplify-iterative-research-interaction --mode plan` before target edits and review the `design.md` target manifest; done when every claim routes cleanly and no new state, Gate, CLI, transition, message transport or persistent interaction surface is planned.
- [ ] 1.2 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003: Update/add failing continuation tests for lifecycle `prohibited -> do_not_initiate` and claim-level interaction omission; done when baselines preserve `required`, `terminal_delivery`, every `next_action`, locator, empty/failed-claim behavior and `work_ids == claimed_work_ids`, while exposing only the intended token/claim duplicate drift.
- [ ] 1.3 @impl GSK-002, REI-003, REI-005, POF-001, POF-003: Add `tests/engine/helpers/rerun-availability.test.mjs` and `tests/integration/cli/rerun-availability-consumers.test.mjs` to require one pure evaluator, exact supported shape, full-profile/HITL2-parent/count fail-closed behavior, current/next boundaries and Gate/HITL2/C5-stage sharing; done when current local comparisons fail sharing, missing/unparseable profile masks dependent Gate count hints, and C5 external envelope/lineage plus definition bytes/digest remain unchanged.
- [ ] 1.4 @impl HIU-001, HIU-002, HIU-003, HIU-005, HIU-006, PRP-002, CDP-001, CDP-004, ACS-001, RUE-005, CHI-001, RWP-002, RWP-018, RWP-019, DEW-013: Add `tests/integration/md/iterative-interaction-contract.test.mjs` plus focused producer/CLI assertions for recommendation-first HITL, semantic-progress guidance, no text sentinel, direction-aware reply, Final terminal semantics and lifecycle-owned hint placement; done when current enum-first/absolute-silence/micro-control/automatic-escalation/duplicate-rerun-logic surfaces fail for behavioral contract reasons rather than presentation trivia.

## 2. One rerun fact path and recommendation-first HITL

- [ ] 2.1 @impl GSK-002, REI-003, REI-005: Add one side-effect-free `evaluateRerunAvailability({ definition, profile, includeNextIncrement })` Engine helper and barrel export; done when it alone validates exact gate/rule/check/target/`less_than`/positive-integer shape, requires a parsed full profile with HITL2 parent, defaults only an absent nested count to `0`, rejects invalid counts, computes the REI-003 closed facts, performs no I/O/finding/routing and adds no schema, CLI, Gate or persisted result.
- [ ] 2.2 @impl GSK-002, REI-003, REI-005: Replace the rerun-ready Gate local count comparison with the shared evaluator using `includeNextIncrement: false`; done when the Gate retains formal verdict/trace/routing, missing/unparseable profile or missing HITL2 parent yields only the parent primary hint with dependent rationale/count masked, and no Gate-local operator/value/count comparison remains.
- [ ] 2.3 @impl POF-001, POF-003, REI-003: Replace C5's local rule/count comparison with the shared evaluator only for fresh eligibility and pre-commit revalidation using `includeNextIncrement: true`; done when supported exhaustion still permits read-only inspect but blocks apply/workspace creation, unsupported facts report their exact boundary, event-bound incremented replay validates recorded count delta/digest without next-next evaluation, formal Gate remains the next owner, and external C5 envelope/event/manifest/lineage stay unchanged.
- [ ] 2.4 @impl HIU-001, HIU-002, HIU-005, HIU-006, PRP-002: Rewrite HITL1 brief/phase/shared UX once: one grounded target/must-answer/topic/profile recommendation, natural-language acceptance/correction, optional shortcuts and minimum real ambiguity/cost/permission/risk question; in the same owner edit remove fixed round nudges, retired search-preference input and `gap_queue_backed` text parsing from HITL1, Wave0 and Seed Topics; done when accepted concrete must-answer questions feed existing owners and no counter, parser, sentinel, intake field or schema expansion exists.
- [ ] 2.5 @impl HIU-003, CDP-001, ACS-001, REI-003, REI-005: Rewrite HITL2 brief/phase/shared UX around current review plus exactly one available recommendation. `phase-hitl2.md` SHALL own one repo-root read-only invocation passing loader output and the full parsed profile to the shared evaluator with `includeNextIncrement: true`; done when it consumes only closed facts, maps clear natural language only at HITL2, routes only supported exhaustion to a new-bundle decision, states unsupported contract facts without guessing a remedy, duplicates no evaluator logic, and preserves legal proceed/rerun handoffs.
- [ ] 2.6 @impl HIU-001, HIU-003, CDP-001: Preserve context-dependent `request_view_revision|repair|stop_blocked` honesty; done when each is recommended/executed only with an existing legal path, missing capability is stated once, and no fixed transition, default readiness, new enum, Gate, controller or handwritten status is added.
- [ ] 2.7 @impl CDP-004, RUE-005, ACS-001, WNC-008: Align HITL exits, `phase-final.md`, the shared Final exception and `TERMINAL_DELIVERY_HEADER`; done when Final initiates no question/wait/progress/repair loop, answers an already-current verified-fact turn without claiming pre-artifact delivery, preserves `terminal_delivery`/`deliver_final_artifacts`, and only explicit post-delivery rerun uses accepted recovery.

## 3. Silent means do not initiate

- [ ] 3.1 @impl SWE-001, SWE-004, WNC-008, GSK-006, CHI-001, REI-003, REI-005: Rewrite `shared-silent-execution.md`, shared repair/anti-cheating guidance, `AUTONOMOUS_MODE_HEADER` and Gate fatigue advice as one direction-aware baseline; in this same owner edit remove the silent override table, absolute no-reply/user-unavailable wording and stale concrete rerun limit while preserving fatigue threshold/verdict, repair/change-strategy/legal-handoff/silent-hold and active-rule references.
- [ ] 3.2 @impl CHI-001, RWP-002, SWE-001, SWE-004, ACS-001: Align non-terminal lifecycle phase consumers once so `repair_kind` and timeout `block` assign responsibility but current `stop` alone places interaction; done when no phase classification initiates a question/status/approval/acknowledgement wait, user-initiated turns receive direct facts without authority changes, legal repair returns to the same checkpoint, and `phase-rerun.md` contains no copied max-rerun number.
- [ ] 3.3 @impl RWP-018, RWP-019, DEW-013, SWE-001, SWE-004: Align shared Sub-agent, actor-decision, persistence and generated-task consumers once; done when external-action/missing-contract boundaries remain honest but initiate no stop:no wait, accepted mechanical fallback/polling continues under Agent ownership and no delegated surface creates interaction authority.
- [ ] 3.4 @impl GSK-002, CHI-001, REI-003: Make rerun/seed-topic Gate action-bearing producers placement-neutral; done when direct fact/true owner/same-check rerun stay intact, immediate ask/return-HITL/surface commands disappear, verdict/routes stay unchanged and the rerun definition remains byte-identical.
- [ ] 3.5 @impl STM-003, RWG-018, RES-002, DEW-013, QIV-006: Make wave-depth, canonical-topic-state, queue and generated-work-unit non-Gate producers placement-neutral; done when immediate involve-user commands disappear, recorded-profile missing style maps to existing `apply-research-style.mjs`, and existing state/side-effect/repair ownership remains unchanged.
- [ ] 3.6 @impl SWE-001, SWE-005: Narrow `surfacing_intent` to framework-initiated progress/question/partial-delivery attempts; done when replying to a user-initiated turn records no violation solely because `stop:no` is active, while prohibited self-initiated surfacing remains aborted and diagnostic-only.
- [ ] 3.7 @impl SWE-006, DEW-003: Preserve delegated polling under the loaded lifecycle authority; done when claim/poll/submit/repair/terminalize continues without user acknowledgement or task notification and no message queue, chat classifier, polling controller or claim-level interaction truth is added.

## 4. Stateless continuation projection migration

- [ ] 4.1 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003: Change loaded-node/Gate stop:no projections from `prohibited` to `do_not_initiate`; remove `interaction` only from successful claim continuation while retaining `inspect_and_poll_claimed_work` and `work_ids`; done when `required`, `terminal_delivery`, all next actions/locators, omission/fail-closed behavior and absence of persistent/chat state remain unchanged.
- [ ] 4.2 @impl CPT-001, CPT-003: Update Gate result, `enter-phase`, covered/bootstrap-compatible `advance-status` and rendered cue blocks atomically; done when real CLI output uses lifecycle-derived `do_not_initiate`, HITL/Final output is unchanged and no command gains route or interaction authority.
- [ ] 4.3 @impl DEW-003, SWE-006: Update successful claim output, shared polling guidance and case-606/current claim assertions for an action-only continuation; done when successful claim omits `interaction`, IDs/action are unchanged, and empty/failed claims still omit a false successful cue.
- [ ] 4.4 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003: Run cue unit/integration claims; done when the declared cue tests pass and an active-source scan finds no non-terminal lifecycle `interaction: prohibited` consumer or claim-level interaction assertion outside archives/history.

## 5. Positioning, registry and compatibility cleanup

- [ ] 5.1 @impl RUE-005: Add one short iterative-research posture to `guidelines/project-charter.md`; done when it states HITL1 -> silent autonomy -> HITL2 -> Final delivery and history retention != permanent current validity while deferring behavior to accepted specs and adding no guideline or duplicate Source of Record.
- [ ] 5.2 @impl RUE-005, ACS-001: Align only owning high-frequency docs: full rhythm/user-turn/Final boundary in `RUN.md` and `COMMANDS.md`, execution handoff in `command_playbook/start-research.md`, and one concise iterative-positioning sentence in framework README; done when no full contract is copied into routing-only `AGENTS.md`/`CLAUDE.md` absent a direct conflict and no third wait is created.
- [ ] 5.3 @impl HIU-002, HIU-003, HIU-005, HIU-006, REI-003, REI-005, VEM-004: Update requirement-registry descriptions to stable semantic-progress, concrete must-answer, active-rule and proposal-declared-version wording; run scoped active-source scans for retired prompt owner/search-preference/sentinel/fixed-nudge/legacy rerun-number/current-version pins; done when implementation cleanup is owned by sections 2-3, this task changes only governance descriptions/tests that still encode retired semantics, and unrelated fatigue thresholds remain.
- [ ] 5.4 @impl REI-003, REI-005: Update case-304 and its runner entry to derive the formal Gate failure boundary from the production-parsed active definition, then execute it; done when native trace verdict is PASS, health is clean or only the documented intentional Gate failure remains, no current count is copied into the runner index and failed evidence is preserved.
- [ ] 5.5 @impl RUE-001, VEM-004: Keep accepted version semantics generic through the version-management delta and authority-based tests; done when historical scenario anchors remain archive-compatible, no accepted requirement permanently pins v0.6/v0.7 as current, and this change's concrete v0.32 remains only in proposal/tasks and later CHANGELOG/banner history.

## 6. Real Agent behavior proof

- [ ] 6.1 @impl VER-001, VER-003, PRP-002, CDP-001, CDP-004, SWE-004: Create setup-only helpers for cases 711-713. Case 711 ends at legal HITL1 with `research_access.status: unprobed` and no recommendation, accepted HITL1 decision, subject-owned profile/topic writes or `hitl1-recorded` attempt/pass; 712 ends at legal HITL2 before brief/recommendation/decision; 713 ends at legal readiness stop:no with seeded direct facts/cue. Disclose required predecessor Gate facts, but setup SHALL write no case verdict or subject-attributed PASS/behavior.
- [ ] 6.2 @impl VER-001, VER-003, HIU-001, HIU-002, PRP-002: Author and statically validate case 711 with an explicit real-Agent dependency/no-substitute contract and ordered role/event-labeled JSONL transcript. Both production branches require one subject conversation containing production-guided recommendation, exact user “按这个开始”, subject-owned existing-owner writes, real bounded probe and real Gate: available additionally requires one real search, one successful fetch of the first usable URL, Gate pass and stop:no entry; honest unavailable requires direct unavailable observation, Gate fail and no Setup entry. Observer SHALL immediately hash the raw transcript into root trace and derive PASS only from transcript tool events plus profile/filesystem/trace facts; only missing independent subject execution is `NOT RUN`, and case 115 remains the focused probe canary. No transcript or PASS is required until task 6.7.
- [ ] 6.3 @impl VER-001, VER-003, HIU-003, CDP-001: Author and statically validate case 712 with an explicit real-Agent dependency/no-substitute contract and role/event-labeled JSONL transcript; done when its subject generates the review/one recommendation from production HITL2, receives only “资本约束这部分还不够，再补一下”, owns the existing rerun/rationale write and real Gate/handoff, and deterministic predicates over assistant/user/tool blocks prove no second user response or canonical enum in user-facing blocks. No transcript or PASS is required until task 6.8.
- [ ] 6.4 @impl VER-001, VER-003, SWE-004, ACS-001, CDP-004: Author and statically validate case 713 with two independent real-subject turns and role/event-labeled JSONL transcripts: capture authority snapshot A before and B after the readiness reply and require A=B; allow only runner-owned real readiness Gate/Final entry/status sync from B to C; capture D after the pre-artifact Final reply and require C=D. Hash both transcripts into root trace, exclude transcript/observer files from authority inventory, require `final/` empty at C and D, and use bounded deterministic patterns for no delivery/progress overclaim or Final loop. No transcript or PASS is required until task 6.9.
- [ ] 6.5 @impl VER-001, VER-003: Constrain all subject instructions to exact bundle, loaded production surface and fixed user utterance without restating expected enum, no-confirmation/no-mutation rule, command sequence, next action or verdict. Runner allowlist is setup of disclosed prerequisites, fixed-utterance delivery, byte-preserving transcript save/hash and deterministic observation; only case 713 additionally permits the declared readiness Gate→Final entry→status sync. Runner SHALL NOT perform subject-owned probe/profile/topic/rationale/Gate/handoff work in cases 711/712; missing independent subject execution is `NOT RUN`, never fixture/scripted PASS.
- [ ] 6.6 @impl VER-001, VER-003: Register cases 711-713 exactly once in `RUN_EXPS.md` with Reality Distance Ledgers, transcript requirements and PASS-cleanup/FAIL-preserve behavior; validate each playbook and run verification routing assets mode.
- [ ] 6.7 @impl VER-001, VER-003: Execute case 711 end to end; done only when the ordered structured transcript, root-trace digest, subject-owned writes and real probe/Gate facts yield native branch-aware PASS: available has real search/fetch, Gate pass and stop:no entry; unavailable has direct honest observation, Gate fail and no Setup entry. Health SHALL be clean or contain only that expected unavailable Gate failure; preserve FAIL/NOT RUN evidence otherwise.
- [ ] 6.8 @impl VER-001, VER-003: Execute case 712 end to end; done only when the structured transcript, root-trace digest, subject-owned profile/Gate/handoff facts and deterministic user-facing block predicates yield native PASS and health is clean; preserve FAIL/NOT RUN evidence otherwise.
- [ ] 6.9 @impl VER-001, VER-003: Execute case 713 end to end; done only when A=B, the B→C transition diff matches its exact allowlist, C=D, both transcript digests are root-traced, `final/` remains empty through D and bounded reply predicates yield native PASS; preserve FAIL/NOT RUN evidence otherwise.

## 7. Focused regression and package verification

- [ ] 7.1 @impl GSK-002, REI-003, REI-005, POF-001, POF-003: Run shared evaluator unit tests plus rerun-ready and post-final recovery unit/integration suites; done when missing nested count versus missing/malformed profile/HITL2 parent, supported exhaustion versus unsupported facts, Gate current-count, HITL2/C5 fresh next-count and C5 incremented replay are correct; parent-root masking, formal Gate/trace, C5 external envelope/lineage and definition bytes/digest/operator/value remain unchanged.
- [ ] 7.2 @impl HIU-001, HIU-002, HIU-003, HIU-005, HIU-006, PRP-002, CDP-001, CDP-004, SWE-001, SWE-004, SWE-005, WNC-008, ACS-001, RUE-005, CHI-001, RWP-002, RWP-018, RWP-019, DEW-013: Run the Markdown contract and affected user-facing-language, command, HITL Gate/phase and workflow-chain suites; done when recommendation/natural-language/semantic-progress/two-HITL/user-reply/Final boundaries pass, HITL2 consumes shared availability only, and no brittle formatting proxy substitutes for behavior.
- [ ] 7.3 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003, GSK-006, STM-003, RWG-018, RES-002, QIV-006: Run affected cue/Gate/entry/status/queue/topic-state/work-unit/generated-task and producer-feedback suites; done when deterministic authority, hints, routes, exits, handoffs, dry-submit side effects and work-unit state are unchanged except the intended interaction wording/owner corrections.
- [ ] 7.4 @impl WNC-008, SWE-001, CHI-001, RWP-002, RWP-018, RWP-019, DEW-013: Run `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`, `node DPT_FRAMEWORK/cli/validate-work-unit-hygiene.mjs` and scoped active-source scans; done when header/dependency injection is consistent, every non-terminal consumer obeys lifecycle-owned interaction timing and no retired interaction/delegated authority surface remains.
- [ ] 7.5 @impl VER-001, VER-003: Run `node openspec/governance/check-verification-routing.mjs --change simplify-iterative-research-interaction --mode assets`; done when every declared proof asset exists at its canonical route with no class/profile mismatch.

## 8. Versioning and lifecycle closeout

- [ ] 8.1 @impl VEM-001, VEM-002, VEM-004: Add concise `CHANGELOG.md` entry `v0.32` summarizing recommendation-first two-HITL interaction, direction-aware user reply including Final, lifecycle cue/claim cleanup and single-source rerun availability; done when it contains no file inventory or unsupported durable-intervention claim.
- [ ] 8.2 @impl VEM-003, RUE-001: Update `DPT_FRAMEWORK/RUN.md` banner/current release to `v0.32` and run version-management tests; done when latest changelog and entry banner match exactly.
- [ ] 8.3 @impl RET-006: Run `openspec validate simplify-iterative-research-interaction --strict`; done with no warning/error.
- [ ] 8.4 @impl RET-006: Run `node openspec/governance/check-project-reqs.mjs`; done only with 0 duplicate, orphan, unregistered and reused-retired requirements.
- [ ] 8.5 @impl RET-006: Run `node openspec/governance/check-project-specs.mjs`; done only with 0 deltaHeaderInMain, missingPurpose, missingRequirements and missingReqHeader violations.
- [ ] 8.6 @impl RET-006: Run this exact read-only OpenSpec archive/spec-sync preflight from repo root; done when every delta capability builds and the JSON summary reports no thrown merge/scenario/rename/structure error:

  ```bash
  node --input-type=module - "$(npm root -g)/@fission-ai/openspec/dist/core/specs-apply.js" <<'JS'
  import { resolve } from 'node:path';
  import { pathToFileURL } from 'node:url';

  const modulePath = process.argv[2];
  const { findSpecUpdates, buildUpdatedSpec } = await import(pathToFileURL(modulePath));
  const changeName = 'simplify-iterative-research-interaction';
  const changeDir = resolve('openspec/changes', changeName);
  const updates = await findSpecUpdates(changeDir, resolve('openspec/specs'));
  const capabilities = [];
  for (const update of updates) {
    const built = await buildUpdatedSpec(update, changeName, { silent: true });
    capabilities.push({ target: update.target, ...built.counts });
  }
  console.log(JSON.stringify({ count: capabilities.length, capabilities }, null, 2));
  JS
  ```
