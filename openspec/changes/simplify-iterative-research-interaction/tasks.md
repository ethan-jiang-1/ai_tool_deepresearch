## 1. Apply preflight and failing contract baselines

- [ ] 1.1 @impl VER-001, VER-002, VER-003: Run `node openspec/governance/check-verification-routing.mjs --change simplify-iterative-research-interaction --mode plan` before any target edit; done when all seven claims validate and the apply target manifest in `design.md` confirms no new state, Gate, CLI, transition, message transport or persistent interaction surface.
- [ ] 1.2 @impl SWE-001, SWE-004, SWE-006, CPT-001, CPT-003, DEW-003, WNC-008: Update/add focused cue and CLI regression assertions so current `interaction: prohibited`, absolute no-reply header wording and all stop:no Gate/entry/status/claim projections fail against the target `do_not_initiate` contract; done when tests preserve every existing `next_action`, locator, `required` HITL and `terminal_delivery` Final expectation while exposing only the intended token/meaning drift.
- [ ] 1.3 @impl HIU-001, HIU-002, HIU-003, PRP-002, CDP-001, CDP-004, ACS-001, RUE-005: Add `tests/integration/md/iterative-interaction-contract.test.mjs` characterization/target assertions for recommendation-first HITL1/HITL2, natural-language acceptance/correction, no blanket second confirmation, only two framework-initiated waits, answerable user-initiated turns without new authority and delivery-only Final; done when old enum-first/absolute-silence surfaces fail without relying on regex presentation trivia.
- [ ] 1.4 @impl REI-003, REI-005: Rewrite the rerun-ready boundary test setup to read the production-parsed active `rerun_count` rule/operator/value and assert below/at/above-boundary behavior; done when the current active definition is unchanged, the two stale expectations fail before implementation cleanup, and no new limit constant/helper is introduced.

## 2. Recommendation-first HITL interaction

- [ ] 2.1 @impl HIU-001, HIU-002, PRP-002: Rewrite `brief/hitl1.md`, `shared-agent-ux-guidance.md` and `phase-hitl1.md` so Agent first presents one grounded target/must-answer/topic/profile recommendation, accepts clear natural-language acceptance/correction or optional A/B/C shortcuts, treats an unambiguous answer as confirmation and asks only the smallest ambiguity/cost/permission/irreversible-risk question; done when the existing profile/topic/style/probe/Gate owners and `stop: yes` semantics remain unchanged and the unowned `search_preference` prompt/write contract is removed rather than added to schema.
- [ ] 2.2 @impl HIU-003, CDP-001: Rewrite `brief/hitl2.md`, `phase-hitl2.md` and shared UX wording so the decision brief exposes current answers, limits/disputes and exactly one Agent recommendation with reason/expected effect, accepts natural-language delivery/view/rerun/repair/stop intent or optional shortcuts and records only the existing five decision enums at the accepted HITL2 boundary; done when clear decisions do not receive blanket confirmation and `proceed_to_readiness`/`rerun` retain their existing Gate/chain handoffs.
- [ ] 2.3 @impl HIU-001, HIU-003, CDP-001: Preserve the honest context-dependent branch boundary: `request_view_revision`, `repair` and `stop_blocked` remain without new fixed transitions, recommendation never substitutes for user input, and unsupported action reports the smallest missing path; done when no parser, recommendation field, new enum, Gate, controller or route is added.
- [ ] 2.4 @impl CDP-004, RUE-005: Align HITL exit wording and Final guidance so the next framework-initiated wait after HITL1 is HITL2 and Final is terminal delivery only, not a third recommendation/confirmation/repair loop; done when accepted post-final audited rerun semantics remain intact and prompt-side HITL2 mapping is not generalized to Final.

## 3. Silent means do not initiate

- [ ] 3.1 @impl SWE-001, SWE-004, WNC-008: Rewrite `shared-silent-execution.md` and `AUTONOMOUS_MODE_HEADER` as one direction-aware contract: no framework-initiated question/progress/partial-delivery/acknowledgement/idle/confirmation, normal response to an already received user-initiated conversation turn allowed, then continued silent autonomy absent an accepted task change; done when “user is unavailable”, absolute no-reply and defer-without-response rules are removed while repair/change-strategy/legal-handoff/silent-hold behavior remains.
- [ ] 3.2 @impl SWE-004, ACS-001: Make the response boundary explicit across Agent-facing command guidance: a user message is not a third HITL, permission, mutation/reentry authority, pause/interrupt lifecycle or guaranteed durable mid-run intent; done when an existing legal path returns mechanics to Agent and a missing path returns one honest boundary without hand-written authority.
- [ ] 3.3 @impl SWE-001, SWE-005: Narrow `surfacing_intent` guidance and static tests to Agent/framework-initiated prohibited output only; done when a reply to a user-initiated turn is not logged as would-have-surfaced solely because the active node is `stop:no`, while self-initiated progress/question/partial-delivery remains aborted and diagnostic-only.
- [ ] 3.4 @impl SWE-006, DEW-003: Preserve delegated active polling semantics under the new wording: claim/poll/submit/repair/terminalize continues without user acknowledgement or task notification; done when no user-message queue, chat classifier or polling controller is added.

## 4. Stateless continuation projection migration

- [ ] 4.1 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003: Change the pure continuation helper's non-terminal stop:no enum from `prohibited` to `do_not_initiate` for loaded nodes, Gate pass/fail and successful work-unit claim; done when `required`, `terminal_delivery`, every `next_action`, locator, omission/fail-closed case and absence of persistent/chat state remain unchanged.
- [ ] 4.2 @impl CPT-001, CPT-003: Update Gate result, `enter-phase` and covered/bootstrap-compatible `advance-status` projections and rendered marker blocks atomically; done when real CLI integration output uses `do_not_initiate`, HITL/Final output is unchanged and no command chooses routes or mutates extra state.
- [ ] 4.3 @impl DEW-003, SWE-006: Update successful claim output, shared Sub-agent polling guidance and case-606 continuation assertions to `do_not_initiate`; done when `work_ids == claimed_work_ids`, empty/failed claim behavior and immediate inspect/poll obligation remain unchanged.
- [ ] 4.4 @impl SWE-001, CPT-001, CPT-003, DEW-003: Run the unit and integration cue claims from `verification-plan.yaml`; done when `tests/engine/helpers/continuation-cue.test.mjs` and `tests/integration/cli/continuation-initiation-contract.test.mjs` pass and repository search finds no active stop:no `interaction: prohibited` consumer outside archives/historical evidence.

## 5. Positioning and one rerun-limit truth path

- [ ] 5.1 @impl RUE-005, ACS-001: Add a short iterative-research posture to `guidelines/project-charter.md` and align `DPT_FRAMEWORK/RUN.md`, `README.md`, `COMMANDS.md`, `command_playbook/start-research.md`, framework-local `AGENTS.md`/`CLAUDE.md` only where they own the same high-frequency boundary; done when surfaces state HITL1 -> silent autonomy -> HITL2 -> Final delivery, distinguish history retention from current judgment, and do not add a guideline or duplicate behavioral Source of Record.
- [ ] 5.2 @impl REI-003, REI-005: Remove stale concrete `<3`/`>=3` wording from `phase-rerun.md` and shared silent guidance, and update REI-003/REI-005 registry descriptions to stable active-rule semantics without numeric limit or rule-count copies; done when the active Gate definition bytes/operator/value are unchanged.
- [ ] 5.3 @impl REI-003, REI-005: Run `tests/integration/cli/check-gate-rerun-ready.test.mjs` and the relevant Gate-definition schema/threshold-source suites; done when the test derives the exclusive boundary from the active definition, passes just below it, fails at/above it and no copied max-rerun constant remains in active docs/registry/tests.
- [ ] 5.4 @impl HIU-002, PRP-002: Remove the stale nonexistent `shared-hitl-prompt-templates.md` ownership and unowned `search_preference`/sentinel contract from active HITL surfaces and focused tests; done when `brief/hitl1.md` is the sole prompt text owner and no profile/schema expansion is made.

## 6. Real Agent behavior proof

- [ ] 6.1 @impl VER-001, VER-003, HIU-001, HIU-002, PRP-002: Create a setup-only helper and `experiments_playbook/exp_iterative_interaction/case-711-heavy-hitl1-natural-acceptance.md`; done when a distinct real subject Agent receives a complete recommendation plus “按这个开始”, writes only existing HITL1/topic owners, runs the real HITL1 Gate and continues without a second confirmation, with transcript plus trace/filesystem verdict and no script-written subject pass fact.
- [ ] 6.2 @impl VER-001, VER-003, HIU-003, CDP-001: Create `case-712-heavy-hitl2-natural-rerun.md` over a legal setup-only HITL2 bundle; done when a distinct real subject Agent receives “资本约束这部分还不够，再补一下”, writes existing `rerun`/rationale fields, runs the real HITL2 Gate and consumes the witnessed rerun handoff without redundant confirmation or internal-enum instruction, with trace/filesystem verdict.
- [ ] 6.3 @impl VER-001, VER-003, SWE-004, ACS-001: Create `case-713-heavy-user-initiated-turn.md` at a legal non-HITL `stop:no` setup boundary using a normal subject-Agent conversation turn, not async interruption transport; done when the real Agent answers from direct bundle facts, leaves HITL/profile/status/permission authority unchanged, does not create pause/request state and retains the existing autonomous next action, with transcript plus trace/filesystem verdict.
- [ ] 6.4 @impl VER-001, VER-003: Register cases 711-713 in `experiments_playbook/RUN_EXPS.md` with explicit Reality Distance Ledgers, real-Agent/no-substitute rules and PASS-cleanup/FAIL-preserve behavior; execute each end to end and require native `rb_trace.jsonl` PASS. Missing independent subject-Agent execution SHALL be `NOT RUN`, never fixture or scripted PASS.

## 7. Focused regression and package verification

- [ ] 7.1 @impl HIU-001, HIU-002, HIU-003, PRP-002, CDP-001, CDP-004, SWE-001, SWE-004, WNC-008, ACS-001, RUE-005: Run the Markdown contract claim plus affected user-facing-language, command-contract, HITL gate/phase and workflow-chain suites; done when recommendation/natural-language/confirmation/two-HITL/Final boundaries pass without brittle formatting-only blockers.
- [ ] 7.2 @impl SWE-001, SWE-006, CPT-001, CPT-003, DEW-003: Run all cue/Gate/entry/status/work-unit unit and integration suites affected by the enum migration; done when deterministic authority, exit codes, handoff witnessing, work-unit state and no-side-effect behavior are unchanged.
- [ ] 7.3 @impl WNC-008, SWE-001: Run `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`, `node DPT_FRAMEWORK/cli/validate-work-unit-hygiene.mjs` and relevant static regression tests; done when dependency/header injection is consistent and no retired interaction or delegated authority surface remains.
- [ ] 7.4 @impl VER-001, VER-003: Run `node openspec/governance/check-verification-routing.mjs --change simplify-iterative-research-interaction --mode assets`; done when every declared proof asset exists at its canonical route with no class/profile mismatch.

## 8. Versioning and lifecycle closeout

- [ ] 8.1 @impl VEM-001, VEM-002, VEM-004: Add concise `CHANGELOG.md` entry `v0.32` summarizing recommendation-first two-HITL interaction, user-initiated reply boundary, `do_not_initiate` cue migration and rerun-limit truth cleanup; done when it contains no file/requirement inventory or unsupported durable-intervention claim.
- [ ] 8.2 @impl VEM-003, RUE-001: Update `DPT_FRAMEWORK/RUN.md` banner/current release to `v0.32` and run version-management tests; done when latest changelog and entry banner match exactly.
- [ ] 8.3 Run `openspec validate simplify-iterative-research-interaction --strict`; done when proposal, design, ten delta specs and tasks validate with no warning/error.
- [ ] 8.4 Run `node openspec/governance/check-project-reqs.mjs`; done only with 0 duplicate, orphan, unregistered and reused-retired requirements.
- [ ] 8.5 Run `node openspec/governance/check-project-specs.mjs`; done only with 0 deltaHeaderInMain, missingPurpose, missingRequirements and missingReqHeader violations.
