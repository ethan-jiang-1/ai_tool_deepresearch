## 0. Apply Preconditions And Plan Review

- [x] 0.1 Complete the change plan review before any target edit (openspec-feedback:plan-review). Reviewed the proposal, all three delta specs, design, task ordering, and verification plan against the existing actor/Phase/Gate ownership surfaces. The shared-helper case-605 documentation impact was incorporated into task 1.1, proposal and design; no unresolved actionable finding remains. This marker records scoped review only, not semantic proof. (CHF-001)
- [x] 0.2 Run `node openspec/governance/check-verification-routing.mjs --change align-real-actor-canary-checkpoint-boundaries --mode plan`; passed before target edits with selected integration/agent-flow claims and explicit non-applicable classes. (VER-002, VER-003)

## 1. Actor Checkpoint Contract

- [x] 1.1 Implement AGT-003 and EXR-006 in `experiments_env/shared/run-fixture-backed-case.mjs` and case-406/case-604/case-605 playbooks. Done when the shared real-source-intake helper no longer invokes a Wave0 Gate after actor submit, case-406 required checks stop at its Subject/submit/trace facts, case-604 cannot leave an unrelated Gate attempt, case-605 no longer claims that helper side effect, and none writes or repairs a Phase-owned projection. Verified by the focused case-406 production-helper test on 2026-07-31.
- [x] 1.2 Implement AGT-003 and RWE-002 in `experiments_playbook/exp_wfn_wave1/case-221-heavy-batch-subagent.md`. Done when its two-Actor batch retains generated task, dry/formal submit, ledger, and work-unit-inspect proof but has no Wave1 Gate/phase-completion required check or fixture-created semantic Phase artifact; a native PASS remains bounded to the actor checkpoint. The matching fixture runner and Markdown flow both omit the Gate/completion path; verified by the focused case-221 contract test on 2026-07-31.

## 2. Deterministic Regression

- [x] 2.1 Add/update the focused `tests/integration/md/case-406-real-subagent-contract.test.mjs` regression for AGT-003 and EXR-006. Done when a test-owned temporary actor-envelope result exercises the production submit path and proves the narrowed case-406 verdict does not invoke or require Wave0 Gate state; the test explicitly remains deterministic-contract evidence, not real Actor proof. Passed on 2026-07-31.
- [x] 2.2 Update `tests/integration/md/case-221-first-return-contract.test.mjs` for AGT-003 and RWE-002. Done when it verifies the retained two-Actor submit/inspect evidence and the absence of Wave1 Gate/phase-completion verdict work, without treating static Markdown or fixtures as real Actor behavior. Passed on 2026-07-31.
- [x] 2.3 Run the selected integration tests. Done when both focused files pass with the production helper/CLI boundaries and no test claims semantic research or Subject Actor behavior. `node --test tests/integration/md/case-406-real-subagent-contract.test.mjs tests/integration/md/case-221-first-return-contract.test.mjs` passed 7/7 on 2026-07-31.

## 3. Actor Evidence And Archive Readiness

- [x] 3.1 Attempt each selected Heavy `agent_flow_e2e` canary once through its existing authorized host path and record every verification-plan claim in `apply-evidence.md` as PASS, FAIL, or NOT_RUN with native-completion/evidence references and host/provider boundary. Done when case-406, case-604, and case-221 have an honest disposition; unavailable actor, budget, provider, or host timeout remains NOT_RUN and is never replaced by fixture, parent-authored bytes, or a retry/controller path. All three claims are host-scoped NOT_RUN because their sole 2026-07-31 attempts ended without native completion; see `apply-evidence.md`. (AGT-003, EXR-006, RWE-002)
- [x] 3.2 Run `node openspec/governance/check-verification-routing.mjs --change align-real-actor-canary-checkpoint-boundaries --mode assets`; done when all selected test and registered playbook assets exist in their canonical routes. Passed for all 5 claims on 2026-07-31. (VER-003)
- [x] 3.3 Run `node openspec/governance/check-project-reqs.mjs`; done when it reports 0 duplicate, orphan, unregistered, and reused-retired requirement IDs. Passed with 610 registered IDs and 0 orphan on 2026-07-31.
- [x] 3.4 Run `node openspec/governance/check-project-specs.mjs`; done when it reports 0 deltaHeaderInMain, missingPurpose, missingRequirements, and missingReqHeader violations. Passed with 81 main spec files and 0 violations on 2026-07-31.
- [x] 3.5 Run `openspec validate align-real-actor-canary-checkpoint-boundaries --strict` and `git diff --check`; done when both pass for the completed change. Both passed on 2026-07-31.
- [x] 3.6 Complete the change-scoped actual-diff closeout review (openspec-feedback:closeout-review). Reviewed the shared actor helper, case-406/case-604/case-605/case-221 playbooks, focused regressions, and the three modified-requirement delta/main pairs. The review found no route that reintroduces a Phase Gate, fixture-created Phase projection, or controller into an actor-only verdict. Selected deterministic evidence was freshly rerun (7/7); the three host-scoped real-actor claims remain correctly recorded as NOT_RUN because no native completion exists. No ordinary repair task is open; the next lifecycle action is exactly `node openspec/governance/finalize-change-archive.mjs --change align-real-actor-canary-checkpoint-boundaries`. (CHF-001, CHF-002, CHF-003, CHF-004)
