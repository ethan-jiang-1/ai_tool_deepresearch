## 1. Apply Preconditions And Characterization

- [x] 1.1 Confirm the C2 archive prerequisite and rebase the C3 `CTS-004` and `RRM-007` delta requirements on the archived main specs. Done when C3 preserves all current main-spec scenario identities without copying a second topic-state or return-map authority. (CTS-004, RRM-007)
- [x] 1.2 Before any target edit, run `node openspec/governance/check-verification-routing.mjs --change make-agent-operation-contracts-direct --mode plan`; read each selected production CLI, helper, Markdown surface, and test asset named by `verification-plan.yaml`. Done when the apply log identifies one current owner for invocation parsing, Zod validation, entry witness, status mutation, claim/timeout fact selection, and omission evaluation. (CLE-001, IOC-004, CTS-004, CPT-003, CPT-004, DEW-014, DEW-016, DEW-021, PHS-007, PRP-012, RRM-007, WNC-010, WNC-011)
- [x] 1.3 Add focused characterization cases for the reported help, invocation, schema-feedback, controls-rendering, entry-presentation, claim/timeout, and Wave0-omission regressions before changing each owner. Include the existing bare Wave bundle acceptance, Zod effects, and malformed action-core-before-witness behavior. Done when every case reaches a production helper or CLI and does not recreate a validator or an Agent-flow controller inside the test. (CLE-001, IOC-004, CTS-004, CPT-003, CPT-004, DEW-014, DEW-016, PHS-007, RRM-007)

## 2. Direct Invocation Contracts

- [x] 2.1 Implement one narrow shared static invocation/presentation helper for selected public operations only: standalone `--help`/`-h`, strict known-option parsing, stable usage, and structured code-`2` invocation/configuration roots. Done when it enforces the design's exact per-command grammar without changing topic-state's operation positional grammar or the renderer's subcommands, and returns control to each command's existing domain owner instead of routing phases, resolving lifecycle authority, or changing a domain verdict. (CLE-001, CLE-003)
- [x] 2.2 Apply that helper to `inspect-wave{0,1,2}-output` before bundle/definition evaluation. Done when help exits `0` without a bundle read; only `--bundle <bundle-path>` is accepted for non-help invocation; bare positional paths, duplicate/mixed flags, and invalid/missing/unsafe bundle input exit `2` through the existing `{ check, inspect, advice, hints }` envelope; and no unvalidated token is reflected into `write_to`, `rerun`, or a command hint. (IOC-004, CLE-001)
- [x] 2.3 Apply the same selected invocation behavior to `operate-topic-state`, `enter-phase`, and `advance-status`, preserving their exact documented non-help forms and their existing successful/domain-rejection envelopes. Done when help and parser rejection cannot create a workspace, entry/load witness, status mutation, trace append, or bundle-side effect. (CTS-004, CPT-003, CPT-004, CLE-001)
- [x] 2.4 Extend focused helper and CLI regressions for code `0` standalone help, code `2` bare/duplicate/mixed/malformed invocation, no-evaluator/no-side-effect preflight, safe placeholder rendering, and unchanged valid domain behavior. (CLE-001, CLE-003, IOC-004, CTS-004, CPT-003, CPT-004)

## 3. Topic-State Authoring Feedback

- [x] 3.1 Implement a narrow fail-closed visitor over the currently supported `TopicApplyPlanSchema` Zod branch graph. Done when it may unwrap `ZodEffects` only for structural discovery; does not restate/reforge any refinement or cross-field rule; and `schema --context <context>` derives context/action forms, required/optional fields, closed values, value shapes, and only examples that the actual top-level schema parses, without a JSON-Schema subsystem or bundle read. (CTS-004)
- [x] 3.2 Add the read-only `operate-topic-state schema --context <context>` operation and make its unknown context/invalid invocation a direct code-`2` root. Done when the command never infers lifecycle authorization, opens a workspace, or obtains a second writer path. (CTS-004, CLE-001)
- [x] 3.3 Project bounded safe `validation_errors[]` from existing `ZodIssue[]` for invalid retained `apply` input. Done when one primary field coordinate, safe expectation detail, and the same apply rerun are exposed without echoing arbitrary values, file bytes, paths, stack traces, or converting owner/lifecycle rejection into writable feedback. (CTS-004)
- [x] 3.4 Test every supported schema form/example against the actual `TopicApplyPlanSchema`, structural effect unwrapping without duplicated cross-field validation, unsupported/unverifiable-construct configuration feedback, safe error redaction, and valid input's unchanged authorization/mutation path. (CTS-004)

## 4. HITL1 Controls Renderer

- [x] 4.1 Expose the existing `renderNoControls` and `renderSuppliedControls` helpers through `plan-hostfile-sections.mjs render-no-controls` and `render-supplied-controls --input <snapshot-path>`. Done when success prints only the deterministic text, no bundle is resolved or written, and malformed invocation is code `2`. (PHS-007, CLE-001)
- [x] 4.2 Align HITL1 phase/action-core and command-playbook guidance with the pure renderer followed by the existing Agent-owned host-file coordinate. Done when a missing renderer or invocation failure gives its direct boundary instead of advising a hand-written alternate fence or a new host-file authority. (PRP-012, PHS-007)
- [x] 4.3 Extend renderer and HITL1 integration tests for exact no-controls/supplied-controls output, UTF-8 snapshot input, invalid subcommand/input, no bundle-side effect, and retained material-conflict ownership. (PHS-007, PRP-012)

## 5. Bounded Phase Entry Presentation

- [x] 5.1 Implement a pure extraction/presentation helper for the target's `## 0. Execution Brief` and shared-file manifest. Done when `enter-phase` preflights missing or ambiguous action-core structure from the selected framework source before `assessNode()` can write `load_complete`; failure is a framework configuration result with no load/current-node/workflow-trace-receipt mutation; and the helper does not invoke the workflow loader, infer a target, mutate a plan, or duplicate lifecycle routing. Define the manifest as the successful ordered dependency refs excluding the target node itself. (WNC-010, WNC-011)
- [x] 5.2 Change successful `enter-phase` default stdout to the bounded order: continuation/interaction cue, exact existing `advance-status --to ...` command, target action core, then target-excluding shared-file manifest; retain the full original dependency closure only behind explicit `--full`. Done when the existing successful `load_complete` and `current_node` witness writes remain unchanged, malformed action-core configuration cannot leave either write, and `enter-phase` does not run `advance-status`. (CPT-003, WNC-010, WNC-011)
- [x] 5.3 Make `advance-status` help/parser feedback side-effect-free while retaining its existing trace-witness validation and direct owner/root feedback for valid domain rejection. Done when it still alone owns status mutation and never treats phase entry presentation as authorization. (CPT-004, CLE-001)
- [x] 5.4 Update phase/action-core guidance and focused CLI/Markdown regressions for cue-first placement, exact status synchronization, ordered target-excluding manifest/default versus complete `--full`, interaction placement, malformed invocation, malformed action-core preflight with no loader/trace/receipt/witness write, and no target-work-completion overclaim. Preserve the existing archive scenario identities whose headings say the cue “ends” output while asserting its cue-first behavior. (CPT-003, CPT-004, WNC-010, WNC-011)

## 6. Delegated Work Feedback Projections

- [x] 6.1 Project existing claim actor-observation validation into bounded top-level feedback: planned role, primary conflict, bounded conflicts, the closed legal tuple vocabulary, and one same-claim rerun. Done when the projection reuses the existing validator and cannot allocate a work unit, manufacture an observation, or weaken role proof. (DEW-016, DEW-021)
- [x] 6.2 Project timeout-preflight's already selected candidate/progress/lease/integrity branch as a direct `recommendation_basis`. Done when it explains why otherwise similar attempts recommend `submit`, `repair`, `wait`, `inspect`, `block`, or `timeout` without changing eligibility, lease, dry-submit, terminalization, or recovery semantics. (DEW-014, DEW-021)
- [x] 6.3 Keep generated Completion Contract, public claim feedback, phase guidance, and timeout advice on the same actor/candidate vocabulary. Done when no task-only cache mapping, role proof, timeout rule, or second validator is introduced. (DEW-016, DEW-021)
- [x] 6.4 Extend focused claim, timeout, and CLI regressions for invalid/empty/omitted observations, one primary direct feedback root, selected recommendation basis, no mutation on rejection, and unchanged legal tuple/terminal behavior. (DEW-014, DEW-016, DEW-021)

## 7. Return-Map Omission Presentation

- [x] 7.1 Group only homogeneous current Wave0 candidate omissions inside the existing pure return-map evaluator after parent-root masking. Done when one finding contains stable, identity-complete `missing_candidate_ids[]` for exactly one topic, writable Seed Topic family, repair kind, and Projection Packet coordinate. (RRM-007)
- [x] 7.2 Reuse that grouped evaluator result in both Wave inspect and formal Gate composition. Done when different topics, Waves, parent roots, or writer coordinates remain separate; every candidate remains mandatory; and no Gate- or CLI-local batcher appears. (RRM-007)
- [x] 7.3 Extend helper and production inspect/Gate regressions for full ordered batching, no cross-family grouping, parent-root masking, exact deferred coverage, and unchanged incomplete-packet failure. (RRM-007)

## 8. Agent-Facing Documentation And Release Surfaces

- [x] 8.1 Update `DPT_FRAMEWORK/COMMANDS.md`, `DPT_FRAMEWORK/cli/README.md`, selected command playbooks, and phase guidance with the selected-operation code `0`/`2` contract, exact standalone-help/non-help grammar, direct feedback shape, pure controls renderer, bounded entry/default `--full` distinction, and existing-owner loops. Done when unselected CLI exceptions remain visibly documented rather than being falsely normalized. (CLE-001, CLE-003, IOC-004, CTS-004, CPT-003, CPT-004, PHS-007, PRP-012, DEW-016, RRM-007, WNC-010, WNC-011)
- [x] 8.2 Update repo-root `CHANGELOG.md` and both version references in `DPT_FRAMEWORK/RUN.md` to the proposal-declared `v0.63`; extend and run `tests/engine/version-management.test.mjs` so both RUN.md version references match the latest root CHANGELOG entry; and record C3 dispositions in BUG-150, BUG-153, BUG-155, BUG-156, BUG-158, BUG-159, BUG-160, BUG-171, BUG-173, BUG-177, BUG-183, and BUG-184. Done when the release describes direct operation feedback without claiming a new controller, automatic repair service, altered evidence authority, or host liveness. (VEM-001, VEM-002, VEM-003, VEM-004, CLE-001, CTS-004, CPT-003, DEW-014, PHS-007, RRM-007)

## 9. Verification And Handoff

- [x] 9.1 Run every selected unit asset in `verification-plan.yaml`, including `tests/engine/version-management.test.mjs`. Done when their native `node:test` results prove only the declared deterministic helper/projection and release-metadata contracts. (VEM-001, VEM-002, VEM-003, VEM-004, CLE-001, CTS-004, DEW-014, DEW-016, PHS-007, RRM-007, WNC-010)
- [x] 9.2 Run every selected integration asset in `verification-plan.yaml` against production CLIs and declared Markdown surfaces. Done when help/invocation behavior, valid command behavior, side-effect boundaries, direct feedback, and action-core presentation are exercised on temporary bundles where applicable. (CLE-001, IOC-004, CTS-004, CPT-003, CPT-004, DEW-014, DEW-016, PHS-007, PRP-012, RRM-007, WNC-010, WNC-011)
- [x] 9.3 Run `node openspec/governance/check-verification-routing.mjs --change make-agent-operation-contracts-direct --mode assets`, `openspec validate make-agent-operation-contracts-direct --strict`, `node openspec/governance/check-project-reqs.mjs`, `node openspec/governance/check-project-specs.mjs`, and `git diff --check`. Done when every applicable check passes and no deterministic fixture is reported as Agent-behavior or host-liveness evidence.
