## 1. Plan Review and Baseline

- [x] 1.1 @impl AGQ-019, GSK-008: Complete the change-scoped plan review (`openspec-feedback:plan-review`) before the first target edit. Re-read the proposal, design, delta specs, current Queue/delegated boundaries, and verification plan; any actionable finding becomes an ordinary unchecked task naming the affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition. Review outcome: `failure_disposition` must be admitted only when the terminal row embeds the original non-delegated item, so its eligibility is a direct stored fact rather than a historical-row inference; task 2.1 owns that repair.
- [x] 1.2 @impl AGQ-019, GSK-008: Run `node openspec/governance/check-verification-routing.mjs --change remove-recursive-queue-failure-repair --mode plan` and `openspec validate remove-recursive-queue-failure-repair --strict` before target edits. Capture the current recursive-failure and queue-drain baseline with authority-byte snapshots; done when the plan and delta validate and the baseline distinguishes a real Engine invocation from a hand-written queue snapshot. Evidence: `apply-evidence.md` records both passing planning checks and the real `repair-repair-*` Engine observation.
- [x] 1.3 @impl AGQ-019: Add red focused coverage for generic failure successor admission: arbitrary `repair` input, repeated failure of a former repair item, current delegated failure, and stale-front failure. Done when each rejection or terminal transition has an exact Queue-byte and terminal-history expectation.

## 2. Close Generic Queue Failure

- [x] 2.1 @impl AGQ-019: Tighten `QueueFailureSchema` to accept only `queue_item_id` and non-empty `reason`, and add closed optional terminal-history `failure_disposition: terminal_no_successor` validation for failed rows whose embedded original item is non-delegated while retaining read compatibility for legacy rows without that field. Done when malformed successor payloads, missing embedded item, or delegated-item disposition fail before any lifecycle mutation.
- [x] 2.2 @impl AGQ-019: Replace generic `fail()` repair creation with a finite non-delegated terminal transition that writes one failed row with `terminal_no_successor`, promotes/refills only existing lawful demand, and never enqueues or preempts a successor. Done when no execution path can create `repair-*` or `repair-repair-*` descendants.
- [x] 2.3 @impl AGQ-019: Make generic Queue failure reject a delegated current demand before mutation and name the existing work-unit terminal/replacement owner; preserve existing delegated timeout, replacement, receipt, ledger, and late-submit behavior. Done when Queue failure neither allocates work identity nor creates replacement lineage.
- [x] 2.4 @impl AGQ-019: Update the public `operate-queue` failure boundary and its supported Agent-facing command guidance for the strict no-successor input/result. Done when supported callers cannot submit arbitrary repair demand and the response gives one current owner/no-path rather than a manual Queue edit.

## 3. Reuse One Terminal Root

- [x] 3.1 @impl AGQ-019, GSK-008: Implement one side-effect-free terminal-failure reader over schema-valid `terminal_history` that returns the exact item, reason, disposition, and no-path facts. Done when it introduces no persisted queue health/status and has no authority writes.
- [x] 3.2 @impl AGQ-019: Route Queue inspect and rendered Queue projection through that reader. Done when both surfaces identify the same terminal item/reason and do not infer a repair card from `failure_route` prose or an empty active window.
- [x] 3.3 @impl GSK-008: Route shared `phase_queue_drained`, Wave inspect, and the formal Gate through the same terminal finding while preserving existing active/refill/delegated roots. Done when `terminal_no_successor` remains one direct `missing_contract` root with the correct invoked rerun coordinate.

## 4. Focused Regression Evidence

- [x] 4.1 @impl AGQ-019: Extend Queue schema and lifecycle tests for legacy-read compatibility, arbitrary-payload rejection, non-delegated finite terminalization, delegated rejection, stale-front rejection, no-preemption, and exact no-mutation bytes. Done when all cases use real Queue manager/schema paths.
- [x] 4.2 @impl AGQ-019, GSK-008: Extend helper and CLI integration tests for Queue inspect/render and Wave inspect/formal-Gate parity on one terminal no-successor row. Done when both public consumers preserve the same root identity and never report a drained queue.
- [x] 4.3 @impl AGQ-019, GSK-008: Run every selected unit and integration asset in `verification-plan.yaml`; record only deterministic-contract results and retain any failure as evidence. Done when finite failure, no-mutation, and shared-root coverage all pass without fabricated Agent/runtime evidence.
- [x] 4.4 @impl AGQ-019: Closeout finding for the generic failure reader question: add a public `operate-queue fail` stale-front regression. Authoritative owner: Queue current-front admission. Smallest repair: submit a non-front, otherwise legal Queue ID and assert non-zero feedback plus exact unchanged `rb_queue.json` bytes. Done when the selected integration asset proves that no terminal row, successor, or projection mutation occurs.

## 5. Sync, Release, and Archive Readiness

- [x] 5.1 @impl AGQ-019, GSK-008: Use the supported OpenSpec spec-sync flow after implementation, then re-read the synchronized `agent/agentic-queue` and `engine/gate-skeleton` main specs. Done when the accepted requirements match the implemented finite Queue failure and terminal-root behavior.
- [x] 5.2 @impl VEM-002, VEM-003, VEM-004: Update root `CHANGELOG.md` for `v0.75` and synchronize the `DEEP_RESEARCH_HARNESS/RUN.md` version banner. Done when both name the same release and the changelog entry concisely describes bounded Queue failure behavior.
- [x] 5.3 @impl AGQ-019, GSK-008: Run `node openspec/governance/check-verification-routing.mjs --change remove-recursive-queue-failure-repair --mode assets` and `openspec validate remove-recursive-queue-failure-repair --strict` after all target edits. Done when each selected asset exists in its canonical route and the active delta remains strict-valid.
- [x] 5.4 @impl AGQ-019, GSK-008: Run `node openspec/governance/check-project-reqs.mjs` and confirm PASS with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired requirements.
- [x] 5.5 @impl AGQ-019, GSK-008: Run `node openspec/governance/check-project-specs.mjs` and confirm PASS with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader findings.
- [x] 5.6 @impl AGQ-019, GSK-008: Complete the change-scoped closeout review (`openspec-feedback:closeout-review`) after the actual diff, synchronized specs, selected evidence, and governance results are reviewed. Any remaining actionable finding must remain an ordinary unchecked task with an owner, smallest repair, and independently observable done condition before archive.
