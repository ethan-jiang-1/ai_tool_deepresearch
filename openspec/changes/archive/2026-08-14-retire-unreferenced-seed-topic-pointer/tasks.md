## 0. Apply Entry Review

- [x] 0.1 @impl C1b reader question `openspec-feedback:plan-review` - Before the first target edit, read current feedback-loop Apply guidance and review proposal, design, `skip_specs: true` rationale, semantic-closure rationale, verification plan, fresh zero-caller evidence, target pointer, manifest, phase dependency closure, and protected shared return-map/template/playbook. Record every actionable finding as an ordinary pending task with its reader question, authoritative owner, smallest repair, and independently observable done condition; complete only when none remains.
- [x] 0.2 @impl C1b reader question - Before the first target edit, run strict change validation, plan-mode requirement governance, project-spec governance, capability discovery, verification-routing plan check, semantic-closure plan check, and `git diff --check`. Done when every command exits 0; record structural results only, without claiming runtime or Agent-behavior proof.

## 1. Retire The Unreferenced Pointer

- [x] 1.1 @impl C1b reader question - Immediately before deletion, repeat an exact current-surface reference review for `shared-seed-topic-authoring`, `shared/shared-seed-topic-authoring.md`, and `shared_scope: seed-topic-authoring` across current Harness, tests, accepted specs, experiments, and docs, allowing only the target itself plus historical planning/archive records. Reconfirm manifest and every phase `requires`/`suggested_context` omit the target, and inspect `resolveDependencyClosure()` to establish that current loading recursively follows only explicit `requires` rather than enumerating shared files. Done when no current caller remains or every finding is recorded as an ordinary pending task.
- [x] 1.2 @impl C1b reader question - Delete only `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-seed-topic-authoring.md`. Do not alter manifest membership, phase frontmatter, `shared-return-map-authoring.md`, `seed-topic-template.md`, `operate-topic-state.md`, or any Engine/Gate/bundle surface; do not add a tombstone, alias, forwarding pointer, fallback, migration, or dynamic directory reader. Done when the target path is absent and the protected surfaces are unchanged.

## 2. Verify The Current-Only Boundary

- [x] 2.1 @impl C1b reader question - Run `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`. Done when it reports `passed: true`; treat this only as manifest/explicit-dependency consistency evidence, not proof of an Agent-flow or runtime behavior change.
- [x] 2.2 @impl C1b reader question - Run a scoped absence and protected-surface review. It SHALL prove the target file and its exact current pointer identity are absent from current source/tests/main specs/guidance while allowing active-change and historical archive references; it SHALL prove manifest, phase `requires`/`suggested_context`, `resolveDependencyClosure()`'s explicit dependency-only loading, shared return-map guidance, template, playbook, router/Gates, bundle formats, and Engine source are unchanged. Done when every unexpected reference or protected change is repaired or recorded as an ordinary pending task.
- [x] 2.3 @impl C1b reader question - Run `git diff --check`, strict change validation, `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-unreferenced-seed-topic-pointer`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, verification-routing assets, and semantic-closure assets checks. Done when every command exits 0 and results are recorded as structural/package evidence only.

## 3. Closeout And Archive

- [x] 3.1 @impl C1b reader question `openspec-feedback:closeout-review` - Before archive, read current feedback-loop Archive guidance and review the change-scoped actual diff, deletion/absence result, protected current-owner evidence, selected verification result, `skip_specs` rationale, and semantic-closure reason. Add and close ordinary repair tasks for every finding; complete only when no actionable finding remains.
- [x] 3.2 @impl C1b reader question - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-unreferenced-seed-topic-pointer`. If finalization fails, restore this task to unchecked and repair its named root.
