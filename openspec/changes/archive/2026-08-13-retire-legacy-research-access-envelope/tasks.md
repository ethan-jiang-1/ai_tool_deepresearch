## 0. Apply Entry Review

- [x] 0.1 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 `openspec-feedback:plan-review` - Before target edits, read the current change-feedback operation guidance and review the approved proposal/design/deltas against the actual ProfileSchema, HITL1 Gate, adapter resolver, shared schema readers, fixtures, and C4a card. Record every actionable scope or reader finding as an ordinary unchecked task with owner, smallest repair, and observable done condition. Done when exactly one plan-review marker is complete and no unrecorded actionable finding remains. Evidence: 2026-08-13 reviewed all planned owners, direct consumers, and semantic-closure roles; added 2.2a because `tests/host_tools/research-access-adapter.test.mjs` is the retained canary/permission contract asset omitted from the original verification plan. Re-ran requirement, verification-routing, and semantic-closure plan checks successfully before target edits.

## 1. Current Contract And Resolver Removal

- [x] 1.1 @impl SCO-002, SCO-008 - Remove legacy research-access envelope schemas, refinements, and union acceptance from `schema/contracts/profile.mjs`; remove the now-unconsumed `SourceClass`, `SourceClassReachability`, `ResearchAccessBoundaryLocation`, and `ResearchAccessBoundaryExtent` declarations from `schema/enums.mjs` and their `schema/index.mjs` exports. Retain optional absence, strict `unprobed`, and the complete current direct-sample vocabulary. Done when every URL/fetch/search/candidate/source-class/access-boundary input fails ProfileSchema without defaulting, conversion, or a new discriminator; no current Harness import/export exposes a retired vocabulary; and each current legal state remains valid. Evidence: 2026-08-13 removed the union branch and four enum exports; scoped Harness source scan found no retired schema/resolver export, and `tests/schema/contracts/profile.test.mjs` passes its current-state and eight-family rejection coverage.
- [x] 1.2 @impl PRG-002, PRG-010 - Remove HITL1's legacy boundary import, resolver branch, field reads, and legacy wording; retain the existing `profile_schema_valid` / `missing_contract` root for schema-invalid profile data and the existing absent/unprobed direct-probe root. Done when one retired envelope gives only the schema root, masks dependent rules, and does not produce adapter, provider, external-action, or inferred repair feedback. Evidence: 2026-08-13 removed the boundary path and explicitly mask `research_access_available` behind `profile_schema_valid`; HITL1 integration passes its legacy-envelope single-root assertion and current direct-sample available/unavailable paths.
- [x] 1.3 @impl REA-001, REA-002, REA-003 - Delete the adapter boundary-location owner map/export and its legacy-only comments without changing selected adapter contract parsing, launcher invocation, or caller permission-bypass protection. Done when no current Harness consumer imports a historical boundary resolver and current canary tests still prove the retained adapter contract. Evidence: 2026-08-13 removed `selectedAdapterBoundaryFact` and its map; `tests/host_tools/research-access-adapter.test.mjs` passes retained contract, URL-binding, launcher, and permission-bypass cases.
- [x] 1.4 @impl SCO-002, PRG-002, PRG-010, REA-001 - Rewrite `gate-hitl1-recorded.definition.json`, `phase-hitl1.md`, and `shared-profile.md` to describe only current direct observations, generic schema rejection, and the existing HITL1 loop; update `tests/integration/md/phase-hitl1-research-access.test.mjs` with the changed Markdown contract. Done when no target guidance, definition, or its static contract test presents legacy URL/fetch/access-boundary fields, owner hints, migration, or compatibility as an operational path. Evidence: 2026-08-13 removed current guidance/definition compatibility and owner-routing language; the Markdown contract suite and scoped target scan pass.

## 2. Fixtures And Behavioral Coverage

- [x] 2.1 @impl SCO-002, SCO-008 - Replace schema tests that assert legacy envelope acceptance with one compact explicit rejection table; retain absent, `unprobed`, available, unavailable, complete-sample, no-request, and current forbidden-field coverage. Done when `tests/schema/contracts/profile.test.mjs` proves all retired field families fail and all protected current states pass. Evidence: 2026-08-13 `node --test tests/schema/contracts/profile.test.mjs` passed 20 tests, including one eight-family retired-field rejection table and all direct-sample invariants.
- [x] 2.2 @impl PRG-002, PRG-010, REA-001, REA-002, REA-003 - Replace HITL1/adapter positive legacy fixture cases with one schema-root rejection case and current direct-sample success/no-provider-diagnosis cases. Done when Gate output has exactly one `profile_schema_valid` `missing_contract` root for a legacy envelope, no `research_access_available` or adapter owner hint, and no adapter test imports/uses the removed resolver. Evidence: 2026-08-13 HITL1 and adapter integrations pass: retired envelope gives only the masked schema root, while complete current direct-sample paths pass without provider diagnosis.
- [x] 2.2a @impl REA-001, REA-002, REA-003 - Update `tests/host_tools/research-access-adapter.test.mjs` to remove boundary-resolver assertions while retaining selected-adapter contract parsing, launcher invocation, and permission-bypass coverage. Done when the unit suite no longer imports the removed resolver and still proves the retained canary contract. Evidence: 2026-08-13 removed the import/assertions; the retained canary unit suite passes 7 tests.
- [x] 2.3 @impl SCO-002, PRG-003 - Convert every current-success fixture profile that writes the retired envelope to a valid current direct-sample shape: `check-gate-setup-ready`, `check-gate-rerun-ready`, HITL2/readiness/gate-chain/handoff lifecycle integrations, `research-chain-fixture`, and the pre-wave, rerun-round, and Setup-hostfile deterministic E2E suites. Add scoped rejection assertions for Setup, rerun-ready, `validate-bundle`, and post-final recovery where their existing public ProfileSchema boundary is exposed. Done when no current-success fixture or helper relies on retired fields; each named shared reader fails closed rather than accepting or translating them; and valid current fixture paths still pass. Evidence: 2026-08-13 converted all named current-success fixtures; Setup, rerun-ready, production validate-bundle, and post-final recovery each pass a scoped old-envelope rejection test while their current paths pass.
- [x] 2.4 @impl RES-001, RES-002 - Keep `apply-research-style` preservation coverage only for schema-valid current available/unavailable observations; ensure an invalid legacy envelope does not become a successful style-write preservation case. Done when style recomputation retains valid direct observations unchanged and test scope contains no legacy positive preservation assertion. Evidence: 2026-08-13 style preservation tests use direct-sample observations; the writer validates the resulting profile before write, and an old-envelope input exits nonzero without a style write.
- [x] 2.5 @impl SCO-002, PRG-002, PRG-003, PRG-010, REA-001 - Run and repair every `verification-plan.yaml` asset, including schema/HITL1/adapter/style, Setup, rerun-ready, bundle validation, post-final recovery, and the named deterministic E2E suites. Done when the selected tests pass with no skipped/cancelled test treated as proof and no positive legacy envelope fixture remains. Evidence: 2026-08-13 all 12 selected unit, integration, and deterministic-E2E assets passed in one `node --test` invocation; no selected test was skipped or cancelled.

## 3. Specification And Governance Synchronization

- [x] 3.1 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 - Sync all three delta requirements into accepted `engine/schema-core`, `research/pre-research-gate-implementation`, and `research/research-access-adapter` specs. Done when main specs describe optional current-only access, the schema-root Gate failure, and no adapter boundary mapping. Evidence: 2026-08-13 synchronized all three accepted requirements and strict change validation passed.
- [x] 3.2 @impl SCO-002, PRG-010 - Update `openspec/governance/semantic-fact-families.yaml` for `research.host-access-envelope` so its bounded question covers only current direct-sample facts and does not claim legacy boundary ownership. Update this change's semantic closure to name the actual current resolver and verdict consumers. Done when the catalog, closure record, code, and accepted specs agree on the current fact boundary. Evidence: 2026-08-13 catalog now names only compact direct observations; closure identifies ProfileSchema and its actual verdict consumers, and semantic-closure plan check passes.
- [x] 3.3 @impl SCO-002, PRG-002, PRG-010, REA-001 - Re-run the scoped current-surface scan across ProfileSchema/HITL1/adapter/guidance/tests. Done when each remaining non-archive legacy token is a negative rejection assertion, an ordinary pending task, or an unrelated protected surface; no selected surface promises legacy acceptance, boundary-owner projection, migration, or upgrade. Evidence: 2026-08-13 scan found only retained canary contract labels and explicit negative-rejection/static-absence assertions; no selected current surface promises retired-envelope acceptance or owner projection.

## 4. Verification And Closeout

- [x] 4.1 @impl VER-001, VER-002, VER-003 - Execute the selected `verification-plan.yaml` unit, integration, and deterministic E2E assets plus workflow package validation. Done when all selected assets pass and results are recorded in this change without claiming unrun Agent behavior. Evidence: 2026-08-13 selected 12 deterministic node-test assets and `validate-workflow-package.mjs` passed; no Agent-runtime behavior was claimed.
- [x] 4.2 @impl VER-002, VER-003 - Run `git diff --check`, strict OpenSpec validation, plan/asset verification-routing checks, capability discovery, and semantic-closure checks. Done when every command exits `0` and the structural result is not presented as runtime proof. Evidence: 2026-08-13 all listed commands exited 0; these are recorded as structural and deterministic verification only.
- [x] 4.3 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 `openspec-feedback:closeout-review` - Review the scoped actual diff, schema contract, former resolver removal, current fixture conversions, semantic catalog/closure, accepted-spec sync, and selected evidence. Record any actionable finding as an ordinary unchecked task with requirement/reader question, owner, smallest repair, and observable done condition. Done when exactly one closeout-review marker is complete and no actionable finding remains. Evidence: 2026-08-13 reviewed the scoped diff and all selected evidence: ProfileSchema is the sole current-shape resolver, HITL1 masks the dependent rule behind its schema root, the adapter has no boundary resolver, all named readers fail closed, accepted specs/catalog/closure agree, and remaining retired tokens are negative tests. No actionable finding remains.

## 5. Archive Preconditions

- [x] 5.1 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 - Compare every modified delta requirement/scenario with its accepted main-spec block and run `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-legacy-research-access-envelope`. Done when modified blocks match and the check reports zero duplicate, orphan, unregistered, or reused-retired IDs. Evidence: 2026-08-13 delta/main comparison complete; archive requirement check passed with 644 registered IDs (54 retired) and zero orphan IDs.
- [x] 5.2 @impl SCO-002, SCO-008, PRG-002, PRG-010, REA-001, REA-002, REA-003 - Run `node openspec/governance/check-project-specs.mjs`. Done when it reports zero delta-header-in-main, missing-purpose, missing-requirements, and missing-requirement-header findings. Evidence: 2026-08-13 passed with 84 main spec files and zero violations.
- [x] 5.3 @impl VER-002, VER-003 - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-legacy-research-access-envelope`. If it fails, restore this task to unchecked and repair its named root. Done when the governed finalizer reports an archive location and all supported mechanical closeout checks pass.
