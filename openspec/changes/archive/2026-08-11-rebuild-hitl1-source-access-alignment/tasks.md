## 1. Apply Admission And Planning Review

- [x] 1.1 `openspec-feedback:plan-review` Review the proposal, six delta specs, design, semantic closure, and verification plan before target edits. Reader question: does the plan keep direct retrieval, user source semantics, and deterministic Gate structure as separate owners while deleting the Claude/search-first prerequisite? Authoritative owner: this change's planning artifacts and `research.host-access-envelope` fact family. Smallest repair: add one ordinary pending task for every actionable omission, with its requirement, owner, and observable done condition. Done when the review has no unresolved finding and the marker is checked immediately before the first target edit.
- [x] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change rebuild-hitl1-source-access-alignment --mode plan` and `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-source-access-alignment --mode plan`. Done when both commands pass before the first target edit; if either reports a finding, record its smallest repair as an ordinary pending task rather than editing target code.

## 2. Direct Observation Contract

- [x] 2.1 Implement SCO-001/SCO-002 in `DEEP_RESEARCH_HARNESS/schema/enums.mjs` and `schema/contracts/profile.mjs`: add closed China/overseas sample, outcome, and executor-neutral surface vocabulary; implement strict current direct-sample observation shape, content/status invariant, all-fixed-samples coverage, whole-probe `not_attempted` versus per-sample `round_budget_not_attempted` distinction, and non-mixing legacy compatibility. Done when the new writer cannot carry URL/content/query/candidate/retry/provider material, a no-request branch cannot imply sample unreachability, and existing legacy observations stay parseable without migration.
- [x] 2.2 Implement SCO-001/SCO-002 unit coverage in `tests/schema/contracts/profile.test.mjs`. Done when positive and negative tests prove exact sample/group coverage, content-only surface, unavailable reason, diagnostic-only status exclusion, duplicate/mixed/dynamic material rejection, whole-probe no-request versus spent-round distinction, unprobed contradiction rejection, and legacy compatibility.
- [x] 2.3 Implement REA-001 in `DEEP_RESEARCH_HARNESS/host_tools/research-access-adapter.md` and `host_tools/lib/research-access-adapter.mjs`: remove the production Claude/search/fetch selection assumption, retain the Claude launcher only as executor-scoped canary metadata, and confine boundary resolution to schema-validated legacy facts. Done when no production helper or contract makes `WebSearch`, `WebFetch`, Claude, or a selected adapter a prerequisite for the direct controller.
- [x] 2.4 Implement REA-001/REA-003 focused helper/contract coverage in `tests/host_tools/research-access-adapter.test.mjs` and `tests/integration/cli/hitl1-research-access-adapter.test.mjs`. Done when tests reject a provider-specific production contract, preserve bounded legacy resolver behavior, and do not claim cross-executor permission or availability.

## 3. Gate And HITL1 Flow

- [x] 3.1 Implement PRG-002/PRG-010 in `DEEP_RESEARCH_HARNESS/cli/gates/check-gate-hitl1-recorded.mjs` and `DEEP_RESEARCH_HARNESS/schema/gate_definitions/gate-hitl1-recorded.definition.json`: require a schema-valid completed observation, not `status: available`; retain absent/unprobed structural failure; remove current-observation provider/root feedback while preserving legal legacy-boundary projection. Done when Gate code has no China/overseas scoring, source-relevance inference, second route, or network/VPN verdict.
- [x] 3.2 Implement PRG-002/PRG-010 integration tests in `tests/integration/cli/check-gate-hitl1-recorded.test.mjs`. Done when temporary bundles prove available and unavailable completed current observations take the same existing Gate path, unprobed still blocks, legacy classified feedback stays exact, and new observations never yield Claude/Search/Fetch feedback.
- [x] 3.3 Implement REA-002/PRP-002 in the separate `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-hitl1-research-access-envelope.md`: replace the ladder with the exact fixed core/reserve suite from design, group-balanced concurrent scheduling when current host permission permits it, global/per-group caps, primary/confirmation/round budgets, terminal-outcome rules including whole-probe no-request versus per-sample budget expiry, executor-neutral direct retrieval, and one current compact return map. Done when it observes both groups without success short-circuit, does not use search, has no bundle/Gate/evidence authority, and CNKI is explicitly catalog-only.
- [x] 3.4 Implement REA-002/PRP-002 in `shared-hitl1-capability-probe.md` and `phase-hitl1.md`: actor-deliver only generic isolation plus the controller; write a complete truthful no-request relay/surface-unavailable observation when needed; use question/must-answer/Topic map/controls for materiality; loop only after a user-managed retry request or final semantic resolution. Done when Phase never directly retrieves pages, names no provider requirement, neither verifies nor records a user network change, adds no automatic retry, and retains sole profile/Gate ownership.
- [x] 3.5 Implement HIU-002/URC-001 in `workflows/nodes/brief/hitl1.md` and `workflows/nodes/shared/shared-profile.md`: render Chinese current-observation and material-gap messages; append only a final literal source-access decision to existing controls; preserve hard source constraints and final-observation replacement semantics. Done when no prompt promises restoration/stability, requests a non-material decision, or turns acceptance into false access success.
- [x] 3.6 Implement PRP-002/HIU-002/URC-001 Markdown integration coverage in `tests/integration/md/phase-hitl1-research-access.test.mjs`. Done when static tests prove physical controller independence, exact samples/bounds, no production `WebSearch`/`WebFetch` prerequisite outside the executor-scoped canary contract, two-group non-short-circuit, no-request versus budget-expiry return guidance, bounded user-led retry/acceptance path without network-change verification, snapshot discipline, and absence of cache/receipt/evidence/Gate authority in the probe.

## 4. End-To-End And Executor Canary

- [x] 4.1 Implement PRP-002/REA-003 deterministic final-observation coverage in `tests/e2e/hitl1-research-access-adapter.test.mjs`. Done when temporary bundles retain a complete current available or unavailable observation and existing HITL1 choices through the Gate, reject unprobed access, and prove Gate evaluation creates no probe evidence/cache/artifact/receipt/ledger/coverage side effect.
- [x] 4.2 Implement REA-003 in `experiments_playbook/exp_wff_pre-research-repair/case-115-heavy-hitl1-research-access-probe.md`, `experiments_env/shared/run-iterative-interaction-subject.mjs`, `experiments_env/shared/observe-iterative-interaction-case.mjs`, `tests/experiments_env/case-115-subject-runner.test.mjs`, and `tests/integration/experiments_env/hitl1-fetch-fallback-observer.test.mjs`. Done when case 115 observes an executor-scoped fixed direct-sample protocol and compact final result; any operation-event predicate comes only from that canary's retained adapter contract and never becomes a production controller, profile, or Gate prerequisite. It shall make no provider-generalization, profile mutation, or Gate impersonation; honest unavailable and NOT_RUN remain valid.
- [x] 4.3 Run the selected case-115 `agent_flow_e2e` only through its accepted real-runtime route with an explicit budget and available current-executor prerequisites. Done when retained native completion/evidence truthfully reports PASS, FAIL, or NOT_RUN; do not substitute fixtures or report its result as Codex/Claude cross-compatibility proof. **Decision (2026-08-11): skipped; recorded as honest NOT_RUN — no explicit spend budget was set for the real-runtime canary, and REA-003/verification-plan explicitly permit NOT_RUN as a legal non-generalizing outcome.**

## 5. Release And Spec Sync

- [x] 5.1 Update `CHANGELOG.md` and `DEEP_RESEARCH_HARNESS/RUN.md` for proposal-declared `v0.86`. Done when both describe the fixed direct China/overseas observation and user-led final-scope alignment without promising provider support, network location, coverage, or future availability.
- [x] 5.2 Sync the six approved delta specs into their accepted main specs and recompare REA-001/002/003, SCO-001/002, PRG-002/010, PRP-002, HIU-002, and URC-001. Done when each modified requirement appears once under its owning capability and main specs contain no delta headers.
- [x] 5.3 Update `openspec/governance/semantic-fact-families.yaml` for `research.host-access-envelope`: its bounded question must describe the latest compact China/overseas direct-sample observation and only an already validated legacy `access_boundary`, without treating current sample outcomes as an Engine-owned network diagnosis. Done when the catalog, this change's semantic closure, and the implemented resolver/consumers all name the same fact boundary.

## 6. Verification And Closeout

- [x] 6.1 Run every selected deterministic command from `verification-plan.yaml`, then `npm test`, `openspec validate rebuild-hitl1-source-access-alignment --strict`, `node openspec/governance/check-verification-routing.mjs --change rebuild-hitl1-source-access-alignment --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change rebuild-hitl1-source-access-alignment --mode assets`, and `git diff --check`. Done when each deterministic command passes; record any real finding as a new ordinary pending task immediately.
- [x] 6.1.1 Sync `INITIAL_SEMANTIC_FACT_FAMILIES` in `openspec/governance/semantic-fact-closure-contract.mjs` and its `semantic-fact-closure-contract.test.mjs` length assertion (13→14) to include `research.host-access-envelope` with the updated bounded question. Requirement owner: `research.host-access-envelope` fact family. Done when the catalog, the contract constant, and the test fixture all name the same 14-family set with the same direct-sample bounded question.
- [x] 6.1.2 Restore the `native completion` terminology anchor in the change's `research/pre-research-gate-implementation` delta spec and its synced main spec. Requirement owner: `research/pre-research-gate-implementation`. Done when `tests/integration/md/agent-experiment-autorun-terminology.test.mjs` "follows every active delta spec" subtest passes for this change.
- [x] 6.1.3 Register `direct_retrieval_probe_only` as a valid HITL1 `execution_contract.search_policy` in `DEEP_RESEARCH_HARNESS/engine/consistency-validator.mjs` and update `tests/engine/consistency-validator.test.mjs` expectations. Requirement owner: `research/pre-research-phase-content` execution contract. Done when `validate-workflow-package` and `exit-code-convention` tests pass.
- [x] 6.1.4 Update `tests/integration/cli/hitl1-research-access-adapter.test.mjs` so the Phase-adapter separation assertion reflects the new controller-only Phase delivery. Requirement owner: `research/pre-research-phase-content`. Done when the integration suite passes.
- [x] 6.1.5 Pre-existing repo failure (reproducible at HEAD before this change): `tests/integration/md/artifact-persistence-contract.test.mjs` "keeps Final-backing vocabulary and current release scopes bounded" asserts `CONTEXT.md` contains `**Final key-finding declaration**`, `**Final Evidence Map**`, and `**Final backing**`. Owner: `CONTEXT.md` / Final-backing vocabulary. Done when the three bolded terms appear in CONTEXT.md. **Filed to `_backlog/bugs/BUG-216-...md` (2026-08-11); outside this change's surface.**
- [x] 6.1.6 Pre-existing repo failure (reproducible at HEAD before this change): `tests/integration/md/parallel-delegated-reference-materialization.test.mjs` asserts `shared-subagent-protocol.md` and `phase-wave0.md`/`phase-wave1.md` contain `bounded top-up` phrasing. Owner: parallel-delegated work-unit guidance. Done when the bounded top-up vocabulary is restored. **Filed to `_backlog/bugs/BUG-217-...md` (2026-08-11); outside this change's surface.**
- [x] 6.2 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change rebuild-hitl1-source-access-alignment`. Done when it passes with 0 duplicate, orphan, unregistered, and reusedRetired requirement IDs.
- [x] 6.3 Run `node openspec/governance/check-project-specs.mjs`. Done when it passes with 0 deltaHeaderInMain, missingPurpose, missingRequirements, and missingReqHeader findings.
- [x] 6.4 `openspec-feedback:closeout-review` Review the implementation diff, synced specs, `v0.86` release text, semantic closure, verification plan, deterministic evidence, and any real canary result. Reader question: is current-executor direct retrieval genuinely provider-neutral while the controller remains isolated and all material access limitations remain user-semantic rather than fake Gate capability? Authoritative owners: synced requirements and the listed target contracts. Smallest repair: add one pending task for each finding with its requirement/owner/observable done condition. Done when no open finding remains and this marker is checked. **Closeout (2026-08-11): verified against the actual diff — Gate has no China/overseas/network/VPN verdict and continues past the availability rule for current direct-sample observations; controller owns the fixed suite with zero WebSearch/WebFetch; adapter is executor-scoped canary metadata only; Phase delivers only guide+controller; schema distinguishes whole-probe `not_attempted` from per-sample `round_budget_not_attempted`; all closure coordinates match the diff. Case-115 real canary recorded as honest NOT_RUN (no spend budget set).**

### 6.5 Governed Archive Transition

After every ordinary checkbox above is complete, invoke only:

`node openspec/governance/finalize-change-archive.mjs --change rebuild-hitl1-source-access-alignment`

This is an operation instruction rather than a checkbox because the governed finalizer
requires every ordinary task to be complete before it can perform the archive
transition.
