## 1. Pre-Target Verification And Red Evidence

- [x] 1.1 @impl PRP-002, PRP-005: Create change-root `implementation-evidence.md`, record the pre-apply worktree baseline, then run `node openspec/governance/check-verification-routing.mjs --change allow-bounded-hitl1-fetch-surface-fallback --mode plan` before target edits; done when all four claims validate with `unit`/`deterministic_e2e` explicitly not applicable and no deterministic asset claims native fetch, curl or Agent behavior.
- [x] 1.2 @impl PRP-002, PRP-005: Replace the one-fetch expectations in `tests/integration/md/phase-hitl1-research-access.test.mjs` with red target assertions for one search, only its first actual HTTP(S) result when eligible with no second-result substitution, native-first, and one exact standalone same-URL `curl --fail --silent --show-error --location --max-time 15 --max-redirs 5 --proto '=http,https' --proto-redir '=http,https' --globoff -- '<same-url>'` fallback; done when current production Markdown fails only assertions for native-success short circuit, single-quoted/no-shell-syntax safety, URL/permission boundary, existing observation/evidence boundary and Agent-owned mechanics, without adding a network call or parser-like wording dependency.
- [x] 1.3 @impl PRP-002, PRP-005: Add red `tests/integration/experiments_env/hitl1-fetch-fallback-observer.test.mjs` for existing case-115, its current manifest registration, Subject 115 runner and `observe-iterative-interaction-case.mjs` entry; done when current code fails because case 115 is unsupported and synthetic public tool events cover stable-ID deduplication, first structured search URL, native-before-curl order, exact standalone command, same URL, native-success short circuit, honest curl-error/empty optional omission, structurally contradictory failed check, applicable curl-success pass/fail projection, and malformed/conflicting exit-3 `NOT RUN` artifact behavior without external calls or deterministic claims about semantic page quality.

## 2. Production HITL1 Contract

- [x] 2.1 @impl PRP-002, PRP-005: Rewrite `phase-hitl1.md` §3d and its payload/stop/anti-cheating surfaces to implement one neutral search, only its first actual HTTP(S) result when eligible, native real-content success short circuit and at most one exact hardened same-URL `curl` fallback from task 1.2; done when HTTP errors are non-success, redirects are capped at five and remain HTTP(S)-only, curl globbing is off, URL credentials/raw quote/whitespace/control plus localhost/loopback/literal private or link-local targets are excluded, success/failure write one existing `research_access` observation, fallback success records `fetch_surface: curl`, exhausted failure preserves choices and the same Gate, independently existing permission keeps mechanics with Agent, and no shell composition, policy widening, second URL/tier or user command handoff is authorized.
- [x] 2.2 @impl PRP-002, PRP-005: Audit the production diff against the one-truth-path boundary; done when `ProfileSchema`, `hitl1-recorded` Gate/CLI, transitions, status, trace, Wave/work-unit guidance and evidence/provenance/receipt/ledger owners are unchanged, with no `fallback_used`, surface list, attempt history, fetch controller, retry tree, new HITL or offline-report path.

## 3. Real-Agent Verification Surfaces

- [x] 3.1 @impl PRP-002, PRP-005: Update case-115 frontmatter/flow and Subject 115 runner text from “at most one fetch” to the new production bounded sequence, and route transcript hash/verdict through the existing observer rather than an embedded parser; done when `not_run_if` includes unavailable required public event facts, the playbook captures observer exit 3, reads the existing `case-115-NOT-RUN.json` reason and invokes the native finalizer with `--not-run-reason`, while other observer errors remain errors, one independent Claude Subject/setup-only bundle/real external calls/one final profile observation/real Gate-handoff-or-unavailable/evidence exclusion stay intact, and no native-success-as-curl or Claude-as-Codex claim appears.
- [x] 3.2 @impl PRP-002, PRP-005: Extend `observe-iterative-interaction-case.mjs` with a case-115 branch that normalizes only public JSONL tool events by stable call/result IDs and consumes structured `WebSearch` Links, `WebFetch`, exact Bash command, profile, Gate/handoff and evidence-boundary facts; done when the general required checks use direct Subject facts, complete native-failure/same-URL/non-error-non-empty-curl facts append one `hitl1-native-to-curl-fallback` check, missing/malformed/conflicting required public facts use the existing exit-3 `NOT RUN` artifact protocol, and no prose/private-reasoning/HTML heuristic or observer-authored fetch/profile/page bytes can satisfy it.
- [x] 3.3 @impl PRP-002, PRP-005: Keep case-115's general verdict independent from its optional fallback witness; done when existing general checks remain required; no curl or one exact curl error/empty result omits the optional success check; wrong order/URL/grammar/count appends it failed; an exact non-error/non-empty curl result appends pass only when profile/Gate/handoff/evidence facts agree and fail otherwise; native success cannot count as fallback PASS; a case-level PASS without the branch check remains case PASS plus claim-level `NOT_RUN` in `implementation-evidence.md`; and Supervisor health/audit/cleanup remain post-completion owners.

## 4. Focused And Real Verification

- [x] 4.1 @impl PRP-002, PRP-005: Run `node --test tests/integration/md/phase-hitl1-research-access.test.mjs tests/integration/experiments_env/hitl1-fetch-fallback-observer.test.mjs` and `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`; done when production guidance, observer behavior, existing case-115/runner/manifest wiring and the shipped workflow package pass without external calls.
- [x] 4.2 @impl PRP-002, PRP-005: Execute updated case-115 once through canonical Agent Experiment Autorun when an authenticated Subject runtime is available; done when `implementation-evidence.md` records the native case outcome and general claim's PASS/FAIL/NOT_RUN with durable evidence refs, then separately records fallback PASS only when the bundle-root optional trace check exists and passes, using the retained transcript as bound source evidence rather than a second verdict.
- [x] 4.3 @impl PRP-002, PRP-005: Classify the case-115 fallback witness independently from its bundle-root `hitl1-native-to-curl-fallback` trace check; done when a passed check produces claim PASS, a failed check produces claim FAIL, absence produces honest claim-level `NOT_RUN` without relabeling the native case outcome, Codex behavior and semantic page identity remain explicit residuals, and no second playbook, rerun solely to chase the condition or fixture substitution is used.
- [x] 4.4 @impl PRP-002, PRP-005: Run the relevant pre-research/HITL1 regression surface and `npm test`; done when all suites pass or every unrelated pre-existing failure is recorded with an exact focused reproduction and excluded from this Change's four verification claims.

## 5. Release, Governance And Closure

- [x] 5.1 @impl VEM-001, VEM-002, VEM-004: Add the proposal-declared `v0.39` entry to root `CHANGELOG.md`; done when one or two concise bullets describe bounded same-URL HITL1 fallback and Agent-owned mechanics without file lists, requirement IDs, delegated-path claims or universal external-access claims.
- [x] 5.2 @impl VEM-003, VEM-004: Synchronize the `DPT_FRAMEWORK/RUN.md` banner and current-release summary to `v0.39`; done when it matches the latest CHANGELOG entry and does not imply BUG-096's delegated Wave path is fixed.
- [x] 5.3 @impl PRP-002, PRP-005: Complete `implementation-evidence.md` and audit non-goals; done when each verification-plan claim maps to its actual authority, PASS/FAIL/NOT_RUN and residual risk are explicit, and the final diff adds no dependency, Python, schema/state/Gate/transition change, hidden retry, second URL/tier, user co-runner or fabricated runtime evidence.
- [x] 5.4 @impl PRP-002, PRP-005: Run `node openspec/governance/check-verification-routing.mjs --change allow-bounded-hitl1-fetch-surface-fallback --mode assets`; done when all selected integration and Agent-flow assets exist at canonical paths and each proof claim retains its declared actor, runtime, external-call and verdict boundary.
- [x] 5.5 @impl PRP-002, PRP-005: Run `node openspec/governance/check-project-reqs.mjs`; done with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired violations, reusing PRP-002/PRP-005 without an unnecessary new requirement ID.
- [x] 5.6 @impl PRP-002, PRP-005: Run `node openspec/governance/check-project-specs.mjs`; done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [x] 5.7 @impl PRP-002, PRP-005, VEM-002, VEM-003: Run `openspec validate allow-bounded-hitl1-fetch-surface-fallback --strict` and `git diff --check`; done when the complete Change validates, v0.39 projections agree, and final review records only BUG-096's HITL1/main-Agent path fixed while leaving delegated closure to `deliver-work-unit-role-contracts-to-actors`.

## 6. Post-Apply Hardening

- [x] 6.1 @impl PRP-002, PRP-005: Close the observer boundary found during task 5.3 audit; done when complete public `WebSearch` facts with an empty Links array or an ineligible first result produce the honest general unavailable verdict without selecting a later URL or creating an optional fallback check, while eligible domain names are not mistaken for literal IPv6 private/link-local targets.
