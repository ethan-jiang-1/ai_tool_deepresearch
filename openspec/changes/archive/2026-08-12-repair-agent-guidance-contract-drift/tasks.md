> Apply discipline: after each completed target-edit or verification task,
> update its checkbox and append concise, directly observed command evidence to
> that task line. A planned command is not completion evidence.

## 0. Feedback Reviews

- [x] 0.1 `openspec-feedback:plan-review` - Following archive-preflight discovery of the missing durable marker, reviewed `proposal.md`, `design.md`, this task list, `verification-plan.yaml`, and `semantic-closure.yaml` against the current accepted Final-backing, Agentic Queue, wave-phase, command-surface, and HITL UX sources. `semantic-closure: not_applicable` remains accurate: the scope repairs only static assertions and existing guidance wording, not a Harness runtime fact, resolver, verdict, or production behavior. No actionable planning finding remains. Completed 2026-08-12: this is a current coherence review and does not claim to substitute for or predate the original target-edit evidence.

## 1. Pre-Edit Governance

- [x] 1.1 @impl VER-002, VER-003, SEF-002, SEF-004: Before the first target edit, run `node openspec/governance/check-verification-routing.mjs --change repair-agent-guidance-contract-drift --mode plan` and `node openspec/governance/check-semantic-closure.mjs --change repair-agent-guidance-contract-drift --mode plan`. Done when both checks pass against the change-local records and no target file has been edited before that evidence. Completed 2026-08-12: requirement plan check, verification-routing plan check, and semantic-closure plan check passed before initial target edits; after both the ACS-002 and HITL1-controls plan expansions, all three checks passed again before their respective test edits.

## 2. Static Test Ownership

- [x] 2.1 @impl FDB-001, FDB-002: In `tests/integration/md/artifact-persistence-contract.test.mjs`, remove the `CONTEXT.md` read and only the three Final-glossary assertions, then rename the remaining release-scope subtest accurately. Done when the file no longer treats Context as a Final-backing owner, retains its Final command/terminal-boundary and release/RUN assertions, and its focused `node --test` command passes. Completed 2026-08-12: focused suite passed 6/6; the retained Final command and terminal-boundary assertions remain green.
- [x] 2.2 @impl ACS-002: In `tests/engine/setup-preflight-docs.test.mjs`, align the `.nvmrc` expectation from 20 to the committed 22 baseline. Done when the focused suite passes while `.nvmrc`, dependency declarations, setup docs, and host-permission assertions remain unchanged. Completed 2026-08-12: changed only the expected literal; focused suite passed 5/5 with all existing setup and host-permission assertions retained.
- [x] 2.3 @impl HIU-002, URC-001, PRP-012, PRP-014: In `tests/integration/md/topic-research-emphasis-guidance.test.mjs`, change both controls-slice starts from `### 3b.1 Optional User Research Controls Snapshot` to the current `### 3b.2 Optional User Research Controls Snapshot`. Done when the focused suite passes with all existing controls content and authority-boundary assertions retained, and no HITL1 guidance file changes. Completed 2026-08-12: changed only both selector literals; focused suite passed 3/3 with all content and authority assertions retained.

## 3. Bounded-Batch Guidance

- [x] 3.1 @impl AGQ-022, RWP-015: In `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-subagent-protocol.md`, add one normal-top-up mapping that names bounded top-up batch claiming, conceptual `--count <claim-count>`, accepted/default cap, and remaining free delegated in-flight capacity as the current `claim_count`, effective profile cap, and `remaining_free_capacity` facts. Done when the current formula, concrete `--count <claim_count>` command, fallback=1, reconstruction, and drain order are unchanged. Completed 2026-08-12: one mapping sentence added; reviewed diff retains the formula, command, fallback=1, reconstruction, and drain sequence.
- [x] 3.2 @impl AGQ-022, RWP-015: Apply the same local conceptual-to-concrete mapping in `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-wave0.md`. Done when its Wave0 role/probe behavior, current formula/command, fallback, and gate-after-drain instruction remain unchanged and the accepted batch/cap/capacity distinctions are explicit. Completed 2026-08-12: one mapping sentence added; reviewed diff retains the Wave0 role/probe, formula/command, fallback, and gate-after-drain text.
- [x] 3.3 @impl AGQ-022, RWP-015: Apply the same local conceptual-to-concrete mapping in `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-wave1.md`. Done when its Wave1 role/probe, depth-review, supplementary demand, current formula/command, fallback, and gate-after-drain behavior remain unchanged and the accepted batch/cap/capacity distinctions are explicit. Completed 2026-08-12: one mapping sentence added; reviewed diff retains the Wave1 role/probe, depth-review, supplementary demand, formula/command, fallback, and gate-after-drain text.

## 4. Verification

- [x] 4.1 @impl FDB-001, FDB-002, VER-001: Run `node --test tests/integration/md/artifact-persistence-contract.test.mjs`. Done when all subtests pass and the result is recorded on this task without claiming a runtime Final-delivery verdict. Completed 2026-08-12: passed 6/6 static subtests; this is deterministic Markdown-contract evidence, not a runtime Final-delivery verdict.
- [x] 4.2 @impl AGQ-022, RWP-015, VER-001: Run `node --test tests/integration/md/parallel-delegated-reference-materialization.test.mjs` without weakening its assertions. Done when all subtests pass, including existing timeout/progress/reference coverage, and the result is recorded on this task. Completed 2026-08-12: passed 12/12 subtests without test changes, including bounded batches, timeout-preflight, progress, and Phase-owned reference materialization coverage.
- [x] 4.3 @impl ACS-002, VER-001: Run `node --test tests/engine/setup-preflight-docs.test.mjs`. Done when all subtests pass and the result is recorded as a deterministic setup-contract verdict, not host-permission proof. Completed 2026-08-12: passed 5/5 static subtests; this is deterministic setup-contract evidence, not host-permission proof.
- [x] 4.4 @impl HIU-002, URC-001, PRP-012, PRP-014, VER-001: Run `node --test tests/integration/md/topic-research-emphasis-guidance.test.mjs`. Done when all subtests pass with no test weakening and the result is recorded as deterministic HITL1 guidance-contract evidence, not a runtime interaction verdict. Completed 2026-08-12: passed 3/3 static subtests with all controls content and authority-boundary assertions retained; this is deterministic guidance-contract evidence, not a runtime interaction verdict.
- [x] 4.5 @impl VER-001, VER-002, VER-003: Run `npm test`, `git diff --check`, `git status --short`, `openspec validate repair-agent-guidance-contract-drift --strict`, `node openspec/governance/check-verification-routing.mjs --change repair-agent-guidance-contract-drift --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change repair-agent-guidance-contract-drift --mode assets`. Done when every command passes, the selected assets keep their valid integration/unit routes, and `git status --short` shows no generated runtime artifact. Completed 2026-08-12: serial `npm test -- --test-concurrency=1` passed 2,772/2,772 tests across 484 suites; `git diff --check`, `openspec validate --strict`, verification-routing assets, and semantic-closure assets passed. `git status --short` contained only this change's 11 intended files and no generated runtime artifact.

## 5. Archive Preconditions

- [x] 5.1 @impl RET-006: Before archive, run `node openspec/governance/check-project-reqs.mjs --mode archive --change repair-agent-guidance-contract-drift`. Done when it exits 0 with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired requirement IDs for the selected change. Completed 2026-08-12: passed with 644 registered IDs, 53 retired IDs, and 0 orphan findings.
- [x] 5.2 @impl RET-006: Before archive, run `node openspec/governance/check-project-specs.mjs`. Done when it exits 0 with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader findings. Completed 2026-08-12: passed with 85 main spec files and 0 violations.
- [x] 5.3 `openspec-feedback:closeout-review` - Reviewed the actual change-scoped diff, all changed test and guidance boundaries, selected focused/full-suite evidence, verification plan, and semantic-closure record. The repair removes a false Context authority assertion, makes existing bounded-batch concepts explicit without changing the formula or fallback, and aligns stale Node/HITL1 selectors with committed facts; no source introduces a runtime fact family, resolver, deterministic verdict, or behavior change. The skipped-spec boundary remains valid, and no actionable closeout finding remains. Completed 2026-08-12.
