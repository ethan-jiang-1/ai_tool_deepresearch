# Workflow FSM Runtime — Delta Spec
> req: WFS-003

Purpose 文字修正，非 Requirement 变更。

原 Purpose 末句 "VM 沙箱、MD 执行或依赖解析——这些属于 `workflow-chain.mjs` 的职责" 改为 "依赖解析和 MD 加载见 `workflow-chain.mjs`"。
去掉 workflow-chain 已不再承担的 VM 沙箱/MD 执行引述。
