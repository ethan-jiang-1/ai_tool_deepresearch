schema_version: verification-routing/v1
change: repair-case-164-verification-contract
test_classes:
  unit:
    status: not_applicable
    rationale: >-
      The changed assertion reads static cross-file protocol data at the
      integration boundary; no isolated production module changes.
  integration:
    status: selected
    rationale: >-
      The existing JS-led test reads the registered Markdown playbook and the
      runner configuration together, which is the smallest boundary that can
      prove the static three-turn/two-child protocol.
  deterministic_e2e:
    status: not_applicable
    rationale: >-
      No workflow-scale deterministic behavior, Engine checkpoint, or runtime
      state transition changes.
  agent_flow_e2e:
    status: not_applicable
    rationale: >-
      The existing real-Agent case claim and its retained native evidence are
      unchanged; this change repairs only the static test that guards the case
      definition.
claims:
  - id: case-164-ordered-subject-child-protocol
    statement: >-
      The test-local case-164 integration contract isolates the runner's
      case-164 entry before extracting its ordered protocol, then verifies
      exactly three Subject messages, a real child actor in turns 1 and 3
      only, and the replacement-only second turn without counting later case
      definitions; synthetic message arrays prove the negative boundaries.
    test_class: integration
    proof_subject: deterministic_contract
    asset:
      kind: node_test
      path: tests/integration/md/case-164-direct-output-candidate-contract.test.mjs
    execution_profile:
      fixture: none
      subject_execution: none
      runtime: none
      external_calls: none
      verdict_judge: deterministic
    verdict_authority: node_test_exit
