## 1. Apply Preconditions

- [x] 1.1 `openspec-feedback:plan-review` Before the first target edit, obtain the current Apply guidance and review `proposal.md`, all ten delta specs, `design.md`, this task list, `verification-plan.yaml`, and `semantic-closure.yaml` against the accepted Final, phase-entry, artifact-persistence, submitted-backing, workflow-node, reentry, C5, verification-routing, and experiment contracts plus their current implementation surfaces. Confirm the bounded reader questions and four fact-family roles, one primary-series resolver, one artifact-persistence Module, first-entry/post-C5 Final inventory admission, Final/C5 owner precedence, no-clobber commit, and the absence of a Final Gate, self-transition, satisfaction state, profile counter/current pointer, handoff rewrite, Engine feedback classifier, or report-quality verdict. Record every actionable finding as an ordinary unchecked task naming its affected requirement/reader question, authoritative owner, smallest repair, and independently observable done condition; complete this marker only when no finding remains. (@impl ACS-001, ARP-004, CPT-003, RUE-005, RRD-007, RRD-008, CDE-003, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, SWE-004, WNC-005, WNC-008)
- [x] 1.2 Run `openspec validate iterate-final-delivery-in-place --strict`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change iterate-final-delivery-in-place --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change iterate-final-delivery-in-place --mode plan` immediately before target edits. Done when all four exit zero and their structural PASS is recorded only as planning/governance evidence, not implementation, Agent behavior, report quality, or user satisfaction. (@impl ARP-004, CPT-003, RRD-008, CDE-003, CDP-003, POF-001)
- [x] 1.3 Add the planned `final.primary-report-series` ID and exact bounded question from `semantic-closure.yaml` to `openspec/governance/semantic-fact-families.yaml` before the first target edit that consumes it. Done when the catalog contains it exactly once, the change-root addition remains byte-equivalent, and plan/assets semantics do not invent another family for labels, latest pointers, or satisfaction. (@impl ARP-004, RRD-008, POF-001)

## 2. Primary Final Series Resolver

- [x] 2.1 Add `DEEP_RESEARCH_HARNESS/engine/helpers/final-report-series.mjs` as the pure Zod-owned resolver for safe direct-root inventory classification, reserved-name grammar, one modern or legacy base, contiguous global versions, latest target, next allocation, and direct blockers; add `tests/engine/helpers/final-report-series.test.mjs`. Done when the full empty/modern/labelled/unlabelled/legacy truth table passes; unsafe entries, case-fold collisions, multiple legacy candidates, malformed reserved names, orphan/gapped/duplicate versions fail closed; and the helper never reads mtime, report content, profile, chat, or a second counter. (@impl ARP-004, RRD-008, POF-001)
- [x] 2.2 Replace independent Final inventory interpretation in `DEEP_RESEARCH_HARNESS/engine/helpers/post-final-recovery.mjs` and planned entry/reentry/publication consumers with the shared resolver and canonical sorted inventory facts. Done when first-Final admission, C5 lineage binding, latest report selection, publisher allocation, and later-Final prior-inventory comparison consume one naming/classification result; compatibility full-inventory digests remain deterministic; legacy/non-primary behavior matches the design; and focused unit tests prove no independent basename sort or mtime/content fallback remains. (@impl ARP-004, CPT-003, RRD-007, RRD-008, POF-001)

## 3. Crash-Safe Primary Publication

- [x] 3.1 Extend the existing artifact-persistence schemas in `DEEP_RESEARCH_HARNESS/engine/helpers/artifact-persistence.mjs` with a backward-readable publication workspace/result kind binding inventory digest, base classification, target, version, optional safe feature, payload digest, and backing result; retain existing generic and supplementary Final workspace parsing. Done when strict Zod cross-field tests reject caller-selected/mismatched allocation, unsafe features, malformed bindings, and unsupported workspace combinations without breaking accepted existing workspace fixtures. (@impl ARP-004, CDP-006)
- [x] 3.2 Implement `publishFinalReport()` on the artifact-persistence Module seam: accept only selected bundle, retained staging, and optional safe snake-case feature; resolve the primary series; run existing Final backing admission; allocate the base only for an empty bundle-wide primary inventory and otherwise global `latest + 1`; and reserve direct-root `final/final*.md` plus the selected legacy base from generic/caller-targeted persistence. Done when invalid backing or inventory creates no target/workspace, `final/final.md` ignores feature for version zero, later Final lineages cannot reset the base, labelled/unlabelled calls share one sequence, supplementary targets retain current behavior, no caller can supply target/version/CAS/force/overwrite, and the result remains a mechanical publication fact that neither validates nor establishes legal Final lineage or delivery. (@impl ARP-004, CDP-005, CDP-006)
- [x] 3.3 Implement same-device atomic no-clobber primary commit and exact sweep recovery using the prepared publication binding; extend real-disk crash/concurrency tests in `tests/engine/helpers/artifact-persistence.test.mjs`. Done when simultaneous callers cannot overwrite or share a version, one collision returns a retryable same-command boundary, crashes before/after target creation recover or clean exactly the recorded target without reallocation, unsupported hard-link semantics fail explicitly, all earlier report bytes remain unchanged, and no lock/lease/PID/counter/recovery tree is added. (@impl ARP-004)
- [x] 3.4 Add `publish-final-report --bundle --source [--feature]` to `DEEP_RESEARCH_HARNESS/cli/operate-artifact-persistence.mjs` with strict argument exclusion, tri-state exit behavior, validated JSON, and current logging projection; extend `tests/integration/cli/artifact-persistence.test.mjs`. Done when production CLI covers base/v1/labelled-v2, unsafe feature/extra target/version/CAS flags, reserved redirects, one legacy v0, ambiguous/invalid inventory, backing rejection, collision retry, prepared sweep, staging retention, and no authority mutation outside the existing content/workspace/log surfaces. (@impl ACS-001, ARP-004, CDP-006)

## 4. Deliver-First Final Interaction

- [x] 4.1 Revise `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-final.md` frontmatter/body/anti-cheating contract to `gate: null`, `stop: "yes"`, no `next`, immediate publication whenever the synchronized current Final lineage lacks a bound report, latest-report regrounding, one-at-a-time immutable presentation revisions, minimum material clarification, satisfaction-without-write, and evidence-expansion-only C5 routing. Done when all nine required sections distinguish entry-admitted empty-bundle base from entry-admitted post-rerun global append; require the exact Readiness status synchronization before composition/publication; preserve composition/backing/evidence duties, the current lineage's accepted HITL2 handoff/receipt and all prior lineage history; add no outgoing/self transition or Final Gate; and make no Engine/report-quality/satisfaction claim. (@impl CPT-003, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, WNC-005)
- [x] 4.2 Extend `DEEP_RESEARCH_HARNESS/engine/helpers/handoff-helpers.mjs` with the bounded Final-entry provenance/baseline evaluation and consume it in `DEEP_RESEARCH_HARNESS/cli/enter-phase.mjs`; extend `tests/engine/handoff-helpers.test.mjs` and `tests/integration/cli/enter-phase.test.mjs`. Done when the first route-bound Final load requires a valid empty primary series; a post-C5 Final return requires one unique retired event and exact equality with its full safe prior-inventory digest; premature canonical/supplementary drift, unsafe/ambiguous inventory, missing witness, or conflicting lineage fails before `assessNode`, `load_complete`, and `current_node`; an existing bound load supports exact partial-entry retry and already-entered legacy compatibility; and no mtime/name/content ordering, delivery event, publisher lifecycle check, or new state is added. (@impl CPT-003, CDP-005, POF-001)
- [x] 4.3 Update `DEEP_RESEARCH_HARNESS/engine/workflow-chain.mjs` so manifest Final plus `gate: null` receives the specialized deliver-first/refinement header before generic `stop: "yes"` handling while retaining `terminal_delivery` / `deliver_final_artifacts`; extend `tests/engine/workflow-chain.test.mjs`, `tests/engine/helpers/continuation-cue.test.mjs`, and `tests/integration/cli/enter-phase.test.mjs`. Done when the bounded entry output keeps the exact Readiness `advance-status` command before Final work; after that synchronization the header directs immediate execution for an entry-admitted empty first inventory or entry-admitted zero-append newer Final lineage, directs latest refinement only for a current-lineage report, leaves HITL1/HITL2 required-interaction and non-terminal silence unchanged, gives work-unit guidance no lifecycle header, and adds no cue enum/state/transport. (@impl CPT-003, SWE-004, WNC-005, WNC-008)
- [x] 4.4 Align `DEEP_RESEARCH_HARNESS/RUN.md`, `DEEP_RESEARCH_HARNESS/COMMANDS.md`, and `openspec/constitution/evolution/helper-oriented-agent.md` with the same deliver-first terminal interaction contract. Done when all three distinguish terminal lifecycle from interactive delivery, preserve entry then status synchronization, say the entry-admitted bundle base or post-rerun next version publishes before asking, keep presentation feedback on Final, return ordinary publisher/repair mechanics to the Agent, write nothing for satisfaction, preserve non-terminal silence, and route only research expansion through C5 without treating chat or user direction as Engine authority. (@impl ACS-001, CPT-003, CDP-004, SWE-004, WNC-008)
- [x] 4.5 Align `DEEP_RESEARCH_HARNESS/rb_templates/BUNDLE_ENTRY.md.tmpl`, `DEEP_RESEARCH_HARNESS/command_playbook/continue-run-bundle.md`, and `DEEP_RESEARCH_HARNESS/command_playbook/start-research.md`. Done when new and resumed entry surfaces distinguish admitted empty-bundle base, admitted zero-append post-rerun delivery, and current-lineage refinement; preserve the two-HITL/non-terminal-silence rhythm; and do not infer satisfaction, reissue an already bound report, accept premature inventory, or default clean Final to C5. (@impl CPT-003, RUE-005, RRD-008, CDP-004, SWE-004, WNC-008)
- [x] 4.6 Align `DEEP_RESEARCH_HARNESS/command_playbook/persist-artifact.md`, `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-hitl2.md`, `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-schemas.md`, and `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-anti-cheating-rules.md`. Done when primary delivery requires admitted Final entry plus canonical publication and legal current-lineage binding, presentation feedback remains on Final, only evidence-expanding feedback uses C5/HITL2 rerun semantics, retained staging/sweep repair stays Agent-owned, and no surface restores arbitrary Final targets, blanket C5, hidden Final transitions, or handoff rewrite. (@impl ACS-001, ARP-004, CPT-003, CDP-004, CDP-005, CDP-006, POF-001, WNC-005)
- [x] 4.7 Extend `tests/integration/md/artifact-persistence-contract.test.mjs`. Done when deterministic assertions cover bundle-base versus later global append naming, primary publisher/no-clobber vocabulary, reserved targets, retained staging/sweep repair, immutable earlier bytes, and rejection of caller-chosen versions or arbitrary primary targets without claiming an Agent followed the prose. (@impl ARP-004, CDP-005, CDP-006)
- [x] 4.8 Extend `tests/integration/md/final-report-composition-contract.test.mjs`, `tests/integration/md/post-final-recovery-contract.test.mjs`, and `tests/integration/md/self-documenting-workflow-nodes.test.mjs`. Done when they cover current-lineage handoff preservation, Final entry admission wording, satisfaction-without-write, prior-inventory zero-append delivery pending, proven append refinement, former blanket-C5 rejection across HITL2/shared/entry guidance, helper-oriented constitutional alignment, non-terminal silence, and rejection of premature inventory, Final self-loop/Gate, handoff/profile rewrite, or report-quality Engine checks without converting instruction text into Agent-behavior evidence. (@impl ACS-001, CPT-003, RUE-005, RRD-008, CDP-003, CDP-004, POF-001, SWE-004, WNC-005, WNC-008)

## 5. Final Reentry And C5 Boundary

- [x] 5.1 Extend the shared handoff projection built in task 4.2 so a newer legal Final handoff retires accepted C5 current recovery ownership, projects the exact normal entry owner until load and the existing Readiness status-sync owner after load, and continues to return the retired event/lineage as an audit witness. Done when `DEEP_RESEARCH_HARNESS/engine/helpers/handoff-helpers.mjs` and `tests/engine/handoff-helpers.test.mjs` distinguish still-active C5, entry-pending newer Final, loaded-pending-status Final, synchronized-delivery-pending Final, retired-by-newer-Final, conflicting descendant evidence, and no event; preserve existing descendant-stage behavior; and do not themselves classify feedback, publish a report, or expose fresh C5 eligibility. (@impl CPT-003, RRD-007, RRD-008, POF-001)
- [x] 5.2 Consume that retired witness in `DEEP_RESEARCH_HARNESS/engine/helpers/post-final-recovery.mjs` and its existing lineage consumers, with focused coverage in `tests/engine/helpers/post-final-recovery.test.mjs` and `tests/integration/cli/post-final-lineage-consumers.test.mjs`. Done when a handoff without a Final load projects exact entry ownership; an admitted load before terminal status synchronization projects exact `advance-status`; only after synchronization does a unique zero-append digest match return current Final immediate-delivery owner and block fresh C5; a unique match after removing one or more highest canonical revisions binds the newest report to the newer lineage and permits later fresh C5 intent; any prior-byte/supplementary/inventory drift blocks; existing accepted v1 workspaces/events remain backward-readable; and no C5 schema field, delivery event, timestamp authority, profile counter, or current pointer is added. (@impl CPT-003, RRD-007, RRD-008, CDP-005, POF-001)
- [x] 5.3 Update `DEEP_RESEARCH_HARNESS/engine/helpers/recovery-contract.mjs` and `DEEP_RESEARCH_HARNESS/cli/check-reentry.mjs` to consume primary inventory and implement precedence: accepted publication owner, inventory blocker, still-active accepted C5 workspace/lineage stage, newer-Final entry pending with exact `enter-phase` boundary, loaded-pending-status with exact `advance-status`, synchronized newer-Final delivery pending/refinement from prior-inventory proof, otherwise current Final owner. Done when `tests/engine/helpers/recovery-contract.test.mjs` and `tests/integration/cli/check-reentry.test.mjs` prove empty first delivery, ordinary committed refinement, pre-load post-C5 drift/entry ownership, post-load status synchronization, synchronized zero-append delivery pending, one-or-more-append latest refinement, publication recovery, ambiguous/drifted inventory, accepted C5 takeover, existing non-Final compatibility, read-only behavior, and no default C5 preparation from mere mechanical eligibility. (@impl CPT-003, RRD-007, RRD-008, POF-001)
- [x] 5.4 Refine `DEEP_RESEARCH_HARNESS/engine/helpers/post-final-recovery.mjs`, its CLI/playbook projections, and focused unit/integration tests so side-effect-free inspect may expose availability but only a strict Agent-selected `apply` request establishes C5 ownership. Done when inspect/no-apply creates no C5 workspace/event/profile mutation, a valid strict request retains existing identity/lineage/rerun-limit checks and replay stages, accepted C5 wins over Final exactly once, the Engine never parses free-form reason/scope or decides whether a report is technical enough, and deterministic tests explicitly leave presentation-versus-research classification to guidance plus the selected Agent-flow observation. (@impl ACS-001, RRD-008, POF-001)

## 6. End-To-End Verification Assets

- [x] 6.1 Add `tests/e2e/final-refinement-continuity.test.mjs` using real production CLIs and isolated temporary bundles. Done when one negative pre-entry case rejects a premature primary-looking file without entry mutation, while the clean legal Readiness-to-Final chain admits an empty primary baseline, completes the exact Readiness status synchronization, and commits `final/final.md`, unlabelled v1, and labelled global v2; proves all earlier bytes immutable, every version independently backed, `current_node` continuously Final, no Final transition/Gate/satisfaction fact, clean reentry latest owner, and accepted C5 ownership only after an explicit evidence-expanding request. The fixtures SHALL be labelled simulated Agent actions and SHALL make no semantic-quality/user-satisfaction or production-repair claim for removal of the negative fixture. (@impl ARP-004, CPT-003, RRD-007, RRD-008, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, WNC-005)
- [x] 6.2 Extend `tests/e2e/post-final-rerun-lineage-continuity.test.mjs` through a newer legal Readiness-to-Final return and primary publication. Done when production lineage helpers/CLIs prove the accepted C5 lineage remains audit-bound through descendants while current recovery ownership retires at the newer Final handoff, Final entry rejects any pre-load inventory drift, exact event-bound prior inventory admits the newer Final load, existing `advance-status` remains the only owner until terminal synchronization, synchronized unchanged prior inventory projects immediate delivery and blocks fresh C5, publication appends the next global version without resetting the base, immutable-prefix proof binds that report to the newer lineage, clean reentry projects refinement, and only a later explicit evidence-expanding request may establish another C5 owner. The fixture SHALL remain deterministic mechanism evidence. (@impl ARP-004, CPT-003, RRD-007, RRD-008, CDP-005, POF-001)
- [x] 6.3 Add and register `experiments_playbook/exp_wff_delivery/case-138-standard-final-refinement.md` with one legal setup-only Final boundary, exactly one minimal finding/Evidence Map row, compact-report instructions, no network research, the existing iterative Subject runner family, and five supplied turns for base delivery, unlabelled v1, labelled v2, satisfaction, and same-lineage evidence expansion. Done when the fixture creates no report or Subject/native verdict before launch, registry/manifest routing is current, case 137 remains quarantined, and the playbook declares only the bounded procedural claim. (@impl CDE-003, CDP-003, CDP-004, POF-001)
- [x] 6.4 Add the case-138 per-turn observation and deterministic verdict bindings using the existing one-session adapter hooks; extend focused adapter/static tests. Done when turn 4 freezes inventory/status/profile/trace before turn 5, turn 5 selects/accepts C5 without another report or completed rerun, retained Subject/native facts bind every verdict, and static checks cannot claim report improvement, generic intent accuracy, genuine satisfaction, or substitute for native behavior. (@impl CDE-003, CDP-004, POF-001)
- [x] 6.5 Validate case 138 with the canonical playbook validator, manifest parser, and focused adapter/static tests, then run the user-confirmed single replacement Agent Experiment Autorun using a real authenticated Subject Agent with a hard 120-second total Subject-runtime cap. Done only when native completion PASS and durable per-turn Subject/native evidence establish the selected procedural claim; the original `NOT_RUN` remains quarantined, and any replacement timeout, threshold breach, missing actor/tool/health, other native failure, or fixture/Playbook-Agent substitution triggers no further automatic retry or completion claim. (@impl CDE-003)
- [x] 6.5a Bind the user-confirmed replacement authorization into case-138's playbook/runner-facing metadata without deleting or overwriting the first-run diagnostic boundary. Done when exactly one replacement invocation is recognizable as explicitly replanned, retains the 120-second cap and all `NOT_RUN` exclusions, and static validation rejects any further automatic retry. (@impl CDE-003)

## 7. Regression And Specification Closure

- [x] 7.1 Run the selected focused unit, integration, deterministic E2E, workflow/package, persistence, backing, phase-entry/status, reentry, C5, and Markdown tests, then run canonical full-suite `npm test`. Done when all selected and full-suite tests pass with no unrelated repair, every failure is traced to its direct owner, and deterministic fixtures are reported only as mechanism evidence. (@impl ACS-001, ARP-004, CPT-003, RUE-005, RRD-007, RRD-008, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, SWE-004, WNC-005, WNC-008)
- [x] 7.1a User-authorized regression repair: align Heavy health source recoverability with the current Wave0 submitted `source_contribution` rather than retired `reference` outputs. Done when a valid current `source_yaml` submission with URL-backed metadata and its declared cache trail reports recoverability `clean`; missing cache leaves still report `issues`; and the focused test passes without restoring retired output roles or claiming Agent behavior. (@impl EXO-001, EXO-002)
- [x] 7.1b Closeout finding: make the standard health timeline recognize the existing `setup-ready` `route_pending` log projection as its trace `gate_attempt`. Affected reader question: does the health report distinguish a missing log record from a legal pending-route projection? Authoritative owner: `experiments_env/shared/verify-bundle-health.mjs`. Smallest repair: count only a `route_pending` log record carrying both `gate` and `gate_attempt_id` as a gate-attempt projection; retain literal mismatch failures. Done when the focused health test proves the legal projection is clean and a missing/unbound log still reports `issues`. (@impl EXO-004)
- [x] 7.2 Synchronize all ten delta specs into their existing accepted main-spec owners through the supported OpenSpec sync route and semantically re-compare each changed requirement with implementation and selected evidence. Done when accepted specs retain stable requirement IDs/anchors, CPT-003 requires empty first-Final or exact post-C5 prior-inventory admission before a new load, ARP-004 names the primary publisher/no-clobber history, Final remains terminal but deliver-first interactive, clean reentry owns Final until accepted C5 lineage, a newer Final handoff requires a proven global append before fresh C5 eligibility, current-lineage handoffs and earlier history remain distinct, case 138 has its honest evidence boundary including its one user-confirmed replacement limit, no delta headers enter main specs, and no old blanket-C5/profile-counter/handoff-rewrite behavior survives as current authority. (@impl ACS-001, ARP-004, CPT-003, RUE-005, RRD-007, RRD-008, CDE-003, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, SWE-004, WNC-005, WNC-008)
- [x] 7.3 Run `openspec validate iterate-final-delivery-in-place --strict`, `node openspec/governance/check-verification-routing.mjs --change iterate-final-delivery-in-place --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change iterate-final-delivery-in-place --mode assets`, and `git diff --check`. Done when all pass against actual files/manifest, semantic fragments and consumer/derived roles match the implemented revision, all selected assets exist, and structural PASS is not presented as Agent behavior or semantic completeness. (@impl ARP-004, CPT-003, RRD-008, CDE-003, CDP-006, POF-001)
- [x] 7.4 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change iterate-final-delivery-in-place`. Done when it passes with zero duplicate, orphan, unregistered, and reused-retired requirement IDs and the new semantic fact family is not misrepresented as a new requirement/capability identity. (@impl ACS-001, ARP-004, CPT-003, RUE-005, RRD-007, RRD-008, CDE-003, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, SWE-004, WNC-005, WNC-008)
- [x] 7.5 Run `node openspec/governance/check-project-specs.mjs`. Done when it passes with zero `deltaHeaderInMain`, `missingPurpose`, `missingRequirements`, and `missingReqHeader` findings after sync. (@impl ACS-001, ARP-004, CPT-003, RUE-005, RRD-007, RRD-008, CDE-003, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, SWE-004, WNC-005, WNC-008)
- [x] 7.6 `openspec-feedback:closeout-review` Before archive, obtain current Archive guidance and review the change-scoped actual diff, all ten delta/main comparisons, Final entry admission, primary-series/no-clobber implementation, Final/C5 owner precedence, case 138 native evidence, full selected verification, `semantic-closure.yaml`, and `verification-plan.yaml`. Reassess every fragment, authority-establishing surface, verdict consumer, derived projection, proof claim, compatibility boundary, and excluded control layer; record each finding as an ordinary unchecked task with requirement/reader question, authoritative owner, smallest repair, and independently observable done condition. Complete only when all findings and ordinary tasks are closed and no report-quality, generic Agent-compliance, or genuine-user-satisfaction claim exceeds the evidence. (@impl ACS-001, ARP-004, CPT-003, RUE-005, RRD-007, RRD-008, CDE-003, CDP-003, CDP-004, CDP-005, CDP-006, POF-001, SWE-004, WNC-005, WNC-008)

After every checkbox is complete, use
`node openspec/governance/finalize-change-archive.mjs --change iterate-final-delivery-in-place`
as the only supported final archive transition.
