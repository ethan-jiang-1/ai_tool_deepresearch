## 1. Plan Review and Migration Baseline

- [x] 1.1 Complete the change-scoped plan review (`openspec-feedback:plan-review`) before any target edit. Verify that RET-001/RET-002/RET-003/RET-006 and CHF-003 have one direct owner, that the two nested deltas match the proposal's discovery record, and that the catalog remains navigation rather than behavior authority.
- [x] 1.2 @impl RET-001, RET-002, RET-003, RET-006, CHF-003: Validate `verification-plan.yaml` with `check-verification-routing.mjs --mode plan`; validate `taxonomy-map.yaml` as 84 unique legacy paths, canonical paths, and prefixes in approved domains; capture current main-spec IDs, requirement counts, `> req:` headers, and active-change delta paths in change-root `migration-evidence.md`. Done when the evidence gives a one-to-one before snapshot and names any active old-path delta that must move.
- [x] 1.3 @impl RET-001, RET-002: Immediately before the first move, rerun active-change discovery and focused current-surface path-reference search. Archive/cancel an incompatible active change or rebase its delta to the exact mapped path in this same change; do not begin a mixed flat/nested state with an unaccounted active delta.

## 2. Deterministic Governance Boundaries

- [x] 2.1 @impl RET-001: Extend `openspec/governance/check-project-reqs.mjs` to resolve each live `prefixes:` owner to its complete canonical main-spec path while retaining valid retired and documented sub-prefix cases. Add focused nested-path, flat/missing-target, sub-prefix, and retired-prefix coverage in `tests/integration/governance/check-project.test.mjs` without weakening existing ID-consistency cases.
- [x] 2.2 @impl RET-003, RET-006: Add `openspec/governance/check-capability-taxonomy.mjs` with direct failures for invalid two-level main/active-delta paths, unapproved domains, catalog inventory mismatch, unresolved project-local relations, and missing explicit ownership fields. Add `tests/integration/governance/check-capability-taxonomy.test.mjs` covering valid nested data and each bounded failure without testing semantic classification quality or optional skill installation.
- [x] 2.3 @impl RET-003, RET-006: Add `openspec/governance/check-capability-discovery.mjs --change <change>` that validates the proposal heading/table, full-path candidates, allowed dispositions, reasons, and the explicit `skip_specs` non-applicability branch only. Add `tests/integration/governance/check-capability-discovery.test.mjs` proving valid, malformed, unsupported-disposition, missing-reason, and skip-specs fixtures without treating a structural pass as semantic approval.
- [x] 2.4 @impl CHF-003: Wire both new checkers into `openspec/governance/finalize-change-archive.mjs` after requirement/main-spec governance and before verification routing. Extend its result schema, ordered check IDs, failure codes, direct repair owners, and `tests/integration/governance/change-feedback-finalizer.test.mjs`; make that fixture a valid nested taxonomy/catalog/discovery fixture before it exercises later failure boundaries.
- [x] 2.5 @impl RET-003: Repair `check-capability-taxonomy.mjs` to aggregate rows from the catalog's documented domain-scoped tables rather than stopping after the first table; add a multi-table valid fixture. Done when a catalog split into `agent` and `engine` sections is accepted and single-table validation coverage remains green.

## 3. Rebaseline Paths and Registry Coordinates

- [x] 3.1 @impl RET-001, RET-002: Use the reviewed `taxonomy-map.yaml` to move the 15 `agent` and 15 `engine` main-spec directories with explicit `git mv` operations. Preserve each leaf's requirement body and `> req:` header, create no flat compatibility copy, and record the completed mapping rows in `migration-evidence.md`.
- [x] 3.2 @impl RET-001, RET-002: Use the reviewed map to move the 9 `bundle` and 22 `research` main-spec directories with explicit `git mv` operations. Preserve each leaf's requirement body and `> req:` header, create no flat compatibility copy, and record the completed mapping rows in `migration-evidence.md`.
- [x] 3.3 @impl RET-001, RET-002: Use the reviewed map to move the 8 `verification`, 11 `workflow`, and 4 `governance` main-spec directories with explicit `git mv` operations. Ensure the two change deltas continue to target `governance/requirement-traceability` and `governance/change-feedback-loop`, and record the completed mapping rows in `migration-evidence.md`.
- [x] 3.4 @impl RET-001, RET-002, RET-003: Update `openspec/governance/req-registry.yaml` so live `prefixes:` values and live group headings use the mapped full paths; extend RET-003 in place during spec sync, retain retired and sub-prefix annotations without inventing new live owners, and allocate no new requirement ID. Repair only the four placeholder main-spec Purpose sections for `verification/experiment-agent-autorun`, `bundle/artifact-persistence-recovery`, `research/user-research-controls`, and `research/plan-hostfile-sections`, grounding each sentence in its existing requirement body and changing no requirement behavior.

## 4. Catalog and Coding-Agent Discovery Surface

- [x] 4.1 @impl RET-003: Create the catalog header and the 15 `agent` rows in `openspec/specs/README.md` from moved main-spec Purposes and `taxonomy-map.yaml`. Include exact paths, task-language keywords, boundaries/neighbors, typed relations, and explicit Agent/Markdown plus Engine/Node ownership; preserve main-spec authority.
- [x] 4.2 @impl RET-003: Add the 15 `engine` catalog rows with the same row contract. Resolve only project-local related entries; leave any GRILLME or other operation-skill reference explicitly optional.
- [x] 4.3 @impl RET-003: Add the 9 `bundle` catalog rows with the same row contract. Resolve only project-local related entries; leave any GRILLME or other operation-skill reference explicitly optional.
- [x] 4.4 @impl RET-003: Add the first 11 `research` catalog rows in `taxonomy-map.yaml`, from `research/canonical-topic-state` through `research/pre-research-phase-content`, with the same row contract. Keep research semantics in the cited main specs and state only concise navigation metadata in the catalog.
- [x] 4.5 @impl RET-003: Add the remaining 11 `research` catalog rows in `taxonomy-map.yaml`, from `research/research-access-adapter` through `research/wave2-synthesis`, with the same row contract. Keep research semantics in the cited main specs and state only concise navigation metadata in the catalog.
- [x] 4.6 @impl RET-003: Add the 8 `verification` and 4 `governance` catalog rows with the same row contract. Preserve main-spec authority and resolve only project-local related entries.
- [x] 4.7 @impl RET-003: Add the 11 `workflow` catalog rows with the same row contract. Verify that the completed catalog has exactly 84 live rows, no flat identity, and no duplicate capability row.
- [x] 4.8 @impl RET-003: Update `openspec/config.yaml` context and proposal/spec rules to require catalog/list lookup, candidate main-spec inspection, full-path discovery records, reuse before New, and exact nested delta paths without copying the 84-row catalog into recurring context. Update current guidance, glossary/ADR knowledge surfaces, and focused Markdown tests that teach accepted-spec paths; preserve historical paths under `openspec/changes/archive/` and do not touch `DEEP_RESEARCH_HARNESS/` unless a concrete current reference is found.
- [x] 4.9 @impl RET-002, RET-004: Repair current registry-group teaching that still presents leaf-only headings after the nested migration. The authoritative owner is `governance/requirement-traceability`; update the config context and its illustrative main-spec coordinates without allocating an ID or changing runtime behavior. Done when they teach full `domain/capability` headings and a focused current-surface scan finds no live leaf-only registry-group example.
- [x] 4.10 @impl RET-003: Repair the active catalog-discovery teaching examples that still present leaf names as capability identities. The authoritative owner is `governance/requirement-traceability`; update `config.yaml` and the active RET-003 delta/main wording to use canonical paths. Done when the examples name `agent/hitl-ux` and `workflow/silent-wave-execution`, delta and main agree, and no live identity example uses either leaf alone.

## 5. Spec Sync and Focused Verification

- [x] 5.1 @impl RET-001, RET-002, RET-003, RET-006, CHF-003: Run the supported OpenSpec spec-sync flow for this change after the moved main specs and registry are coherent, then re-read the synchronized `governance/requirement-traceability` and `governance/change-feedback-loop` main specs. Done when RET-001/RET-002/RET-003/RET-006 and CHF-003 are present at their new canonical paths and no flat main-spec duplicate remains.
- [x] 5.2 @impl RET-001, RET-002, RET-003, RET-006: Run `openspec list --specs --json`, `openspec show agent/agent-command-surface --type spec --json --requirements`, strict main-spec/change validation, `check-project-reqs.mjs`, `check-project-specs.mjs`, `check-capability-taxonomy.mjs`, `check-capability-discovery.mjs --change rebaseline-capability-taxonomy`, and verification-routing asset validation. Record native output summaries and any repair in `migration-evidence.md`; do not claim catalog semantic correctness beyond these checks.
- [x] 5.3 @impl RET-001, RET-003, RET-006, CHF-003: Run every selected integration asset from `verification-plan.yaml`: `check-project.test.mjs`, `check-capability-taxonomy.test.mjs`, `check-capability-discovery.test.mjs`, `change-feedback-finalizer.test.mjs`, `canonical-harness-vocabulary-contract.test.mjs`, and `agent-experiment-autorun-terminology.test.mjs`. Record deterministic-contract results only; do not claim real-Agent or runtime proof.
- [x] 5.4 @impl RET-001, RET-002, RET-003: Compare the final main-spec inventory against the pre-move snapshot and map. Record 84 one-to-one successors, unchanged pre-existing requirement IDs and headers, one catalog row and registry owner per live spec, no active delta that can recreate a flat path, and no accidental current-surface flat reference in `migration-evidence.md`.
- [x] 5.5 @impl EXA-001, VER-006: Make the static test-source walk in `agent-experiment-autorun-terminology.test.mjs` tolerate a temporary test directory that disappears during a concurrent `node --test` run. Done when the selected six-file integration invocation no longer fails with `ENOENT` and all assertions remain active.

## 6. Review and Archive Readiness

- [x] 6.1 Complete the change-scoped closeout review (`openspec-feedback:closeout-review`) after the actual diff, synchronized specs, catalog, governance results, and selected evidence are reviewed. If any issue remains, add an ordinary pending task naming the affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition before this marker can be completed.
