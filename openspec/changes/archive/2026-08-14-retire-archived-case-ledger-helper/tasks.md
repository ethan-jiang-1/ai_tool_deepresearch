## 0. Apply Entry Review

- [x] 0.1 @impl C1f reader question `openspec-feedback:plan-review` - Before the first target edit, read current feedback-loop Apply guidance and review proposal, design, `skip_specs: true` rationale, semantic-closure rationale, verification plan, user migration-record decision, current helper/test evidence, archived migration-boundary evidence, and protected manifest/Supervisor paths. Record every actionable finding as an ordinary pending task with its reader question, authoritative owner, smallest repair, and independently observable done condition; complete only when none remains.
- [x] 0.2 @impl C1f reader question - Before the first target edit, run strict change validation, plan-mode requirement governance, project-spec governance, capability taxonomy/discovery, verification-routing plan check, semantic-closure plan check, and `git diff --check`. Done when every command exits 0; record structural results only, without claiming runtime or Agent-behavior proof.

## 1. Retire The Archive-Only Helper Cluster

- [x] 1.1 @impl C1f reader question - Immediately before editing, repeat exact current-surface searches for `normalizeLedgerBundlePlan`, `validateCaseCompatibilityLedger`, `readCaseCompatibilityLedger`, `case-compatibility-ledger`, and the dedicated archive-path test helper across Harness, tests, accepted specs, experiments, and guidance. Allow only the three source definitions, their dedicated test projection, and active-change/historical archive records; inspect `readAndValidateManifest()` and focused current-path tests. Done when no supported current caller remains or every finding is recorded as ordinary repair work.
- [x] 1.2 @impl C1f reader question - Delete only the complete archive-only helper cluster from `DEEP_RESEARCH_HARNESS/host_tools/lib/agent-experiment-contract.mjs` and its dedicated archive-path test/helper/imports from `tests/host_tools/agent-experiment-autorun.test.mjs`. Do not alter manifest parsing, runnable-corpus discovery, V2 frontmatter validation, Supervisor selection/lifecycle/native completion/health/audit/cleanup, `yaml` dependency loading, remaining tests, current playbooks, accepted specs, or archived artifacts; add no alias, tombstone, fallback, migration, count lock, or reader branch. Done when the retired identities and dedicated archive path are absent and all protected surfaces are unchanged.

## 2. Verify The Current Autorun Contract

- [x] 2.1 @impl C1f reader question - Run `node --test tests/host_tools/agent-experiment-autorun.test.mjs tests/integration/host_tools/agent-experiment-targeting.test.mjs tests/integration/host_tools/run-agent-experiment.test.mjs`. Done when all selected current-path suites pass; treat this only as deterministic unit/integration regression evidence.
- [x] 2.2 @impl C1f reader question - Run `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`. Done when it reports `passed: true`; treat this as package-consistency evidence only.
- [x] 2.3 @impl C1f reader question - Run scoped absence and protected-surface review. It SHALL prove the three helper identities, dedicated test, and current archive-path test plumbing are absent from current source/tests/main specs/guidance while allowing active-change/historical archive prose; it SHALL prove `readAndValidateManifest()`, runnable-corpus discovery, V2 frontmatter validation, Supervisor source outside the removed cluster, current playbooks, package dependencies, and all remaining tests are unchanged. Done when every unexpected reference or protected change is repaired or recorded as ordinary repair work.
- [x] 2.4 @impl C1f reader question - Run `git diff --check`, strict change validation, `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-archived-case-ledger-helper`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, verification-routing assets, and semantic-closure assets checks. Done when every command exits 0 and results are recorded as structural or selected-regression evidence only.

## 3. Closeout And Archive

- [x] 3.1 @impl C1f reader question `openspec-feedback:closeout-review` - Before archive, read current feedback-loop Archive guidance and review the change-scoped actual diff, helper/archive-path absence result, protected current-manifest/Supervisor evidence, selected verification result, `skip_specs` rationale, and semantic-closure reason. Add and close ordinary repair tasks for every finding; complete only when no actionable finding remains.
- [x] 3.2 @impl C1f reader question - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-archived-case-ledger-helper`. If finalization fails, restore this task to unchecked and repair its named root.
