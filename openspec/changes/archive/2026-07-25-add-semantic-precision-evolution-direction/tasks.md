## 1. Apply Preconditions And Planning Reconciliation

- [x] 1.1 Reconcile the existing change-root verification-plan.yaml with GCO-007/GCO-008: canonical new path/title, the complete EWD 340 Argument Four source paragraph plus clearly labelled project interpretation, current ordered triad, and no claim of Agent comprehension or permanent numeric cardinality. Run openspec validate add-semantic-precision-evolution-direction --strict and node openspec/governance/check-verification-routing.mjs --change add-semantic-precision-evolution-direction --mode plan. Done when the plan selects only static documentation/config integration proof.
- [x] 1.2 Re-establish the baseline for tests/integration/md/evolution-direction-governance.test.mjs and record the pre-change hierarchy defects: Charter defers externally, every other guideline has external `defers_to` entries, and support-guideline siblings omit the current direction. Confirm DPT_FRAMEWORK, runtime behavior, archived/closed records, and the delegated-work change artifacts remain excluded.
- [x] 1.3 Record the user-directed scope expansion: audit every effective `guidelines/*.md` for Charter-root hierarchy and same-layer navigation; preserve factual mechanism prose while removing external/downstream navigation targets.

## 2. Abstraction-As-Semantic-Precision Direction And Core Route

- [x] 2.1 @impl GCO-007: Create guidelines/evolution-abstraction-semantic-precision.md with Charter-only companion frontmatter; the complete original EWD 340 Argument Four paragraph containing the canonical sentence and its primary-source link; an immediately following, clearly labelled Chinese project-context interpretation; the core claim that an abstraction earns its place by enabling precise bounded reasoning; the Chinese “引入一个新东西之前，先退后一步” reflection; scoped precision/explicit-unknown guidance; gradual convergence; and a non-runtime boundary. Done when source text and interpretation are visibly distinct, frontmatter defers only to Project Charter, the document asks for question, distinctions, and normal reasoning stop point as connected thought rather than a fixed checklist, and it does not import Source-of-Record, legal-repair, permission, or deterministic-verdict rules from other owners.
- [x] 2.2 @impl GCO-007, GCO-008: Update guidelines/project-charter.md and guidelines/README.md. Done when Project Charter has no `defers_to`, has a short Dijkstra entry context, and routes only to same-layer guidance; Guidelines Index defers only to Charter; constitutional Reading Order/Related Guidance does not descend to AGENTS/config/spec/framework/experiment/bundle targets; and both files expose semantic precision → simple reliable control → helper-oriented responsibility without presenting the new direction as runtime authority.
- [x] 2.3 @impl GCO-008: Update openspec/config.yaml, guidelines/evolution-simple-reliable-control.md, and guidelines/evolution-helper-oriented-agent.md. Done when relevant proposal/design work is directed to leave a brief semantic reflection before the simplicity and helper reviews, while the two existing companions defer only to Project Charter, retain their separate ownership, and neither becomes a duplicate semantic-precision rulebook.
- [x] 2.4 @impl GCO-008: Audit and normalize the remaining effective guidelines: agentic-execution-model, agentic-queue-mechanism, agentic-subagent-mechanism, agentic-workflow-mechanism, command-experiments, framework-runtime-boundary, and logging-conventions. Done when each defers only to Project Charter; its `siblings` route places Charter first and the applicable semantic → simple → helper companions in order; and its navigation blocks do not route outside `guidelines/`. Preserve factual mechanism prose and focused same-layer links.

## 3. Focused Documentation-Contract Regression

- [x] 3.1 @impl GCO-007, GCO-008: Update tests/integration/md/evolution-direction-governance.test.mjs. Done when it asserts the new canonical path/title, the complete EWD 340 Argument Four source-context markers and clearly labelled project interpretation, the current ordered triad in Charter/README/config, Project Charter's absence of `defers_to`, every other effective guideline's Charter-only `defers_to`, ordered sibling routing, and no external/downstream constitutional navigation; it SHALL NOT claim to prove model comprehension, runtime behavior, or a permanently fixed number of directions.

## 4. Verification And Archive Readiness

- [x] 4.1 Run node --test tests/integration/md/evolution-direction-governance.test.mjs, git diff --check, openspec validate add-semantic-precision-evolution-direction --strict, and node openspec/governance/check-verification-routing.mjs --change add-semantic-precision-evolution-direction --mode assets. Record that this is guidance-only and requires no version bump.
- [x] 4.2 Run node openspec/governance/check-project-reqs.mjs and confirm 0 duplicate / 0 orphan / 0 unregistered / 0 reusedRetired.
- [x] 4.3 Before archive, use the OpenSpec sync/archive flow to merge the GCO-007/GCO-008 delta into the accepted guidance-constitution spec, then run node openspec/governance/check-project-specs.mjs and confirm 0 deltaHeaderInMain / 0 missingPurpose / 0 missingRequirements / 0 missingReqHeader.

## Verification Record — 2026-07-25

- 4.1: `node --test tests/integration/md/evolution-direction-governance.test.mjs` passed 5/5; `git diff --check`, `openspec validate add-semantic-precision-evolution-direction --strict`, and `node openspec/governance/check-verification-routing.mjs --change add-semantic-precision-evolution-direction --mode assets` passed. This is guidance-only; no version bump is required.
- 4.2: `node openspec/governance/check-project-reqs.mjs` passed with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired.
- 4.3: GCO-007/GCO-008 were synced into `openspec/specs/guidance-constitution/spec.md`; `node openspec/governance/check-project-specs.mjs` passed with 0 violations (including 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader).
