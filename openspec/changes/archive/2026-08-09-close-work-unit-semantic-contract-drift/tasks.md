## 0. Plan Review And Entry Checks

- [x] 0.1 Complete the scoped plan review for DEW-004, DEW-013, SNC-006, WAI-005, and WPG-007 (`openspec-feedback:plan-review`): compare the proposal, four delta specs, design, `semantic-closure.yaml`, and `verification-plan.yaml` against BUG-212/213/214; confirm each affected fact has one resolver, every known verdict consumer, a real negative boundary, immutable v1/v2 compatibility, and no new state/controller/fallback. Record every finding as a new unchecked task with affected requirement/family, authoritative owner, smallest repair, and independently observable done condition before target edits.
- [x] 0.2 Run `node openspec/governance/check-verification-routing.mjs --change close-work-unit-semantic-contract-drift --mode plan` and `node openspec/governance/check-semantic-closure.mjs --change close-work-unit-semantic-contract-drift --mode plan`; resolve any plan root before target edits. Done when both report valid selected proof assets and exactly the three declared semantic fact families.
- [x] 0.3 Repair the malformed `+###` requirement heading in the DEW-004 delta (`agent/delegated-work-units`). Authoritative owner: the DEW-004 delta requirement structure. Smallest repair: remove only the stray diff marker. Done when the current Wave0 requirement is a normal OpenSpec Requirement heading and strict change validation reports no delta-structure failure.

## 1. Assignment Output Obligation

- [x] 1.1 Implement DEW-004/DEW-013 versioned assignment resolution in `DEEP_RESEARCH_HARNESS/schema/contracts/work-unit.mjs`, `DEEP_RESEARCH_HARNESS/engine/work-unit-assignment-contract.mjs`, and the smallest existing claim/submit reconstruction wiring: emit `work-unit.assignment.v3` for new claims; preserve v1/v2 and markerless historical interpretation. Done when v3 primary Wave1 still requires exact declarations, v3 supplementary Wave1 has no generic non-empty declaration requirement, and an empty Wave2 direct-output set retains its existing base declaration behavior.
- [x] 1.2 Implement DEW-013 submit consumption in `DEEP_RESEARCH_HARNESS/engine/work-unit-validation.mjs`/existing submit callers without adding an alternate success path. Done when dry-submit and formal submit read the same version-selected resolved contract, an already claimed v2 supplementary attempt retains manifest/beacon parity, and a valid v3 empty supplementary result still must pass identity, receipt, source claim, accepted URL, cache/degraded, queue, and provenance checks.
- [x] 1.3 Add DEW-004/DEW-013 focused and CLI regressions in `tests/engine/work-unit-assignment-contract.test.mjs`, `tests/engine/work-unit-submit.test.mjs`, and `tests/integration/cli/operate-work-unit.test.mjs`. Done when they prove v3 empty supplementary `output_files[]` passes dry/formal submit, missing primary required declarations fail, v1/v2 and Wave2 requiredness are not reinterpreted, and no submit/gate authority is created by dry-submit.
- [x] 1.4 Update the current assignment-binding guidance in `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-schemas.md` for DEW-004's `work-unit.assignment-output-obligation` family. Authoritative owner: `resolveWorkUnitAssignmentContract`. Smallest repair: state v3 as the new-claim marker and v1/v2 as immutable historical interpretations. Done when the shared Agent-facing contract no longer identifies v2 as current and accurately preserves v1/v2 compatibility.

## 2. Source-Claim Provenance

- [x] 2.1 Implement SNC-006's one pure current-or-prior source-ref authorization helper in `DEEP_RESEARCH_HARNESS/engine/work-unit-validation.mjs`, reusing the existing hash-valid ledger/index/manifest/queue/Topic lineage inputs and preserving stable diagnostic coordinates. Done when formal submit and dry-submit consume it without accepting filesystem-only, cross-Topic, wrong-role, wrong-wave/kind, ambiguous, or invalid prior paths.
- [x] 2.2 Implement WAI-005 consumption in `DEEP_RESEARCH_HARNESS/engine/helpers/wave1-reference-convergence.mjs`: reviewed backing shall call the same source-ref helper, then retain its own reviewed-row accepted-URL, current cache/degraded, and physical source-path projection validation. Done when a legal prior evidence-summary no longer requires duplicate current `output_files[]`, while bad current cache/degraded backing and missing physical source backing still fail distinct submitted-backing roots.
- [x] 2.3 Add SNC-006 and WAI-005 regression coverage in `tests/engine/work-unit-submit.test.mjs` and `tests/integration/cli/wave1-reference-convergence.test.mjs`. Done when the same exact prior source ref passes formal submit and Wave1 reviewed backing, and each neighboring invalid authorization, cache, or physical-projection case remains rejected at its correct root.

## 3. Ledger And Supersession Consumer

- [x] 3.1 Implement WPG-007 in `DEEP_RESEARCH_HARNESS/engine/helpers/gate-helpers-provenance.mjs`: have bypass classification consume both current and `historical` results from `evaluateNormalizedSubmittedWorkUnitLedger`; suppress a raw row only when it exactly matches a `hash_valid_historical` work-id/hash entry with validated relation and lineage. Retain raw declaration observation for display and genuine raw-only, unsubmitted, or drift evidence. Done when a valid historical predecessor cannot be reported as delegated bypass, but malformed, attributable-drift, or non-submitted current rows remain fail-closed.
- [x] 3.2 Add WPG-007 focused and Wave1 Gate regressions in `tests/engine/helpers/gate-helpers-provenance.test.mjs` and `tests/integration/cli/check-gate-wave1-complete.test.mjs`. Done when supersede then successor submit does not emit `delegated_bypass_suspected` for an exact hash-valid predecessor, while a raw-only or hash-drift fixture still emits the blocking root.

## 4. Version And Spec Sync

- [x] 4.1 Implement VEM-002/VEM-004: create or restore the required repo-root `CHANGELOG.md` as the single concise version source and add the v0.82 entry describing semantic-contract convergence; do not create `DEEP_RESEARCH_HARNESS/CHANGELOG.md`. Done when the root source exists and its latest entry is v0.82.
- [x] 4.2 Implement VEM-003: update the version banner in `DEEP_RESEARCH_HARNESS/RUN.md` to v0.82. Done when it exactly matches the latest root CHANGELOG version.
- [x] 4.3 After the targeted code and regression work is complete, synchronize the approved DEW-004, DEW-013, SNC-006, WAI-005, and WPG-007 delta specs into their accepted main specs. Done when the main specs retain their full prior requirements plus v3 assignment compatibility, current-or-prior source authorization, assignment-output obligation, reviewed-backing, and historical supersession scenarios.

## 5. Semantic Closeout And Archive Preconditions

- [x] 5.1 Complete the actual-surface closeout review for DEW-004, DEW-013, SNC-006, WAI-005, and WPG-007 (`openspec-feedback:closeout-review`): compare the final implementation and tests to all three `semantic-closure.yaml` entries, including v1/v2 compatibility, raw-reader overlaps, established-by surfaces, and every known submit/inspect/Gate consumer. Record any omitted consumer or changed fact as a new unchecked repair task with family, owner, smallest repair, and observable done condition; do not treat structural checker success as semantic completeness.
- [x] 5.2 Run the selected regression assets: `node --test tests/engine/work-unit-assignment-contract.test.mjs tests/engine/work-unit-submit.test.mjs tests/engine/helpers/gate-helpers-provenance.test.mjs tests/integration/cli/operate-work-unit.test.mjs tests/integration/cli/wave1-reference-convergence.test.mjs tests/integration/cli/check-gate-wave1-complete.test.mjs`. Done when all selected unit and integration proofs, including v1/v2 compatibility, pass without claiming Subject-Agent behavior proof.
- [x] 5.3 Run `openspec validate close-work-unit-semantic-contract-drift --strict`, `node openspec/governance/check-verification-routing.mjs --change close-work-unit-semantic-contract-drift --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change close-work-unit-semantic-contract-drift --mode assets`. Done when each passes against the actual version, Engine, and test assets.
- [x] 5.4 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change close-work-unit-semantic-contract-drift` and `node openspec/governance/check-project-specs.mjs`. Done when both pass with zero duplicate, orphan, unregistered, reused-retired, delta-header-in-main, missing-purpose, missing-requirements, and missing-req-header findings.
