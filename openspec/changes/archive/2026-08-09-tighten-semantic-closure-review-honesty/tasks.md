## 0. Apply Gate

- [x] 0.1 @impl SEF-002, CHF-002 `openspec-feedback:plan-review` - Review the proposal, both full modified requirement blocks, design, `verification-plan.yaml`, and `semantic-closure.yaml` before the first target edit. Compare the planned fragment/role rubric with the archived dogfood evidence and current owned guidance surfaces; confirm `not_applicable` still holds and no v2/checker/runtime expansion is hidden. Done when every actionable finding is either absent or recorded as a separate ordinary unchecked task with the affected requirement/reader question, authoritative owner, smallest repair, and independently observable done condition.
- [x] 0.2 @impl SEF-002, CHF-002, VER-001 Run `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change tighten-semantic-closure-review-honesty --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change tighten-semantic-closure-review-honesty --mode plan` before target edits. Done when all three exit zero and the semantic result remains structural rather than semantic-completeness proof.

## 1. Failing Delivery Proof

- [x] 1.1 @impl SEF-002, CHF-002 Extend `tests/integration/governance/change-feedback-finalizer.test.mjs` with assertions over generated proposal, Apply, and Archive instructions plus the central guideline: actual-fragment-or-bare-path author/review guidance, verdict-consumer versus `overlap: derived` classification, ordinary pending-task routing, and the checker's non-semantic boundary. In its existing `SUPPORTED_ENTRY_SURFACES` loop, assert that all eight adapters name the shared guideline. Run the focused asset before changing guidance; done when the new assertions fail for the intended missing guidance rather than fixture setup or an unrelated contract.

## 2. Central Guidance

- [x] 2.1 @impl SEF-002, CHF-002 Update the `openspec/config.yaml` proposal rule for `semantic-closure.yaml` and the `change-feedback-loop/apply` and `/archive` operation guidance. Done when generated proposal instructions retain the catalog and strict branch starters, generated Apply/Archive instructions name the two closure-honesty checks and central guideline, all distinguish verdict consumers from derived Agent-facing projections, and all state that the checker does not validate fragment/role semantics.
- [x] 2.2 @impl SEF-002, CHF-002 Update `guidelines/change-feedback-loop.md` plan and closeout review sections. Done when both revisions require fragment and role review against planned/actual surfaces, preserve explicit unknown, route every false-precision or misclassification finding to ordinary pending work, and do not turn guidance or checker PASS into proof.
- [x] 2.3 @impl CHF-002 Inspect every current path in `SUPPORTED_ENTRY_SURFACES` against the central guidance route. Done when each Apply/Archive entry still obtains current operation guidance and reaches the shared review guideline; edit an adapter only if a recorded ordinary finding proves the route is missing, and do not copy the full rubric into all adapters.

## 3. Native Verification

- [x] 3.1 @impl SEF-002, CHF-002, VER-001 Run `node --test tests/integration/governance/change-feedback-finalizer.test.mjs` after the guidance edits. Done when `node_test_exit` is zero and the recorded result is described only as deterministic instruction/guideline/entry delivery evidence, not Agent semantic-compliance proof.
- [x] 3.2 @impl SEF-002 Re-read the implementation diff against the no-code boundary. Done when `semantic-fact-closure-contract.mjs`, `check-semantic-closure.mjs`, `finalize-change-archive.mjs`, the v1 schema, the family catalog, and archived records are unchanged, or any necessary exception has first been added as an ordinary pending task and approved through the change artifacts.

## 4. Specification And Tracking Sync

- [x] 4.1 @impl SEF-002, CHF-002 Sync the two approved delta requirements into `openspec/specs/governance/semantic-fact-closure/spec.md` and `openspec/specs/governance/change-feedback-loop/spec.md`, then semantically compare each full synchronized requirement with its delta. Done when the main specs preserve every existing scenario plus the approved fragment/role obligations and contain no delta headers.
- [x] 4.2 @impl SEF-002, CHF-002 Update `_backlog/learning/semantic-fact-closure-progressive-rollout-plan-2026-08-09.md` with the user-directed focused-change route, completed task IDs, actual native/governance evidence, current counts, decision-log changes, and one next action. Done when the tracker distinguishes this governance calibration from runtime affected dogfood and does not retroactively rewrite historical evidence.

## 5. Closeout And Archive

- [x] 5.1 @impl SEF-002, CHF-002 `openspec-feedback:closeout-review` - Review the change-scoped actual diff, synchronized main specs, selected native test result, tracker update, and both change-local governance records. Done when actual symbols/bare paths and roles are honest, no unsupported checker/runtime/history edit exists, and every finding has been resolved through an ordinary task or remains explicitly pending with this marker open.
- [x] 5.2 @impl SEF-002, CHF-002, VER-001 Run `openspec validate tighten-semantic-closure-review-honesty --strict`, `node openspec/governance/check-project-reqs.mjs --mode archive --change tighten-semantic-closure-review-honesty`, `node openspec/governance/check-project-specs.mjs`, `node openspec/governance/check-verification-routing.mjs --change tighten-semantic-closure-review-honesty --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change tighten-semantic-closure-review-honesty --mode assets`. Done when every command exits zero; structural/asset PASS is not reported as native test or semantic-review proof.
- [x] 5.3 @impl CHF-002 After all earlier tasks and any review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change tighten-semantic-closure-review-honesty`, because the finalizer requires zero incomplete tasks. If the command returns non-zero, restore this task to unchecked and repair the reported root; do not use a raw move or alternate archive path. Done when the governed finalizer reports native archive success.
