## 1. Sequence, Plan Review, and Baseline

- [x] 1.1 @impl GSK-011, RWG-005, DEW-006: Before any target edit, confirm `remove-recursive-queue-failure-repair` has completed its supported archive transition and released `v0.75`; preserve its accepted Queue terminal boundary as an input, not a competing implementation path. Done when the current A archive/state is recorded in change evidence. Evidence: `apply-evidence.md` records the archived Change A task/evidence boundary and matching `v0.75` release surfaces.
- [x] 1.2 @impl GSK-011, RWG-005: Complete the change-scoped plan review (`openspec-feedback:plan-review`) before the first target edit. Re-read the proposal, design, delta specs, derived-audit authority, Wave1 evaluator, submitted-evidence reader, assignment contract, and verification plan; convert every actionable finding into an ordinary unchecked task with the affected requirement or reader question, owner, smallest repair, and independently observable done condition. Review outcome: task 1.2.1 prevents the existing target-only direct-output cache from bypassing a parsed `semantic_sections` descriptor; no other untracked planning finding remains.
- [x] 1.2.1 @impl RWG-005 plan-review finding -- Descriptor-specific direct-output evaluation: Authoritative owner: the Wave1 evaluator and parsed `semantic_sections` descriptor. Smallest repair: prevent a target-only cached default direct-output result from satisfying a semantic-section rule; evaluate or cache by its parsed `required_sections`. Done when a focused regression proves the active descriptor's declared names, rather than the default contract list, decide pass/fail.
- [x] 1.3 @impl GSK-011, RWG-005, DEW-006: Run `node openspec/governance/check-verification-routing.mjs --change align-gate-contract-descriptors-and-terminal-recovery-tests --mode plan` and `openspec validate align-gate-contract-descriptors-and-terminal-recovery-tests --strict` before target edits. Capture the 77-pass/6-role-drift baseline and keep deterministic Engine evidence distinct from Agent-flow evidence. Evidence: both planning checks pass; `apply-evidence.md` records the current `79`-pass/`6`-role-drift result and explains that the tracker count predates two Change A Queue regressions.

## 2. Make the Wave1 Descriptor One Truth Path

- [x] 2.1 @impl GSK-011, RWG-005: Extend the Gate-definition contract with a typed `semantic_sections` descriptor requiring a non-empty unique `required_sections` list and rejecting incompatible `pattern` or `negate` fields; retain all independent `pattern_match` behavior. Done when malformed descriptor fields fail schema parsing without a schema-core expansion.
- [x] 2.2 @impl RWG-005: Replace the Wave1 `question_list_has_four_sections` ordered regex metadata with the declared four semantic section names and dispatch evaluation by `rule.check`, not rule ID. Done when the existing heading normalizer is reused and no regex or rule-ID fallback participates.
- [x] 2.3 @impl GSK-011: Reconcile the active Gate audit with parsed type-specific descriptor validation and focused behavioral coverage; remove the contradictory permanent per-rule catalog expectation without weakening unknown-check or missing-root fail-closed coverage. Done when all audit facts derive from production definitions and evaluator behavior.
- [x] 2.3.1 @impl GSK-011 verification finding -- Current seed-definition count: Authoritative owner: the schema-parsed active `gate-seed-topics-ready` definition. Smallest repair: replace the stale four-rule test expectation with the current five-rule production count, including `seed_initialization_structure`. Done when `gate-definition.test.mjs` parses the active definition at its current count without creating a second rule inventory.

## 3. Preserve Direct Wave1 Roots

- [x] 3.1 @impl RWG-005: Add focused semantic-section regressions for equivalent heading case, order, level, spacing, and list style plus missing and empty named sections. Done when the tolerant cases pass and each invalid case emits only the direct `question_list_has_four_sections` root naming the affected section.
- [x] 3.2 @impl RWG-005: Add the exact depth-review case where no reviewed ref resolves to a submitted current-topic work unit. Done when inspect and formal Gate return one submitted-evidence/binding root, mask dependent source/depth symptoms, and never advise fabricating a ref or unassigned output.
- [x] 3.2.1 @impl RWG-005 closeout finding -- Single submitted-backing root across the Wave1 evaluator: Authoritative owner: the Wave1 evaluator's depth-contract prerequisite and per-Topic reference-floor branch. Smallest repair: mask `per_topic_ref_md_count_floor` when the same Topic already has a submitted-backing parent root, and report `missing_contract` rather than an unproven generic claim/submit operation. Done when formal Gate and inspect expose one depth binding root, mark the floor as masked, and name neither a fabricated ref nor an unproved replacement operation. Evidence: the focused depth and Wave1 CLI regressions pass on 2026-08-07; `apply-evidence.md` records the exact proof boundary.
- [x] 3.3 @impl RWG-005: Verify inspect/formal-Gate parity for both semantic-section and no-submitted-evidence outcomes while retaining their different invoked rerun coordinates. Done when they share root identity, repair kind, missing fact, and write target without a duplicate parser.

## 4. Repair Terminal Test Candidates, Not Production Protections

- [x] 4.1 @impl DEW-006, RWG-005: Refactor terminal test candidate construction to read the claimed attempt's required output tuple before writing candidate files/results. Done when the current Wave0 source-intake fixture contributes exactly its assigned `source_yaml` path/role rather than an unassigned `reference` output.
- [x] 4.2 @impl DEW-006, RWG-005: Rerun the six late-submit and timeout-preflight regressions through real candidate admission after fixture alignment. Done when each passes or a remaining red result identifies a narrower production Engine defect with an independently reproducible counterexample.
- [x] 4.3 @impl DEW-006: Preserve output-role, identity, receipt, hash, snapshot, submitted-ledger, and provenance rejection coverage while removing the stale fixture role. Done when no test-only bypass, accepted-role expansion, or relaxed terminal contract is introduced.

## 5. Focused Evidence, Sync, and Release

- [x] 5.1 @impl GSK-011, RWG-005, DEW-006: Run every selected unit and integration asset from `verification-plan.yaml`, including definition/audit, depth-root, Wave1 CLI, terminal, and operate-work-unit coverage. Record deterministic-contract results only; do not claim real Agent adherence.
- [x] 5.2 @impl GSK-011, RWG-005: Use the supported OpenSpec spec-sync flow after implementation, then re-read synchronized `engine/gate-skeleton` and `research/research-wave-gate-implementation` main specs. Done when audit, descriptor, tolerant presentation, and root-first no-submitted behavior agree with the applied delta. Evidence: both main requirement blocks were synchronized and reread on 2026-08-07; `apply-evidence.md` records the semantic merge.
- [x] 5.3 @impl VEM-002, VEM-003, VEM-004: Update root `CHANGELOG.md` for `v0.76` and synchronize the `DEEP_RESEARCH_HARNESS/RUN.md` version banner after Change A's `v0.75` release. Done when both name the same version and the entry concisely describes descriptor/audit parity and terminal fixture alignment. Evidence: both release surfaces declare `v0.76` on 2026-08-07.
- [x] 5.4 @impl GSK-011, RWG-005: Run `node openspec/governance/check-verification-routing.mjs --change align-gate-contract-descriptors-and-terminal-recovery-tests --mode assets` and `openspec validate align-gate-contract-descriptors-and-terminal-recovery-tests --strict` after all target edits. Done when every selected asset is at its canonical route and the active delta is strict-valid. Evidence: assets validation and strict validation PASS on 2026-08-07.
- [x] 5.5 @impl GSK-011, RWG-005: Run `node openspec/governance/check-project-reqs.mjs` and confirm PASS with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired requirements. Evidence: PASS on 2026-08-07 with 630 registered IDs, 53 retired, 0 orphan, and 736 main-spec/active-delta occurrences.
- [x] 5.6 @impl GSK-011, RWG-005: Run `node openspec/governance/check-project-specs.mjs` and confirm PASS with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader findings. Evidence: PASS on 2026-08-07 with 84 main spec files and 0 violations.
- [x] 5.7 @impl GSK-011, RWG-005: Complete the change-scoped closeout review (`openspec-feedback:closeout-review`) after the actual diff, synchronized specs, selected verification evidence, and governance results are reviewed. Any remaining actionable finding must remain an ordinary unchecked task with an owner, smallest repair, and independently observable done condition before archive. Evidence: reviewed the Change B-scoped implementation/tests, synced `GSK-011`/`RWG-005` main requirements, verification plan results, and governance results on 2026-08-07; the archived Change A Queue diff was treated as an explicit released dependency. No open actionable finding remains.
