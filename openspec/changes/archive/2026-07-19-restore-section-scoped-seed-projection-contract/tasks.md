## 1. Verification Baseline and Negative Proof

- [x] 1.1 Run `node openspec/governance/check-verification-routing.mjs --change restore-section-scoped-seed-projection-contract --mode plan` before any target edit; done when all five routed claims validate with no routing issue.
- [x] 1.2 Add normalized-reader parity fixtures in `tests/engine/helpers/gate-helpers-readers.test.mjs`; done when one-index-load facts and legacy reader row/throw parity cover valid, invalid, mixed legacy and late-accept ledgers.
- [x] 1.3 Add focused `RRM-007` unit fixtures in `tests/engine/helpers/return-map.test.mjs` for section extraction, entry-local fields/metadata/lineage, sibling masking, empty-section union, refs-only W2F pending selection, exact work-id tokens/prefix rejection, count summaries, heading suffixes and wave-owned tokens; done when false passes fail pre-change and compatibility cases are explicit.
- [x] 1.4 Add CLI fixtures in `tests/integration/cli/inspect-wave-return-map.test.mjs` for current-token skip, current-demand/inactive/legacy-only missing families, one-fact plan-derived seed selection, canonical seed missing/mismatch, per-entry lineage, current-row omission/disposition, authority repair ownership, affected-topic/round roots and legacy advisory; done when exit/classification/cardinality/coordinates/same-check assertions are explicit and a seed-binding failure does not inherit full topic-state progress/workspace blockers.
- [x] 1.5 Add focused seed-binding evaluator fixtures in `tests/engine/helpers/canonical-topic-state.test.mjs`; done when all canonical binding fields, missing seed, and mismatched seed results retain parity with `inspectCanonicalTopicState()` while the focused API does not evaluate submitted progress or accepted workspaces.
- [x] 1.6 Add focused eligible-projection fixtures in `tests/engine/work-unit-projection.test.mjs`; done when absent/malformed profile round, valid-empty declarations, invalid existing ledger/index/manifest/result/topic authority, recomputed snapshot drift, renamed layouts, unrelated health defects and the one-way module boundary are asserted against the projection owner directly.

## 2. Shared Current-Round Authority

- [x] 2.1 Refactor the submitted-reader owner for `RRM-007` to expose normalized `{ledger_row,index_record}` facts from one index load while preserving `readSubmittedWorkUnitDeclarations()` row shape and throw semantics; done when parity tests cover valid, mixed legacy, invalid and late-accept ledgers without a second index read.
- [x] 2.2 Add `DPT_FRAMEWORK/engine/work-unit-projection.mjs` as the sole narrow eligible-projection owner: import normalized facts from `helpers/gate-helpers-readers.mjs`, reuse `readAndValidateManifest()`, recompute `queueItemSnapshotHash(manifest.queue_item)`, and validate profile round, safe existing regular canonical result path and canonical topic binding but not full health; done when absent nested profile round becomes 0, malformed present profile round fails closed, no declarations are valid empty, malformed existing authority fails closed, stored hashes cannot drift together, result-content/queue-state/lease/beacon/receipt/cache/orphan defects remain independently owned, and static import review proves gate readers do not import projection/work-unit validation.
- [x] 2.3 Update `operate-work-unit inspect --eligible-rows` and return-map inspect to consume the same eligible result; done when CLI JSON fields/exit/warnings remain compatible, renamed layouts bind once, and authority roots preserve `missing_contract`/Engine repair ownership.
- [x] 2.4 Add/update `// @impl RRM-007` annotations and an acyclic-import regression assertion; done when return-map does not read index/ledger directly, gate readers do not validate manifests or import the projection module, `work-unit-inspect.mjs`/return-map consume `work-unit-projection.mjs`, the eligible path does not rerun full `inspectWorkUnits()` per seed, and normalized facts are the only ledger/index pairing path.

## 3. Section-Scoped Return-Map Evaluation

- [x] 3.1 Build one invocation-scoped canonical topic-registry fact containing complete canonical rows plus the existing compatible current/previous-slug layout view, and implement the centralized target-family H2 extractor; done when each inspect invocation passes the same fact to its Wave evaluator, seed-binding check, eligible projection and seed checker, formal Gate fallback API/verdict stays compatible, Wave expansion is byte-for-byte equivalent for valid plans, suffix/spacing is tolerated, any family occurrence supplies a legal surface, repeated canonical occurrences join one bounded union without a new blocker, Wave0/1 do not raw-read plan twice, Wave2 no longer scans the directory, plan roots mask projection symptoms, current tokens skip, current-demand unavailable families block once, legacy-only unavailable families advise per finding/topic, and inactive families do not migrate.
- [x] 3.2 Export/refactor the focused canonical seed-binding evaluator currently owned by `canonical-topic-state.mjs` and consume it from Wave projection without calling `inspectCanonicalTopicState()`; done when UID/slug/id/title/must_answer/scope_role/dependency mismatches and missing seeds produce one `missing_contract` prerequisite with `operate-topic-state inspect` as diagnostic operation and invoking Wave inspect as rerun, while submitted progress and accepted-workspace state do not enter the projection prerequisite chain.
- [x] 3.3 Refactor entry parsing to return entry-local canonical fields, parsed refs, section coordinates and optional `metadata.entry_id`; done when before/after metadata in one list item/contiguous field block binds locally, deeper-indented refs bullets remain continuations, and dangling/duplicate metadata, peer list items, next evidence entry, intervening prose or H2 boundaries fail without cross-binding.
- [x] 3.4 Apply five-field/enum/concrete-navigation and accepted Wave1 statement-lineage checks per non-empty entry; keep Wave2 ledger/index family-level and W2F per finding/topic coverage; done when siblings cannot mask per-entry failures, empty Wave1 siblings remain legal, and general Wave2 judgments gain no new per-entry W2F quota.
- [x] 3.5 Implement Wave0/Wave1 row coverage using exact parsed-ref work-id tokens/path segments or entry-local `<work_id>/<n>` metadata; done when bare exact refs pass, prefix/prose/anonymous-none fail, identity-bound dispositions pass and prior rows do not block.
- [x] 3.6 Implement inspect-only Wave2 affected-topic resolution by composing existing canonical topic-layout facts/pure resolvers into a deduplicated candidate set; done when exactly one UID/current-or-previous-slug/legacy-id owner resolves, zero/multiple owners fail explicitly, same-owner aliases deduplicate, and no new token-owner map or existing pair-fact behavior change is introduced.
- [x] 3.7 Make Wave2 inspect load one finding-index fact for both existing evaluator and projection, then implement inspect-only normalization over that shared parse/resolver fact; done when formal Gate fallback stays compatible, parent YAML/findings failures mask all projection, invalid ids mask one finding through existing roots, affected-topic/round defects emit one exact-field root, and unrelated formal defects do not suppress usable projection checks.
- [x] 3.8 Implement current/legacy Wave2 projection per finding/topic pair without duplicating targeted backing; done when affected current omissions block, unaffected seeds stay clean, and legacy omissions advise without being upgraded by sibling shape checks.
- [x] 3.9 Implement `IOC-005` root-first findings with stable ids, correct repair owner, exact field/seed coordinates and invoking inspect rerun; done when missing/unbound seeds use the narrow canonical prerequisite, family roots occur once per seed/wave, rows once per row, findings once per finding/topic pair, an invalid exact-identity entry masks its dependent omission, summaries agree and prerequisites mask dependent symptoms.
- [x] 3.10 Add `// @impl RRM-007, IOC-005` annotations to shared evaluators and focused tests; done when traceability points to owners rather than CLI-local variants.

## 4. CLI and Workflow-Scale Verification

- [x] 4.1 Complete Wave0/Wave1 `RRM-007` / `IOC-005` integration cases; done when per-entry isolation, current-row exact identity/disposition, demanded/inactive section behavior, authority repair owner, exact feedback and no-write assertions pass.
- [x] 4.2 Complete Wave2 cases; done when refs-only shared-pending selection, plan seed scope, per-entry lineage, canonical topic candidate zero/one/many behavior, absent/malformed profile round, finding current/legacy/invalid-round behavior pass and inspect-only rule ids do not appear in formal Gate output.
- [x] 4.3 Run `node --test tests/integration/cli/inspect-wave-return-map.test.mjs`; done when all Wave inspect integration cases pass.
- [x] 4.4 Extend `tests/e2e/rerun-round-continuity.test.mjs` for `RRM-007`: prove omitted current-row projection fails the real Wave inspect, then an Agent-labeled identity-bound repair passes the same inspect without authority-lineage mutation; done when its focused command passes.
- [x] 4.5 Run focused authority/contract regressions: `node --test tests/engine/helpers/canonical-topic-state.test.mjs tests/engine/helpers/gate-helpers-readers.test.mjs tests/engine/work-unit-projection.test.mjs tests/engine/helpers/return-map.test.mjs tests/integration/cli/rerun-round-continuity.test.mjs tests/integration/cli/inspect-wave-contract-output.test.mjs`; done when all pass with no contradictory seed-binding parity, reader parity, classification or eligible-row behavior.
- [x] 4.6 Run `npm test`; done when all repository `node:test` suites pass, or any pre-existing unrelated failure is captured with reproducible command/output and does not affect the five verification claims.

## 5. Release and Traceability

- [x] 5.1 Audit `RRM-007` and `IOC-005` across registry, accepted specs, delta specs, `@impl` annotations and test claims; done when no new requirement ID is added and the historical tasks 7.2/7.3 checkbox is not cited as executable proof.
- [x] 5.2 Update `CHANGELOG.md` with concise `v0.35` notes describing section-scoped seed projection and current-round per-row/per-finding enforcement; done when `v0.35` is the newest release entry.
- [x] 5.3 Update the `DPT_FRAMEWORK/RUN.md` version banner and current-release summary to `v0.35`; done when it matches the newest CHANGELOG entry.
- [x] 5.4 Record executed commands and outcomes in change-root `apply-evidence.md`, including the original false-pass reproducer and its post-fix blocking result; done when every verification-plan claim points to real command evidence rather than a checklist assertion.

## 6. Required Archive Checks

- [x] 6.1 Run `node openspec/governance/check-verification-routing.mjs --change restore-section-scoped-seed-projection-contract --mode assets`; done when all five declared claims resolve to their proof assets at canonical routes and validate.
- [x] 6.2 Run `node openspec/governance/check-project-reqs.mjs`; done only with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired requirements.
- [x] 6.3 Run `node openspec/governance/check-project-specs.mjs`; done only with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [x] 6.4 Run `openspec validate restore-section-scoped-seed-projection-contract --strict`; done when the complete change validates with no error.
- [x] 6.5 Review the final diff against the manifest; done when no whole-seed/family-aggregate validator, duplicate ledger/index pairing, gate-reader/work-unit-validation import cycle, directory-derived Wave2 scope, per-section content quota, anonymous disposition, full work-unit/topic-state health prerequisite, duplicate topic-token map, pair-fact behavior change, formal-Gate wiring, CLI/state/migration was added, and deviations are documented before archive.
