## 1. Apply Preconditions And Review

- [x] 1.1 @impl AGQ-022, QIV-001, DEW-003, RWP-001 `openspec-feedback:plan-review` - Before the first target edit, obtain the current change-feedback Apply guidance and review `proposal.md`, all four delta specs, `design.md`, `verification-plan.yaml`, and `semantic-closure.yaml`. Confirm that exact assignment target remains the conflict identity, existing queue order supplies the only unclaimed owner, submitted contribution semantics remain unchanged, and no persistent lock/merge/recovery path enters scope. Record every actionable finding as a separate unchecked task naming its requirement/reader question, authoritative owner, smallest repair, and independently observable done condition. Done when no unrecorded actionable finding remains; the marker is review evidence only.
- [x] 1.2 @impl VER-001, VER-002, VER-003 - Before the first target edit, run `openspec validate guard-wave0-source-target-exclusivity --strict`, `node openspec/governance/check-capability-discovery.mjs --change guard-wave0-source-target-exclusivity`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change guard-wave0-source-target-exclusivity --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change guard-wave0-source-target-exclusivity --mode plan`. Done when all exit zero and structural success is not represented as runtime or semantic proof.

## 2. Shared Target-Owner Admission

- [x] 2.1 @impl QIV-001, AGQ-022 - Add strict Zod shapes for ephemeral Wave0 candidate/owner/conflict facts and derive each unclaimed candidate's exact `source_yaml` tuple only from canonical Topic binding plus the current assignment contract. Done when cross-field validation requires an in-flight `work_id`, rejects mismatched targets/unsafe identities, persists no new field, and focused schema/evaluator tests pass.
- [x] 2.2 @impl QIV-001 - Extend the side-effect-free current-facts adapter to read schema-valid active/refill queue order and delegated in-flight/index/manifest bindings, exclude the checked item itself, prefer an in-flight owner, and otherwise select the queue-order-earliest owner for each target. Resolve queued targets from current canonical assignment and in-flight targets from their current-profile-valid immutable manifest output contract; do not reinterpret a claimed assignment from mutable Topic state. Done when different targets admit, later same-target cards conflict, malformed current-profile facts keep their existing fail-closed identity, terminal/submitted history does not retain ownership, and `tests/engine/helpers/queue-demand-admission.test.mjs` passes.
- [x] 2.3 @impl QIV-001 - Emit one closed `wave0_source_target_conflict` admission result containing exact target, candidate ID, available owner queue/work identity, the existing admission `repair_kind: agent_action`, and caller-provided rerun while rejecting queue-ID/prose/cache/URL independence. Keep the attempt-recovery `wait` vocabulary out of this surface. Done when the pure truth table covers enqueue candidate, queue self-exclusion, legacy duplicate queue, in-flight owner, and malformed owner facts without mutation.

## 3. Queue And Claim Consumers

- [x] 3.1 @impl QIV-001 - Route `operate-queue enqueue` and `check` through the shared ordered owner view. Enqueue must reject a second target owner before write; check must report later persisted conflicts with owner/ordering/same-check feedback and must not recommend or perform stale removal for this reason. Done when enqueue/check authority snapshots remain byte-identical on rejection, `repair --remove-stale` preserves a valid blocked same-target card byte-for-byte, and `tests/integration/cli/operate-queue-demand-admission.test.mjs` passes.
- [x] 3.2 @impl DEW-003, AGQ-022 - Extend claim planned-prefix preflight to reject duplicate targets or an in-flight owner atomically, returning the structured admission fields and zero allocations. Repeat the identical owner evaluation from fresh queue/index/manifest facts under the existing work-unit transaction before first mutation. Done when requested conflicting batches leave queue/index/envelopes/trace unchanged and transaction-drift tests prove the recheck.
- [x] 3.3 @impl DEW-003, AGQ-022 - Preserve legacy serial drain: allow a claim whose prefix contains only the earliest currently unowned target, keep later same-target demand queued while the attempt is in flight, and admit it after the owner terminalizes. Preserve full legal batches for pairwise-distinct targets. Done when focused `tests/engine/work-unit-claim.test.mjs` cases pass for duplicate, in-flight, distinct-target, reduced-count, and release-after-terminal paths.
- [x] 3.4 @impl RRM-007, AGO-003, AGO-005 - Add integration coverage showing a first submitted contribution releases target ownership, a later legal supplement submits a strict append owned only at its new global ordinals, and deliberately non-monotonic historical rows still fail closed. Done when the selected `tests/integration/cli/operate-work-unit.test.mjs` and existing `tests/engine/work-unit-projection.test.mjs` cases pass without changing `SourceContributionSchema` or reader ownership logic.

## 4. Agent-Facing Guidance

- [x] 4.1 @impl RWP-001, AGQ-022 - Update Wave0 Phase and shared delegated batch guidance so one new Topic creates one standard demand, different exact targets remain batchable, same-target research dimensions are combined or scheduled as serial supplements, and conflict feedback returns to the named owner-completion/reduced-count checkpoint. Done when guidance contains no queue-ID/cache namespace/ledger/source-array workaround and no user-directed permission path.
- [x] 4.2 @impl RWP-001, AGQ-022 - Extend the selected Markdown integration contract to verify the target-exclusivity wording, one-demand new-topic example, different-target fan-out, and unchanged submitted contribution handoff. Done when `tests/integration/md/work-unit-attempt-recovery-guidance.test.mjs` and applicable workflow-package/static tests pass.

## 5. Verification, Spec Sync, And Release

- [x] 5.1 @impl VER-001, VER-002, VER-003 - Run every selected unit/integration asset in `verification-plan.yaml` plus `tests/engine/work-unit-projection.test.mjs`; repair only actual findings and record any scope-expanding finding as a pending task. Done when all selected `node --test` processes exit zero with no skipped/cancelled case treated as proof.
- [x] 5.2 @impl AGQ-022, QIV-001, DEW-003, RWP-001 - Run the full serial repository suite and applicable workflow/package validators. Done when all deterministic tests pass, no test/runtime artifact is tracked, and no result is described as real actor concurrency or research-quality proof.
- [x] 5.3 @impl AGQ-022, QIV-001, DEW-003, RWP-001 - Sync the approved deltas into their four canonical main specs through the supported OpenSpec sync route, preserving full requirement/scenario content and the unchanged verify-only RRM/AGO contracts. Done when delta/main comparison is equivalent and no delta header appears in a main spec.
- [x] 5.4 @impl AGQ-022, QIV-001, DEW-003, RWP-001 - Use the repository's governed `--build` version-bump route for the next Harness history entry, add concise fail-early target-exclusion history to root `CHANGELOG.md`, and preserve the accepted VEM-003/RUE-001 rule that `DEEP_RESEARCH_HARNESS/RUN.md` carries no changelog version projection. Done when the governed bump reports the next build, the changelog does not claim same-target merge support or historical repair, and `RUN.md` remains version-free.

## 6. Closeout And Archive Preconditions

- [x] 6.1 @impl VER-001, VER-002, VER-003 - Run `openspec validate guard-wave0-source-target-exclusivity --strict`, `node openspec/governance/check-capability-discovery.mjs --change guard-wave0-source-target-exclusivity`, `node openspec/governance/check-verification-routing.mjs --change guard-wave0-source-target-exclusivity --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change guard-wave0-source-target-exclusivity --mode assets`, and `git diff --check`. Done when every command exits zero and structural checks are reported only within their proof boundaries.
- [x] 6.1a @impl AGQ-022, QIV-001, DEW-003, RWP-001 - Repair the closeout review's `queue.demand-lifecycle` / `work-unit.assignment-output-obligation` semantic-surface omission. The authoritative owner is `semantic-closure.yaml`; add the actual read-only queue loader, immutable-manifest validator, and derived shared batch guidance coordinates without changing family authority or inventing a verdict consumer. Done when every changed fact-establishing/presentation surface is classified, the assets checker passes, and a repeated closeout review finds no remaining omission.
- [x] 6.2 @impl AGQ-022, QIV-001, DEW-003, RWP-001 `openspec-feedback:closeout-review` - Before archive, obtain the current feedback-loop Archive guidance and review the scoped actual diff, synchronized specs, target-owner/transaction consumers, selected evidence, release surfaces, `verification-plan.yaml`, and `semantic-closure.yaml`. Record every actionable finding as an ordinary unchecked task with requirement/reader question, authoritative owner, smallest repair, and independently observable done condition; complete only when no actionable finding remains.
- [x] 6.3 @impl VER-002 - Run `node openspec/governance/check-project-reqs.mjs --mode archive --change guard-wave0-source-target-exclusivity`. Done when it passes with zero duplicate, orphan, unregistered, or reused-retired requirement identities and any selected reservation transition is complete.
- [x] 6.4 @impl VER-002 - Run `node openspec/governance/check-project-specs.mjs`. Done when it passes with zero delta header in main specs, missing Purpose, missing Requirements, or missing requirement header.
