## 0. Apply Entry Review

- [x] 0.1 `openspec-feedback:plan-review` - Before the first target edit, obtain current Apply guidance and review `proposal.md`, `design.md`, this task list, `semantic-closure.yaml`, `verification-plan.yaml`, the eight failing selected assets, `RUN.md`, the current run-entry/context-routing/feedback/delegated-work-unit main specs, and the C3/C4a/C5a-2/C6 archive boundaries. Add each finding as an ordinary unchecked task naming the affected reader question, authoritative owner, smallest repair, and independently observable done condition. Complete only when no finding remains and plan-mode requirement, verification-routing, and semantic-closure checks pass.

## 1. Align Existing Current Projections

- [x] 1.1 @impl DEW-004 - Add one bounded statement to `DEEP_RESEARCH_HARNESS/RUN.md`: `actor_execution` binds a logical work-unit attempt but does not authenticate a physical writer or prove host/sub-agent liveness. Keep current entry routing, Section 0 shortcut limits, probe behavior, commands, and recovery authority unchanged. Done when the public-recovery guide test detects the same current boundary and no new command/state/permission wording is added.
- [x] 1.2 @impl RUE-001, RUE-006 - Update `tests/integration/md/dpt-research-entry-routing-contract.test.mjs` to assert current selected-pair entry behavior: supplied candidate preflights the same-root `BUNDLE_ENTRY.md` plus `BUNDLE_MAP.md`; an incomplete supplied pair stops as `unsupported_current_entry_contract` without legacy entry, `RUN.md` fallback, creation, migration, or upgrade; and no supplied candidate selects the current new-run entry. Keep root/Harness synchronized-pair and pre-entry research prohibitions covered without requiring retired literal phrases. Done when the test fails if those current behaviors drift and no documentation target changes.
- [x] 1.3 @impl CHI-001, CHI-003, GSK-002, GSK-004 - Update `tests/integration/md/gate-hint-controller-contract.test.mjs` so the universal assertions preserve current structured-hint ordering and the four actual HITL1 kinds (`user_decision`, `agent_action`, `engine_operation`, `missing_contract`), while other early phases retain their current independently supported kinds. Do not require or add a HITL1 `external_action` branch merely because the generic feedback vocabulary permits one. Done when the test accepts the current HITL1 controller and fails if its existing owner/action/rerun boundaries are removed.
- [x] 1.4 @impl REF-007 - Update `tests/integration/md/parser-aligned-guidance.test.mjs` to assert the current reference distinction: new output uses exactly one UID form, while historical `related_topic` remains human-readable but is rejected by the current Engine. Do not restore a historical-reader success path or rewrite historical Markdown. Done when the test fails if a new writer offers `related_topic` or the current rejection boundary disappears.
- [x] 1.5 @impl DEW-022, DEW-023, DEW-024 - Re-run/update `tests/integration/md/work-unit-attempt-recovery-guidance.test.mjs` only as required to verify the RUN.md logical-actor/liveness boundary alongside its existing caller, same-checkpoint, recovery-precedence, and manual-authority protections. Done when it passes without weakening any public recovery guard.
- [x] 1.6 @impl DEW-004, DEW-014 - Add one test-only explicit current delegated-actor execution helper in `tests/engine/work-unit-test-helpers.mjs`, parameterized by the caller-supplied current role key. It SHALL return the exact delegated actor-v1 profile required by `writeWorkUnitEnvelope`; it SHALL not infer a role, modify production code, or create a legacy profile. Update `tests/integration/md/work-unit-direct-output-guidance.test.mjs` to use it for its direct `createWorkUnit` fixture. Done when the test exercises generated direct-output guidance through a complete current envelope and production envelope rejection remains unchanged.
- [x] 1.6a @impl DEW-004 - Apply finding. Reader question: does the direct-output anti-authoring guard reject only an actor being told to edit, choose, or select `direct_contract`, rather than correct current task text that separately mentions actor content, selected route, and contract? Authoritative owner: the existing `agent/delegated-work-units` task-authoring boundary. Smallest repair: make the test's negative assertion line-bounded and action-specific. Done when the current generated task passes while an instruction to edit, choose, or select `direct_contract` fails.
- [x] 1.7 @impl DEW-004, DEW-014 - Update `tests/integration/md/work-unit-receipt-guidance.test.mjs` to use the same test-only current delegated-actor helper for its direct `createWorkUnit` fixture. Keep its receipt-authority assertions unchanged. Done when the test proves receipt guidance with a complete current profile rather than a profile-less legacy fixture.
- [x] 1.7a @impl DEW-004, DEW-014 - Plan-review finding. Reader question: can the receipt-guidance fixture reach the existing current envelope without relying on the retired assignment-absent shape? Authoritative owner: `agent/delegated-work-units` current assignment-v3 envelope contract. Smallest repair: give its Wave0 queue item the existing `topic_uid`, `topic_slug`, `wave`, `required_receipts`, and `writes_to` facts before supplying the explicit actor-v1 helper. Done when the receipt test reaches generated task/spawn receipt assertions through the complete current assignment/actor envelope and no production fallback is added.
- [x] 1.8 @impl DEW-004, DEW-014 - Update `tests/integration/engine/work-unit-role-contract-delivery.test.mjs` so its retained no-claim repair scenario asserts that direct construction without an actor-v1 profile fails as `unsupported current work-unit contract` before manifest, beacon, task, result-schema, or actor-guidance publication. Do not use the helper or add a legacy success path to this negative branch. Done when the test preserves the no-claim repair and fails if an incomplete envelope publishes.
- [x] 1.8a @impl DEW-004, DEW-014 - Closeout-review finding. Reader question: does the negative actor-delivery fixture independently protect the promised pre-publication boundary for every work-unit artifact without mistaking the permitted transaction audit journal for a legacy envelope success? Authoritative owner: the existing fail-closed envelope admission in `writeWorkUnitEnvelope`. Smallest repair: assert that incomplete construction leaves no work-unit index and no manifest, beacon, task, result-schema, or actor-guidance file. Done when the direct no-actor fixture still throws the current unsupported-contract error, its transaction audit may remain, and any index or named envelope publication makes the test fail.
- [x] 1.9 @impl DEW-004, DEW-014 - Update `tests/engine/work-unit-index.test.mjs` to supply current Wave0 assignment facts plus the same caller-supplied delegated-actor helper to its focused valid work-ID fixture. Keep its ID parser and mismatch assertions unchanged. Done when it proves current work-ID binding through a complete in-process envelope and does not add any Engine fallback.

## 2. Verification And Closeout

- [x] 2.1 @impl RUE-001, RUE-006, CHI-001, CHI-003, GSK-002, GSK-004, REF-007, DEW-004, DEW-014, DEW-022, DEW-023, DEW-024 - Run the eight selected assets together: `tests/engine/work-unit-index.test.mjs`; `dpt-research-entry-routing-contract`, `gate-hint-controller-contract`, `parser-aligned-guidance`, `work-unit-attempt-recovery-guidance`, `work-unit-direct-output-guidance`, `work-unit-receipt-guidance`, and `work-unit-role-contract-delivery`. Done when all pass and their changed assertions/fixtures prove only the named current contracts.
- [x] 2.2 @impl RUE-001, DEW-004 - Run `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`, focused protected-surface scans, and `git diff --check`. Confirm the target diff contains only `RUN.md`, the eight selected test assets, and the one test helper; no root/Harness entry doc beyond RUN.md, production Engine source, accepted spec, registry, or historical artifact changes. Done when each command passes and the comparison records no behavior transfer.
- [x] 2.3 @impl RUE-001, DEW-004 - Run strict OpenSpec validation, plan-mode requirement governance, project-spec/taxonomy/discovery checks, verification-routing plan/assets checks, and semantic-closure plan/assets checks. Reconfirm `skip_specs: true` remains accurate because no accepted requirement changed. Done when all governance checks pass and no delta/main sync is required.
- [x] 2.4 `openspec-feedback:closeout-review` - Before archive, obtain current Archive guidance and review the scoped actual diff, current-owner assertions, RUN.md boundary, fixture profile, selected unit/integration evidence, protected-surface comparison, and `semantic-closure.yaml` reason. Add and close ordinary repair tasks for each finding; complete only when no actionable finding remains.

## 3. Archive Preconditions

- [x] 3.1 @impl RUE-001, DEW-004 - Run `node openspec/governance/check-project-reqs.mjs --mode archive --change align-current-guidance-contract-guards`. Done when it reports zero duplicate, orphan, unregistered, or reused-retired IDs.
- [x] 3.2 @impl RUE-001, DEW-004 - Run `node openspec/governance/check-project-specs.mjs`. Done when it reports zero delta-header-in-main, missing-purpose, missing-requirements, and missing-requirement-header findings.
- [x] 3.3 @impl RUE-001, DEW-004 - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change align-current-guidance-contract-guards`. If finalization fails, restore this task to unchecked and repair its named root.
