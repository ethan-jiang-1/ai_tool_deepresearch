## 0. Apply Gate

- [x] 0.1 `openspec-feedback:plan-review` - Before the first target edit, obtain the current feedback-operation guidance and review `proposal.md`, `design.md`, `verification-plan.yaml`, and `semantic-closure.yaml`. Confirm that the fixture, not the canonical-topic-state evaluator, is the stale owner; record every actionable finding as an ordinary unchecked task with its authoritative owner, smallest repair, and independently observable done condition.
- [x] 0.2 `VER-001` - Run `openspec validate restore-wave1-wave2-receipt-closure-e2e-baseline --strict`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change restore-wave1-wave2-receipt-closure-e2e-baseline --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change restore-wave1-wave2-receipt-closure-e2e-baseline --mode plan`. Done when every command exits zero and structural PASS results are not represented as native E2E proof.
- [x] 0.3 Read the completed `polish.md` readiness record before target edits. Done when its two independent review passes are clean or every evidence-settled repair has been reflected in the planning artifacts; polish itself is not target-edit permission or test evidence.

## 1. Canonical Fixture Alignment

- [x] 1.1 Update `tests/e2e/wave1-target-receipt-wave2-closure.test.mjs` so `scaffoldTopic` receives the complete canonical Topic fixture plus explicit test-only evidence inputs, constructs the evaluator's seven binding fields (`topic_uid`, `id`, `slug`, `title`, `must_answer`, `scope_role`, `depends_on_topic_uids`) from that Topic, and serializes their parsed values through JSON-compatible YAML. Done when no hand-written seed `must_answer`, identity, scope role, or dependency value can drift from the canonical fixture passed to `createBundle`, and non-binding `previous_layouts` is not invented in seed frontmatter.
- [x] 1.1a Re-align the test's direct Wave1 evaluator input. Reader question: does its temporary-bundle call provide the current canonical registry fact required for per-Topic reference resolution? Authoritative owner: the existing `buildCanonicalTopicRegistryFact` plus `evaluateWave1Contract` invocation used by the Wave1 CLI. Smallest repair: build that fact in `wave1PassAndProject` and pass it through the evaluator's established argument. Done when the fixture no longer fails on missing `uidByAnySlug` and no test-local layout map, evaluator fallback, or production behavior change is introduced.
- [x] 1.1b Re-establish the Wave1 canonical projection prerequisite exposed by the repaired evaluator path. Reader question: once submitted Wave1 backing is present, does the temporary bundle contain the closed canonical projection that the existing reference-convergence rule requires? Authoritative owner: `applyCanonicalTopicState`'s existing `wave_projection/apply_seed_projection` operation and the existing canonical Wave1 reference resolver. Smallest repair: before direct Gate evaluation, enter the existing `wave0_complete -> wave1_complete` Wave1 status/handoff window and materialize the three Wave1 slots from the submitted work ID and canonical reference path through that operation, then restore the existing Gate status window. Done when the valid scenario no longer reports `materialize_projection`, the projection operation returns `committed`, and the fixture introduces no raw seed-projection write, evaluator bypass, or production behavior change.
- [x] 1.1c Align the fixture's initial projection layout with the current writer postcondition. Reader question: can the existing Wave1 writer materialize and reread exactly one canonical/card target for each of its three slots? Authoritative owner: `renderSeedProjectionAppendix` and the existing projection writer postcondition. Smallest repair: render that existing appendix in `scaffoldTopic` instead of hand-authoring legacy Wave1 mechanism/trend/pending-question sections; retain the writer as the only entry materializer. Done when all three slot writes return `committed` without `writer_postcondition_failed`, and the fixture contains neither prefilled projection entries nor a raw projection write.
- [x] 1.2 Update each of the eight receipt-closure scenario call sites to pass its same canonical Topic fixture to `scaffoldTopic` while preserving its source URL, carried-target setup, finding setup, and existing assertions. Done when all eight cases retain their intended positive, empty, missing, stale, drift, layout-only, and same-origin coverage boundaries.

## 2. Native Verification

- [x] 2.1 `VER-001` - Run `node --test tests/e2e/wave1-target-receipt-wave2-closure.test.mjs`. Done when all eight scenarios pass through the existing canonical admission and their existing Wave1-to-Wave2 receipt-closure assertions.
- [x] 2.2 `VER-001` - Run `npm test` after the focused suite passes. Record its exit status and complete pass/fail inventory, distinguishing the eight repaired scenarios from unrelated baseline failures; do not claim a globally green baseline unless the command actually exits zero. Recorded 2026-08-10: two deterministic invocations exited 1; both reported 2,746 tests, 482 suites, 2,713 passes, 33 failures, 0 cancelled, and 0 skipped. The repaired eight-scenario `Wave1→Wave2 carried-target receipt closure` suite passed in both. The 33 unrelated failures are contained by these 19 pre-existing failing suites: Agent-facing command contract docs; human setup preflight docs; work-unit playbook utils; Gate chain consistency (instantiation→hitl1→setup→seed-topics); Gate dynamic threshold — wave1; handoff witnessing lifecycle integration; operate-topic-state direction candidates; post-final recovery CLI and lifecycle integration; Wave1 carried-target receipt integration; BUG-088 declaration fault recovery through real CLI; artifact persistence contract stays small and Agent-facing; case-406 native Sub-agent contract; Evolution Direction governance; §4 Expected Artifacts — three-artifact group; §3 Allowed Actions — three-stage queue-driven structure; Seed Topic template and projection protocol boundary; verification routing knowledge surfaces; generated direct-output guidance; Wave1 finding-source admission.

## 3. Closeout And Archive

- [x] 3.1 `openspec-feedback:closeout-review` - Before archive, obtain the current feedback-operation guidance and review the actual test diff, focused native result, full-suite inventory, and plan records. Confirm the closeout still contains no production behavior change, semantic-fact-family impact, or overclaimed global-test conclusion; capture every actionable finding as an ordinary unchecked task with authority, smallest repair, and observable done condition. Completed 2026-08-10: the only closeout finding was the pre-existing task-order cycle in 3.6; its text now records the legal pre-/post-archive split. Re-review found no remaining actionable scoped finding.
- [x] 3.2 `VER-001` - Run `node openspec/governance/check-verification-routing.mjs --change restore-wave1-wave2-receipt-closure-e2e-baseline --mode assets` and `node openspec/governance/check-semantic-closure.mjs --change restore-wave1-wave2-receipt-closure-e2e-baseline --mode assets`. Done when both structural checks pass without being presented as native test verdicts. Completed 2026-08-10: both commands exited zero; they are structural routing/closure results, not the native E2E verdict.
- [x] 3.3 Run `openspec validate restore-wave1-wave2-receipt-closure-e2e-baseline --strict` and `git diff --check`. Done when the approved change artifacts and final diff have no validation or whitespace error. Completed 2026-08-10: both exited zero.
- [x] 3.4 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change restore-wave1-wave2-receipt-closure-e2e-baseline`. Done when it passes with zero duplicate, orphan, unregistered, and reused-retired requirement IDs. Completed 2026-08-10: 644 registered, 53 retired, 0 orphan, 745 occurrences.
- [x] 3.5 Run `node openspec/governance/check-project-specs.mjs`. Done when it passes with zero delta-header, missing-purpose, missing-requirements, and missing-requirement-header defects. Completed 2026-08-10: 85 main spec files, 0 violations.
- [x] 3.6 Before archive, update this task checklist and `_backlog/learning/semantic-fact-closure-progressive-rollout-plan-2026-08-09.md` with the actual focused-suite result, full-suite inventory, named debt state, and the explicit next action of governed archive. Done when the tracker continues to identify this change as verification-only, does not advance Semantic Fact Closure Phase 2, and does not claim an archive or commit that has not happened. After the governed finalizer succeeds, update the tracker with its actual archive outcome and then create the requested commit; those post-archive records are intentionally outside this pre-archive task so the finalizer does not face a task-order cycle. Completed 2026-08-10: tracker records focused 8/8 pass, the 2713 pass/33 unrelated-failure full-suite inventory, governed archive as the next action, and no archive or commit claim.
