> Apply discipline: after each completed target-edit or verification task,
> update its checkbox and append concise, directly observed command evidence to
> that task line. A planned command is not completion evidence.

## 0. Feedback Review

- [x] 0.1 `openspec-feedback:plan-review` - Before the first target edit, obtain current Apply guidance and review `proposal.md`, both delta specs, `design.md`, this task list, `verification-plan.yaml`, and `semantic-closure.yaml` against RUE-001/RUE-002 and VEM-001 through VEM-004. Done when the review confirms that C2b is a real precondition rather than in-scope work, no current reader outside the owner matrix is missed, `semantic-closure: not_applicable` remains accurate, and each actionable finding is recorded as an ordinary unchecked task with its owner, smallest repair, and observable done condition. (CHF-001, CHF-002) Completed 2026-08-13: current Apply guidance and `openspec/operations/change-feedback-loop.md` were read. C2b archive `2026-08-13-retire-framework-version-stamp` is a real prerequisite, not C2c scope; C2c changes no runtime fact family, resolver, schema, Gate, receipt, trace, or verdict, so `semantic-closure: not_applicable` remains accurate. The active-source review found no unplanned decision reader; it found two current catalog/static-vocabulary projections, recorded as ordinary task 3.5 with `openspec/specs/README.md` and `canonical-harness-vocabulary-contract.test.mjs` ownership. No other actionable finding remains.

## 1. Preconditions And Governance

- [x] 1.1 @impl VEM-001, VEM-002, VEM-003, VEM-004: Verify that the separately approved C2b `retire-framework-version-stamp` change has been applied and governed-archived before any C2c target edit. Done when its archive evidence proves that new bundle creation neither reads `CHANGELOG.md` nor writes `framework_version`, while current unknown-frontmatter preservation remains characterized; otherwise stop C2c Apply without editing its targets. Observed 2026-08-13: governed finalizer archived C2b as `2026-08-13-retire-framework-version-stamp` with all 11 mechanical checks passed. Archive task evidence records the deleted writer/helper/template chain, no new-bundle stamp, and real generic unknown-frontmatter mutation preservation; fresh C2b focused suites passed 55/55.
- [x] 1.2 @impl VER-002, VER-003, SEF-004: Before the first C2c target edit, run `openspec validate retire-internal-version-choreography --strict`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-capability-discovery.mjs --change retire-internal-version-choreography`, `node openspec/governance/check-verification-routing.mjs --change retire-internal-version-choreography --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change retire-internal-version-choreography --mode plan`. Done when all pass after task 0.1 and no C2c target file has changed before this evidence. Observed 2026-08-13: all five commands exited 0; plan requirement check reported 644 registered / 54 retired / 0 orphan, and discovery/routing/closure plan records were valid before C2c target edits.
- [x] 1.3 @impl VEM-001, VEM-002, VEM-003, VEM-004, RUE-001: Run an active-source consumer scan over `DEEP_RESEARCH_HARNESS/`, `tests/`, `openspec/specs/`, `openspec/config.yaml`, and root `CHANGELOG.md`, excluding historical OpenSpec archives, to find positive current uses of the internal banner/version choreography. Done when every hit is either included in tasks 2-3, proven history-only, or added as an ordinary scoped repair task; no new version router, migration, or compatibility branch is introduced. Observed 2026-08-13: positive current owners are the two accepted specs, config rules, registry, `RUN.md`, version/entry/migration tests, plus catalog/static-vocabulary projections. The first group is covered by tasks 2.1-3.4; `openspec/specs/README.md` and `canonical-harness-vocabulary-contract.test.mjs` are recorded in task 3.5. `DEEP_RESEARCH_HARNESS/CHANGELOG.md` hygiene allowance is fixture-only, C6 transaction wording is unrelated history, and schema discriminators/old bundle facts are excluded. No router, migration, or compatibility branch was found or added.

## 2. Retire The Internal Version Choreography

- [x] 2.1 @impl VEM-001: Add a concise root `CHANGELOG.md` note that classifies its existing internal `v0.x` headings as retained human history, not a current version, package/Git release, compatibility selector, or runtime Source of Record. Preserve every existing historical entry byte-for-byte after the new note; do not add a new version heading, delete history, or fabricate tags. Done when the note and the retained entries satisfy the VEM-001 delta scenarios. Observed 2026-08-13: the root note names human history and excludes version/release/compatibility/execution authority. A direct `git show HEAD:CHANGELOG.md` comparison proves the historical suffix from `## v0.89` onward is byte-identical; the separate C2b-owned interim `v0.90` entry is recorded under task 4.4, and C2c adds no version heading, tag, or deleted history.
- [x] 2.2 @impl VEM-002, VEM-004: Remove the proposal-time version-bump requirement and the Apply task rule that forces root changelog and `RUN.md` banner updates from `openspec/config.yaml`. Done when current guidance still requires bounded proposal/design/tasks/verification/closeout discipline but no Harness behavior change is required to declare an internal `v0.x` target or create synchronized history/entry edits. Observed 2026-08-13: the two forced proposal/tasks rules were removed; the surrounding proposal/design/tasks/verification/closeout rules remain. `tests/engine/version-management.test.mjs` passes its direct negative assertions for version bump, target version, and synchronized banner wording.
- [x] 2.3 @impl RUE-001, RUE-002, VEM-003: Remove only the internal Harness version banner from `DEEP_RESEARCH_HARNESS/RUN.md`, making the existing Section 0 immediately follow the title. Preserve the existing Section 0, trigger-context blockquote, Sections 1-3, shortcut prohibition, and downstream route wording. Done when `RUN.md` has no `DEEP_RESEARCH_HARNESS v<major>.<minor>` banner, Section 0 follows the title, and the retained entry instructions are otherwise unchanged. Observed 2026-08-13: a direct comparison against `HEAD:DEEP_RESEARCH_HARNESS/RUN.md` proves the only byte change is deletion of the internal banner; the focused entry test proves title -> Section 0 -> trigger context order and passed.

## 3. Align Current Contracts And Focused Verification

- [x] 3.1 @impl VEM-001, VEM-002, VEM-003, VEM-004: Update `tests/engine/version-management.test.mjs` so it proves concise non-authoritative root history, no Harness-local changelog, no forced config choreography, and no `RUN.md` internal version banner. Done when the test no longer parses a current `v0.x` heading or compares it to entry text, while it still detects a return of version authority. Observed 2026-08-13: the test checks the root non-authority note, absent Harness-local changelog, absent internal banner, Section 0's first-content position, and absent forced config rules; it contains no changelog-heading parser or equality check and passed in task 4.1.
- [x] 3.2 @impl RUE-001, RUE-002: Update `tests/integration/md/dpt-research-entry-routing-contract.test.mjs` to prove the actual title -> Section 0 -> trigger-context coordinate order and absence of the internal version banner while retaining all current shortcut-prohibition and downstream-routing checks. Done when the test directly establishes that Section 0 is the first section after the title and precedes the trigger context, and no assertion treats a banner or changelog heading as entry authority. Observed 2026-08-13: the test compares title index 0, Section 0 index `title.length + 2`, and trigger-context order; it retains shortcut/downstream assertions and passed in task 4.1.
- [x] 3.3 @impl CMI-001, CMI-002, CMI-003, CMI-004, CMI-009: Narrow `tests/e2e/deep-research-harness-migration.test.mjs` to canonical temporary-bundle creation, inspection, trace placement, and source-tree isolation; remove only release-identity helper/assertions. Done when the E2E still covers its own bundle/runtime boundary and does not inspect `RUN.md` or `CHANGELOG.md` as a release pair. Observed 2026-08-13: release helper/assertions were removed while both temporary-bundle creation/inspection and runtime-write/trace/source-tree-isolation cases passed in task 4.1; the E2E has no `RUN.md` or `CHANGELOG.md` reference.
- [x] 3.4 @impl VEM-001, VEM-002, VEM-003, VEM-004, RUE-001, RUE-002: Synchronize the two delta specs into `openspec/specs/governance/version-management/spec.md` and `openspec/specs/bundle/run-entry/spec.md`, then update the existing VEM/RUE registry descriptions in `openspec/governance/req-registry.yaml` to match the new stable requirement names. Done when main specs contain no delta headers, preserve unchanged requirements verbatim, and no accepted current surface still claims a required internal target/changelog/banner choreography. Observed 2026-08-13: the six modified RUE/VEM requirement blocks exactly match their delta sources; main specs contain no delta headers, their Purpose text now names the current boundary, and `openspec validate --specs` passed 85/85 while the plan requirement check reported 0 orphan IDs.
- [x] 3.5 @impl VEM-001, VEM-003, RUE-001: Align the current catalog and static vocabulary projections discovered by the Apply consumer scan: update the `bundle/run-entry` and `governance/version-management` rows in `openspec/specs/README.md`, and update `tests/integration/md/canonical-harness-vocabulary-contract.test.mjs` to read those rows plus the accepted run-entry vocabulary and name the new current boundary rather than the retired banner requirement. Done when the test proves that neither catalog nor static vocabulary presents a version banner, required version update, or banner consistency as a current capability contract, while both retain their current entry/history ownership boundaries. Observed 2026-08-13: both catalog rows now state the current entry/history boundary; the static-vocabulary test reads both rows plus accepted run-entry/version-management specs, rejects retired wording, and passed in task 4.1.

## 4. Verification And Closeout

- [x] 4.1 @impl VEM-001, VEM-002, VEM-003, VEM-004, RUE-001, RUE-002: Run `node --test tests/engine/version-management.test.mjs tests/integration/md/dpt-research-entry-routing-contract.test.mjs tests/integration/md/canonical-harness-vocabulary-contract.test.mjs tests/e2e/deep-research-harness-migration.test.mjs`. Done when all selected unit, integration, and deterministic-E2E suites pass with native `node:test` verdicts and no skipped/cancelled test is treated as proof. Observed 2026-08-13: 15 tests in 4 suites passed; 0 fail, cancelled, skipped, or todo.
- [x] 4.2 @impl RUE-001, RUE-002: Run `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs` and the scoped current-surface hygiene scan from task 1.3: `rg -n --hidden --glob '!openspec/changes/archive/**' --glob '!node_modules/**' --glob '!_backlog/**' 'DEEP_RESEARCH_HARNESS v[0-9]+\\.[0-9]+|framework_version|readFrameworkVersion|CHANGELOG\\.md.*version|version bump|target version|版本横幅|版本号' DEEP_RESEARCH_HARNESS tests openspec/specs openspec/config.yaml CHANGELOG.md`. Done when the package validates, current entry documentation has no internal version banner, and every remaining match is retained root history, an archive, a negative assertion, or a separately owned current schema/history fact rather than a positive entry/version contract. Observed 2026-08-13: package validation returned `passed: true`; remaining scan hits are two retained historical root entries, negative VEM requirements/tests, and C2b's retired-stamp negative assertions. No positive current entry/version reader or banner remained.
- [x] 4.3 @impl VER-001, VER-002, VER-003, SEF-004: Run `git diff --check`, `openspec validate retire-internal-version-choreography --strict`, `node openspec/governance/check-capability-discovery.mjs --change retire-internal-version-choreography`, `node openspec/governance/check-verification-routing.mjs --change retire-internal-version-choreography --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change retire-internal-version-choreography --mode assets`. Done when all pass and the selected verification route remains unit/integration/deterministic-E2E only. Observed 2026-08-13: all five commands exited 0; routing reports 4 valid claims selected only as unit, integration, and deterministic-E2E, and closure remains structurally valid as `not_applicable`.
- [x] 4.4 @impl CHF-003: Establish a reproducible C2c closeout boundary before review because C2b archive work and C2c target work coexist relative to `HEAD`. Use `openspec/changes/archive/2026-08-13-retire-framework-version-stamp/tasks.md` as the C2b owner record and tasks 2.1-3.5 as the C2c owner record; classify every relevant `git diff HEAD --name-only` path as C2b-only, C2c-only, deliberate overlap, or unrelated-and-excluded. Done when task evidence identifies the C2b-only files, the deliberate overlaps, the C2c review set, and no unclassified target file remains; a C2b deletion is not credited as C2c work. Observed 2026-08-13: C2b-only: `DEEP_RESEARCH_HARNESS/cli/instantiate-run-bundle.mjs`, `DEEP_RESEARCH_HARNESS/engine/helpers/framework-version.mjs`, `DEEP_RESEARCH_HARNESS/rb_templates/rb_plan.md.tmpl`, `openspec/specs/bundle/cmd-bundle-instantiation/spec.md`, `tests/engine/framework-version.test.mjs`, `tests/engine/helpers/canonical-topic-state.test.mjs`, `tests/integration/cli/instantiate-run-bundle.test.mjs`, and `tests/integration/md/canonical-topic-state-contract.test.mjs`. Deliberate C2b/C2c overlap: `CHANGELOG.md` (C2b `v0.90` versus C2c note), `DEEP_RESEARCH_HARNESS/RUN.md` (C2b interim sync versus C2c removal), `openspec/governance/req-registry.yaml` (CMI-007 retirement versus RUE/VEM names), and `tests/integration/md/canonical-harness-vocabulary-contract.test.mjs` (C2b CMI-007 removal versus C2c catalog/static vocabulary). C2c-only review set: `openspec/config.yaml`, `openspec/specs/README.md`, `openspec/specs/bundle/run-entry/spec.md`, `openspec/specs/governance/version-management/spec.md`, `tests/e2e/deep-research-harness-migration.test.mjs`, `tests/engine/version-management.test.mjs`, and `tests/integration/md/dpt-research-entry-routing-contract.test.mjs`; plus the C2c portions of the four overlaps. `_backlog/**`, the untracked active C2c artifacts, and the untracked C2b archive record are excluded from raw `git diff HEAD --name-only` scope. No relevant target path is unclassified, and no C2b-only deletion is credited to C2c.
- [x] 4.5 `openspec-feedback:closeout-review` - After delta/main sync and task 4.4's scoped-file classification, review the C2c review set, the C2b archive prerequisite evidence, `semantic-closure.yaml`, and the selected verification results. Done when C2c removes only its release choreography, does not reopen `framework_version`/bundle template/schema/runtime behavior or historic entries, no actionable finding remains, and all ordinary repair tasks are complete. (CHF-001, CHF-003) Completed 2026-08-13: reviewed the classified C2c-only set and C2c portions of the four deliberate overlaps against the C2b archive task evidence. C2c removes only the root-history/entry/governance release projections: it neither changes the C2b writer/template/schema/instantiation boundary nor rewrites historical entries; direct comparisons prove `RUN.md` changed only by banner deletion and retained historical entries remain intact. The six delta/main requirement blocks match exactly, selected verification passed 15/15 with no skipped/cancelled tests, package validation passed, strict/discovery/routing/closure assets passed, and the focused hygiene scan found only history or negative assertions. `semantic-closure: not_applicable` remains accurate because the actual C2c review set changes no runtime fact family, resolver, Gate, receipt, trace, or verdict. No actionable finding remains.

## 5. Archive Preconditions

- [x] 5.1 @impl RET-006: Before archive, run `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-internal-version-choreography`. Done when it exits 0 with 0 duplicate, orphan, unregistered, and reused-retired requirement IDs for the selected change. Observed 2026-08-13: exited 0; 644 registered, 54 retired, and 0 orphan IDs.
- [x] 5.2 @impl RET-006: Before archive, run `node openspec/governance/check-project-specs.mjs`. Done when it exits 0 with 0 delta-header-in-main, missing-purpose, missing-requirements, or missing-requirement-header findings. Observed 2026-08-13: exited 0; 85 main spec files and 0 violations.
- [x] 5.3 @impl CHF-003, VER-002, VER-003, SEF-004: After all prior tasks and any review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-internal-version-choreography`. If it returns non-zero, restore this task to unchecked and repair its named root; do not use a raw move or alternate archive path. Done when the governed finalizer reports the resolved archive location and all mechanical closeout checks pass. All prior tasks and both feedback markers are complete; invoking the governed finalizer immediately after this task record.
