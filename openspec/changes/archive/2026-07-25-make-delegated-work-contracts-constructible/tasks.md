## 1. Pre-Target Evidence And Contract Tests

- [x] 1.1 @impl DEW-021: Create `implementation-evidence.md`; record the v0.47 worktree/baseline and reproduce malformed-versus-omitted claim observation, incomplete direct-output/cache authoring delivery, and mixed-root dry-submit behavior on fresh disposable bundles without editing diagnostic production state; record BUG-120 only as a separately owned Phase-Agent guidance boundary; run `node openspec/governance/check-verification-routing.mjs --change make-delegated-work-contracts-constructible --mode plan` before target edits. Done when every planned claim is classified as deterministic evidence, real-actor evidence, or an honest external boundary.
- [x] 1.2 @impl DEW-021: Add focused red unit coverage in `tests/engine/work-unit-contract-constructibility.test.mjs` for four observation case shapes expanded to seven exact tuples, malformed supplied vocabulary without synthetic outcome, validator-owned direct-output/cache authoring projection parity, and candidate primary selection parity between recommended action and all public primary repair fields. Done when the tests fail on the current opaque parse, incomplete authoring projection, and `violations[0]` split without a bundle, queue mutation, or external call.
- [x] 1.3 @impl DEW-021: Add focused red integration coverage in `tests/integration/engine/work-unit-contract-constructibility.test.mjs` for Wave0 plus primary/supplementary Wave1 claim/envelope parity, `task.md` first Completion Contract section, spawn pointing only to that task, no pre-populated result/receipt-event/cache-leaf/output/source-claim/ledger content, old-envelope readability, and prerequisite masking. Extend `tests/integration/cli/operate-work-unit.test.mjs` for structured malformed-CLI rejection/no-mutation/no-trace, including a present empty `--actor-*` value distinct from omission, omitted-observation compatibility, dry/normal-formal selected-primary consistency, and timeout forwarding only the same action/code. Done when tests distinguish independently evaluable roots from derived symptoms, preserve the existing empty receipt-file envelope behavior and late-submit boundary, and use real temporary bundle contracts only.

## 2. One Contract Lineage At Claim And Actor Entry

- [x] 2.1 @impl DEW-021: Refactor `DPT_FRAMEWORK/schema/contracts/work-unit.mjs` and `DPT_FRAMEWORK/engine/work-unit-actor.mjs` so one closed immutable observation-case definition supplies both Zod validation and the output-only role-bound vocabulary projection. Define code-only Zod schemas for exact legal tuples, the seven-tuple contract projection, and JSON-safe malformed-input issues. Done when legal tuple/fallback/no-claim semantics remain unchanged, invalid input names the conflicting fact and valid cases, no generic HITL/trace conversion exists, and code comments carry `@impl DEW-021`.
- [x] 2.2 @impl DEW-021: Refactor `DPT_FRAMEWORK/schema/contracts/reference.mjs`, `DPT_FRAMEWORK/engine/helpers/direct-output-contract.mjs`, and `DPT_FRAMEWORK/engine/helpers/cache-leaf-contract.mjs` so validator-owned declarative definitions expose Zod-validated, read-only direct-output/cache authoring projections. Done when `ReferenceMetadataSchema` and its Wave0 authoring fields share one definition, cache projection states resolved leaves/page/degraded/mapping facts from its existing owner, no task parser or second validator exists, and code comments carry `@impl DEW-021`.
- [x] 2.3 @impl DEW-021: Update `DPT_FRAMEWORK/engine/work-unit-lifecycle.mjs` and `DPT_FRAMEWORK/cli/operate-work-unit.mjs` to return malformed supplied observations through the structured claim decision path before trace or allocation mutation while preserving omitted-observation no-claim/audit behavior; CLI presence detection SHALL use option presence rather than truthiness, so a present empty `--actor-*` value is malformed rather than omitted. Done when no concrete observation result is fabricated, existing valid claim/fallback behavior and exit semantics remain compatible, and the same claim checkpoint is the sole repair path.
- [x] 2.4 @impl DEW-021: Update `DPT_FRAMEWORK/engine/work-unit-envelope.mjs` to render the non-authoritative `Completion Contract` as the first authoring section of existing `task.md`, from existing manifest/beacon/result-schema/direct-output/cache/source/receipt owners; have spawn identify only that task section while removing task/spawn duplicated attempt-bound completion fragments. Done when parity holds for Wave0, primary Wave1 and supplementary Wave1, the task retains semantic role/lifecycle context, no new file/path or manifest/beacon/result/receipt/cache/output/ledger schema is added, and claim does not pre-populate actor-produced result content, receipt events, cache leaves, source claims, output, or ledger content.

## 3. One Primary Root Across Existing Checkpoints

- [x] 3.1 @impl DEW-021: Refactor `DPT_FRAMEWORK/engine/work-unit-candidate-projection.mjs` to expose one pure selected public root together with its existing action/code, preserving all independently evaluated violations and their coordinates. Done when scope precedence and phase ordering remain deterministic, selection adds no persistent state or validator, and no consumer independently selects `violations[0]`.
- [x] 3.2 @impl DEW-021: Update `DPT_FRAMEWORK/engine/work-unit-submit.mjs` so claimed normal dry-submit and normal formal rejection consume the selected root for every primary repair field, and retain timeout-preflight's existing forwarding of only selected action/code into lease advice; short-circuit only checks that lack their prerequisite authority. Done when normal candidate action/code/repair fields agree, timeout adds no second selector/detail surface, same-check repair remains the only legal mechanical loop, and ledger/queue/provenance/Gate/late-submit/terminal-replacement semantics do not change.

## 4. Real-Actor Boundary And Regression

- [x] 4.1 @impl DEW-021: Update the existing `experiments_playbook/exp_wfn_wave1/case-221-heavy-batch-subagent.md` evidence contract so retained supplied task evidence proves the real actor was given a task containing the first `Completion Contract` section before its first returned work, without claiming private reading/reasoning, parent-authored output, invented receipt/trace facts, a new verdict check, research quality, or BUG-120 Phase-Agent guidance delivery. Done when the case retains its existing durable evidence roles and native submit/inspect/Gate authority.
- [x] 4.2 @impl DEW-021: Run the selected unit/integration suites, `validate-work-unit-hygiene`, `validate-workflow-package`, and the existing case-221 Agent-flow path when authenticated real actor runtime/budget are available. Done when deterministic tests pass and the real path records native PASS/FAIL/NOT_RUN with exact evidence; fixture, mock, or manually authored artifacts never substitute for actor behavior.

## 5. Release, Governance And Closure

- [x] 5.1 @impl DEW-021: Update root `CHANGELOG.md` for `v0.48` and synchronize `DPT_FRAMEWORK/RUN.md` banner/current-release summary. Done when both describe generated constructible contract entry and unified primary feedback without claiming a real actor behavior result not observed.
- [x] 5.2 @impl DEW-021: Complete `implementation-evidence.md` and audit the final surface list. Done when it identifies the restructured existing task section, validator-owned authoring projections, and two output-only claim/root projections; the removed duplicate task/spawn/primary-selection paths; no new file/state/validator/controller/retry/success authority; BUG-114/115 closure only at their stated evidence boundaries; and BUG-120 as unclosed outside this change.
- [x] 5.3 @impl DEW-021: Run `node openspec/governance/check-verification-routing.mjs --change make-delegated-work-contracts-constructible --mode assets`. Done when selected assets exist at canonical paths and the actor/runtime/external-call/verdict boundaries remain declared.
- [x] 5.4 @impl DEW-021: Run `node openspec/governance/check-project-reqs.mjs`. Done with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired violations, including registered `DEW-021`.
- [x] 5.5 @impl DEW-021: Run `node openspec/governance/check-project-specs.mjs`. Done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [x] 5.6 @impl DEW-021: Run `openspec validate make-delegated-work-contracts-constructible --strict`, `git diff --check`, and focused compatibility review. Done when proposal/spec/design/tasks agree on no generic HITL proof, no pre-created authority, one primary selector, v0.48 release metadata, and unchanged queue/Gate/ledger/replacement authority.
