## 1. Apply Entry and Planning Evidence

- [x] 1.1 Complete the change-scoped plan review (`openspec-feedback:plan-review`) before the first target edit. Verify that HIU-002/003, URC-001, PRP-012/014, CDP-001, and REI-006 retain one direct owner; the controls snapshot and rationale remain narrative carriers; and P2/P3 behavior is excluded. Done when any finding is either resolved or recorded as an ordinary unchecked repair task with an owner and direct done condition.
- [x] 1.2 @impl HIU-002, HIU-003, URC-001, PRP-012, PRP-014, CDP-001, REI-006: Run `node openspec/governance/check-verification-routing.mjs --change align-topic-focus-and-rerun-guidance --mode plan`, `openspec validate align-topic-focus-and-rerun-guidance --strict`, and `node openspec/governance/check-capability-discovery.mjs --change align-topic-focus-and-rerun-guidance` before target edits. Done when the planning artifacts and their declared test routes pass without creating a target-code edit.

## 2. HITL1 Alignment and Durable Narrative Carrier

- [x] 2.1 @impl HIU-002: Update `DEEP_RESEARCH_HARNESS/workflows/nodes/brief/hitl1.md` and `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-hitl1.md` so the initial preview is the minimum independent Topic map: one-or-more proposed Topics, no preset upper cap, and reviewable thread grouping for a large map. After acceptance, the existing one-approved-Topic lower bound remains unchanged. Add the optional recommendation-first natural-language focus brief and a concise user-correctable Agent interpretation. Done when no `3-5` or other numeric target remains in this HITL1 guidance and the existing no-focus path, one Topic path, and broad-map path are all explicit.
- [x] 2.2 @impl URC-001, PRP-012, PRP-014: Update HITL1/controls guidance to pass the accepted user's verbatim focus wording and separately labelled Agent interpretation through the existing pure `plan-hostfile-sections` renderer into the one `rb_plan.md## Constraints > ### User Research Controls` literal snapshot before canonical topic-state apply. Done when the target guidance explicitly preserves literal text across apply/recovery and rejects a profile field, Topic field, parser, Gate input, source quota, or new interaction checkpoint.

## 3. HITL2 and Rerun Increment Guidance

- [x] 3.1 @impl HIU-003, CDP-001: Update `DEEP_RESEARCH_HARNESS/workflows/nodes/brief/hitl2.md` and `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-hitl2.md` so a legal natural-language rerun focus is reflected, corrected in the existing HITL2 loop, and recorded in the existing rationale as verbatim user wording plus separately labelled current Agent interpretation. Done when the existing five decision enums, availability evaluator, Gate, handoff, `stop: yes` boundary, and no-focus rerun compatibility remain unchanged.
- [x] 3.2 @impl REI-006: Update `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-rerun.md` so the legal recorded focus rationale informs only the current existing topic-state direction candidate and affected `## 本轮重跑方向` guidance. Done when historical direction/evidence remains context rather than newly produced or focus-covered work, and the phase retains the existing route-bound witness, topic-state, style handoff, count, rerun-ready Gate, and `stop: no` boundaries.

## 4. Deterministic Contract Coverage

- [x] 4.1 @impl HIU-002, URC-001, PRP-012, PRP-014: Add `tests/integration/md/topic-research-emphasis-guidance.test.mjs` and extend `tests/integration/cli/user-research-controls-contract.test.mjs`. Done when they prove the no-cap/minimum-map and optional-focus Markdown contract, a labelled literal focus snapshot survives canonical topic-state apply/recovery, and it does not become profile/queue/Gate authority; the tests do not claim Agent semantic understanding.
- [x] 4.2 @impl HIU-003, CDP-001, REI-006: Add `tests/integration/md/topic-research-emphasis-rerun-guidance.test.mjs`. Done when it proves the existing rationale/direction carrier order, labelled current focus handling, no new route/checkpoint/Gate, and the explicit historical-versus-current-increment boundary without simulating a Subject Agent.
- [x] 4.3 @impl HIU-002, HIU-003, URC-001, PRP-012, PRP-014, CDP-001, REI-006: Run the selected integration assets and `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`. Done when each command's native exit result is recorded in the change evidence and no fixture result is reported as Agent behavior.

## 5. Real Agent-Flow Evidence

- [x] 5.1 @impl HIU-002, URC-001, PRP-012, PRP-014: Add `experiments_playbook/exp_iterative_interaction/case-716-heavy-hitl1-topic-focus.md` and register it in the active playbook manifest. It SHALL define a legal HITL1 setup-only boundary, an independent real Subject Agent, exact prompt/transcript/result evidence roles, and the `NOT_RUN` boundary. Done when the playbook validates without creating Subject-owned focus, decision, probe, or Gate facts in its fixture.
- [x] 5.2 @impl HIU-002, URC-001, PRP-012, PRP-014: Add the smallest fixture/adapter/observer support for case 716. Done when it observes a broad reviewable map and a natural-language focus correction through the existing literal snapshot/handoff, while an unavailable Subject Agent or real tool finalises only `NOT_RUN`, never fixture PASS.
- [x] 5.3 @impl HIU-003, CDP-001, REI-006: Extend `case-712-heavy-hitl2-natural-rerun.md` and its deterministic observer only as needed to check the accepted labelled rerun rationale and existing HITL2 Gate/rerun handoff. Done when the real Subject Agent owns the decision/rationale/Gate/handoff, the observer does not invent those facts, and unsupported runtime remains `NOT_RUN`. Per-Topic direction and historical-versus-current-increment behavior remain the static integration-contract proof for this slice.
- [x] 5.4 @impl HIU-002: Run the existing `case-711-heavy-hitl1-natural-acceptance.md`, new case 716, and updated case 712 through the supported Agent experiment path when their required real runtime is available. Done when retained native reports distinguish PASS, FAIL, ERROR, and NOT_RUN honestly: `NOT_RUN` proves only the unavailable runtime, while a `FAIL` proves only its retained failed checks and never semantic success.
- [x] 5.5 @impl HIU-003, CDP-001, REI-006: Owner: Apply Agent. Quarantine case 712 after its retained native completion shows an unacceptable runtime. Move its playbook to `experiments_playbook/exp_extrem_slow/` with a quarantine banner, remove it from the active manifest and active agent-flow verification plan, and update active-playbook static wiring without deleting the existing deterministic rerun-carrier contract. Done when Autorun/Interactive cannot select case 712, `check-verification-routing --mode assets` validates the remaining active claims, and implementation evidence records the exact retained FAIL coordinate, elapsed time, and cost.

## 6. Release and Closeout

- [x] 6.1 @impl VEM-002, VEM-003, VEM-004: Add the concise `v0.79` behavior summary to root `CHANGELOG.md` and update the `DEEP_RESEARCH_HARNESS/RUN.md` banner/current-release section to the same version. Done when both surfaces use `v0.79` and do not present implementation detail or Agent-flow observations as broader behavioral proof.
- [x] 6.2 @impl HIU-002, HIU-003, URC-001, PRP-012, PRP-014, CDP-001, REI-006: Record the actual selected test commands, native results, Agent-flow outcome/NOT_RUN coordinates, and residual risk in change-root implementation evidence. Run `node openspec/governance/check-verification-routing.mjs --change align-topic-focus-and-rerun-guidance --mode assets` after the assets exist. Done when deterministic and Agent-behavior evidence boundaries remain separately stated.
- [x] 6.3 Sync the accepted delta behavior into the five main specs through the supported spec-sync entry, then re-read and compare the synchronized main requirements with this change's deltas. Done when the archive candidate retains the existing behavior plus this change's exact focus/no-cap/rerun-history boundary, without an unregistered requirement ID.
- [x] 6.4 Complete the change-scoped closeout review (`openspec-feedback:closeout-review`) after actual diff and selected evidence are available. Done when every review finding has become an ordinary resolved task or an ordinary unchecked repair task; the marker itself is not treated as proof of semantic quality or test success.
- [x] 6.5 Run strict change validation, requirement traceability, main-spec validation, capability discovery, verification-routing asset validation, `git diff --check`, and the governed archive prerequisites. Done when all checks pass and the change is ready for the supported archive flow; do not use a raw move or alternate archive route.
