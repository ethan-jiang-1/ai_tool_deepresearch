## 1. Pre-Target Verification And Red Evidence

- [x] 1.1 @impl DEW-009, SNC-007, RWP-015: Create change-root `implementation-evidence.md`, record the pre-apply commit/worktree baseline, and run `node openspec/governance/check-verification-routing.mjs --change deliver-work-unit-role-contracts-to-actors --mode plan` before target edits; done when all four routed claims validate, deterministic E2E is explicitly not applicable, deterministic assets do not claim actor/search/fetch behavior, and the conditional curl observation is neither a fifth routed claim nor a playbook verdict check.
- [x] 1.2 @impl DEW-009: Add red `tests/engine/work-unit-contract-delivery.test.mjs` using temporary framework roots for role/dependency marker, containment, missing/unknown/mismatch and direct descriptor/evaluator parity; done when current code lacks both owners, helper failures require no bundle and cannot mutate queue/index/envelope, production root override is absent, and the test owns no second heading or role-to-shared map.
- [x] 1.3 @impl DEW-009, RWP-015: Add red `tests/integration/engine/work-unit-role-contract-delivery.test.mjs` over delegated primary/supplementary Wave1 claims, accepted Phase Agent fallback, existing no-claim decisions and one legacy unbound constructor case; done when both allow-claim actor classes receive identical kind-owned delivery, no-claim retains its nearest repair without delivery reads, assignment scope stays exact, and legacy construction invents no actor role or production test hook.
- [x] 1.4 @impl SNC-007, RWP-015: Add red `tests/integration/md/subagent-fetch-contract-delivery.test.mjs` for one shared per-URL chain, actor-delivery ref filtering, exact bounded curl/permission/exhaustion/batching/cache/source boundaries, stable `fetch_attempt_done` diagnostic fields, and absence of Python, wget, role-local duplicate chains, automatic retry and user command handoff; done when current duplicated/missing/Python guidance fails only those target assertions without external calls.

## 2. Contract-Owned Projections And Envelope Delivery

- [x] 2.1 @impl DEW-009: Deepen the existing direct-output contract module so each closed ID owns its evaluator and one bounded immutable actor descriptor; done when the descriptor names only contract purpose/minimum structure, unknown/missing descriptors fail closed, fresh-byte evaluation/root semantics are unchanged, and no envelope/test/role file owns a separately accepted deterministic heading inventory even though rich role examples may show the same labels.
- [x] 2.2 @impl DEW-009: Add narrow `engine/helpers/work-unit-role-guidance.mjs`; accept only lifecycle-resolved `{kind, delegated_role_key, actor_policy}` and verify policy/key parity without rebuilding queue kind lookup; derive production root from `import.meta.url`, keep explicit root test-only, parse one-level role `requires[]`, and project the one marked page-fetch id/scope; done when output is deep-frozen/contained and claim/CLI/queue/actor cannot supply root or refs.
- [x] 2.3 @impl DEW-009, RWP-015: Refactor `work-unit-envelope.mjs` task/spawn renderers to accept one explicit validated ephemeral delivery projection without deriving from cwd, queue or actor input and without activating production claim wiring yet; done when the same projection renders identical role/shared refs and direct descriptors on both surfaces, legacy calls without current actor binding retain prior behavior, and renderer tests cannot use task text as acceptance authority.

## 3. Shared Agent-Facing Fetch Contract

- [x] 3.1 @impl SNC-007: Add `shared-page-fetch-guidance.md` with exact frontmatter `id: shared-page-fetch-guidance`, `shared_scope: subagent-fetch`, `authority: guidance-only`, `actor_delivery: required`; done when each URL uses available built-in/browser then Node `fetch` and at most one hardened same-URL curl under independent permission, every completed tier records `fetch_attempt_done` detail `{url,tier,surface,outcome,reason_code}`, and neither file nor event becomes manifest/submit authority.
- [x] 3.2 @impl SNC-007, RWP-015: Update registered source-intake/evidence-extractor/topic-scout and active auxiliary claim-verifier/source-diagnostic roles to directly require the shared fetch owner, remove role-local chains/Python fallback, and teach generated delivery; make evidence-extractor authoring assignment-scoped so primary writes its pair while supplementary never rewrites prior pair; update package consistency checks for direct dependencies/one actor-delivered fetch node while preserving the three registered/two auxiliary distinction.
- [x] 3.3 @impl DEW-009, SNC-007, RWP-015: Activate formal `claimWorkUnits` wiring only after 3.1/3.2: preserve existing actor decision, run delivery preflight only for `allow_claim`, pass the same projection through envelope/response spawn, and replace kind-only Wave1 paired wording with `required_outputs[]`-conditioned wording; done when delegated/fallback primary and supplementary claims receive correct scope, no-claim repairs remain unchanged, invalid lineage has zero mutation, and legacy construction remains compatible.
- [x] 3.4 @impl DEW-009, SNC-007, RWP-015: Run unit/envelope/Markdown suites plus `validate-work-unit-hygiene` and `validate-workflow-package`, then audit assignment compatibility; done when index/manifest/beacon/result schema/version, queue snapshot/resolver, dry-submit/formal submit, receipt/provenance/ledger/Gate/transition owners and historical claimed envelopes are unchanged, with no network/Agent-behavior claim, migration, second submit or parallel success path.

## 4. Real Actor Verification And Regression

- [x] 4.1 @impl DEW-009, SNC-007, RWP-015: Strengthen existing manifest-registered case-221 without adding a case, Subject identity, required check ID or durable role; done when both real dpt-evidence-extractors start from generated surfaces and independently dry-submit before formal submit, one representative actor's two manifest-derived targets are hashed before Phase edit and after submit, and any dry-submit failure is finalized through existing false checks without parent content repair or attempted formal acceptance.
- [x] 4.2 @impl DEW-009, SNC-007, RWP-015: Deepen existing required check `real-wave1-batch-submit` to bind both work IDs plus the representative actor's two target refs, pre/post hashes, dry-submit/formal-submit outcomes and existing Subject task/result/receipt/output evidence in the durable trace prefix; done when existing case frontmatter, four durable roles, compatibility ledger and check-count contracts remain unchanged and the documented limitation says only one output has retained raw Subject bytes.
- [x] 4.3 @impl DEW-009, SNC-007, RWP-015: Keep general first-return verdict checks and delegated fallback observation independent; classify fallback only from the representative actor's durable structured receipt details plus result/cache/source declarations and formal submit as OBSERVED_PASS/OBSERVED_FAIL/UNOBSERVED, never as a playbook check or new evidence role; then execute case-221 once through canonical Autorun when authenticated Playbook/real-subagent/search/fetch runtimes are available, with no fixture substitution, parent-authored output or rerun solely to chase a branch.
- [x] 4.4 @impl SNC-007: Update BUG-096 evidence only from the durable delegated fallback observation; done when OBSERVED_PASS can close the delegated residual, OBSERVED_FAIL preserves the exact contradiction, UNOBSERVED keeps the bug active, and no observation is called claim-level NOT_RUN or extrapolated across Claude/Codex runtimes.
- [x] 4.5 @impl DEW-009, SNC-007, RWP-015: Run focused work-unit/direct-output/role/phase/Autorun regression and `npm test`; done when relevant suites pass, case-221 durable-role policy remains valid, optional observation cannot alter `verdict_mode: all`, and every unrelated pre-existing failure has an exact focused reproduction recorded outside these four routed claims.

## 5. Release, Bug Status, Governance And Closure

- [x] 5.1 @impl DEW-009, SNC-007, RWP-015: Add proposal-declared `v0.40` to root `CHANGELOG.md`; done when one or two concise bullets describe actor contract delivery/shared fetch guidance without file lists, requirement IDs, universal first-pass or external-access claims.
- [x] 5.2 @impl DEW-009, SNC-007, RWP-015: Synchronize `DPT_FRAMEWORK/RUN.md` banner/current-release to `v0.40`; done when it matches CHANGELOG and does not claim BUG-097 or unobserved runtime behavior fixed.
- [x] 5.3 @impl DEW-009, SNC-007, RWP-015: Complete `implementation-evidence.md` and audit non-goals/control surfaces; done when every routed claim maps to its native PASS/FAIL/NOT_RUN authority, the receipt-backed conditional observation maps separately to OBSERVED_PASS/OBSERVED_FAIL/UNOBSERVED, added/removed surfaces are explicit, and final diff has no dependency, Python, persisted work-unit schema/state/role marker, second validator/submit path, actor selector, fetch controller/retry tree, user co-runner or fabricated evidence.
- [x] 5.4 @impl DEW-009, SNC-007, RWP-015: Update BUG-098 and BUG-096 status only from native evidence; done when BUG-098 moves to fixed only if the general real-actor first-return claim passes, BUG-096 moves only if the durable delegated fallback observation is OBSERVED_PASS in addition to archived HITL1 closure, otherwise each remains active with exact NOT_RUN/FAIL/OBSERVED_FAIL/UNOBSERVED residual, and BUG-097 remains open.
- [x] 5.5 @impl DEW-009, SNC-007, RWP-015: Run `node openspec/governance/check-verification-routing.mjs --change deliver-work-unit-role-contracts-to-actors --mode assets`; done when all selected assets exist at canonical paths and actor/runtime/external-call/verdict boundaries remain declared.
- [x] 5.6 @impl DEW-009, SNC-007, RWP-015: Run `node openspec/governance/check-project-reqs.mjs`; done with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired violations while reusing the existing requirement IDs.
- [x] 5.7 @impl DEW-009, SNC-007, RWP-015: Run `node openspec/governance/check-project-specs.mjs`; done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [x] 5.8 @impl DEW-009, SNC-007, RWP-015: Run `openspec validate deliver-work-unit-role-contracts-to-actors --strict` and `git diff --check`; done when the complete Change validates, v0.40 projections agree, archive wording distinguishes whole-case NOT_RUN from fallback UNOBSERVED, and final review confirms one contract owner plus one existing repair/submit path.
