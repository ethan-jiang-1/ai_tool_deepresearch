> Apply discipline: after each completed target-edit or verification task,
> update its checkbox and append concise, directly observed command evidence to
> that task line. A planned command is not completion evidence.

## 0. Feedback Review

- [x] 0.1 `openspec-feedback:plan-review` - Before the first target edit, obtain current Apply guidance and review `proposal.md`, the CMI-007 removal delta, `design.md`, this task list, `verification-plan.yaml`, and `semantic-closure.yaml` against CMI-004, CMI-007, CMI-008, CMI-009, VEM-001 through VEM-004, and the accepted generic canonical-plan boundary. Done when the review confirms that removing the writer does not change schema/mutation ownership, C2c remains out of scope until this change archives, `semantic-closure: not_applicable` remains accurate, and every actionable finding is an ordinary unchecked task naming its owner, smallest repair, and independently observable done condition. (CHF-001, CHF-002) Completed 2026-08-13: current Apply guidance and `openspec/operations/change-feedback-loop.md` were read. The recorded writer chain and CMI-007 removal delta are coherent with the current plan schema/mutation boundary: generic `.passthrough()` plus clone/render preservation remains unchanged, while no Engine/Gate/CLI execution reader selects behavior from the stamp. `semantic-closure: not_applicable` remains accurate, C2c target files remain out of scope pending this governed archive, and no actionable finding was found.

## 1. Preconditions And Governance

- [x] 1.1 @impl VER-002, VER-003, SEF-004: Before the first target edit, run `openspec validate retire-framework-version-stamp --strict`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-capability-discovery.mjs --change retire-framework-version-stamp`, `node openspec/governance/check-verification-routing.mjs --change retire-framework-version-stamp --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change retire-framework-version-stamp --mode plan`. Done when all pass after task 0.1 and no target file has changed before this evidence. Observed 2026-08-13: all five commands exited 0; plan requirement check reported 644 registered / 54 retired / 0 orphan, and discovery/routing/closure plan records were valid before target edits.
- [x] 1.2 @impl CMI-007: Scan `DEEP_RESEARCH_HARNESS/`, `tests/`, `openspec/specs/`, and `openspec/governance/req-registry.yaml` for `framework_version`, `framework-version`, and `readFrameworkVersion` before edits. Done when every current-positive hit belongs to the recorded helper -> instantiator -> template chain, CMI-007, or its focused tests; any additional current reader is added as an ordinary repair task or returns the change to planning, and no migration/router/adapter is introduced. Observed 2026-08-13: the pre-edit scan found only the recorded helper -> instantiator -> template chain, template field, CMI-007, and focused assertions; no decision reader, migration, router, or adapter was found.

## 2. Retire New-Bundle Stamp Creation

- [x] 2.1 @impl CMI-004, CMI-007, CMI-008, CMI-009: Delete `DEEP_RESEARCH_HARNESS/engine/helpers/framework-version.mjs`; remove its import, root-CHANGELOG read, and `{{framework_version}}` replacement from `DEEP_RESEARCH_HARNESS/cli/instantiate-run-bundle.mjs`; and remove the field/placeholder from `DEEP_RESEARCH_HARNESS/rb_templates/rb_plan.md.tmpl`. Done when new bundle creation has no version-stamp writer or replacement path, keeps the existing argument validation and bundle isolation/navigation behavior, and does not add a substitute metadata field or reader. Observed 2026-08-13: the helper was deleted; the instantiator no longer imports or reads version data; and the template has no field/placeholder. Production instantiation and package validation pass.
- [x] 2.2 @impl CMI-004, CMI-007, CMI-008, CMI-009: Delete `tests/engine/framework-version.test.mjs`; change `tests/integration/cli/instantiate-run-bundle.test.mjs` from a changelog-derived stamp assertion to parsed absence of `framework_version`; and align `tests/integration/md/canonical-harness-vocabulary-contract.test.mjs` plus `tests/integration/md/canonical-topic-state-contract.test.mjs` so they no longer present CMI-007 or the retired name as a positive current contract. Done when the test suite contains no assertion that new output must carry the stamp and no test reads `CHANGELOG.md` solely to populate or compare it. Observed 2026-08-13: dedicated helper test was deleted; fresh-bundle integration asserts parsed absence; static contracts reject current CMI-007/stamp vocabulary; selected suites pass.
- [x] 2.3 @impl CMI-007: Add one focused real-mutation regression in `tests/engine/helpers/canonical-topic-state.test.mjs`: seed a valid existing canonical plan with an arbitrary unknown key such as `legacy_metadata: retain-me`, perform a normal topic-state mutation, and assert the key remains after parse/render. Done when the assertion proves the existing generic `.passthrough()`/clone/render behavior without using `framework_version`, changing plan schemas, or adding a field-specific compatibility branch. Observed 2026-08-13: the real mutation test seeds `legacy_metadata: retain-me`, receives `committed`, reparses `rb_plan.md`, and observes the unchanged key; selected unit suite passes.
- [x] 2.4 @impl CMI-007, RET-005: Synchronize the CMI-007 removal delta into `openspec/specs/bundle/cmd-bundle-instantiation/spec.md` by removing its header ID, requirement, and scenarios, then mark `CMI-007` `[DEPRECATED]` in `openspec/governance/req-registry.yaml` without reusing it. Done when the accepted main spec contains no positive creation stamp requirement, the active delta remains the historical removal record, and the registry records permanent retirement. Observed 2026-08-13: the main-spec header and complete CMI-007 block were removed; the delta remains the removal record; registry entry is `never reuse [DEPRECATED]`; strict/spec governance checks pass.
- [x] 2.5 @impl VEM-001, VEM-002, VEM-003, VEM-004: Under the still-accepted interim version-management rule, add one concise `v0.90` root `CHANGELOG.md` entry for retiring new-bundle stamp creation and synchronize the existing `DEEP_RESEARCH_HARNESS/RUN.md` banner to `v0.90`. Done when both values match, no version router/migration/release process is added, and C2c remains the separate owner of later removing this choreography. Observed 2026-08-13: root `CHANGELOG.md` has concise `v0.90` entry and `RUN.md` banner is `v0.90`; version-management selected suite passes; no router/migration/release surface was added.

## 3. Verification

- [x] 3.1 @impl CMI-004, CMI-007, CMI-008, CMI-009, VEM-001, VEM-003, VER-001: Run `node --test tests/engine/helpers/canonical-topic-state.test.mjs tests/integration/cli/instantiate-run-bundle.test.mjs tests/integration/md/canonical-harness-vocabulary-contract.test.mjs tests/integration/md/canonical-topic-state-contract.test.mjs tests/engine/version-management.test.mjs`. Done when all selected unit/integration suites pass with native `node:test` verdicts and no skipped, cancelled, or weakened assertion is treated as proof. Observed 2026-08-13: 60 tests in 5 suites passed; 0 fail, cancelled, skipped, or todo.
- [x] 3.2 @impl CMI-004, CMI-008, CMI-009: Run `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs` and a focused current-surface scan over `DEEP_RESEARCH_HARNESS/`, `tests/`, and `openspec/specs/`. Done when the package validates, fresh-output code/tests/specs have no positive `framework_version` writer or requirement, and any retained occurrence is proven historical/removal-only rather than a current success path. Observed 2026-08-13: package validation returned `passed: true`; current-surface scan found no writer/helper/main-spec requirement. Retained hits are the negative fresh-output assertion, static absence assertion, permanently retired registry entry, and C6's unrelated transaction-history phrase.
- [x] 3.3 @impl VER-002, VER-003, SEF-004: Run `git diff --check`, `openspec validate retire-framework-version-stamp --strict`, `node openspec/governance/check-capability-discovery.mjs --change retire-framework-version-stamp`, `node openspec/governance/check-verification-routing.mjs --change retire-framework-version-stamp --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change retire-framework-version-stamp --mode assets`. Done when all pass and the selected proof route remains unit/integration only. Observed 2026-08-13: all five commands exited 0; asset routing reports 4 claims, all selected as unit/integration, and closure remains structurally valid.

## 4. Closeout Review

- [x] 4.1 `openspec-feedback:closeout-review` - After delta/main-spec synchronization, review the actual change-scoped diff, CMI-007 registry retirement, generic-key mutation evidence, selected verification results, and `semantic-closure.yaml`. Done when the diff removes only C2b's creation stamp and related current-positive assertions, leaves schema strictness/old-bundle handling/C2c targets untouched, contains the required interim v0.90 history/banner pair, has no actionable finding, and all ordinary repair tasks are complete. (CHF-001, CHF-003) Completed 2026-08-13: reviewed C2b-scoped code/test/spec/registry diff and all selected results. It removes only creation-time stamp ownership and current-positive assertions; `CanonicalPlanSchema.passthrough()` and old-bundle generic handling remain unchanged; C2c's removal of version choreography is not applied, apart from the required interim `v0.90` changelog/banner pair. Main/delta removal matches, registry retirement is permanent, the generic-key real mutation passed, and `semantic-closure: not_applicable` remains accurate because no decision reader, semantic fact family, resolver, Gate, receipt, trace, or verdict changed. No actionable finding remains.

## 5. Archive Preconditions

- [x] 5.1 @impl RET-006: Before archive, run `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-framework-version-stamp`. Done when it exits 0 with 0 duplicate, orphan, unregistered, and reused-retired requirement IDs for the selected change. Observed 2026-08-13: exited 0; 644 registered, 54 retired, 0 orphan.
- [x] 5.2 @impl RET-006: Before archive, run `node openspec/governance/check-project-specs.mjs`. Done when it exits 0 with 0 delta-header-in-main, missing-purpose, missing-requirements, or missing-requirement-header findings. Observed 2026-08-13: exited 0; 85 main spec files and 0 violations.
- [x] 5.3 @impl CHF-003, VER-002, VER-003, SEF-004: After all prior tasks and any review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-framework-version-stamp`. If it returns non-zero, restore this task to unchecked and repair its named root; do not use a raw move or alternate archive path. Done when the governed finalizer reports the resolved archive location and C2c's recorded target-edit prerequisite is satisfied. Rechecked the two-change dependency on 2026-08-13; all C2b archive checks remain passed. Invoking the governed finalizer immediately after this task record.
