## 0. Apply Entry Review

- [x] 0.1 @impl C1e reader question `openspec-feedback:plan-review` - Before the first target edit, read current feedback-loop Apply guidance and review proposal, design, `skip_specs: true` rationale, semantic-closure rationale, verification plan, current definition-only evidence, the private helper, exported `parseFrontmatter()`, and focused parser tests. Record every actionable finding as an ordinary pending task with its reader question, authoritative owner, smallest repair, and independently observable done condition; complete only when none remains.
- [x] 0.2 @impl C1e reader question - Before the first target edit, run strict change validation, plan-mode requirement governance, project-spec governance, capability taxonomy/discovery, verification-routing plan check, semantic-closure plan check, and `git diff --check`. Done when every command exits 0; record structural results only, without claiming runtime or Agent-behavior proof.

## 1. Retire The Unreachable Helper

- [x] 1.1 @impl C1e reader question - Immediately before editing, repeat an exact current-surface search for `parseYAMLSubset` across Harness, tests, accepted specs, experiments, and guidance, allowing only the private definition plus historical planning/archive records. Inspect `parseFrontmatter()` and its focused tests to reconfirm the current JSON-first, package-backed YAML parse path and its protected assertions. Done when no current caller remains or every finding is recorded as an ordinary pending task.
- [x] 1.2 @impl C1e reader question - Delete only the full private `parseYAMLSubset()` declaration from `DEEP_RESEARCH_HARNESS/engine/workflow-chain.mjs` and correct only the stale focused-test comment that misnames the YAML fallback. Do not alter `parseFrontmatter()`, `loadYamlParse()`, JSON input, YAML input, schema validation, error text, dependency loading, test assertions, package dependencies, or workflow/package assets; do not add an alias, tombstone, fallback, migration, or parser-selection branch. Done when the helper identity is absent and all protected surfaces are unchanged.

## 2. Verify The Current Parser Contract

- [x] 2.1 @impl C1e reader question - Run `node --test tests/engine/workflow-chain.test.mjs` and `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`. Done when the selected unit suite passes and package validation reports `passed: true`; treat these as existing parser regression and package-consistency evidence only.
- [x] 2.2 @impl C1e reader question - Run a scoped absence and protected-surface review. It SHALL prove the private helper identity is absent from current source/tests/main specs/guidance while allowing active-change and historical archive references; it SHALL prove the exported parser, lazy `yaml` loader, parser tests other than the named comment, node-loading/dependency logic, manifest, Gate/router, bundle formats, Engine source outside the target declaration, and package dependencies are unchanged. Done when every unexpected reference or protected change is repaired or recorded as an ordinary pending task.
- [x] 2.3 @impl C1e reader question - Run `git diff --check`, strict change validation, `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-unreachable-yaml-subset-parser`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, verification-routing assets, and semantic-closure assets checks. Done when every command exits 0 and results are recorded as structural or selected-regression evidence only.

## 3. Closeout And Archive

- [x] 3.1 @impl C1e reader question `openspec-feedback:closeout-review` - Before archive, read current feedback-loop Archive guidance and review the change-scoped actual diff, helper-absence result, protected current-parser evidence, selected verification result, `skip_specs` rationale, and semantic-closure reason. Add and close ordinary repair tasks for every finding; complete only when no actionable finding remains.
- [x] 3.2 @impl C1e reader question - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-unreachable-yaml-subset-parser`. If finalization fails, restore this task to unchecked and repair its named root.
