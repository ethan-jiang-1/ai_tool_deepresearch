# Apply Target Manifest

## Added Control Surfaces

None. The change adds no runtime state, schema, CLI, Gate, queue, receipt,
trace event, controller, or lifecycle checkpoint.

## Modified Control Surfaces

| Surface | Owner | Change |
| --- | --- | --- |
| Root/framework entry guidance | Markdown Agent Flow | One entry-first directive for selected DPT research. |
| `RUN.md` Section 0 | Markdown Agent Flow | Explicitly rejects both generic shortcut and atomic research substitution until the existing flow authorizes work. |
| One new integration document contract | JS test | Verifies synchronized routing language without becoming Agent-behavior or host-enforcement authority. |

## Removed Or Avoided Control Surfaces

- No provider skill blacklist, settings hook, tool interceptor, session state, or entry token.
- No second route selector beyond the accepted explicit/reachable existing-bundle rule and `RUN.md` new-run entry.
- No global WebSearch/WebFetch prohibition; later phase-authorized use remains unchanged.
