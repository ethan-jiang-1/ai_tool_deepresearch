## 1. Review and Preconditions

- [x] 1.1 `openspec-feedback:plan-review` Review proposal, delta specs, design, tasks, and verification plan for DEW-025/REF-007/REF-009/WPG-017/RWG-022/CTS-009/RWP-001 authority, writer/reader, compatibility, and proof coherence before the first target edit; record every actionable finding below as an ordinary task with owner, smallest repair, and observable done condition.
  Review record (2026-08-04): corrected the existing `submitted_work` wire-discriminator ambiguity, named the existing artifact-persistence writer, corrected unit verification routing, and added a version-selected v1/v2 assignment compatibility decision through task 1.4. Polish follow-up: recorded markerless legacy compatibility, made `source_identity.work_id` plus `entry_id` the exact Wave0 identity representation, fixed the deferred writer's existing limitation values, redirected DEW-025 proof to its actual unit asset, and made downstream v2 fixture migration explicit in task 4.4. The pre-existing Wave0 repair-kind expectation remains task 4.5 with its direct provenance-recovery owner to be adjudicated.
- [x] 1.2 Register DEW-025, REF-009, WPG-017, RWG-022, and CTS-009 in `openspec/governance/req-registry.yaml` under their existing capability groups; done when the active deltas contain no unregistered C1 IDs.
- [x] 1.3 Validate `verification-plan.yaml` with `check-verification-routing.mjs --change materialize-wave0-submitted-references-and-batch-projections --mode plan`; done when its selected test classes and assets pass the plan schema/checker.
- [x] 1.4 Plan-review finding for DEW-025: the assignment-contract version owner must distinguish immutable v1 Wave0 reference-capable manifests from v2 source-only new claims. Amend the C1 design/task evidence to name that version-selected compatibility path; done when no reader reconstructs a v1 manifest through v2 semantics.

## 2. Current Wave0 Producer Contract

- [x] 2.1 Implement DEW-025 in the Wave0 assignment resolver and generated work-unit contract projections: new v2 `wave0_source_intake` claims expose only the assigned source YAML plus existing cache/result/receipt obligations and reject a delegated rich-reference completion route; version-selected v1 readers preserve bound legacy attempts without reconstructing them through v2 semantics. Evidence: `node --test tests/engine/work-unit-assignment-contract.test.mjs tests/engine/work-unit-actor-submit.test.mjs tests/engine/work-unit-lifecycle.test.mjs tests/schema/contracts/work-unit.test.mjs tests/engine/work-unit-submit.test.mjs tests/integration/cli/operate-work-unit.test.mjs` passed after explicit current-v2, marked-v1, and marker-absent fixtures were separated.
- [x] 2.2 Implement REF-007 and RWP-001 in the Wave0 Phase and source-intake Markdown delivery chain: source-intake guidance produces submitted source/cache facts only; post-submit guidance uses the existing `operate-artifact-persistence` boundary for exact-backing materialization or the contribution-scoped deferred intent followed by the same inspect.
  Evidence (2026-08-04): `phase-wave0.md`, `subagent-dpt-source-intake.md`, the shared reference template/schema guidance, and `operate-topic-state.md` now use the submitted-backing closeout path; the focused Markdown contract tests passed.
- [x] 2.3 Add focused producer-contract and Markdown-delivery regression coverage for DEW-025, REF-007, and RWP-001, including marked-v1 and markerless legacy task/ledger compatibility, plus the absence of a current actor reference-output route.
  Evidence (2026-08-04): the focused `node --test` command covering assignment, actor-submit, lifecycle, schema, submit, CLI, and Wave0 Markdown delivery passed with 155 tests and 0 failures; it includes current-v2, marked-v1, and markerless legacy assertions.

## 3. Submitted Backing and Reference Authority

- [x] 3.1 Implement REF-009's one bounded Wave0 submitted-backing reader at the existing work-unit projection/direct-output seam; it resolves exact current `<work_id>/<ordinal>` source URL, source YAML, cache, result, and work-unit facts without exposing a second source catalog or reparsing source facts in each consumer.
  Evidence (2026-08-04): `readSubmittedWave0Backing()` consumes the authenticated retained submitted-contribution lineage through the current rerun and returns one exact source identity with source/cache/result/work-unit refs; the focused five-suite unit command passed (76 tests, 0 failures).
- [x] 3.2 Implement WPG-017 in reference authority classification and numeric eligibility: retain submitted legacy Wave0 reference coverage, accept exact submitted-backed Phase-owned shared projections, and fail closed for URL-only, unsubmitted, ambiguous, index-only, and filesystem-only inputs.
  Evidence (2026-08-04): `classifyReferenceAuthority()` preserves submitted legacy output precedence and accepts only exact backed `00-shared-*` consumer projections; `countReferences()` continues to consume classifier eligibility. The focused five-suite unit command passed (76 tests, 0 failures).
- [x] 3.3 Add unit regression coverage for REF-009/WPG-017: exact identity binding, duplicate URL isolation, legacy reference compatibility, backed Phase-owned projection acceptance, and unbacked rejection.
  Evidence (2026-08-04): `node --test tests/engine/work-unit-projection.test.mjs tests/engine/helpers/gate-helpers-checks.test.mjs tests/engine/helpers/ref-count.test.mjs tests/engine/helpers/direct-output-contract.test.mjs tests/engine/helpers/wave-contract-evaluators.test.mjs` passed with 76 tests and 0 failures.

## 4. Wave0 Convergence and Deferred Projection

- [x] 4.1 Implement RWG-022 as one pure Wave0 submitted-reference convergence evaluator, then route `evaluateWave0Contract`, Wave0 inspect, and formal Gate through its ordered result without adding state, a controller, or a Gate mutation path.
  Evidence (2026-08-04): `evaluateWave0ReferenceConvergence()` is the sole pure reader composition consumed by `evaluateWave0Contract`; Wave0 inspect, formal Gate, and the persisted Gate diagnostic expose the same materialization root. `node --test tests/engine/helpers/return-map.test.mjs tests/engine/helpers/wave-contract-evaluators.test.mjs tests/engine/helpers/seed-topic-projection.test.mjs` passed (31 tests, 0 failures), and `node --test tests/integration/cli/check-gate-wave0-complete.test.mjs` passed (26 tests, 0 failures).
- [x] 4.2 Integrate convergence diagnostics with existing reference/index/count helpers so materializable backing appears before only its dependent floor outcome, while independent malformed/provenance/queue/receipt/cache/navigation roots remain visible.
  Evidence (2026-08-04): the Wave0 Gate integration suite asserts materialization-before-floor, true degraded floor only after both candidates are explicitly deferred, and an independently invalid shared-reference backing alongside a materialization candidate. The focused Gate command passed (26 tests, 0 failures).
- [x] 4.3 Implement CTS-009's strict Wave0 contribution-scoped deferred packet branch in the existing topic-state schema/writer; preserve the existing `source_identity.kind: submitted_work` wire discriminator while atomically expanding current unprojected identities, preserving explicit entries, rejecting collisions/unsubmitted input, and retaining idempotent replay/recovery semantics.
  Evidence (2026-08-04): the strict schema accepts only the selector/meaning/next-hop packet, while the writer derives exact entries with `defers`/`[none]`/`deferred`. `node --test tests/integration/cli/operate-topic-state-projection.test.mjs` passed (13 tests, 0 failures), including collision, equivalent replay, caller ordinal/disposition rejection, unsubmitted selector rejection, `1..19` versus later `/20`, and explicit entry compatibility.
- [x] 4.4 First migrate the selected Wave0 integration fixtures to current v2 source/cache-only submitted results, retaining direct actor-owned reference outputs only in explicit marked-v1 legacy fixtures. Then add RWG-022/CTS-009 regression coverage for inspect/Gate parity, materialization-before-floor, true deficit, independent invalid backing, `1..19` versus later `/20`, collision rejection, idempotent replay, and explicit-entry compatibility. Done when `check-gate-wave0-complete` and `operate-topic-state-projection` fail only on their asserted C1 behavior rather than pre-submit v2 output-contract rejection.
  Evidence (2026-08-04): selected fixtures submit source/cache-only current v2 Wave0 results, then materialize Phase-owned references or deferred Seed Projections after submit. `check-gate-wave0-complete` passed with 26 tests and `operate-topic-state-projection` passed with 13 tests; neither depends on a current delegated rich-reference output.
- [x] 4.5 Adjudicate the pre-existing `wave0_work_unit_submission_presence.repair_kind` regression against its direct provenance-recovery owner. If C1 changes that owner or surface, implement the narrow repair and regression assertion; otherwise record the isolated owner and retain the C1 expectation without weakening it. Done when the focused C1 test command has an evidence-backed classification for the assertion.
  Evidence (2026-08-04): current v2 fixtures retain enough hash-bound submitted contribution evidence for the pre-existing `recover-declaration` Engine operation, so the parent root correctly reports `engine_operation`, not `missing_contract`. The Gate integration removes the declarations, verifies the single parent root masks dependent provenance rules, recovers every submitted row through the real CLI, and reruns to a passing Gate. The opposite boundary remains fail-closed: `node --test --test-name-pattern="fails closed when a hash-bound source contribution row is lost" tests/engine/work-unit-submit.test.mjs` passed (1 selected test, 0 failures; 60 nonmatching skipped).

## 5. Documentation and Release

- [x] 5.1 Update `CONTEXT.md` with the C1 glossary only: work unit/attempt, submitted ledger row, submitted Wave0 contribution, source identity, reference, consumer projection, seed projection, deferred disposition, and the existing `submitted_work` wire discriminator; do not add implementation rules or broaden it into a runtime alias. Evidence (2026-08-04): the `Submitted Evidence and Projections` glossary adds only the resolved terms and preserves existing Work unit/Submitted ledger row definitions.
- [x] 5.2 Complete the C1 contract-coherence matrix as change verification evidence, linking each assignment, task-delivery, submit/provenance, materialization, topic-state packet/result, inspect/Gate, and feedback row to its `verification-plan.yaml` claim. Extend `tests/integration/md/wave-producer-contract-guidance.test.mjs` as the static delivery guard so it rejects both a current actor reference-output route and missing Phase-side submitted-backing/deferred rerun guidance. Done when every matrix row has a direct proof claim and a changed delivery surface cannot silently reintroduce conflicting terminology or authority claims. Evidence (2026-08-04): `contract-coherence-matrix.md` maps all eight rows to direct claims/assets; `node --test tests/integration/md/wave-producer-contract-guidance.test.mjs` passed (3 tests, 0 failures).
- [x] 5.3 Release the implemented framework as v0.69: update `CHANGELOG.md` and synchronize the `DPT_FRAMEWORK/RUN.md` version banner/entry with the same concise behavior statement. Evidence (2026-08-04): both release surfaces state the v0.69 submitted-backing/materialize-or-defer behavior.
- [x] 5.4 Update BUG-189 and BUG-191 plus the progressive systemic remediation plan only after implementation evidence exists; record the concrete C1 behavior, verification, residual real-Actor boundary, and truthful stage/status transition. Evidence (2026-08-04): both cards now record focused deterministic evidence and E2's unobserved Actor boundary; the progressive plan advances C1 to P2 closeout rather than calling it proposal-only.

## 6. Verification, Sync, and Archive

- [x] 6.1 Run the selected unit/integration assets from `verification-plan.yaml` and the focused C1 command set; fix C1 failures or record a direct, out-of-scope owner without silently changing coverage claims. Evidence (2026-08-04): the seven selected plan assets plus the direct assignment/submit, output-contract, reference-count, return-map, and Seed Projection support suites passed through one `node --test --test-reporter=dot` C1 command with zero failures; no out-of-scope C1 owner was found.
- [x] 6.2 Run the repository-appropriate broader deterministic regression suite and lint/format checks required by changed surfaces; record commands and results in C1 evidence/task notes. Evidence (2026-08-04): `npm test -- --test-reporter=dot` exited 1. The C1-focused regression command covering Wave0 producer/projection/convergence, deferred projection, repaired E2E rerun, and health fixtures passed with zero failures; `git diff --check` passed; `package.json` defines no separate lint or format script. The broader suite remains red only at direct non-C1 owners: REI-006's post-final style-projection freshness behavior, Wave1 fixture/admission and convergence surfaces, preflight documentation's declared dependency baseline, and existing work-unit terminal/supersession expectation drift. No remaining failure is a current-v2 Wave0 actor-reference fixture route or C1 retained-lineage failure.
- [x] 6.2.1 Closeout finding — owner: deterministic E2E Wave0 fixtures. Current v2 E2E setup in `tests/e2e/helpers/research-chain-fixture.mjs` and `tests/e2e/rerun-round-continuity.test.mjs` still submits an actor-owned `output_files[].role: reference`, so E2E chains fail at C1's source-only submission boundary before their intended assertions. Replace those current fixture routes with source/cache-only submit plus the legal Phase-owned submitted-backing materialization/projection closeout, retaining a marked-v1 fixture only where an E2E assertion specifically needs legacy delegated-reference coverage. Done when the formerly red post-final/rerun E2E commands pass their intended chain assertions and the full deterministic suite has no remaining C1 fixture-contract failure. Evidence (2026-08-04): the current fixture submits only `source_yaml`, persists its reference through `operate-artifact-persistence` plus `sync-reference-index`, and commits the exact Seed Projection after submit. `rerun-round-continuity.test.mjs` now passes. `post-final-rerun-lineage-continuity.test.mjs` reaches the unrelated REI-006 `style_projection_freshness` boundary rather than failing the C1 source-only contract; the broader suite has no remaining C1 fixture-contract failure.
- [x] 6.2.2 Closeout finding — owner: existing Wave1 deterministic E2E fixture. After C1 lets the chains reach Wave1, `tests/e2e/helpers/research-chain-fixture.mjs` and `tests/e2e/rerun-round-continuity.test.mjs` expose a Wave1 submitted source claim whose URL does not match the hand-authored reference metadata, so the established Wave1 convergence correctly returns `materialize_projection`. Repair the fixture through its existing submitted-backing/canonical-projection path and remove the unstable assertion that treats a submit response field as the ledger hash authority. Done when the E2E chain reaches its intended Wave1/Wave2 assertions and the fixture proves ledger/index agreement from durable state. Evidence (2026-08-04): both fixtures derive the canonical Wave1 reference path from the submitted source URL, persist and index the Phase-owned projection, and assert the durable submitted-ledger record hash. The repaired `rerun-round-continuity.test.mjs` reaches and passes its intended Wave1/Wave2 assertions.
- [x] 6.2.3 Closeout finding — owner: REF-009/RWG-022 Wave0 submitted-contribution time boundary. `collectEligibleWorkUnitProjectionFacts()` correctly selects current-round demand coverage, but C1 reused it to construct source-array prefix intervals; a later rerun append therefore re-owned ordinal `/1`, invalidated a valid prior Phase-owned projection, and omitted the retained source identity. Add one Wave0-only lineage read that preserves ledger-ordered accepted source-contribution rows through the current round for one canonical topic/direct target while retaining generic eligible-row current-round behavior. Done when a cross-rerun append resolves the original `work-a/1..19`, assigns only `/20` to the later work unit, keeps the prior backed projection valid, and the rerun E2E chain passes without a historical-projection bypass. Evidence (2026-08-04): `collectSubmittedWave0ContributionProjection()` uses a separate `through_current` ledger-lineage read while generic `collectEligibleWorkUnitProjection()` remains current-round demand coverage. Unit and Gate fixtures prove the retained `work-a/1..19`, later `/20`, prior backed projection, and no re-ownership; `rerun-round-continuity.test.mjs` passes.
- [x] 6.3 Run `check-verification-routing.mjs --change materialize-wave0-submitted-references-and-batch-projections --mode assets`; done when every selected verification asset exists and passes routing validation. Evidence (2026-08-04): `Verification routing assets valid: materialize-wave0-submitted-references-and-batch-projections (7 claims).`
- [x] 6.4 Sync all six C1 delta specs into their accepted main specs, then semantically compare each synced requirement with its delta and implementation evidence. Evidence (2026-08-04): synced `canonical-topic-state` (CTS-009), `delegated-work-units` (DEW-025), `reference-flat-format` (REF-007/009), `research-wave-gate-implementation` (RWG-022), `research-wave-phase-content` (RWP-001), and `work-unit-provenance-gate` (WPG-017). A requirement-by-requirement comparison found every delta requirement in its main spec; the semantic audit also corrected the pre-C1 main-spec statement that v1 was current, preserving v1/markerless compatibility while making v2 the new-claim contract. The glossary and both specs distinguish current-round demand coverage from retained Wave0 contribution lineage.
- [x] 6.5 Run `node openspec/governance/check-project-reqs.mjs` and `node openspec/governance/check-project-specs.mjs`; done only when both report zero required violations. Evidence (2026-08-04): requirements check passed with `625 registered (53 retired, 0 orphan), 725 occurrences`; main-spec check passed with `83 main spec files` and `0 violations`.
- [x] 6.6 Run strict OpenSpec validation for the change and re-check the complete task list, release docs, glossary, backlog evidence, and active-change diff against the C1 boundary. Evidence (2026-08-04): `openspec validate materialize-wave0-submitted-references-and-batch-projections --strict`, `check-project-reqs.mjs`, `check-project-specs.mjs`, `check-verification-routing.mjs --change materialize-wave0-submitted-references-and-batch-projections --mode assets`, and `git diff --check` all passed. The seven selected C1 assets passed 99 tests with 0 failures. The scoped actual diff contains only C1 implementation, C1 guidance/release/glossary surfaces, its six delta/main spec pairs, selected tests, BUG-189/BUG-191, and this progressive plan; the three untracked C1 artifacts are `wave0-reference-convergence.mjs`, `markdown-semantic-sections.mjs`, and `contract-coherence-matrix.md`. Release docs name v0.69 and preserve the source/cache-only, exact submitted-backing, and legacy-compatibility boundary. The glossary/matrix keep current-round Queue-demand eligibility distinct from retained Wave0 submitted-contribution lineage. BUG-189/BUG-191 accurately remain `archive pending` until the governed transition succeeds.
- [x] 6.6.1 Closeout finding — owner: C1 requirement traceability. The changed Wave0 deferred-packet and provenance-classification implementations lack adjacent `@impl CTS-009` / `WPG-017` markers, and the retained-lineage reader lacks its `RWG-022` marker. Add only accurate implementation annotations and rerun syntax plus focused C1 regression coverage. Done when the three implementation owners expose their requirement IDs without changing behavior. Evidence (2026-08-04): `canonical-topic-state.mjs`, `gate-helpers-checks.mjs`, and `work-unit-projection.mjs` now expose the accurate C1 IDs; `node --check` passed for those files plus the two untracked C1 helpers, and the seven-suite focused C1 regression command passed with zero failures.
- [x] 6.7 `openspec-feedback:closeout-review` Review the scoped implemented C1 diff, synced specs, verification evidence, and outstanding findings; complete only with no open C1 finding and all ordinary repair work complete. Closeout review (2026-08-04): reviewed the C1-only tracked and untracked implementation/artifact boundary, all six delta/main requirement pairs, release/glossary/backlog wording, the retained-lineage time boundary, and the selected verification evidence. The only closeout finding, 6.6.1 requirement traceability, was repaired and rerun. No open C1 finding remains; broader-suite failures recorded in 6.2 are owned by REI-006, Wave1, setup-preflight, or pre-existing terminal/supersession surfaces rather than C1.

### 6.8 Governed archive transition

After every checkbox above is complete, invoke only `node openspec/governance/finalize-change-archive.mjs --change materialize-wave0-submitted-references-and-batch-projections` and use its structured result as the archive evidence. This is intentionally an operation instruction, not a checkbox: the governed finalizer correctly requires every ordinary checkbox task to be complete before it can perform the archive, so a checkbox whose done condition is the archive result would be self-blocking and could not be truthfully pre-completed.
