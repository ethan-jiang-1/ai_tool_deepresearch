## 0. Apply Gate

- [x] 0.1 `openspec-feedback:plan-review` - Review the proposal, REF-009 delta, design, `verification-plan.yaml`, and `semantic-closure.yaml` before the first target edit. Confirm the `+###` heading is the sole target defect, REF-009 and all four Scenario blocks remain complete, and `not_applicable` is honest. Done when every actionable finding is an ordinary unchecked task with its authoritative owner, smallest repair, and independently observable done condition.
- [x] 0.2 @impl REF-009 Run `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change repair-reference-flat-format-scenario-coverage --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change repair-reference-flat-format-scenario-coverage --mode plan` before target edits. Done when all commands exit zero and their structural results are not reported as native runtime or Agent evidence.
- [x] 0.3 Run mandatory planning polish after all planning artifacts exist: make one whole-change coherence pass and one distinct risk-led pass over the parser attachment, delta completeness, and the independent global failure boundary. Record the passes and outcome in `polish.md`; repair every fact-settled finding before Apply. Done when the polish report has a clean final pass or an explicit unresolved decision, without treating polish as target-edit permission or runtime proof.

## 1. REF-009 Scenario Attachment Repair

- [x] 1.1 @impl REF-009 In `openspec/specs/bundle/reference-flat-format/spec.md`, remove only the stray leading `+` from the `Wave0 Phase-owned shared references SHALL preserve exact submitted source backing` requirement heading. Keep the complete requirement body and its four existing four-hash Scenario blocks unchanged. Done when the corrected heading is a `### Requirement:` header, all four existing Scenario headers remain directly beneath REF-009, and no submitted-backing, metadata, index, registry, runtime, or guidance wording changes.

## 2. Direct Validation

- [x] 2.1 @impl REF-009 Run `openspec validate bundle/reference-flat-format --type spec --json`. Done when it no longer reports `requirements.8.scenarios`; a newly exposed `requirements.9.scenarios` root is recorded as the separate REF-010 successor debt rather than reported as REF-009 success or failure.
- [x] 2.2 Run `openspec validate --specs` as a diagnostic inventory. Done when it no longer reports `requirements.8.scenarios` and its remaining errors are exactly `bundle/reference-flat-format` `requirements.9.scenarios` plus `engine/check-inspect-feedback` `requirements.6.scenarios`; record both boundaries without suppressing either or calling the repository-wide command green.
- [x] 2.3 Record the newly exposed `reference-flat-format-requirement-9-scenario` debt and its exact planned successor change in the named-debt ledger. Done when the ledger identifies `REF-010`, `requirements.9.scenarios`, the planned change name, and the boundary that this current change does not repair it.

## 3. Closeout And Archive

- [x] 3.1 `openspec-feedback:closeout-review` - Review the actual diff, target and inventory validator outputs, verification plan, semantic-closure record, and named-debt ledger. Done when the diff is the one-character accepted-spec repair plus governed planning/tracking artifacts, both successor roots remain named and out of scope, no behavior or Semantic Fact Closure change is hidden, and any actionable finding is an ordinary pending task with owner, smallest repair, and observable done condition.
- [x] 3.2 @impl REF-009 Run `openspec validate repair-reference-flat-format-scenario-coverage --strict`, `node openspec/governance/check-project-reqs.mjs --mode archive --change repair-reference-flat-format-scenario-coverage`, `node openspec/governance/check-project-specs.mjs`, `node openspec/governance/check-verification-routing.mjs --change repair-reference-flat-format-scenario-coverage --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change repair-reference-flat-format-scenario-coverage --mode assets`, and `git diff --check`. Done when every command exits zero; these governance checks do not replace the target grammar verdict or imply that either separately named successor debt is repaired.
- [x] 3.3 After every earlier task and any review-created repair task are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change repair-reference-flat-format-scenario-coverage`. If it returns non-zero, restore this task to unchecked and repair its named root. Done when the governed finalizer reports native archive success and the named-debt ledger can move this debt to archived.
