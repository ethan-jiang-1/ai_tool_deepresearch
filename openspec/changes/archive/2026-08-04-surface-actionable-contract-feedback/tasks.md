## 1. Plan Review And Preconditions

- [x] 1.1 `openspec-feedback:plan-review` Review the proposal, both delta specs, design, tasks, and verification plan against CTS-004/QIV-001 plus the CTS-009 structural-restoration boundary. Reconfirm the reader question, direct rule source, closed values, writer boundary, compatibility, proof class, BUG-195 exclusion, and no-runtime C1 preservation before the first target edit. Record every actionable finding as an ordinary unchecked task with its affected requirement, owner, smallest repair, and observable done condition.
  Review record (2026-08-04): CTS-004 and QIV-001 each project one existing deterministic rule into the retained input's same-checkpoint repair loop. Their reader questions, closed values, owner/writer boundaries, normal-error compatibility, and unit/integration-only proof classes agree across proposal, deltas, design, tasks, and verification plan. BUG-195 remains outside the evidence boundary, and CTS-009 remains a no-runtime structural restoration from the archived C1 delta. No actionable planning finding remains.
- [x] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change surface-actionable-contract-feedback --mode plan`. Done only when its four claims and selected test classes pass route validation before target edits. Passed on 2026-08-04: `Verification routing plan valid: surface-actionable-contract-feedback (4 claims).`
- [x] 1.3 Recheck the current `TopicApplyPlanSchema`/Wave refinement and `resolveWorkUnitAssignmentContract()` seams immediately before edits. Confirm that they still own the discriminator and `payload.assignment_mode` rules, and that no current task/guidance surface contradicts C2's retained-input repair boundary. Done when the implementation targets one rule source per fact and the task list is amended for any discovered authoritative drift. Confirmed on 2026-08-04: `ProjectionSourceIdentitySchema` and `ProjectionPacketSchema.superRefine()` still own the source-identity union and Wave legality; `requiredOutputsFor()` inside `resolveWorkUnitAssignmentContract()` still owns missing/unknown `payload.assignment_mode`. The current enqueue path persists only after admission. No authoritative drift was found.

## 2. Topic-State Feedback

- [x] 2.1 Implement CTS-004 at the existing canonical-topic-state validation-result boundary. Preserve `TopicApplyPlanSchema` as the only validator, select the direct nested union issue safely, add RFC 6901 JSON Pointer and context-legal discriminator values without echoing retained input, and factor the Wave-to-kind projection fact with the existing refinement rather than recreating validation. Derive raw union vocabulary from the selected Zod issue and selected-Wave legality from the shared Wave rule, never a per-slot label such as multi-Wave `pending_questions`. Done when a valid Wave0/Wave1 result directs `submitted_work`, a valid Wave2 result directs `finding`, and invalid/missing Wave context does not fabricate a narrowed value. Implemented 2026-08-04 with one Wave-to-kind declaration shared by refinement and result projection; the blocked result now retains `repair_surface: retained_input`.
- [x] 2.2 Extend `tests/engine/helpers/canonical-topic-state.test.mjs` for CTS-004. Cover raw union versus Wave-context distinction, exact pointer, no `work_unit` alias, no unsafe input echo, ordinary enum/type compatibility, invalid-Wave fallback, and a Wave1 `pending_questions` packet whose multi-Wave slot metadata does not broaden the Wave1 value. Done when the unit suite proves the feedback is a projection of the existing contract, not a second accept/reject path. Passed 2026-08-04: `node --test tests/engine/helpers/canonical-topic-state.test.mjs` (34 passing, 0 failing).
- [x] 2.3 Extend `tests/integration/cli/operate-topic-state-projection.test.mjs` for CTS-004. Invoke the production CLI against a temporary authorized Wave bundle with the BUG-190 packet shape; assert JSON result/exit `1`, retained-input same-apply rerun, and no topic-state workspace, plan, seed, status, trace, Queue, Work-unit, or ledger mutation. Keep one valid packet case passing. Done when the test exercises the production CLI rather than a hand-written result. Passed 2026-08-04: `node --test tests/integration/cli/operate-topic-state-projection.test.mjs` (14 passing, 0 failing).

## 3. Queue Assignment Feedback

- [x] 3.1 Implement QIV-001 at the existing work-unit assignment-contract and delegated-demand-admission boundaries. Attach direct structured metadata only at the existing missing/unknown `payload.assignment_mode` failure, preserve it through `assignment_contract_rejected`, and make enqueue emit only that metadata-bearing admission result as JSON stdout/exit `1`. Do not select the branch from an error string or generic reason code, infer a mode, mutate the task card, or direct repair to `rb_queue.json`. Done when valid queue/claim/check/repair contracts and unrelated assignment-contract failures retain their current rule source and behavior. Implemented 2026-08-04: the resolver attaches one `payload_assignment_mode` record; the evaluator preserves it, and enqueue selects JSON only from that record while queued-card repair retains its prior admission path.
- [x] 3.2 Extend `tests/engine/helpers/queue-demand-admission.test.mjs` for QIV-001. Cover missing, unknown, and top-level/misplaced Wave1 mode feedback; assert coordinate, JSON Pointer, closed values, retained-unqueued-task-card ownership, same enqueue boundary, no inferred mode, and no evaluator input/current-facts mutation. Also prove a legal mode with malformed receipts does not acquire assignment-mode metadata. Done when the direct assignment resolver remains the only rule owner. Passed 2026-08-04: `node --test tests/engine/helpers/queue-demand-admission.test.mjs` (7 passing, 0 failing).
- [x] 3.3 Extend `tests/integration/cli/operate-queue-validation.test.mjs` for QIV-001. Enqueue a temporary-bundle Wave1 card with top-level/missing `assignment_mode`; assert JSON stdout/exit `1`, task-card repair direction, no `rb_queue.json` direction, Queue bytes unchanged, no queue success/repair trace event, and a corrected `payload.assignment_mode` card succeeds. Prove a legal mode with malformed receipts stays outside the special JSON feedback branch. Done when the production CLI preserves unrelated error/exit behavior. Passed 2026-08-04: `node --test tests/integration/cli/operate-queue-validation.test.mjs` (36 passing, 0 failing).

## 4. Release And Remediation Tracking

- [x] 4.1 Update `CHANGELOG.md` with v0.71 after C2 deterministic evidence passes. Describe only the two actionable-feedback projections, their retained-input/same-check repair boundary, and the absence of aliases, automatic repair, or Actor-behavior proof. Done when it matches the shipped behavior and declared proposal target. Completed 2026-08-04 with the two CTS-004/QIV-001 projection records and their explicit no-alias/no-automatic-repair/no-Actor-proof boundary.
- [x] 4.2 Update the v0.71 banner and current-release summary in `DPT_FRAMEWORK/RUN.md`. Done when its version and bounded feedback description match `CHANGELOG.md` without adding a controller, lifecycle state, or new runtime authority. Completed 2026-08-04; `node --test tests/integration/md/artifact-persistence-contract.test.mjs` passed (6 passing, 0 failing) with the retained v0.70 Final-backing scope and v0.71 current-release boundary both covered.
- [x] 4.3 After focused verification passes, update BUG-190, BUG-194, and the C2 tracking rows in `_backlog/plans/bug-187-199-systemic-remediation-plan.md` with the actual change name, deterministic evidence, and residual BUG-195/E2 boundary. Done when no backlog surface claims an Actor-delivery fix or direct Queue edit path. Completed 2026-08-04: both cards identify `surface-actionable-contract-feedback`, record deterministic-only evidence, and now record completed main-spec sync/closeout with governed archive pending; BUG-195 remains an E2 real-Actor observation boundary.

## 5. Verification, Spec Sync, And Closeout

- [x] 5.1 Run the selected CTS-004 and QIV-001 unit/integration suites, then `node openspec/governance/check-verification-routing.mjs --change surface-actionable-contract-feedback --mode assets`. Record exact commands/results in these tasks. Done when all four routed deterministic claims pass and no test claims real Agent behavior. Passed 2026-08-04: `node --test tests/engine/helpers/canonical-topic-state.test.mjs` (34 passing); `node --test tests/integration/cli/operate-topic-state-projection.test.mjs` (14 passing); `node --test tests/engine/helpers/queue-demand-admission.test.mjs` (7 passing); `node --test tests/integration/cli/operate-queue-validation.test.mjs` (36 passing); `node openspec/governance/check-verification-routing.mjs --change surface-actionable-contract-feedback --mode assets` (`Verification routing assets valid: surface-actionable-contract-feedback (4 claims)`).
- [x] 5.2 Run `openspec validate surface-actionable-contract-feedback --strict` and `node openspec/governance/check-project-reqs.mjs`. Done only when strict validation passes and requirement governance reports 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired entries. Passed after spec sync on 2026-08-04: `Change 'surface-actionable-contract-feedback' is valid`; `All project requirement IDs consistent: 629 registered (53 retired, 0 orphan), 725 occurrences in main specs/active deltas.`
- [x] 5.3 Run `node openspec/governance/check-project-specs.mjs` and `git diff --check`. Done only when main-spec governance reports 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader, and the scoped diff has no whitespace error. Passed after spec sync on 2026-08-04: `All project specs valid: 84 main spec files under openspec/specs, 0 violations.`; `git diff --check` exited 0 with no output.
- [x] 5.4 During the supported agent-driven OpenSpec spec-sync operation, first normalize the accepted `canonical-topic-state` literal `+### Requirement` CTS-009 prefix in place from the archived C1 delta, preserving its body and adding no duplicate. Then merge the approved `canonical-topic-state` and `queue-input-validation` C2 deltas, and rerun strict validation plus both project-governance checks. Done when CTS-004, QIV-001, and CTS-009 each occur exactly once as parsed accepted requirements; CTS-009 matches its archived C1 semantic block; and the active delta/main comparison remains coherent. Completed 2026-08-04: removed only the stray C1 `+` prefix, merged the two C2 full modified requirement blocks, and confirmed CTS-004, CTS-009, and QIV-001 occur once in their accepted capability specs. Both C2 blocks match their active deltas exactly; CTS-009 matches the archived C1 body (apart from one terminal blank line); strict validation and both project-governance checks passed.
- [x] 5.5 `openspec-feedback:closeout-review` Review the scoped implemented diff, synced specs, release/backlog updates, verification evidence, and every ordinary task. Complete only with no open C2 finding; turn each actionable finding into an unchecked task and repair it before archive. Completed 2026-08-04: reviewed the C2-only code/test/release/backlog/spec/change-artifact boundary; found 5.5.1, repaired its whitespace-safe rerun rendering, and reverified it. Final evidence: CTS helper 34/34, CTS CLI 14/14, QIV helper 7/7, QIV CLI 36/36, release-doc regression 6/6; verification routing assets valid (4 claims); strict OpenSpec, requirement governance, main-spec governance, and `git diff --check` all passed. No open C2 finding remains; BUG-195 stays explicitly outside this deterministic proof boundary.
- [x] 5.5.1 QIV-001 closeout finding: preserve executable same-enqueue feedback when the supplied bundle or retained task-card path contains whitespace. Owner: `operate-queue` feedback projection. Smallest repair: quote both emitted rerun operands using the repository's established JSON-string argument form, without changing admission or error ownership. Done when the production CLI integration test covers a task-card path containing a space and asserts the emitted rerun preserves both operands as executable quoted arguments. Completed 2026-08-04: `enqueueAssignmentModeFeedback()` now JSON-quotes bundle/task operands; `node --test tests/integration/cli/operate-queue-validation.test.mjs` passed 36/36 with `task card.json` coverage.

### 5.6 Governed archive transition

After every checkbox above is complete, invoke only `node openspec/governance/finalize-change-archive.mjs --change surface-actionable-contract-feedback` and use its structured result as archive evidence. This is an operation instruction rather than a checkbox because the governed finalizer requires every ordinary task to be complete before it can perform the archive transition.
