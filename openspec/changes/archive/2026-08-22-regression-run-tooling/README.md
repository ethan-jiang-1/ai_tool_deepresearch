# 2026-08-22-regression-run-tooling

Regression run tooling: exclude disposable output dirs (.test-*) from the canonical  discovery glob (correctness/hygiene), add  (deterministic file partitioning across N parallel runs, wall ~136/N) and  (schema+helpers+md triage lane). No test semantics change.
