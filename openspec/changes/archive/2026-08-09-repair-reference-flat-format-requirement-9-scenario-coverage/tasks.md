## 0. Apply Gate

- [x] 0.1 `openspec-feedback:plan-review` - Review the proposal, REF-010 delta, design, `verification-plan.yaml`, and `semantic-closure.yaml` before the first target edit. Confirm that the source requirement is truncated, the archived REF-010 block is corroborated by the current template and focused Engine test, the delta contains exactly two existing Scenarios, and `not_applicable` remains honest. Done when every actionable finding is an ordinary unchecked task with its affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition. Completed 2026-08-09: the accepted main spec truncates REF-010 at `exact seriali`; the active delta exactly matches the archived recovery block with two Scenarios, and the template plus `tests/engine/feedback-contract-naming.test.mjs` corroborate the existing YAML/diagnostic contract. `semantic-closure.yaml` remains honestly `not_applicable`; no actionable plan-review finding exists.
- [x] 0.2 @impl REF-010 Before target edits, run `openspec validate repair-reference-flat-format-requirement-9-scenario-coverage --strict`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change repair-reference-flat-format-requirement-9-scenario-coverage --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change repair-reference-flat-format-requirement-9-scenario-coverage --mode plan`. Done when every command exits zero and its structural result is not presented as a native runtime or Agent-execution proof. Completed 2026-08-09: strict change, requirements plan, verification-routing plan (0 claims), and semantic-closure plan each exited zero; none is native runtime or Agent-execution evidence.
- [x] 0.3 Complete mandatory planning polish after all planning artifacts exist: make one whole-change coherence pass and one distinct risk-led pass over recovery evidence, REF-010 delta completeness, boundary preservation for REF-009/REF-011, and the independent CHI-006 global failure. Record the passes and outcome in `polish.md`; repair every fact-settled finding before Apply. Done when the report has a clean final pass or an explicit unresolved decision, without treating polish as target-edit permission or runtime proof. Completed 2026-08-09: `polish.md` records whole-change coherence, recovery/sibling-scope risk review, and final readiness-boundary passes; no fact-settled planning repair remained. Its `not ready` result is limited to an all-specs-green baseline claim and does not broaden this user-authorized Apply scope.

## 1. REF-010 Specification Restoration

- [x] 1.1 @impl REF-010 In `openspec/specs/bundle/reference-flat-format/spec.md`, replace only the truncated REF-010 requirement block with the complete existing text and both Scenario blocks from the approved recovery evidence. Keep REF-009 and REF-011 unchanged. Done when REF-010 ends with the exact existing offending-value diagnostic scenario and both Scenario headers are parser-visible directly under REF-010. Completed 2026-08-09: restored the archived, corroborated REF-010 text and both parser-visible Scenario headers; no adjacent requirement changed.
- [x] 1.2 @impl REF-010 Compare the restored main-spec REF-010 block with this change's complete modified delta. Done when the requirement text and both Scenario blocks match exactly, with no requirement-ID allocation, Engine/template/test/runtime behavior edit, or adjacent-requirement change. Completed 2026-08-09: a read-only Node comparison returned `exact-match`, `scenarios: 2`, and preserved adjacent requirements; the target diff is only the formerly truncated REF-010 tail and its two existing scenarios.

## 2. Direct Validation And Debt Tracking

- [x] 2.1 @impl REF-010 Run `openspec validate bundle/reference-flat-format --type spec --json`. Done when it no longer reports `requirements.9.scenarios` and the command's target grammar verdict is recorded without claiming a runtime behavior change. Completed 2026-08-09: the target spec is valid (`1/1` passed, `0` failed); only non-blocking long-requirement information notices remain. This is an OpenSpec grammar verdict, not a runtime behavior claim.
- [x] 2.2 Run `openspec validate --specs` as a diagnostic inventory. Done when the only remaining failure is `engine/check-inspect-feedback` `requirements.6.scenarios`; retain its named debt and do not call the repository-wide command green or expand this change. Completed 2026-08-09: global inventory is `84/85` and the only failure is CHI-006 (`requirements.6.scenarios`); the targeted CHI validator confirms that exact root. Repository-wide validation is intentionally not called green.
- [x] 2.3 Update `_backlog/learning/semantic-fact-closure-progressive-rollout-plan-2026-08-09.md` with the current Apply state. Done when it records REF-010 target validation as passed, keeps CHI-006 as the unique named next repair, distinguishes this spec hygiene work from Semantic Fact Closure dogfood, and does not claim governed archive evidence before the finalizer succeeds. Completed 2026-08-09: tracker records target `1/1` validation, global `84/85` inventory, CHI-006 as the unique remaining root, and closeout/archive as pending; it makes no archive claim.
- [x] 2.3.1 @impl REF-010 Repair the tracker/finalizer task order found during Apply. Affected reader question: when may the tracker state that a named debt is archived? Authoritative owner: `finalize-change-archive.mjs` result. Smallest repair: make 2.3 a pre-archive status update and make 3.3 own the post-finalizer ledger transition. Done when all tasks can be complete before finalization without a premature archive claim, while finalizer success still requires the ledger transition. Completed 2026-08-09: 2.3 now records only verified Apply state; 3.3 owns the post-finalizer ledger transition, removing the circular completion condition.

## 3. Closeout And Archive

- [x] 3.1 `openspec-feedback:closeout-review` - Review the actual diff, target and inventory validation, delta/main comparison, `verification-plan.yaml`, `semantic-closure.yaml`, and named-debt ledger. Done when the diff restores only accepted REF-010 prose and scenarios, REF-009/REF-011 and all runtime behavior remain unchanged, and every actionable finding is an ordinary unchecked task with its affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition. Completed 2026-08-09: reviewed the two target files plus change artifacts and tracker. REF-010 delta/main is exact, the truncated fragment is absent, target validation is `1/1`, the focused Engine corroboration is `3/3`, and CHI-006 remains explicitly excluded. `verification-plan.yaml` and `semantic-closure.yaml` remain honestly not-applicable. The task-order finding was recorded and repaired as 2.3.1; no open closeout finding remains.
- [x] 3.2 @impl REF-010 Run `openspec validate repair-reference-flat-format-requirement-9-scenario-coverage --strict`, `node openspec/governance/check-project-reqs.mjs --mode archive --change repair-reference-flat-format-requirement-9-scenario-coverage`, `node openspec/governance/check-project-specs.mjs`, `node openspec/governance/check-verification-routing.mjs --change repair-reference-flat-format-requirement-9-scenario-coverage --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change repair-reference-flat-format-requirement-9-scenario-coverage --mode assets`, and `git diff --check`. Done when each command exits zero; these checks do not replace the target grammar verdict or imply that the separately named CHI-006 debt is repaired. Completed 2026-08-09: all six commands exited zero; routing and semantic-closure assets each report 0 claims/not-applicable structure, and the checks do not replace the target grammar verdict or repair CHI-006.
- [x] 3.3 After every earlier task and any review-created repair task are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change repair-reference-flat-format-requirement-9-scenario-coverage`. If it returns non-zero, restore this task to unchecked and repair its named root. After archive success, update the named-debt ledger before final handoff. Done when the governed finalizer reports archive success and the named-debt ledger moves this REF-010 debt to archived.
