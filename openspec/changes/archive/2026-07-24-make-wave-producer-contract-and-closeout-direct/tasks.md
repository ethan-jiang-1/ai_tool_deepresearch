## 1. Baseline And Red Contract

- [x] 1.1 @impl REF-007, RWP-001, RWP-002: Before target edits, create change-root `apply-evidence.md`; run `node openspec/governance/check-verification-routing.mjs --change make-wave-producer-contract-and-closeout-direct --mode plan` and `openspec validate make-wave-producer-contract-and-closeout-direct --strict`; done when the two selected proof boundaries and both not-applicable classes are recorded without execution claims.
- [x] 1.2 @impl REF-007, RWP-001, RWP-002: Add `tests/integration/md/wave-producer-contract-guidance.test.mjs` as a red static contract; done when it fails against the current guidance for missing source-intake template delivery, missing Wave1 dry-submit-before-submit, and missing visible post-submit checklist, while asserting that Wave0 reference production stays Sub-agent-owned.

## 2. Deliver Existing Guidance

- [x] 2.1 @impl REF-007, RWP-001: Add `shared/shared-reference-template` to the real `requires` chains of `subagent-dpt-source-intake.md` and `phase-wave0.md`, and make their local wording consistent with the existing direct Sub-agent `reference` output/declaration; done when the actor receives canonical `00-shared-<slug>.md`, parser-aligned metadata/sections, and `source_url` declaration facts without a Phase-owned Wave0 projection, parser, or new backing path.
- [x] 2.2 @impl RWP-002: Update `phase-wave1.md` at the returned-work decision point to run existing `operate-work-unit dry-submit` before formal submit and consume the existing Engine disposition; done when same-candidate repair, actor return, fail-and-replace, and owner/no-path stay at their current owners and no receipt/cache/ledger/provenance fabrication route is introduced.
- [x] 2.3 @impl RWP-002: Add one short post-successful-submit Wave1 checklist at that same decision point, pointing to existing reference/index materialization, depth review, seed return-map backfill, and full-drain inspect sections; done when the checklist is after formal submit, binds to submitted backing, and never makes a Sub-agent responsible for a Phase projection.
- [x] 2.4 @impl REF-007, RWP-001, RWP-002: Complete the static integration contract from task 1.2; done when it proves actual template delivery, Wave0 ownership, Wave1 dry-submit ordering, and the submitted-backed closeout checklist without reimplementing parser, submit, or Gate behavior in test code.

## 3. Real Agent Evidence Boundary

- [x] 3.1 @impl RWP-002: Add `experiments_playbook/exp_wfn_wave1/case-225-heavy-returned-work-closeout.md`, its one runner-manifest registration, and the smallest extension to the existing iterative Subject adapter family; done when an independent real Phase Agent, after a real child return, preserves native child/result/receipt/cache evidence, runs dry-submit then formal submit, completes submitted-backed Phase closeout, and runs inspect. Missing Agent, child, tool, or external capability SHALL record `NOT_RUN`, never fixture replacement.
- [x] 3.2 @impl RWP-002: Review the new case's evidence boundary; done when setup establishes only preconditions, the Playbook Agent cannot impersonate the Subject Phase Agent, and trace/bundle evidence names the exact Phase and child facts needed for the claim.

## 4. Verification And Release

- [x] 4.1 @impl REF-007, RWP-001, RWP-002: Run the selected integration test and focused existing work-unit/reference regressions; done when `apply-evidence.md` records deterministic contract PASS/FAIL separately from the unrun or native result of case 225.
- [ ] 4.2 @impl RWP-002: Run case 225 only when its real Agent/child/tool prerequisites are available; done when `apply-evidence.md` records native PASS/FAIL/NOT_RUN at the stated claim boundary and does not elevate console output or static fixtures into Agent evidence.
- [x] 4.3 @impl REF-007, RWP-001, RWP-002: Update `CHANGELOG.md` with the proposal-declared `v0.46` entry and synchronize the `DPT_FRAMEWORK/RUN.md` version banner/current-release summary; done when it describes only producer-contract delivery and Wave1 flow visibility, not unobserved live behavior.
- [x] 4.4 @impl REF-007, RWP-001, RWP-002: Run `node openspec/governance/check-verification-routing.mjs --change make-wave-producer-contract-and-closeout-direct --mode assets`; done when both selected assets exist at their canonical boundaries and the real-agent claim remains native-evidence scoped.
- [x] 4.5 @impl REF-007, RWP-001, RWP-002: Complete the non-goal review; done when no parser/evaluator, work-unit command, Gate, lifecycle state, controller, retry tree, generic linter, filesystem scan, manual authority route, or Wave0 Phase-owned reference production was added.
- [x] 4.6 @impl REF-007, RWP-001, RWP-002: Sync accepted delta specs through the OpenSpec spec-sync/archive workflow; done when only `REF-007`, `RWP-001`, and `RWP-002` are changed and no redundant capability delta remains.
- [x] 4.7 @impl REF-007, RWP-001, RWP-002: Run `node openspec/governance/check-project-reqs.mjs` and `node openspec/governance/check-project-specs.mjs`; done with zero duplicate, orphan, unregistered, reused-retired, delta-header-in-main, missing-purpose, missing-requirements, and missing-req-header violations.
- [x] 4.8 @impl REF-007, RWP-001, RWP-002: Run `openspec validate make-wave-producer-contract-and-closeout-direct --strict` plus `git diff --check`; done when the change is apply/archive-ready without treating unrelated worktree edits as this change's failure.
