> Apply discipline: after each completed target-edit or verification task,
> update its checkbox and append concise, directly observed command evidence to
> that task line. A planned command is not completion evidence.

## 1. Pre-Edit Governance

- [x] 1.1 `openspec-feedback:plan-review` - Before the first target edit, review `proposal.md`, the REF-002 delta, `design.md`, `verification-plan.yaml`, and `semantic-closure.yaml` against CTS-010 and REF-002. Record every actionable finding as an ordinary unchecked task with its requirement/reader question, authoritative owner, smallest repair, and independently observable done condition. Done when no untracked planning finding remains; this marker records review only, not test, semantic, or archive success. Evidence: 2026-08-12 review found and resolved the projection-versus-consumer classification through 1.3; re-review found no remaining planning finding.
- [x] 1.2 @impl VER-002, VER-003, SEF-002, SEF-004: Before the first target edit, run `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change harden-agent-authored-contracts --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change harden-agent-authored-contracts --mode plan`. Done when all pass and the record still uses only existing catalog families; do not edit target code if any command fails. Evidence: 2026-08-12 all three commands passed (644 registered requirements, 4 routed claims, affected semantic record valid).
- [x] 1.3 @impl SEF-004: Correct the `wave2.finding-and-target-authority` reader classification: `operate-topic-state.mjs` only projects the schema, while `applyCanonicalTopicState` is the actual verdict consumer for the unchanged `TopicApplyPlanSchema`. In `semantic-closure.yaml`, retain the CLI only as `overlap: derived` and name `canonical-topic-state.mjs#applyCanonicalTopicState` as the consumer. Done when the record has no projection in `consumers` and plan-mode semantic closure validation passes. Evidence: 2026-08-12 `node openspec/governance/check-semantic-closure.mjs --change harden-agent-authored-contracts --mode plan` passed.

## 2. Schema Authoring Projection

- [x] 2.1 @impl CTS-010: Strengthen `tests/integration/topic-state-schema-wave-identity.test.mjs` so it selects the public `apply_seed_projection.conditional_forms` Wave2 entry and verifies its `{ kind: "finding", finding_id }` shape, `condition`, required field path(s), `entry_id === finding_id`, and complete template pass the existing `TopicApplyPlanSchema`; retain Wave0/Wave1 coverage. Done when the focused integration test fails against the old incomplete projection and has no test-local duplicate validator. Evidence: 2026-08-12 `node --test tests/integration/topic-state-schema-wave-identity.test.mjs` passed (3 tests); the new assertion failed before `conditional_forms` existed.
- [x] 2.2 @impl CTS-010: Extend `describeTopicApplyPlanSchema('wave_projection')` in `DEEP_RESEARCH_HARNESS/engine/helpers/canonical-topic-state.mjs` with backward-compatible `conditional_forms` on `apply_seed_projection`: one Zod-derived entry per legal Wave/source-identity branch, containing `condition`, branch `required_fields`/`closed_values`/`value_shapes`, and a complete template. Each emitted template must be accepted by the unchanged top-level schema; unsupported discovery must preserve current fail-closed behavior. Done when the focused schema test passes without globally requiring both `work_id` and `finding_id` or adding another validator. Evidence: 2026-08-12 `node --test tests/integration/topic-state-schema-wave-identity.test.mjs` passed; each emitted conditional template is accepted through the unchanged exported `TopicApplyPlanSchema`.
- [x] 2.3 @impl CTS-010: Extend `tests/integration/cli/operate-topic-state-projection.test.mjs` to obtain the Wave2 `conditional_forms` entry through the public `schema` CLI, use its template in a legal disposable Wave2 apply context, and assert any failure is not `input_invalid` from a hidden identity shape. Done when the public CLI test passes and preserves existing lifecycle/finding-authority checks rather than hand-writing a bypass. Evidence: 2026-08-12 `node --test tests/integration/cli/operate-topic-state-projection.test.mjs` passed (15 tests); the public template committed in a fixture-backed Wave2 lifecycle window, and the CLI emitter flushed the complete 16697-byte schema JSON through a pipe.

## 3. Reference Format Contract

- [x] 3.1 @impl REF-002: Add focused helper regressions in `tests/engine/helpers/gate-helpers-checks.test.mjs` for each bounded raw document signature, exact reference/section `reference_format` finding, code-fence exclusion, ordinary non-document presentation, missing-section precedence, and source-byte non-mutation. Done when they fail before the evaluator change and do not claim research-quality or HTML-sanitization behavior. Evidence: 2026-08-12 `node --test tests/engine/helpers/gate-helpers-checks.test.mjs` passed (24 tests); the new markup case failed before the predicate existed.
- [x] 3.2 @impl REF-002: In `DEEP_RESEARCH_HARNESS/engine/helpers/gate-helpers-checks.mjs`, add one fenced-code-excluded, case-insensitive document-signature predicate inside `checkReferenceFormatFiles`, matching named opening or closing tags only at exact tag-name boundaries. Reuse the existing section parser and `checkerFinding`; emit one direct format root per contaminated non-empty section and preserve all existing metadata/binding/section checks. Done when the helper regressions pass with no new checker, command, cleanup, or route. Evidence: 2026-08-12 `node --test tests/engine/helpers/gate-helpers-checks.test.mjs` passed; the shared checker emits `reference_format:<reference>:section:<section>:document_markup` without writing source bytes.
- [x] 3.3 @impl REF-002: Extend `tests/integration/cli/check-gate-wave1-complete.test.mjs` with a complete temporary reference whose required section has raw document markup. Assert the existing Gate and side-effect-free inspect outputs expose equivalent blocking `reference_format` repairs for its exact section; allow the formal Gate's existing gate-attempt/trace side effect, but assert no reference repair mutation or new format route. Done when the integration test passes without adding a Wave-specific format path. Evidence: 2026-08-12 `node --test --test-name-pattern='raw document markup' tests/integration/cli/check-gate-wave1-complete.test.mjs` passed; Gate and inspect exposed the same `reference_format` agent-action section coordinate, while inspect trace/reference bytes stayed unchanged.
- [x] 3.4 @impl REF-002: Update `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/subagent-dpt-evidence-extractor.md` so rich-reference authors interpret fetched page content into Markdown facts and do not copy raw document markup into required section bodies. Done when the guidance remains a derived consumer, retains existing metadata/serialization instructions, and claims no format-verdict authority. Evidence: 2026-08-12 re-read §3.3 confirms existing metadata/serialization rules remain and the new text directs authors to Engine checkpoint feedback for the format verdict.

## 4. Spec And Release Alignment

- [x] 4.1 @impl REF-002: Re-read the complete REF-002 delta against the implemented evaluator and selected tests, then run the Agent-owned delta/main spec synchronization for `harden-agent-authored-contracts`. Done when the accepted main requirement contains the full bounded predicate, fenced-code exclusion, inspect-read-only/formal-Gate-side-effect boundary, and scenarios without altering CTS-010's existing owner. Evidence: 2026-08-12 synchronized `openspec/specs/bundle/reference-flat-format/spec.md` from the sole delta and `openspec validate --specs` passed (85 specs); CTS-010 remained unchanged.
- [x] 4.2 @impl CTS-010, REF-002: Update `CHANGELOG.md` with `v0.88`, describing only the conditional authoring form and bounded raw-markup format rule plus their non-goals. Done when no release text implies source-quality scoring, automatic cleanup, Agent behavior proof, or a new Gate route. Evidence: 2026-08-12 CHANGELOG v0.88 names only conditional forms and fenced-code-excluded markup feedback, with explicit no-validator/no-apply-authorization/no-quality/no-cleanup/no-new-route boundaries.
- [x] 4.3 @impl CTS-010, REF-002: Synchronize `DEEP_RESEARCH_HARNESS/RUN.md` to banner/current-release `v0.88` and the same concise behavior boundary. Done when RUN and CHANGELOG agree and no lifecycle/permission claim is introduced. Evidence: 2026-08-12 `rg -n "v0\\.88|conditional source-identity|raw document-markup|reference_format" CHANGELOG.md DEEP_RESEARCH_HARNESS/RUN.md` confirmed matching v0.88 release text and banner/current-release values.

## 5. Verification

- [x] 5.1 @impl CTS-010, VER-001: Run `node --test tests/integration/topic-state-schema-wave-identity.test.mjs` and `node --test tests/integration/cli/operate-topic-state-projection.test.mjs`. Done when both pass and are recorded as deterministic projection/CLI evidence, not proof of Agent authoring behavior. Evidence: 2026-08-12 both commands passed (3 schema tests; 15 fixture-backed CLI tests), proving deterministic projection/CLI behavior only.
- [x] 5.2 @impl REF-002, VER-001: Run `node --test tests/engine/helpers/gate-helpers-checks.test.mjs` and `node --test tests/integration/cli/check-gate-wave1-complete.test.mjs`. Done when both pass and are recorded as deterministic helper/Wave feedback evidence, not research-quality evidence. Evidence: 2026-08-12 both commands passed (24 helper tests; 33 fixture-backed Wave feedback tests), proving deterministic checker/Gate feedback behavior only.
- [x] 5.3 @impl VER-001, VER-002, VER-003: Run `npm test -- --test-concurrency=1`, `git diff --check`, `openspec validate harden-agent-authored-contracts --strict`, `node openspec/governance/check-verification-routing.mjs --change harden-agent-authored-contracts --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change harden-agent-authored-contracts --mode assets`. Done when every command passes, the two selected routes remain unit/integration only, and no generated runtime artifact is present. Evidence: 2026-08-12 full serial suite passed (2,776 tests, 484 suites, 0 failures); `git diff --check`, strict change validation, and both assets-mode route checks passed, with no new tracked runtime artifact.

## 6. Archive Preconditions

- [x] 6.1 `openspec-feedback:closeout-review` - Before archive, review the actual change-scoped diff, synced REF-002 main/delta comparison, v0.88 release surfaces, selected evidence, verification plan, and semantic closure. Record every actionable finding as an ordinary unchecked task with its requirement/reader question, authoritative owner, smallest repair, and independently observable done condition. Complete only when no open scoped finding remains; this marker does not itself authorize archive. Evidence: 2026-08-12 reviewed the scoped 15-file/420-insertion diff; REF-002 delta and main both retain the bounded fenced-code-excluded markup rule and scenarios, v0.88 release surfaces share the stated no-cleanup/no-new-route boundary, selected unit/integration evidence passed, and semantic consumers/derived projections match actual symbols. No open scoped finding.
- [x] 6.2 @impl RET-006: Run `node openspec/governance/check-project-reqs.mjs --mode archive --change harden-agent-authored-contracts`. Done when it exits 0 with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired requirement IDs for the selected change. Evidence: 2026-08-12 archive-mode requirement check passed: 644 registered IDs, 53 retired, 0 orphan.
- [x] 6.3 @impl RET-006: Run `node openspec/governance/check-project-specs.mjs`. Done when it exits 0 with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader findings. Evidence: 2026-08-12 project spec check passed: 85 main spec files, 0 violations.
