## 0. Review And Preflight

- [x] 0.1 Re-complete the scoped plan review for RET-001, RET-006, CHF-002, and CHF-003 after the user-directed current-entry conformance repair (`openspec-feedback:plan-review`); inspect the proposal, both deltas, design, and verification plan, confirm that the record distinguishes pending from live identity without relaxing the registry and that the eight-entry repair delivers only existing lifecycle guidance, then convert each actionable finding into an ordinary unchecked task with owner, smallest repair, and observable done condition.
- [x] 0.2 Re-run `node openspec/governance/check-verification-routing.mjs --change add-change-local-requirement-reservations --mode plan` before the current-entry target edits; done when the plan routes only the reservation parser, project-checker CLI, configuration/finalizer wiring, and no runtime, Agent-behavior, or semantic-closure claim.

## 1. Reservation Contract

- [x] 1.1 Implement RET-001 in `openspec/governance/requirement-reservation-contract.mjs`; define strict Zod parsing for `requirement-reservation/v1`, safe change/path/prefix/ID grammar, same-record uniqueness, and an explicit pending/transitioned/invalid lifecycle table derived from supplied direct facts. Done when the module is pure, read-only, exports structured direct failures and accepted ownership facts, and does not create a second registry or persistent lifecycle state.
- [x] 1.2 Add `tests/governance/requirement-reservation-contract.test.mjs` for RET-001; cover exact closed YAML shapes, change-directory identity, ID-prefix matching, duplicate entries, cross-change ID/prefix/path collisions, canonical delta-header ownership, pending acceptance, transitioned acceptance, mixed-state rejection, and selected archive requiring transition. Done when the focused unit suite proves root-first structural behavior without claiming semantic capability correctness.

## 2. Project Requirement Checker

- [x] 2.1 Implement RET-001 in `openspec/governance/check-project-reqs.mjs`; preserve current main-spec duplicate/orphan/retired/live-prefix checks while collecting active delta occurrences with owning change, change-relative delta path, and declaration status. Add backwards-compatible CLI parsing for default `plan` and explicit `archive --change <active-change>` scope, rejecting invalid mode/scope input without writes.
- [x] 2.2 Integrate RET-001's shared reservation evaluator into `check-project-reqs.mjs`; discover only change-root records, report malformed/colliding records before dependent unregistered symptoms, accept only same-change pending ownership, require selected archive reservations to be transitioned, and allow other active reservations only in complete pending or transitioned form. Done when the checker retains one deterministic truth path and one nearest repair coordinate per direct root.
- [x] 2.3 Extend `tests/integration/governance/check-project.test.mjs` for RET-001; use isolated filesystem fixtures to prove default/explicit plan compatibility, malformed records, ID/prefix/path collisions, declaration-path mismatch, unowned prose references, pending versus transitioned behavior, selected archive failure, other active complete-form tolerance, and read-only execution. Done when all coverage invokes the production checker CLI.

## 3. OpenSpec Entry And Finalizer Wiring

- [x] 3.1 Implement RET-001 and RET-006 in `openspec/config.yaml` and the current `/opsx` Apply route; replace the contradictory new-capability pre-registration instruction with proposal guidance to inspect live and active-reservation identity, create the matching change-local record, and run plan mode, while Apply guidance keeps the live registry/main-spec sync in its legal phase. Update the archive/check closing guidance to use selected archive mode. Done when fresh `openspec instructions proposal` and `openspec instructions apply` expose the record as a required active route and `/opsx:apply` turns its named guidance into the plan-check boundary rather than optional prose.
- [x] 3.2 Implement CHF-003 in `openspec/governance/finalize-change-archive.mjs`; pass the resolved planning root plus `--mode archive --change <selected-change>` to the existing requirement checker, preserve the `requirement_governance` result schema/check name and first-failure order, and do not add a raw archive alternative. Done when an incomplete selected transition stops before subsequent governance checks and native archive.
- [x] 3.3 Extend `tests/governance/change-feedback-finalizer.test.mjs` and `tests/integration/governance/change-feedback-finalizer.test.mjs` for RET-001, RET-006, and CHF-003. Done when unit coverage asserts exact finalizer arguments/order and integration fixtures copy the new contract module, exercise generated proposal/apply guidance, selected archive transition failure, another active valid reservation without a bypass, and the current `/opsx` Apply route's plan-check boundary.

## 4. Bootstrap The Existing Consumer

- [x] 4.1 After tasks 1.1 through 3.3 have made the record/checker legal, use the normal OpenSpec update route for `establish-semantic-fact-closure-governance` to add its exact `SEF` `requirement-reservation.yaml` (`governance/semantic-fact-closure`, `SEF`, and `SEF-001` through `SEF-004`). Done when its delta remains unchanged, no fake live registry/main-spec fact is created, and `node openspec/governance/check-project-reqs.mjs --mode plan` accepts that pending record.

## 5. Verification And Closeout

- [x] 5.1 Run `node --test tests/governance/requirement-reservation-contract.test.mjs tests/governance/change-feedback-finalizer.test.mjs tests/integration/governance/check-project.test.mjs tests/integration/governance/change-feedback-finalizer.test.mjs`; done when the selected unit/integration suites pass and no test claims Harness runtime or Agent behavior.
- [x] 5.2 Perform Agent-owned delta/main sync and re-comparison for RET-001, RET-006, and CHF-003; update the accepted governance specs and their direct configuration/finalizer behavior together, without adding a registry ID or a new capability catalog row. Done when main specs, deltas, config, and implementation have one coherent meaning.
- [x] 5.3 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change add-change-local-requirement-reservations` and `node openspec/governance/check-project-specs.mjs`; done when both pass with zero requirement-identity or main-spec defects after the SEF bootstrap record exists.
- [x] 5.4 Run `node openspec/governance/check-capability-taxonomy.mjs`, `node openspec/governance/check-capability-discovery.mjs --change add-change-local-requirement-reservations`, and `node openspec/governance/check-verification-routing.mjs --change add-change-local-requirement-reservations --mode assets`; done when all structural checks pass with the real governance code/tests/config assets present.
- [x] 5.5 Complete the scoped closeout review after the selected change diff, RET/CHF delta-main comparison, SEF bootstrap record, and verification evidence are available (`openspec-feedback:closeout-review`); leave any finding as an ordinary unchecked repair task, and do not invoke the governed finalizer until no open task remains.

- [x] 5.6 Closeout repair for RET-001; owner: `openspec/governance/requirement-reservation-contract.mjs`. Reject a reserved ID that is declared both at its canonical delta path and at another delta path in the same active change, so only one canonical header declaration authorizes pending identity. Done when the pure evaluator and production checker CLI both reject the duplicate declaration without reintroducing generic unregistered-ID noise.

- [x] 5.7 User-directed current-entry conformance repair for CHF-002 and CHF-003; owner: `openspec/governance/finalize-change-archive.mjs` supported-entry inventory. Replace deleted `.codex` inventory paths with the four current Apply and four current Archive paths, add the existing feedback-lifecycle delivery route to any listed entry that lacks it, and assert the exact current inventory without weakening the test or adding semantic-closure checks. Done when `tests/integration/governance/change-feedback-finalizer.test.mjs` passes its declared-entry route test against all eight current surfaces.
