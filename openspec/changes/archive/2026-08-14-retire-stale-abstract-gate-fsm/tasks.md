## 0. Apply Entry Review

- [x] 0.1 @impl SCO-003, TRT-011, TRT-012, WDC-003 `openspec-feedback:plan-review` - Before the first target edit, read current feedback-loop Apply guidance and review proposal, three delta specs, design, semantic-closure rationale, verification plan, live `SCO-003`/`TRT-011` registry entries, the stale source/barrel/tests, and protected current chain/router. Record every actionable finding as an ordinary pending task with its affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition; complete only when none remains.
- [x] 0.2 @impl SCO-003, TRT-011, WDC-003 - Before the first target edit, run `openspec validate retire-stale-abstract-gate-fsm --strict`, plan-mode requirement governance, capability discovery, verification-routing plan check, semantic-closure plan check, and `git diff --check`. Done when every command exits 0; record their structural result only, without treating it as runtime or Agent-behavior proof.
- [x] 0.3 @impl TRT-012 - Feedback finding: the retained `TRT-012` requirement still names the deleted `gate.mjs` FSM as a current comparison target. Update the active transition-table delta with the complete modified requirement and reconcile proposal, design, and tasks. Done when the delta preserves the existing manifest-to-chain scenarios without naming the deleted module, and the change artifacts are coherent before the main-spec sync task resumes.

## 1. Retire The Stale Internal Contract

- [x] 1.1 @impl SCO-003, TRT-011 - Delete `DEEP_RESEARCH_HARNESS/schema/contracts/gate.mjs`; remove exactly `GATE_MACHINE_STATES`, `GATE_EVENT_TYPES`, `GATE_TRANSITIONS`, `validateTransitions`, and `isValidTransition` from `schema/index.mjs`; delete `tests/schema/gate.test.mjs` and `tests/schema/contracts/gate.test.mjs`. Done when the source/tests are absent, the schema barrel has none of the five exports, and no adapter, alias, or fallback remains.
- [x] 1.2 @impl WDC-003 - Correct only the current-reader projections in `DEEP_RESEARCH_HARNESS/README.md`, `DEEP_RESEARCH_HARNESS/workflows/nodes/shared/shared-schemas.md`, and `openspec/guidance/models/framework-runtime-boundary.md`: name `workflows/transitions.chain.json` plus `resolveNodeTransitionDetailed()` as the current routing owner and remove the deleted schema module from the current file tree/table/target descriptions. Done when these surfaces contain no claim that `gate.mjs` is a current Gate transition contract and Gate definitions, helpers, CLIs, bundle truth, and Agent-flow meaning are otherwise unchanged.
- [x] 1.3 @impl SCO-003, TRT-011, TRT-012, WDC-003 - Sync the three approved delta specs into their accepted main specs; update the `schema-core` Purpose directly to remove the obsolete transition-table claim; retain `SCO-003` and `TRT-011` in `openspec/governance/req-registry.yaml` as `[DEPRECATED]` history that cannot be reused; update their registry summaries to name the retirement and align the retained `TRT-012` summary with its chain-only requirement. Done when no live main requirement retains the old abstract FSM/export contract, `TRT-012` retains its manifest-to-chain behavior without naming the deleted module, `WDC-003` names the current chain/router owner, and no unrelated requirement changes.

## 2. Verify The Current Contract And Absence Boundary

- [x] 2.1 @impl TRT-005, TRT-006, WDC-003 - Run `node -e "import('./DEEP_RESEARCH_HARNESS/schema/index.mjs')"`, `node --test tests/engine/ask-next.test.mjs`, and `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`. Done when the barrel still imports, the selected real-chain routing claim passes, and the workflow package validates; do not claim a new Gate verdict, phase transition, or Agent-flow result.
- [x] 2.2 @impl SCO-003, TRT-011, WDC-003 - Run a scoped current-surface absence/protection review. It SHALL prove the deleted source, exact five barrel exports, stale-only tests, and current-reader claims are absent from `DEEP_RESEARCH_HARNESS/`, `tests/`, `openspec/specs/`, and current guidance; it SHALL allow the active delta and historical archive references. Review the bounded diff to prove `workflows/transitions.chain.json`, `engine/ask-next.mjs`, Gate definitions, Gate helpers/CLIs, bundle formats, and Agent flow were not changed. Done when every unexpected reference or protected-surface change is repaired or recorded as an ordinary pending task.
- [x] 2.3 @impl SCO-003, TRT-011, WDC-003 - Run `git diff --check`, strict change validation, `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-stale-abstract-gate-fsm`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, verification-routing assets, and semantic-closure assets checks. Done when every command exits 0 and its result is recorded as deterministic structural/package evidence only.

## 3. Closeout And Archive

- [x] 3.1 @impl SCO-003, TRT-011, WDC-003 - Recompare every delta block with its synchronized accepted main-spec block and the two retired registry entries. Run `openspec validate --specs`. Done when both removed requirements are absent as live main requirements, `SCO-003`/`TRT-011` are retained only as deprecated registry history, the complete modified `WDC-003` block names the existing chain/router, no main spec contains a delta header, and no unaffected current requirement was removed or rewritten.
- [x] 3.2 @impl SCO-003, TRT-011, WDC-003 `openspec-feedback:closeout-review` - Before archive, read current feedback-loop Archive guidance and review the change-scoped actual diff, deletion/absence result, protected current chain/router evidence, applied specs/registry, verification result, and semantic-closure reason. Add and close ordinary repair tasks for every finding; complete only when no actionable finding remains.
- [x] 3.3 @impl SCO-003, TRT-011, WDC-003 - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-stale-abstract-gate-fsm`. If finalization fails, restore this task to unchecked and repair its named root.
