## 1. Pre-Edit Scope And Registry

- [x] 1.1 @impl DEW-014, SNC-008, RWP-018, RWE-011: Read this proposal/design/tasks, all delta specs, `_backlog/plans/delegated-attempt-timeout-and-redo-postmortem-修复计划.md`, and the relevant archived changes: `stabilize-agent-facing-work-unit-contracts`, `parallel-delegated-phase-execution-and-reference-materialization`, and `harden-delegated-preflight-and-fetch-hygiene`; record scope readback before target-code edits.
- [x] 1.2 @impl DEW-014, SNC-008, RWP-018, RWE-011: Confirm `DEW-014`, `SNC-008`, `RWP-018`, and `RWE-011` are registered in `openspec/governance/req-registry.yaml`; if missing or misplaced, register them in sorted capability groups before implementation.
- [x] 1.3 @impl DEW-014, SNC-008, RWP-018, RWE-011: Confirm this change excludes audited late accept, normal submit on terminal `timed_out`, retry supersede, gate floor changes, reference authority changes, Engine-owned fetcher/watcher/daemon/wait helper, pause/resume lifecycle, new dependencies, environment-variable configuration, and Python usage.
- [x] 1.4 @impl DEW-014, SNC-008, RWP-018, RWE-011: Create change-local `implementation-evidence.md` before target-code edits; include timeout mutation audit, static timeout bypass audit plan, exported Engine/API timeout guard plan, progress-source matrix, dry-submit integration plan, and explicit Change B deferrals.

## 2. Timeout Preflight Engine And CLI

- [x] 2.1 @impl DEW-014: Audit current `operate-work-unit timeout`, `closeWorkUnitAttempt`, queue retry/preempt, work-unit status/index writes, trace/log writes, and `inspectWorkUnits` expired-lease diagnostics; record mutation boundaries in implementation evidence.
- [x] 2.2 @impl DEW-014: Add a focused timeout-preflight helper that loads the claimed work-unit record, manifest, queue in-flight binding, status/result/receipt refs, and relevant declared output/cache observations from the explicit active bundle path.
- [x] 2.3 @impl DEW-014: Implement read-only progress observation using Engine-trusted sources: filesystem mtime / Engine observation time for assigned candidate result, identity-matched runtime receipt/log surfaces, declared output/cache surfaces tied to the candidate or manifest, and work-unit trace/log events where available; report source type, observed timestamp, path/event ref, identity verification, lease-extension status, and suspicious timestamp flag; do not treat Sub-agent receipt `ts` as sole freshness authority.
- [x] 2.4 @impl DEW-014: Compute effective timeout eligibility from `lease_anchor_at + idle_timeout_ms`, where `lease_anchor_at` is the latest Engine-observed progress time or `claimed_at` when no progress exists; default `idle_timeout_ms` to the work-unit `timeout_ms` unless an accepted explicit runtime/profile surface exists; preserve existing `deadline_at`/`initial_deadline_at` as initial lease hint; support injected `nowMs` or equivalent helper clock for tests while CLI uses real time.
- [x] 2.4a @impl DEW-014: Diagnose filesystem mtimes that are later than the chosen current time as suspicious and prevent those future mtimes from extending the effective lease beyond current time plus idle timeout.
- [x] 2.5 @impl DEW-014: Integrate dry-submit-equivalent validation when the assigned result or optional `--result <result.json>` candidate exists, returning `recommended_action: submit` for expected pass and `recommended_action: repair` only for same-claimed-`work_id` repairable failure; route wrong identity, missing binding, terminal status, and ambiguous authority to `inspect`/`block`; do not duplicate submit validation logic where the existing dry-submit helper can be reused; ensure external `--result` mtime does not extend idle lease by itself.
- [x] 2.6 @impl DEW-014: Define a Zod-backed timeout-preflight output schema with closed-enum `recommended_action`, `timeout_eligible`/`check` consistency, structured progress fields, inspect/advice arrays, and timeout metadata that distinguishes `latest_engine_observed_progress_at` from `lease_anchor_at`.
- [x] 2.7 @impl DEW-014: Add `operate-work-unit timeout-preflight <bundle> --work-id <id> [--result <result.json>]` with structured JSON output for both eligible and non-eligible cases, including `timeout_eligible`, `check`, closed-enum `recommended_action`, `progress`, `initial_deadline_at`, `lease_anchor_at`, `idle_timeout_ms`, `effective_timeout_at`, `inspect`, and `advice`.
- [x] 2.8 @impl DEW-014: Define and implement timeout-preflight CLI exit behavior so false timeout eligibility blocks shell-driven timeout loops while still writing structured JSON to stdout.
- [x] 2.9 @impl DEW-014: Add read-only snapshot coverage for timeout-preflight itself, proving eligible and non-eligible preflight checks do not mutate index, queue, status, ledger, transaction, trace/log, result, receipt, cache, or gate-consumable output surfaces.

## 3. Guarded Timeout Terminalization

- [x] 3.1 @impl DEW-014: Update the Engine-owned timeout terminalization path used by `operate-work-unit timeout` and exported lifecycle/API helpers so default timeout runs the preflight guard before terminalization; do not leave a direct `timed_out` bypass through `closeWorkUnitAttempt` or an equivalent exported helper.
- [x] 3.1a @impl DEW-014: Add a static export/call-site audit or focused hygiene test that enumerates Engine-owned `timed_out` transition paths and proves they route through guarded timeout or explicit force.
- [x] 3.2 @impl DEW-014: Refuse default timeout for progress-positive, submit-ready, repairable, invalid-binding, ambiguous, or not-yet-idle attempts through both CLI and exported Engine/API timeout paths; verify the refusal leaves `_work_units/_index.json`, `rb_queue.json`, work-unit status file, ledger, transaction directory, trace/log terminalization records, and retry demand unchanged.
- [x] 3.3 @impl DEW-014: Preserve no-progress timeout retry behavior: timeout terminalizes the claimed attempt, clears delegated in-flight binding, requeues retry demand, writes status/index/queue/trace/log through the existing transaction path, and allocates replacement `work_id` only on later claim.
- [x] 3.4 @impl DEW-014: Add explicit `--force` support for timeout with required `--reason`; force must still run preflight for audit, can terminalize progress-positive attempts only through the explicit forced path, and must record `forced_timeout: true`, reason, `preflight_timeout_eligible`, `preflight_recommended_action`, `default_timeout_would_refuse`, `effective_timeout_at`, `latest_engine_observed_progress_at`, `lease_anchor_at`, and structured `progress_sources[]` in durable trace/log or equivalent diagnostic output; prefer `work_unit_forced_timeout` trace event or prove the existing timeout event carries equivalent fields.
- [x] 3.5 @impl DEW-014: Keep terminal fail-closed semantics unchanged: `failed`, `timed_out`, and `abandoned` attempts do not count gate coverage, and normal late submit after terminal timeout remains rejected.
- [x] 3.6 @impl DEW-014: Update touched engine/CLI/schema files with `// @impl DEW-014` annotations on the implementation surfaces that enforce timeout-preflight, guarded timeout, force audit, and output schema behavior.

## 4. Sub-Agent And Phase Guidance

- [x] 4.1 @impl SNC-008: Update active Sub-agent role/task guidance so slow search/fetch/cache batches emit concise lifecycle progress before and after batch work, including identity fields and receipt nonce, without making receipt progress a gate or submit authority.
- [x] 4.2 @impl SNC-008: Ensure progress guidance remains JS/Node-first and does not reintroduce Python fallback, Python one-liners, `.py` scripts, or Engine-owned fetch orchestration.
- [x] 4.3 @impl RWP-018: Update Wave0 delegated drain guidance to route expired/stale work units through `timeout-preflight` and follow `submit | repair | wait | inspect | block | timeout` advice before calling terminal timeout.
- [x] 4.4 @impl RWP-018: Update Wave1 delegated drain guidance with the same timeout-preflight advice path, preserving depth review, supplementary queue demand, and Phase-owned reference materialization boundaries.
- [x] 4.5 @impl RWP-018: Update Wave2 targeted-evidence drain guidance with the same timeout-preflight advice path, preserving pure synthesis vs targeted evidence boundaries.
- [x] 4.6 @impl RWP-018: Ensure phase docs still teach bounded top-up claim, active polling, queue/in-flight drain before gate, no user/notification wait as the continuation mechanism, parsing structured `timeout-preflight` JSON even when false-check exits non-zero, and `timeout --force` as exceptional rather than normal drain behavior.
- [x] 4.7 @impl SNC-008, RWP-018: Preserve requirement traceability in touched Markdown/tests using the repo's existing local convention; do not introduce a broad frontmatter migration unrelated to this change.

## 5. Regression And Controlled Coverage

- [x] 5.1 @impl DEW-014: Add unit tests for timeout-preflight no-progress eligibility, recent receipt/output/cache progress refusal, dry-submit-pass submit advice, dry-submit-fail same-`work_id` repair advice, invalid candidate identity/missing binding/terminal status/ambiguous authority inspect-block routing, optional `--result` candidate path semantics including external-candidate-mtime non-extension, invalid binding fail-closed behavior, injected-clock effective timeout calculation, suspicious future mtime handling, and no-progress output where `lease_anchor_at` falls back to `claimed_at` without falsely reporting observed progress.
- [x] 5.2 @impl DEW-014: Add CLI integration tests for `operate-work-unit timeout-preflight` structured output and exit behavior in eligible and non-eligible cases.
- [x] 5.3 @impl DEW-014: Add default timeout no-side-effect refusal tests for progress-positive, submit-ready or repairable, invalid-binding/ambiguous, and not-yet-idle attempts through both CLI and exported Engine/API timeout paths, including queue/index/status/ledger/transaction/trace/log surfaces.
- [x] 5.4 @impl DEW-014: Add `timeout --force` coverage proving terminalization occurs and forced diagnostics are durable, including structured `progress_sources[]` fields.
- [x] 5.5 @impl DEW-014: Add or update no-progress timeout retry happy-path coverage to prove existing REDO behavior still works.
- [x] 5.6 @impl SNC-008, RWP-018: Add Markdown/static tests proving active Sub-agent guidance emits progress around slow batches and Wave0/Wave1/Wave2 phase docs require timeout-preflight before terminal timeout, follow all advice branches including inspect/block, and do not present `timeout --force` as normal drain behavior.
- [x] 5.7 @impl RWE-011: Add or update controlled wave progress-lease playbook setup using approved disposable-bundle infrastructure, production `operate-work-unit` CLI/API boundaries, explicit fixture-distance ledger, and trace-backed verdict assertions.
- [x] 5.8 @impl RWE-011: Add controlled coverage for no-progress timeout retry and progress-positive default-timeout refusal through production work-unit CLI/API boundaries, including runtime assertions that refusal leaves queue/index/status/ledger/retry surfaces unchanged.
- [x] 5.9 @impl RWE-011: Add controlled coverage for timeout-preflight candidate-result routing: dry-submit-pass advice to `submit`, dry-submit-fail advice to same-`work_id` repair, optional `--result <candidate>` path semantics, and external-candidate mtime non-extension of the idle lease.
- [x] 5.10 @impl RWE-011: Add controlled coverage proving `timeout --force` records durable forced-timeout diagnostics, including structured `progress_sources[]`, and normal late submit after terminal timeout still rejects until a later audited late-accept change.

## 6. Release And Governance

- [x] 6.1 @impl DEW-014, SNC-008, RWP-018, RWE-011: Update `CHANGELOG.md` with framework `v0.15` and a concise timeout-preflight/progress-lease summary.
- [x] 6.2 @impl DEW-014, SNC-008, RWP-018, RWE-011: Sync `DPT_FRAMEWORK/RUN.md` version banner with the `v0.15` changelog entry.
- [x] 6.3 @impl DEW-014, SNC-008, RWP-018, RWE-011: Run focused engine, CLI, Markdown/static, and controlled playbook tests; record PASS/FAIL in implementation evidence.
- [x] 6.4 @impl DEW-014, SNC-008, RWP-018, RWE-011: Run `node openspec/governance/check-project-reqs.mjs` and require PASS.
- [x] 6.5 @impl DEW-014, SNC-008, RWP-018, RWE-011: Run `node openspec/governance/check-project-specs.mjs` and require PASS.
- [x] 6.6 @impl DEW-014, SNC-008, RWP-018, RWE-011: Run OpenSpec validation for `harden-delegated-timeout-preflight-and-progress-lease` if available; if CLI is unavailable, record the exact unavailable outcome and rely on Node governance checks plus artifact review.
- [x] 6.7 @impl DEW-014, SNC-008, RWP-018, RWE-011: Before declaring apply complete, update change-local implementation evidence with touched surfaces, tests, no-side-effect proof, exported API guard proof, force-timeout audit proof, deferred Change B findings, and residual risks.
