## 1. Apply Admission and Contract Ownership

- [x] 1.1 Run the verification-routing plan check and re-read the C1 delta specs, design, Wave inspect composition, shared reference reader, and reentry audit. Done when the apply target list identifies one declared owner for Seed Topic return maps, rich references, Wave1 artifacts, Phase-owned reference backing, and index navigation. (RRM-001, REF-002, REF-007, WAI-005, RWG-005, RRD-002)
- [x] 1.2 Lock focused pre-change regressions for the current false-parser paths and the true Seed Topic failure path. Done when tests distinguish rich-reference/Wave1-artifact absence of return-map fields from malformed Seed Topic entries. (RRM-001, WAI-005, RWG-005)

## 2. One Artifact-Family Evaluation Path

- [x] 2.1 Implement RRM-001/WAI-005/RWG-005 by removing generic return-map scans over rich references and phase artifacts from Wave inspect composition. Done when Wave0/Wave1/Wave2 return-map classification derives only from the existing Seed Topic projection evaluator and all direct artifact/reference evaluators retain their current owners.
- [x] 2.2 Implement REF-002/REF-007 with one shared rich-reference metadata reader: canonical YAML frontmatter for new output, read compatibility for legacy bullet metadata, and a root-first malformed-frontmatter diagnostic. Done when reference-format, source URL, index, count, and backing consumers use the same semantic metadata values without a second parser.
- [x] 2.3 Update only the affected shared template and producer/Phase guidance to teach canonical YAML frontmatter, legacy read compatibility, the five semantic sections, and existing submitted-backing boundaries. Done when no current rich-reference authoring surface tells a new producer to use the retired canonical bullet format. (REF-002, REF-007)

## 3. One Submitted-Reference Authority Interpretation

- [x] 3.1 Implement RRD-002 by replacing check-reentry's direct-ledger-only reference scan with the existing pure reference-authority classifier. Done when direct delegated outputs and valid Phase-owned projections share the normal Wave interpretation, while unbacked/delegated-bypass files remain blocking with classifier-derived detail.
- [x] 3.2 Extend unit coverage for return-map input selection, canonical/legacy/invalid reference metadata, and the shared authority classifier. Done when false-parser regressions, malformed metadata short-circuiting, valid Phase-owned backing, and true unbacked rejection all have deterministic proof. (RRM-001, REF-002, REF-007, RRD-002)
- [x] 3.3 Extend production CLI integration coverage for Wave inspect, formal Wave1 Gate, reentry, and parser-aligned guidance. Done when valid rich references/Wave1 artifacts need no Return Map workaround, malformed Seed entries remain blocking, existing artifact roots persist, and reentry accepts only valid Phase-owned backing. (WAI-005, RWG-005, RRD-002)

## 4. Release and Verification

- [x] 4.1 Update CHANGELOG.md for v0.61 and synchronize the DPT_FRAMEWORK/RUN.md version banner and concise release notes. Done when both describe artifact-family evaluator convergence without claiming a new authority, controller, or Agent capability. (RRM-001, REF-002, REF-007, WAI-005, RWG-005, RRD-002)
- [x] 4.2 Run every selected unit and integration asset in verification-plan.yaml and record the deterministic results in the change evidence. Done when no deterministic-E2E or Agent-flow proof is fabricated.
- [x] 4.3 Run the real existing-bundle counterexample for the former Wave0/Wave1 parser roots and the reentry authority mismatch. Done when evidence distinguishes eliminated false return-map/ledger findings from any independent remaining bundle blockers.
- [x] 4.4 Run the verification-routing asset check, strict OpenSpec validation, project requirement governance, and project spec governance. Done when all report zero applicable violations.
- [x] 4.5 Record the C1 disposition and verification coordinates in BUG-146, BUG-162, BUG-172, and BUG-178 without rewriting unrelated backlog material. Done when each card names the archive/change evidence and any explicitly out-of-scope remaining work.

## 5. Post-Apply Spec Coherence

- [x] 5.1 Reconcile the stale accepted Wave1 sub-agent reference-format wording discovered during archive assessment. Done when the active `reference-flat-format` delta updates that requirement to the shared canonical YAML-frontmatter/legacy-read contract, main-spec sync leaves no conflicting YAML prohibition, and parser-guidance plus strict/spec governance pass.
