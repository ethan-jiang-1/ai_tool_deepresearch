## 0. Apply Entry Review

- [x] 0.1 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007, IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 `openspec-feedback:plan-review` - Before the first target edit, read the current feedback-loop Apply guidance and review proposal, six complete-replacement deltas, design, semantic closure, verification plan, shared binding adapter, all named verdict consumers, production `checkReferenceFormatFiles()` bundle-path callers, and current-success legacy fixtures. Record every actionable finding as an ordinary pending task with owner, smallest repair, and independently observable done condition; complete only when none remains and plan-mode requirement, verification-routing, and semantic-closure checks pass.

## 1. Shared Reference-Binding Boundary

- [x] 1.1 @impl CTS-007 - Change `resolveReferenceTopicBinding()` to reject presence of `related_topic`, including empty, legacy-only, and dual declarations, with exactly `reference_topic_binding_legacy_unsupported` before legacy parsing. Remove only private legacy Reference Markdown parsing made unreachable; preserve scalar UID, `all`, exact UID subset, structured topic binding, and non-reference previous-layout behavior. Done when the adapter truth table proves every retained and rejected form without consumer code.
- [x] 1.2 @impl CTS-007, REF-002, REF-007 - Add focused unit cases for legacy current id/slug, previous id/slug, sentinel/list-like value, empty key, and agreeing/conflicting dual forms. Assert the same stable rejection, no inferred Topic set, unchanged current-form diagnostics, and unchanged input bytes.

## 2. Shared Verdict Consumers

- [x] 2.1 @impl REF-002, REF-007, IOC-001, IOC-002, IOC-003, RWG-019 - Update the common reference-format Gate/inspect finding path to preserve the adapter reason code and `metadata.related_topic` root, remove compatible-legacy expectations and direct historical-file rewrite advice, and suppress legacy lookup/countability symptoms. Confirm every production caller supplies bundle/layout facts and no no-layout fallback becomes a current success path. Done when Wave0/Wave1/Wave2 Gate and inspect expose the same single root while UID forms still pass.
- [x] 2.2 @impl REF-003, FIO-006, FIO-007 - Make reference index synchronization and file observability propagate `reference_topic_binding_legacy_unsupported` without a legacy identity map, topic footprint, dangling-id finding, or partial derived-file write. Done when legacy-only and dual references block through the shared code, existing reference/index bytes remain unchanged on rejection, and UID-bound previous-layout/non-reference coverage remains valid.
- [x] 2.3 @impl RWG-019, RTI-001 - Verify provenance, submitted backing, Gate floors, reference navigation, and rerun consumption cannot regain legacy binding through a downstream fallback. Route any needed replacement evidence only through the existing current UID-bound materialization path; add no metadata-only work unit, migration, or direct rewrite path. Done when the selected integration cases prove non-countability plus unchanged current UID rerun behavior.

## 3. Current Fixtures And Guidance

- [x] 3.1 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007 - Convert unit/helper fixtures that model current successful references from `related_topic` to scalar UID, `all`, or exact UID subsets. Retain only named legacy rejection/byte-preservation fixtures. Done when focused adapter, format, index, observability, and provenance suites contain no accidental legacy-positive success path.
- [x] 3.2 @impl IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 - Convert integration and deterministic-E2E current-success fixtures used by Wave Gate, inspect, reference evidence map, provenance, and rerun paths to UID forms, while adding explicit legacy-only and dual-form rejection cases. Done when current UID workflows pass and legacy references cannot satisfy Gate/index/provenance/rerun claims.
- [x] 3.3 @impl REF-002, REF-007, RTI-001 - Scan current Harness guidance and non-archive tests for positive historical-reader instructions. Rewrite only statements that still advertise `related_topic` as current Engine input; preserve the derived `_INDEX.md` column, human-readable history wording, and C5b/C6 exclusions. Done when a scoped scan reports no unowned positive legacy-reader route.

## 4. Specification And Verification

- [x] 4.1 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007, IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 - Synchronize each complete delta requirement into its accepted main spec with heading-bounded replacement, retaining every unaffected original scenario. Done when all six delta/main blocks are semantically equivalent and no accepted current requirement presents legacy reference binding as a success path.
- [x] 4.2 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007, IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 - Run every selected verification-plan asset, workflow-package validation, `git diff --check`, strict change validation, plan-mode requirement governance, verification-routing assets mode, semantic-closure plan/assets checks, capability discovery, and project-spec checks. Record only actual deterministic evidence and the expected negative meaning of legacy-rejection PASS cases. Evidence (2026-08-14): the six selected `node --test` assets passed 86 tests with 0 failures/cancelled/skipped; `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs` returned `{ "passed": true, "issues": [] }`; `git diff --check`, strict change validation, plan requirements, capability discovery, verification-routing plan/assets, semantic-closure plan/assets, and project-spec checks all passed. Legacy-rejection PASS cases prove deterministic refusal and byte preservation, not current evidence acceptance; no Agent-runtime behavior is claimed.
- [x] 4.3 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007, IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 `openspec-feedback:closeout-review` - Before archive, read the current feedback-loop Archive guidance and review the scoped actual diff, selected evidence, delta/main sync, positive-legacy scan, byte-preservation proof, and semantic-closure roles. Add and close ordinary repair tasks for every finding; complete only when no actionable finding remains.

## 5. Archive Preconditions

- [x] 5.1 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007, IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 - Compare every modified delta requirement/scenario with its accepted main-spec block and run `node openspec/governance/check-project-reqs.mjs --mode archive --change decide-historic-reference-reader-policy`. Done when modified blocks match and the check reports zero duplicate, orphan, unregistered, or reused-retired IDs.
- [x] 5.2 @impl REF-002, REF-003, REF-007, CTS-007, FIO-006, FIO-007, IOC-001, IOC-002, IOC-003, RWG-019, RTI-001 - Run `node openspec/governance/check-project-specs.mjs`. Done when it reports zero delta-header-in-main, missing-purpose, missing-requirements, and missing-requirement-header findings.
- [x] 5.3 @impl CTS-007, RWG-019 - After all prior tasks and review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change decide-historic-reference-reader-policy`. If finalization fails, restore this task to unchecked and repair its named root.
