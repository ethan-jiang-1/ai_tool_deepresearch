## 1. Apply Entry And Boundary Review

- [x] 1.1 `openspec-feedback:plan-review` — Before the first target edit, obtain current feedback-loop Apply guidance and review the proposal, delta, design, `semantic-closure.yaml`, verification plan, accepted DEW-023/DEW-024 owners, v1 raw-safety boundary, v2 lock/rollback/recovery behavior, original-acceptance evidence readers, and selected tests. Record every actionable finding as an ordinary unchecked task with its affected requirement or reader question, authoritative owner, smallest repair, and independently observable done condition; complete this marker only when no finding remains.
- [x] 1.1a DEW-024 / `work-unit.submission-ledger-and-supersession` review finding: declaration recovery's `readOriginalSubmitEvidence` is a separate verdict consumer from supersession. Repair the change-root semantic closure consumer inventory to name `DEEP_RESEARCH_HARNESS/engine/work-unit-submit.mjs#readOriginalSubmitEvidence`; done when the record has both evidence readers, their roles are verdict consumers, and the semantic-closure plan check passes.
- [x] 1.2 Before the first target edit, run `openspec validate retire-transaction-v1-history --strict`, `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, `node openspec/governance/check-verification-routing.mjs --change retire-transaction-v1-history --mode plan`, `node openspec/governance/check-semantic-closure.mjs --change retire-transaction-v1-history --mode plan`, and `git diff --check`. Done when all pass and results are recorded as structural planning evidence only.
- [x] 1.3 Re-run scoped searches across current Harness source, tests, accepted specs, active guidance, and active changes for `work-unit.transaction.v1`, transaction union and barrel exports, raw orphan/inspect readers, `readOriginalSubmitEvidence`, and `originalAcceptanceEvidence`. Do not inspect a real run bundle. Done when every current reader is in this task list or recorded as an ordinary repair finding, and current v2 writers/recovery/rollback plus C6a/C6b/C6c complete-profile behavior are explicitly protected.

## 2. Current Transaction And Evidence Boundary

- [x] 2.1 Implement DEW-023 in `DEEP_RESEARCH_HARNESS/schema/contracts/work-unit-transaction.mjs`, `DEEP_RESEARCH_HARNESS/schema/index.mjs`, and `DEEP_RESEARCH_HARNESS/engine/work-unit-transaction.mjs`: remove the positive v1 journal schema/union/barrel export as a current transaction protocol and introduce the minimal raw safety classification needed to distinguish a structurally complete committed-v1 diagnostic file from unresolved or malformed v1/invalid journal bytes. Done when only valid v2 journals can become busy, recoverable, settled, or current authority; raw uncommitted/malformed v1 and invalid files remain deterministic suspect inputs, while a complete committed v1 file alone neither blocks a current v2 mutation nor receives a v1 adapter, migration, or inferred target binding.
- [x] 2.2 Implement DEW-023 in `DEEP_RESEARCH_HARNESS/engine/work-unit-submit-integrity.mjs`, `DEEP_RESEARCH_HARNESS/engine/work-unit-timeout-preflight.mjs`, `DEEP_RESEARCH_HARNESS/engine/work-unit-inspect.mjs`, and transaction recovery surfaces: route raw unresolved v1, malformed, unreadable, and proof-incomplete journals to the existing `suspect_transaction` no-mutation boundary; leave committed v1 bytes diagnostic-only and never recoverable. Done when submit/dry-submit/timeout/inspect/recover-transaction preserve current v2 contention, exact before-image rollback, and idempotent settled recovery, with no wait/force/delete advice for v1 suspects.
- [x] 2.3 Implement DEW-024 in `DEEP_RESEARCH_HARNESS/engine/work-unit-submit.mjs`, `DEEP_RESEARCH_HARNESS/engine/work-unit-supersession.mjs`, and their direct provenance/inspect consumers: require a committed v2 original-submit journal/trace binding whenever current declaration recovery, supersession, historical inspection, normalized ledger, or Gate lineage needs acceptance evidence. Done when a complete-current predecessor whose only candidate proof is committed v1 returns the existing `missing_contract` proof boundary before ledger/successor/Gate authority mutation; valid v2 evidence retains its existing path.
- [x] 2.4 Implement the catalog addition declared by `semantic-closure.yaml` in `openspec/governance/semantic-fact-families.yaml` at the first target edit, exactly matching its bounded question. Done when there is one shared transaction-mutation-integrity family rather than an ad hoc v1 compatibility concept.

## 3. Deterministic Regression Evidence

- [x] 3.1 Update DEW-023 schema and unit coverage in `tests/schema/contracts/work-unit-attempt-recovery.test.mjs` and `tests/engine/work-unit-transaction.test.mjs`. Prove the public transaction schema/barrel exports only v2; raw v1 `started`/`failed`, malformed committed-looking, and unreadable journal entries retain `suspect_transaction` and zero mutation; a complete committed v1 file alone does not block a current v2 path, cannot be recovered, and cannot become current authority; retain v2 busy, rollback, and idempotent recovery cases. Done when no test expects a positive v1 schema/parser/reader result.
- [x] 3.2 Update DEW-024 declaration-recovery unit coverage in `tests/engine/work-unit-submit.test.mjs` and update the protected current-v2 transaction inventory in `tests/engine/work-unit-attempt-recovery.test.mjs`. Prove a complete-current predecessor with only committed-v1 original-submit evidence returns `missing_contract` and cannot restore a declaration; retain complete-current committed-v2 declaration recovery. Done when the assertion identifies the direct rejection root and confirms no authority mutation.
- [x] 3.3 Update DEW-023/DEW-024 integration coverage in `tests/integration/cli/operate-work-unit.test.mjs` and `tests/integration/cli/work-unit-declaration-recovery.test.mjs`. Prove the CLI reports v1 unresolved journal safety blocks and committed-v1 declaration-recovery rejection with unchanged temporary bundles, while current v2 command behavior stays intact. Done when fixture tests claim deterministic CLI mechanics only.
- [x] 3.4 Update DEW-024 deterministic-E2E coverage in `tests/e2e/work-unit-attempt-recovery.test.mjs`, the direct supersession behavior owner. Prove a complete-current predecessor whose only original-submit proof is committed v1 returns `missing_contract`, creates neither successor nor historical-lineage authority, and leaves core authority unchanged; retain the complete-current committed-v2 correction/normalized-ledger/Gate lineage. Done when this is fixture-backed deterministic evidence, not an Agent-flow claim.

## 4. Specification, Verification, And Scoped Closeout

- [x] 4.1 Run every selected verification asset in `verification-plan.yaml`: `tests/schema/contracts/work-unit-attempt-recovery.test.mjs`, `tests/engine/work-unit-transaction.test.mjs`, `tests/engine/work-unit-submit.test.mjs`, `tests/engine/work-unit-attempt-recovery.test.mjs`, `tests/integration/cli/operate-work-unit.test.mjs`, `tests/integration/cli/work-unit-declaration-recovery.test.mjs`, and `tests/e2e/work-unit-attempt-recovery.test.mjs`; also run `node DEEP_RESEARCH_HARNESS/cli/validate-workflow-package.mjs`. Done when all pass and selected evidence is recorded only as deterministic unit/integration/deterministic-E2E evidence.
- [x] 4.2 Perform scoped absence/protected-surface review. Prove current source/tests/guidance/main specs contain no positive v1 transaction parser, recovery, acceptance-evidence, supersession, or compatibility branch, while raw unresolved-journal safety detection and all current v2 behaviors remain. Done when every unexpected reference or protected-surface change is repaired or recorded as an ordinary task.
- [x] 4.3 Sync the applied DEW-023/DEW-024 delta into `openspec/specs/agent/delegated-work-units/spec.md`, then re-read the synchronized requirements against the delta. Done when the main spec states v2-only current authority, raw v1/invalid mutation blocking, and no committed-v1 acceptance evidence.
- [x] 4.4 Run `openspec validate retire-transaction-v1-history --strict`, `node openspec/governance/check-project-reqs.mjs --mode archive --change retire-transaction-v1-history`, `node openspec/governance/check-project-specs.mjs`, capability taxonomy/discovery, `node openspec/governance/check-verification-routing.mjs --change retire-transaction-v1-history --mode assets`, `node openspec/governance/check-semantic-closure.mjs --change retire-transaction-v1-history --mode assets`, and `git diff --check`. Done when all pass; record structural/governance evidence separately from selected deterministic tests.

## 5. Archive Readiness

- [x] 5.1 `openspec-feedback:closeout-review` — Before archive, obtain current feedback-loop Archive guidance and review the change-scoped actual diff, synchronized main spec, semantic fact catalog addition, selected verification results, raw safety boundary, removed v1 evidence path, and protected v2 behavior. Record every actionable finding as an ordinary unchecked task with owner, smallest repair, and independently observable done condition; complete this marker only when no finding remains.
- [x] 5.2 After all ordinary tasks and both feedback reviews are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change retire-transaction-v1-history`. If finalization fails, restore this task to unchecked and repair only its named root.
