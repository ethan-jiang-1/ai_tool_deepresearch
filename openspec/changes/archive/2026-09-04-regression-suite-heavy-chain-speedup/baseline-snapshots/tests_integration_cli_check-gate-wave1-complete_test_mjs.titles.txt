projects the same candidate-exact materialization hint through inspect and formal Gate
keeps the floor outcome while making omitted supplementary review sync the primary inspect and Gate repair
1. happy path: evidence-summary with source URL + key findings passes
\n
1h. treats an exact hash-valid superseded predecessor as historical rather than delegated bypass
\n
1j. rejects a malformed carried target declaration at the existing depth-review coordinate
1i. masks Wave1 declaration-dependent symptoms behind one recoverable parent
1h. keeps bounded fresh/rerun-added Wave1 contract and Gate parity
1a. keeps a historical Wave1 binding as the direct current-topic root before count-floor evaluation
1d. tolerates equivalent headings, bare URLs, paragraph findings, and numbered Key Facts
1g. accepts UID-only reference metadata through the canonical topic resolver
1g. reports one retired-key root for a dual binding without downstream floor noise
1j. routes raw document markup through the existing Gate and side-effect-free inspect repair
1e. fewer Key Facts does not affect numeric count or revive the retired quantity blocker
1f. an empty required question-list semantic section remains blocking once
1a. rejects noncanonical required output roles at submission
1b. fails when a required artifact exists but canonical submitted coverage is missing
1c. passes and reports canonicalization when depth-review reviewed refs have harmless trailing slash
2. fails when per-topic evidence-summary is missing
2b. emits delegated bypass diagnostics once per formal invocation
\n
\n
3. fails when source URL is missing from evidence-summary
4. fails when key findings section is empty
5. fails when backfill tokens are stale in seed_topics
6. fails on status drift
7. fails when trace event (wave1_completion) is missing from rb_trace.jsonl
7a. @impl TRW-007 ignores a forged wave1_completion whose bundle is the rb_status.json short name
7b. @impl TRW-007 accepts a CLI-written wave1_completion with the canonical bundle basename
8. rejects reference files with placeholder source_url (example.com)
9. rejects orphan reference files not declared in ledger
10. accepts canonical YAML rich references without return-map fields
11. fails when depth-review.yaml is missing
11a. gives inspect and Gate one current-topic submitted-backing root without fabrication advice
12. fails when depth review has too few exact-new source URLs
13. fails when accepted source claims lose cache content coverage
14. passes after supplementary Wave1 repair is submitted and reviewed
0a. projects the same direct roots for Key Findings and question sections
0b. keeps URL, provenance, depth, return-map, and phase facts Wave-only
0c. evaluates a parsed semantic descriptor without reusing a target-only default result
