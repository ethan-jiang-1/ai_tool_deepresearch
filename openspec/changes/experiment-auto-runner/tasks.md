## 0. Reopen and route verification

- [ ] 0.1 Treat the prior 35/35 checklist as invalidated by the terminology/authority correction; inventory current committed and dirty surfaces without discarding user changes. (`EXA-001`–`EXA-008`, `PLR-001`, `PLR-003`)
- [x] 0.2 Run `node openspec/governance/check-verification-routing.mjs --change experiment-auto-runner --mode plan` before target edits; PASS on 2026-07-18 with 4 routed claims.
- [x] 0.3 Run `openspec validate experiment-auto-runner --strict`; PASS on 2026-07-18 after rewriting the delta structure and scenarios.

## 1. Terminology and accepted contract correction

- [ ] 1.1 Update `openspec/governance/req-registry.yaml` so EXA maps to `experiment-agent-autorun`, EXA-001–008 descriptions match the rewritten delta, AGT-005 is retained in place as deprecated, and the previously unregistered current Topic rewrite requirement receives PRE-008 in the existing `pre-research-experiments` group; preserve requirement history without a parallel capability group. (`EXA-001`–`EXA-008`, `AGT-005`, `PRE-008`)
- [ ] 1.1a Register `VER-006` under `verification-routing` and update the prefixes/group ordering without inventing a new verification capability. (`VER-006`)
- [ ] 1.1b Update retained registry descriptions whose accepted meaning changes in this delta—at minimum AGQ-006 current case taxonomy, PRG-008/RWG-007 one-root-trace event ownership, and any STM/RWE/VER wording exposed by the sync—without changing IDs, creating parallel groups or erasing history. (`AGQ-006`, `PRG-008`, `RWG-007`, `STM-005`, `RWE-007`–`RWE-011`, `VER-001`)
- [ ] 1.2 Confirm all implementation/Markdown `@impl` markers use the retained EXA and PLR IDs; add missing markers only on actual owner surfaces. (`EXA-001`–`EXA-008`, `PLR-001`, `PLR-003`)
- [ ] 1.3 Add a focused terminology knowledge-surface guard covering Agent Experiment Autorun, Autorun Supervisor, Agent CLI Launcher, Headless/Interactive Playbook Agent, Subject Agent/Sub-agent, and the ordinary-CI boundary. (`EXA-001`, `EXA-002`, `EXA-008`)
- [ ] 1.4 Apply and later sync delta-covered accepted specs: `playbook-runner`, `agent-testing`, `experiment-observability`, `pre-research-experiments`, `trace-writer`, `verification-routing`, `agentic-queue`, `research-wave-experiments`, `pre-research-gate-implementation`, `research-wave-gate-implementation`, `seed-topic-materialization`, and `local-deepseek-claude-launcher`; do not directly edit a main requirement without its delta and execute every declared rename during sync. (`PLR-001`, `PLR-003`, `AGT-005`–`AGT-007`, `EXO-002`, `EXO-006`, `PRE-001`–`PRE-008`, `TRW-005`, `VER-001`, `VER-006`, `AGQ-006`, `AGQ-008`, `AGQ-010`, `RWE-007`–`RWE-011`, `PRG-007`, `PRG-008`, `RWG-007`, `STM-005`, `LDC-001`, `LDC-002`, `LDC-005`, `LDC-008`, `LDC-009`)
- [ ] 1.5 Run a full main-spec terminology audit before target edits; if another accepted requirement gives old terminology behavioral authority, add its delta to this change before modifying that main spec or consumer.

## 2. Canonical manifest and instruction surfaces

- [ ] 2.1 Implement the manifest v1 parser/format with exact one-time HTML markers and one-column `Path` table whose row order is execution order; reject second projections and unsafe/non-POSIX/stale/duplicate/symlink entries. Final path reconciliation/count occurs at 5.17. (`EXA-002`, `EXA-004`, `PLR-001`)
- [ ] 2.2 Implement deterministic manifest/V2 validation for exact registration, safe files, identity/cost/native/proof policy and 9NN consistency; filesystem scan is unregistered-case drift only. Make directory `validate-playbook` collect only runnable `case-*.md`, never README/manifest/instructions. (`EXA-004`, `PLR-001`, `VER-006`)
- [ ] 2.3 Create `RUN_AGENT_AUTORUN_EXPS.md`: execute the injected full rendered case from validated repo command cwd and all verdict-affecting Markdown/Agent steps, consume feedback, never rediscover/rewrite runtime paths, never skip Subject Agent work, invoke finalizer once, then stop without source edits, optional smoke, health or cleanup. (`EXA-001`, `EXA-002`, `EXA-003`, `EXA-005`, `PLR-001`, `PLR-003`)
- [ ] 2.4 Create `RUN_INTERACTIVE_EXPS.md` as single-case debug/replay and real-human judgment guidance using the same host-created run context, native completion and post-run health rules; it must not instruct an existing repo-cwd TUI to improvise autorun paths. (`EXA-001`, `EXA-002`, `EXA-006`, `EXA-008`, `PLR-001`)
- [ ] 2.5 Remove/retire `RUN_CLI_EXPS.md` and `RUN_TUI_EXPS.md`, then update every test, README and manifest consumer still naming `RUN.md`/`RUN_EXPS.md`/old instruction names; done when `rg` finds no active stale consumer. (`EXA-002`, `PLR-001`)
- [ ] 2.6 Implement/test strict closed V2: required identity/goal, verdict mode, unique required checks, exact unique bundle/verdict/health roles matching the lowercase-hyphen grammar + one verdict role + non-empty health subset containing it, health profile, unique lowercase-underscore durable evidence roles and exact accepted proof/subject/fixture/runtime/external/judge fields; deterministic contracts require empty evidence roles and Agent-behavior cases require the audit-ledger non-empty minima; optional req refs/`not_run_if` only. Reject all V1 routing/runtime/verdict/agent fields. Case conversion is section 5; corpus gate is 5.17. (`AGT-005`–`AGT-007`, `EXA-004`, `EXA-005`, `EXO-002`, `VER-006`)
- [ ] 2.7 Parse and validate the checked `case-compatibility-ledger.yaml`: exact target count 97, unique case/path, no case-316, restored case-901, task-batch union exactly once over all target IDs, 39/37/21 filename-cost split, 43/26/28 health split, 39 wff/23 fixture/31 custom/2 prose/2 actor-pair families, 16 Agent-behavior profiles, non-empty unique stable required checks, valid bundle/health counts, exact evidence-role cross-fields and migration flags. Refresh observed facts only for real worktree drift and document every target-row change; never weaken a row to fit implementation. Preserve repo-relative source commands under fixed repo command cwd. (`EXA-003`–`EXA-006`, `VER-006`)

## 3. Native playbook completion contract

- [ ] 3.1 Implement strict Zod/pure contracts for manifest, V2, run context, bundle-role registry and completion: independently retained mode/manifest/instruction/source-playbook/rendered-playbook/context identity/digests, case-root realpath/device/inode identity, fixed context-parent registry/completion coordinates, exact allowlisted runtime-token rendering, cost/proof/judge, strict playbook-owned verdict-check events and exact `all|last-per-gate` projection, required-check coverage, role-bound outcomes/bundles, raw trace bindings, health roles, evidence and NOT_RUN reason; canonicalize completion/audit arrays by V2 role order, never CLI/registry/filesystem order. (`EXA-004`–`EXA-006`, `PLR-003`)
- [ ] 3.2 Implement `finalize-agent-experiment.mjs` with exact repeatable `--bundle role=path`/`--evidence role=file` and optional non-empty `--not-run-reason` CLI; reject case/target/outcome/mode/profile/judge/bundle/verdict/health/evidence-policy overrides. Ground authority in the validated explicit context file's real parent and fixed completion sibling, never a mutable target field. For PASS/FAIL exact-match actual registry/observed roles to context V2 policy; for NOT_RUN exact-match actual sets and require a policy subset. Derive verdict/health roles from context, compute required-check-covered PASS/FAIL, validate structured judge provenance and evidence-role coverage/files/hashes, bind raw traces, then publish the previously absent fixed target by fsynced same-directory temp + exclusive atomic hardlink + parent fsync (never overwrite/partial-write). Return 0 for valid native outcomes and never run health/export/cleanup/Agent work. (`EXA-005`, `EXA-006`, `PLR-003`)
- [ ] 3.3 Implement narrow `agent-experiment-state.mjs register-bundle|get-bundle` with explicit context/role; require a non-symlink regular context and derive the fixed registry from its real parent rather than mutable fields. Serialize register via fixed `bundles.lock` exclusive create with fail-closed no stale-lock recovery, then temp+fsync+atomic-rename the pre-created registry, prohibit overwrite of an existing role or a second role for one path, validate direct-child name/realpath/non-symlink and provide exact get stdout; release/sync the lock directory. It persists cross-tool-call paths only and never owns verdict, health, Agent Flow or cleanup. (`EXA-005`, `EXA-006`, `PLR-003`)
- [ ] 3.4 Update shared `wff-playbook-utils` and work-unit verdict paths to delegate to the finalizer contract while preserving existing check production and `all|last` semantics. (`EXA-005`, `EXA-006`, `PLR-003`)
## 4. Autorun Supervisor refactor

- [ ] 4.1 Rename/refactor `run-experiment.mjs` to `run-agent-experiment.mjs` and update host-tool docs/consumers; remove the old active entry instead of leaving two success paths. (`EXA-001`, `EXA-002`)
- [ ] 4.2 Implement manifest-order selection: no filter = autorun-compatible Light; exact full `--case` exclusive; exact `--group` optionally combined with one `--tier`; `--all` = all autorun-compatible but excludes real-human manual cases; empty/unknown/ambiguous combinations fail before roots. Add mutation/runtime-free `--dry-run`, `--json`, timeout/cleanup args, required Headless `--max-total-budget-usd`, optional bounded `--max-case-budget-usd`, and mutually exclusive `--interactive --case <exact-case>`; reject `--interactive` with Headless budget/`--cleanup-pass` and preserve every Interactive root. (`EXA-001`, `EXA-004`, `EXA-008`)
- [ ] 4.3 Create `.exp-bundles/runs/<batch-id>/<ordinal>-<safe-case-id>-<uuid>/` per case with `crypto.randomUUID()`; capture its non-symlink directory realpath/device/inode identity; write/retain expected fixed-name context and fixed completion sibling with validated repo-command/framework/case-run-root and source/rendered coordinates, empty schema-valid `_playbook_state/bundles.json`, `_diagnostics/`, fixed repo-command Agent cwd and multi-bundle support without cross-tool-call env/chat memory. Implement strict `{{RUN_CONTEXT_SH}}`/`{{CASE_RUN_ROOT_SH}}`/`{{PLAYBOOK_STATE_DIR_SH}}` substitution: only unquoted/unconcatenated standalone words in `bash|sh` fences, required context/case-root minima, standard POSIX single-quote encoding, zero unknown/unresolved tokens and source/rendered digest binding; do not parse Markdown flow. Assert no `DPT_FRAMEWORK`/`experiments_env`/`tests` copy, symlink or hardlink exists anywhere under `.exp-bundles/`. (`EXA-006`)
- [ ] 4.3a Make disposable, production-instantiate, fixture-case and iterative/rerun preparation paths consume the explicit rendered target while preserving ordinary repo/test invocation; do not add env/cwd/latest-run context discovery. Add escape, malformed-token, quoting, source/rendered-digest and repo-root-leak regressions. (`EXA-006`)
- [ ] 4.4 Extract/reuse the LDC-covered pure invocation contract so generic launcher stays transparent and Supervisor owns Claude. Prevent env override. Autorun sets cwd to validated repo command root; sends full instruction + rendered playbook + source/rendered identities/digests + explicit context through stdin; uses `-p --output-format stream-json --verbose --no-session-persistence --max-budget-usd <current-cap>`, no resume, actual bypass, outside-run-root prompt/stdout/stderr streaming and process-group termination. Parse exactly one final cost, accumulate remaining total, and stop on missing/malformed/exhausted cost. Interactive sends the exact same payload as one documented positional prompt argument under a conservative 128 KiB UTF-8 cap while inheriting real TTY stdin/out/err; it uses no `-p`, stream output, no-session, Headless bypass/budget or PTY wrapper. (`EXA-003`, `EXA-008`, `LDC-001`, `LDC-002`, `LDC-005`, `LDC-008`)
- [ ] 4.5 Remove repo/shared bundle guessing/`BUNDLE=`/chat-memory authority; exact-match V2 policy, case-local registry, completion role/path declarations and observed non-symlink containment-valid direct-child `dpt_disp_*|dpt_rb_*` directories, excluding reserved run-root surfaces. Revalidate the case-root creation identity before completion acceptance, health and cleanup; replacement/symlink swap is ERROR. (`EXA-005`, `EXA-006`)
- [ ] 4.6 Remove Supervisor raw trace verdict extraction; validate native completion, exact declared-vs-observed run-root bundles and trace prefixes, and classify approval request, timeout, signal, nonzero exit, malformed/missing completion or digest/path mismatch as ERROR. (`EXA-003`, `EXA-005`, `EXA-006`)
- [ ] 4.7 Run health only for completion-declared required targets using the frontmatter profile, then implement the native/lifecycle/effective/health matrix: cleanup the entire case run root only for effective PASS + all required CLEAN + flag after durable audit; preserve PASS+ISSUES, health ERROR, FAIL, NOT_RUN, ERROR and CANCELLED; no prose-derived safe exception in v1. (`EXA-007`, `PLR-003`, `EXO-002`, `EXO-006`)
- [ ] 4.8 Produce terminal and `--json` reports with separate native/lifecycle/effective/process/health fields, reasons, duration, per-case/accumulated USD cost or unknown, preservation/cleanup, full completion/health and durable prompt/transcript/evidence refs; retain valid native completion as diagnostic when lifecycle becomes ERROR, implement CANCELLED/not-started signal/budget handling, and enforce exit precedence 130/143 > 2 incomplete/infrastructure > 1 negative/health-issues > 0 clean pass. (`EXA-003`, `EXA-007`, `PLR-003`)
- [ ] 4.9 Before Headless spawn, create `.exp-bundles/_logs/<batch-id>/` exact-prompt, sanitized Agent `stream-json` and stderr files; flush/fsync/close/hash them and new directory entries. Before any PASS cleanup, revalidate/export every declared bundle's exact verdict-boundary trace-prefix bytes by V2 role (invalid auxiliary bytes preserved, missing is null) to `.exp-bundles/_evidence/<batch-id>/.../traces/`; for Agent-behavior PASS also export required exact Subject evidence under `subject/`. Apply known-credential scan/fsync/copy-hash verification to all exported bytes. Append/fsync the full per-case `case_result` with durable log/trace/Subject-evidence refs at `.exp-bundles/_audit/agent-experiment-runs.jsonl` before cleanup, append a hash-linked `cleanup_result` with any outcome override after an attempt, and atomically write `.exp-bundles/_reports/<batch-id>.json` by folding the same event schema. Interactive records use null Headless-transcript refs and preserved roots. Retire new writes to `_run_log.jsonl`, `_temp/exp_verdicts.jsonl` and per-bundle `exp_result.json`; classify transcript/evidence/audit/cleanup failure as ERROR and stop further cleanup after infrastructure failure. (`EXA-007`, `PLR-003`, `EXO-006`)

## 5. Case compatibility migration (after tasks 3.1–4.9)

- [ ] 5.1 Re-run the ledger invariants against the apply-start worktree and lock its inventory: 39 wff-shared, 23 fixture-shared, 31 custom, 2 prose-actor and 2 actor-pair target cases; execute restore-901/retire-316 and keep final active count 97. (`EXA-004`, `EXA-005`, `VER-006`)
- [ ] 5.2a Migrate pilot cases 41, 111, 112 and 135: one-bundle Light, `last`, a retained bad-JSON non-health auxiliary whose stable fact is aggregated into the healthy repair verdict trace, and a separate malformed non-health auxiliary. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.2b Migrate pilot cases 51, 153 and 711: three required bundles, ten bundles/PASS+ISSUES, and Heavy-cost/Light-health real-subject NOT_RUN. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.3 Run the eleven `feasibility-audit.md` go/no-go probes through production contracts. If any requires mechanism-specific Supervisor logic, raw-trace fallback, source link, repo-root bundle leak, live-production overclaim, missing Subject evidence, filesystem-order bundle authority, profile downgrade or proof weakening, stop apply and return to `/opsx:explore`; do not continue mass migration. (`EXA-003`–`EXA-008`, `VER-006`)
- [ ] 5.4a Migrate custom cases 11, 12, 13, 21 and 22 to V2/run-context/finalizer with no local health/cleanup. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.4b Migrate custom cases 23, 31, 32 and 33 on the same contract. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.5a Migrate custom cases 42, 43, 53 and 71. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.5b Migrate custom cases 73, 74, 75 and 78; case-73 uses a fresh disposable verdict bundle plus an explicit-target real-CLI `dpt_rb_*` production-instantiator subject bundle, both required Standard health, without live-production overclaim. (`EXA-005`–`EXA-007`, `PLR-003`, `VER-006`)
- [ ] 5.6a Migrate custom cases 307, 308, 309, 310 and 311. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.6b Migrate custom cases 312, 313, 314 and 315; move case-315 `/tmp` state into the run root. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.7a Migrate custom cases 317, 403, 406 and 114; restore visible checkpoints, keep 317/403/114 deterministic, and require independent real Sub-agent for 406. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`, `VER-006`)
- [ ] 5.7b Migrate cases 604, 605 and 214: use the audit's exact real claim→native Task/Sub-agent→submit profiles/required checks/NOT_RUN for 604/605, never parent output; keep 214 deterministic. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`, `VER-006`)
- [ ] 5.8a Migrate wff cases 601, 602, 603, 606 and 712. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.8b Migrate wff cases 713, 131, 132 and 133; preserve real-subject/NOT_RUN and run-root state. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.9a Migrate wff cases 134, 113 and 115; add the audit-declared independent real Subject Agent probe boundary for 115. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.9b Migrate wff cases 101, 102, 103 and 104; case-102 uses a fresh disposable verdict bundle plus an explicit-target real-CLI `dpt_rb_*` production-instantiator subject bundle, both required Standard health, without live-production overclaim. (`EXA-005`–`EXA-007`, `PLR-003`, `VER-006`)
- [ ] 5.10a Migrate wff cases 105, 106, 181, 182 and 52. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.10b Migrate wff cases 123, 124, 301 and 302. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.11a Migrate wff cases 303, 304, 305, 306 and 318; keep 306 deterministic and preserve independent real Subject Agent/NOT_RUN for 318. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.11b Migrate wff cases 201, 202 and 203; case-951 moves with its pair under 5.15 rather than retaining self-judgment. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.12a Migrate work-unit cases 401, 402, 404, 405 and 407; helpers produce facts, not completion/Agent Flow/cleanup. (`EXA-005`–`EXA-007`, `PLR-003`)
- [ ] 5.12b Migrate work-unit cases 161, 162, 163, 151 and 152; case-163 alone uses the independent real Sub-agent/NOT_RUN profile, others keep deterministic proof. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.13a Migrate work-unit cases 211, 212, 213, 221 and 222; 211/221 require independent real Sub-agents + NOT_RUN, while 212/213/222 remain deterministic. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.13b Migrate work-unit cases 223, 224, 231 and 232; 223 uses real Sub-agent, 232 adds independent real Subject Agent triage, 231 remains deterministic, and 224 declares its exact happy verdict + three negative auxiliary bundle roles. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.14 Migrate work-unit cases 233, 234 and 235; case-234 alone requires independent real Sub-agent/NOT_RUN; preserve all bundle roles. (`EXA-003`, `EXA-005`–`EXA-008`, `PLR-003`)
- [ ] 5.15 Retire case-316 while retaining JS tests. Migrate cases 901 and 951 as the restored co-located `exph_workflow-foundation/` independent Subject Agent → real-human/AI-judge pair; remove moved duplicate/hard-coded AI PASS and preserve distinct V2/check provenance. (`EXA-003`–`EXA-005`, `EXA-008`, `VER-006`)
- [ ] 5.16 Implement 901–949 Autorun HUMAN skip and 950–999 Headless execution using V2 judge profile, band/pair cross-validation and structured completion/trace provenance. (`EXA-004`, `EXA-008`)
- [ ] 5.17 Finalize manifest at exact active count 97 and add the compatibility validator: final manifest identities/paths equal the ledger target; every entry is V2/run-context-safe, repo-command-cwd-safe with only allowed resolved runtime tokens and no run-root source link/repo-root bundle target, one finalizer, no local health/cleanup/optional-only path, and exact ledger verdict mode/required checks/bundle-verdict-health policy/NOT_RUN/proof/evidence roles; no fallback. (`EXA-004`–`EXA-006`, `PLR-003`, `VER-006`)
- [ ] 5.18 Ensure docs/reports state deterministic tests/CI fixtures prove only Supervisor mechanics; real Agent-flow proof requires Agent CLI + model/tools + runtime evidence. (`EXA-001`, `EXA-003`, `EXA-008`)

## 6. Raise the model into command experiment guidance

- [ ] 6.1 Update `guidelines/command-experiments.md` with Canonical Terminology and the Interactive → Agent Autorun launch-model diagram; remove CLI/TUI-as-execution-mode wording. (`EXA-001`, `EXA-002`, `PLR-001`)
- [ ] 6.2 Update the guideline ownership/runner sections so Supervisor supervises, Headless/Interactive Playbook Agent executes Markdown, Subject Agent performs case-owned intelligent work, and Engine owns deterministic checkpoints/native completion. (`EXA-003`, `EXA-005`, `PLR-003`)
- [ ] 6.3 Update guideline manifest, native completion, CI boundary, outcome/health/cleanup, isolation and no-reinterpretation rules; remove stale `RUN.md`/`RUN_EXPS.md` references. (`EXA-004`–`EXA-008`, `PLR-001`, `PLR-003`)
- [ ] 6.4 Update `experiments_playbook/README.md` and `DPT_FRAMEWORK/host_tools/README.md` to make Agent Autorun the normal entry and Interactive execution a diagnostic/manual path; Headless examples must show explicit total budget, optional case cap, cost/stop semantics and dry-run exposure. (`EXA-001`, `EXA-002`, `EXA-008`)
- [ ] 6.5 Update `openspec/config.yaml` Experiments and verification-routing context with the canonical glossary, Agent Autorun/Interactive launch chains, ordinary-CI boundary, V2/manifest/native-completion authority and current surfaces. Clarify that the no-env rule forbids cross-tool-call experiment state while the LDC-owned child env carries host provider credentials/routing; keep config a pointer, not a second capability spec. (`EXA-001`–`EXA-008`, `VER-006`, `LDC-002`, `LDC-009`)
- [ ] 6.6 Extend the static knowledge-surface guard to cover `openspec/config.yaml`, `guidelines/command-experiments.md`, experiment/host READMEs and delta-synced main specs without copying the full route matrix. (`VER-005`, `VER-006`)

## 7. Focused deterministic verification

- [ ] 7.1 Add unit coverage for manifest selection/cost filtering, strict runtime-token vocabulary/bash-sh-fence/standalone-unquoted placement/POSIX quote encoding/substitution and prose/other-fence/quoted/concatenated/heredoc/unknown-token rejection, fixed context-parent registry/completion coordinates, atomic bundle-role register/get/no-overwrite, V2/ledger/registry/observed/completion role matching and canonical V2-order serialization despite shuffled flags/filesystem entries, source/rendered/context/completion cross-field digests, strict accepted verdict-event filtering, explicit expected booleans, exact `all|last-per-gate` considered rows/counts, required-check presence, explicit native policy/judge provenance, PASS/FAIL computation, NOT_RUN with/without bundles, raw-byte trace-prefix binding, malformed auxiliary versus malformed verdict/health target, outcome mapping and path containment. (`EXA-004`–`EXA-007`)
- [ ] 7.2 Add integration coverage with a test-owned Claude executable fixture for exact repo-root cwd, rendered prompt/explicit targets, argv/env isolation, truly enabled Headless permission mode, required total/per-case budget and exact child cap, valid/missing/malformed/exhausted final cost, timeout/signal/nonzero/approval failures, missing/malformed completion, source mutation/digest mismatch and no shell. Separately test Interactive argv construction: exact positional prompt under/over 128 KiB, inherited TTY stdio class and absence of all Headless-only flags; label all fixture evidence as deterministic Supervisor mechanics only. (`EXA-003`, `EXA-005`, `EXA-008`)
- [ ] 7.3 Add negative regressions proving successful generic `enqueue/save` checks cannot PASS a Heavy case either when completion is missing or when finalizer is invoked with a missing V2 required check. (`EXA-005`)
- [ ] 7.4 Add cleanup regressions for PASS+CLEAN, PASS+ISSUES, FAIL, NOT_RUN, ERROR, Heavy-cost/Light-health, multi-bundle required/auxiliary roots, missing/extra bundle, context/root replacement or symlink swap, mutable target-field attack and path escape; assert no framework/source copy/link under `.exp-bundles`, durable prompt/transcript/full-result plus exact valid/invalid trace-prefix export before delete, missing-trace null refs, Agent-behavior Subject-evidence role coverage/exact-byte export, secret/source-mutation rejection, hash-linked cleanup result, legacy thin-log retirement and no outside mutation. (`EXA-006`, `EXA-007`)
- [ ] 7.5 Add manifest/knowledge-surface integration guards and update all existing tests that read removed `RUN_EXPS.md` or old instruction names. (`EXA-002`, `EXA-004`, `PLR-001`)

## 8. Real Headless Playbook Agent proof

- [ ] 8.1 Run the selected Light canary through production `run-agent-experiment.mjs` and a real Claude Code CLI/Headless Playbook Agent; require fixed repo command cwd, exact rendered runtime bindings, full Markdown execution against the original repo-root framework with no run-root source link or repo-root bundle leak, native completion, CLEAN health, durable prompt/transcript/full report, and optional cleanup that leaves those records readable. (`EXA-003`, `EXA-005`, `EXA-007`)
- [ ] 8.2 Run one negative/repair-loop canary proving native verdict mode is preserved and Supervisor does not reinterpret the trace. (`EXA-005`, `PLR-003`)
- [ ] 8.3 Record real Headless Playbook Agent execution separately from deterministic subprocess-fixture evidence; the selected playbook may remain fixture-backed and prove only `deterministic_contract`, while no subprocess fixture, dry-run or route validation may satisfy the real Headless execution observation or be relabeled as Subject Agent behavior. (`EXA-003`, `EXA-008`, `VER-006`)
- [ ] 8.4 Run migrated case-406 through production Autorun with a real native Sub-agent and require actual PASS rather than NOT_RUN: independent subject task/result/receipt/output files, native completion, CLEAN required health, durable evidence export, optional cleanup, and readable outside-root evidence after deletion. If the current Agent CLI/runtime cannot perform this no-network native Sub-agent canary, stop before archive and return to explore; deterministic fixtures cannot waive it. (`EXA-003`, `EXA-005`–`EXA-008`, `VER-006`)

## 9. Version, governance and archive readiness

- [ ] 9.1 Update `CHANGELOG.md` with v0.34 Agent Experiment Autorun and native completion summary. (`EXA-001`–`EXA-008`)
- [ ] 9.2 Update `DPT_FRAMEWORK/RUN.md` banner/current release to v0.34. (`EXA-001`–`EXA-008`)
- [ ] 9.3 Run `node DPT_FRAMEWORK/cli/validate-playbook.mjs experiments_playbook/` and the canonical manifest validator; both must PASS with exact active count.
- [ ] 9.4 Run focused unit/integration tests and the real Headless Agent canary; record deterministic versus Agent-flow evidence separately.
- [ ] 9.5 Run `node openspec/governance/check-verification-routing.mjs --change experiment-auto-runner --mode assets`; must PASS.
- [ ] 9.6 Run `node openspec/governance/check-project-reqs.mjs`; must PASS with 0 duplicate/orphan/unregistered/reusedRetired.
- [ ] 9.7 Run `node openspec/governance/check-project-specs.mjs`; must PASS with 0 deltaHeaderInMain/missingPurpose/missingRequirements/missingReqHeader.
- [ ] 9.8 Run `openspec validate experiment-auto-runner --strict`; must PASS after all post-apply spec/task corrections.
