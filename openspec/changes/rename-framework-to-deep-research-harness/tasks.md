## 0. Plan Review and Baseline

- [x] 0.1 `openspec-feedback:plan-review` - Reviewed the proposal, design, all listed delta specs, current-root vocabulary, compatibility boundaries, and verification plan on 2026-08-05 before the first target edit. No actionable findings; requirement ownership, reader-visible outcomes, writer/reader contracts, and deterministic verification boundaries are coherent.
- [x] 0.2 Ran `node openspec/governance/check-verification-routing.mjs --change rename-framework-to-deep-research-harness --mode plan` and `node openspec/governance/check-project-reqs.mjs` on 2026-08-05: both passed. (ACR-001, BUM-005, CMI-009, FRE-001, WDC-004)
- [x] 0.3 Created `reference-inventory.md` on 2026-08-05. It records the scan boundary, canonical migration classes, the explicit source-alias and legacy-entry compatibility forms, excluded historical/runtime content, and the residual-scan done condition. (WDC-001, WDC-004, BUM-005)

## 1. Canonical Harness Root

- [x] 1.1 Moved the 229 tracked source files to `DEEP_RESEARCH_HARNESS/` and created the sole relative `DPT_FRAMEWORK -> DEEP_RESEARCH_HARNESS` symlink. `readlink` and both `realpath` values verify one physical source tree; no bundle prefixes or runtime state changed. (FRE-001, WDC-001, WDC-006)
- [x] 1.2 Update ESM imports, executable entrypoints, CLI/path diagnostics, and root/Harness operating documents so new authoring uses the canonical Harness root while legacy invocations resolve through the symlink. Preserve every runtime write under an explicit current run bundle root. (FRE-001, WDC-001, WDC-004, WDC-006)
- [x] 1.3 Update the root glossary, ADR discovery, root/Harness `AGENTS.md`, `CLAUDE.md`, and README routes to use Deep Research Harness, run bundle, research run, current run bundle, and current run bundle root consistently. Preserve the existing selected-entry routes and their non-authority boundaries. (ACR-001, ACR-002, ACR-004, RUE-006)
- [x] 1.4 Normalize current non-archive specifications, guidance, tests, experiment instructions, and command examples from the inventory to canonical Harness coordinates. Retain `DPT_FRAMEWORK/` only where it explicitly documents or tests the compatibility alias; do not edit existing run bundles or archived material. (WDC-001, WDC-004, WDC-005, EXA-002)

## 2. Bundle Entry and Continuation Compatibility

- [x] 2.1 Replace the new-bundle template with `BUNDLE_ENTRY.md.tmpl` and update production instantiation to render `BUNDLE_ENTRY.md` with a coordinate calculated from the physical canonical Harness root. Resolve the created bundle before stdout handoff, so an explicit relative `--target-dir` yields its canonical absolute current-run-bundle-root coordinate. Update `BUNDLE_MAP.md`'s rendered `framework_root` coordinate under the same rule; do not emit `RUN_BUNDLE.md` for a newly created production bundle. (BUM-005, CMI-009, WDC-004)
- [x] 2.2 Update the disposable-bundle creator and its shared template to render the same `BUNDLE_ENTRY.md` and canonical physical-Harness coordinate contract for non-sibling and relative targets. Resolve the created `dpt_disp_*` bundle before stdout handoff, without adding mutable state or a second Harness copy. (BUM-005, EXS-004, WDC-004)
- [x] 2.3 Update `inspect-bundle.mjs` and `engine/helpers/file-observability.mjs` so a new `BUNDLE_ENTRY.md` is expected, legacy `RUN_BUNDLE.md` is bounded non-authoritative compatibility, and a fresh bundle receives neither a stale missing-`RUN_BUNDLE.md` warning nor an unexpected-entry diagnostic. Make inspector source-root/containment diagnostics identify the canonical Harness root rather than treating the legacy alias as normal source identity. Keep `BUNDLE_MAP.md` passive navigation and leave reentry's map-only diagnostics outside the entry-selection authority. (BUM-005, WDC-001)
- [x] 2.4 Update existing-bundle routing and `continue-run-bundle.md` to accept only an explicitly supplied, reachable bundle and apply `BUNDLE_ENTRY.md` -> legacy `RUN_BUNDLE.md` -> `BUNDLE_MAP.md` precedence. Resolve the selected directory to the canonical absolute current run bundle root before handoff; never scan or infer it from cwd, chat, or chronology. (RUE-006, ACS-005, WDC-004)
- [x] 2.5 Preserve `BUNDLE_MAP.md` as passive navigation and preserve historical bundle contents. Make an absent entry card/map an honest boundary failure rather than a migration or discovery path. (BUM-005, ACS-005)

## 3. Test and Experiment Surfaces

- [x] 3.1 Migrate the fixture source coordinate to `tests/fixtures/DEEP_RESEARCH_HARNESS/` and update fixture generation, test imports, and paths without adding an independently authored legacy fixture tree. Refresh its copied `inspect-bundle.mjs` with the canonical `BUNDLE_ENTRY.md` / legacy `RUN_BUNDLE.md` reader contract, then exercise that fixture-backed inspector. (TEF-001, BUM-005, WDC-005)
- [x] 3.2 Update Autorun Supervisor, experiment helpers, playbooks, containment diagnostics, and runner-facing documentation so they use the canonical Harness source root and recognize the legacy alias only as the same source identity. Preserve the prohibition on copying/linking source trees into `.exp-bundles/`. (EXA-002, WDC-001, WDC-005)
- [x] 3.3 Update the existing production/disposable creator integrations, `inspect-bundle` integration, file-observability unit test, continuation-document contract, context-routing contract, command-document contract, and version-management regression. Cover new-card/no-`RUN_BUNDLE.md` output, legacy entry compatibility, stale-reader diagnostics, canonical physical-Harness coordinates through both command paths, and relative-target absolute-root stdout. (BUM-005, CMI-009, EXS-004, RUE-006, ACS-005, ACR-001, VEM-001, WDC-004)
- [x] 3.4 Add `tests/integration/deep-research-harness-entry-contract.test.mjs` for canonical-root/symlink identity, static documentation routes, new entry rendering, legacy entry precedence, and explicit current-run-bundle-root negative cases. (ACR-001, ACR-002, BUM-005, RUE-006, CMI-009, ACS-005, EXS-004, WDC-004)
- [x] 3.5 Add `tests/e2e/deep-research-harness-migration.test.mjs` to create and inspect a temporary bundle through canonical and legacy Harness coordinates, prove one source tree and bundle-root-only runtime writes, creator absolute-root handoffs, and fixture/Autorun/release-path compatibility at the deterministic boundary. (FRE-001, WDC-001, WDC-004, WDC-005, EXA-002, TEF-001, VEM-001, VEM-003)

## 4. Release and Verification

- [x] 4.1 Add the proposal-declared `v0.73` CHANGELOG entry and update `DEEP_RESEARCH_HARNESS/RUN.md` to the identical banner version. Do not leave a second changelog authority under either Harness path. (VEM-001, VEM-002, VEM-003, VEM-004)
- [x] 4.2 Run the focused unit, integration, and deterministic-E2E assets from `verification-plan.yaml`, plus affected canonical/legacy CLI and fixture regressions. Record results as deterministic contract evidence only; do not claim real-Agent behavior. (ACR-004, BUM-005, RUE-006, CMI-009, FRE-001, ACS-005, EXA-002, EXS-004, TEF-001, WDC-005)
- [x] 4.3 Run `node openspec/governance/check-verification-routing.mjs --change rename-framework-to-deep-research-harness --mode assets`, `openspec validate rename-framework-to-deep-research-harness --type change --strict`, `node openspec/governance/check-project-reqs.mjs`, `node openspec/governance/check-project-specs.mjs`, and `git diff --check`; resolve only change-scoped failures and record unrelated pre-existing failures separately.
- [ ] 4.4 `openspec-feedback:closeout-review` - Review the implementation diff, compatibility evidence, verification routes, delta/main-spec merge readiness, and release notes before archive. Record every actionable finding as a normal pending task with its requirement ID, authoritative owner, smallest repair, and observable done condition.
