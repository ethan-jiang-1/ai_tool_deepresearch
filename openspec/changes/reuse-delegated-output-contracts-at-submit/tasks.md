## 1. Pre-Target Evidence Gates

- [ ] 1.1 Create change-root `implementation-evidence.md` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007; done when it records every pre-target command, native artifact reference, result boundary, `PASS`/`FAIL`/`NOT_RUN`, and residual risk without treating console summaries as verdict evidence.
- [ ] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change reuse-delegated-output-contracts-at-submit --mode plan` for AGQ-013, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007 before any target edit; done when all ten claims validate against the selected unit, integration, and Agent-flow routes.
- [ ] 1.3 Baseline DEW-005 and RWG-018 through the current production claim -> actor surfaces -> dry-submit -> formal submit -> Wave inspect path in a fresh disposable bundle; done when evidence shows whether a deterministically malformed exact required output can pass current submit and fail the later shared Wave fact, with no hand-written ledger, receipt, trace, or claimed PASS.
- [ ] 1.4 Spike the DEW-005/RWG-018 Node 20 reader policy without editing production or test targets; done when `implementation-evidence.md` records lexical escape, stable symlink/realpath escape, regular-file/special-file, same-handle `fstat + bounded read`, oversize/growth, UTF-8/BOM, hardlink, and concurrent-replacement observations on supported platforms.
- [ ] 1.5 Audit current legitimate Wave0/Wave1 artifact sizes and file types for DEW-005 and RWG-018; done when native sample paths/counts/maxima justify the 4 MiB regular-file cap or the design/spec/tasks are revised before target edits.
- [ ] 1.6 Run a pre-change production real Subject Agent/Sub-agent baseline for DEW-014, SNC-006, and RWG-018 through an existing canonical real-Agent path when available; done when native output/receipt/dry-submit/submit/Wave timing is retained, or an honest native `NOT_RUN` and capability boundary is recorded without fixture substitution or an Agent-frequency claim.
- [ ] 1.7 Review the preventive invariant for AGQ-013, DEW-005/014, SNC-006, and RWG-018 against tasks 1.3-1.6; done when implementation is explicitly authorized to continue, or target edits stop and proposal/design/specs/tasks are updated because the evidence shows version skew only, post-submit Phase ownership, ambiguous assignments, infeasible safe reads, or a false invariant.

## 2. Failing Focused Verification

- [ ] 2.1 Add failing DEW-004/005/014 and AGQ-013 target-level module cases to `tests/engine/helpers/direct-output-contract.test.mjs`; done when supported mode/receipt assignment forms, closed reserved-key selector rejection, unsafe/duplicate/conflicting paths, absolute/empty/dot/traversal/backslash/non-canonical path rejection, 4 MiB plus one bounded reads, shorter EOF, UTF-8/BOM, symlink/realpath/special files, tolerant Wave facts, root short-circuiting, and three repair scopes each fail before implementation.
- [ ] 2.2 Add failing DEW-004/005/009 and SNC-006 schema/projection cases to `tests/schema/contracts/work-unit.test.mjs` and focused envelope tests; done when current marker/closed IDs/unique tuples/exact equality/genuine legacy absence/partial-marker rejection, per-required `contains`/path-role constraints, and schema-default/Result-Starter self-validity for non-empty and empty contracts are covered.
- [ ] 2.3 Add failing DEW-005/013/014/015, AGO-007, and SNC-006 lifecycle cases to `tests/engine/work-unit-submit.test.mjs` and `tests/engine/work-unit-lifecycle.test.mjs`; done when fresh reads, post-dry-submit drift, complete candidate/receipt/output/source/cache/Engine root-to-scope mapping, work_done-sensitive closed recommended_action precedence, legacy/current roles, supplementary reuse, replay/recovery, manifest/beacon drift, and zero-mutation rejection are observable.
- [ ] 2.4 Add failing AGQ-013/WAI-007 enqueue/storage/repair cases to `tests/integration/cli/operate-queue-validation.test.mjs` and DEW-004/005/015/SNC-006 cases to `tests/integration/cli/operate-work-unit.test.mjs`; done when historical missing-mode queue rows remain loadable, new enqueue/claim rejects missing/unknown/mismatched mode and direct selectors, valid unclaimed mode repair derives canonical receipts while preserving ID/location/created_at/Topic/producer/kind/delegation/contract fields and emits one audit event, already-classified/unknown-mode/wrong-producer/in-flight/terminal repair has zero queue mutation and no success event, a mixed-validity batch leaves zero partial allocation, non-selector customization survives, and later candidate checkpoints preserve their boundaries.
- [ ] 2.5 Add failing RWG-018 adapter-parity cases to `tests/integration/cli/check-gate-wave0-complete.test.mjs` and `tests/integration/cli/check-gate-wave1-complete.test.mjs`; done when direct root codes/coordinates match candidate validation while `source_url_present`, count, provenance, queue, return-map, depth, and phase rules remain Wave-only.
- [ ] 2.6 Add failing AGQ-013, DEW-004/014, SNC-006, RWG-018, and WAI-007 guidance coverage in `tests/integration/md/work-unit-direct-output-guidance.test.mjs`; done when generated task plus the four whitelisted production Markdown owners expose assignment mode, the narrow missing-mode repair command, and the new checkpoint/replacement boundary while all non-whitelisted phase/role docs remain free of duplicated direct-contract inventories.

## 3. Assignment Contract And Projection

- [ ] 3.1 Implement AGQ-013/WAI-007 operation-specific enqueue/producer admission and `repair --queue-item-id --set-assignment-mode` so primary `topic_deepening` emits/derives `payload.assignment_mode: primary` plus the exact pair and supplementary emits/derives `assignment_mode: supplementary` plus empty receipts; done when generic queue storage remains backward-readable, repair accepts only genuinely mode-absent cards, preserves immutable card fields, changes only mode/receipts/updated_at plus primary paired write inclusion with one save and emits one structured audit event, already-classified/wrong-producer/in-flight/terminal/invalid repair has zero mutation, semantic replacement uses a fresh globally unused queue ID while preserving mode/receipts, and no suffix/prose/`writes_to` inference remains.
- [ ] 3.2 Implement DEW-004 and SNC-006 literal `assignment_contract_version: work-unit.assignment.v1`, the three direct contract IDs, canonical roles, `required_outputs[]`, and strict Zod schemas; done when there is no separate resolver_version, and refinements reject unknown/partial markers, unsafe/duplicate paths, and conflicting tuples.
- [ ] 3.3 Implement DEW-004's Engine-owned full-batch resolver preflight from registered kind, hash-bound queue snapshot, canonical Topic UID/current slug, assignment mode, receipts, and index marker; done when all matrix rows resolve, one invalid later candidate rejects the planned batch before its first work-ID/batch/queue/envelope mutation, and diagnostics name that queue item.
- [ ] 3.4 Implement DEW-004/009 selector integrity by strictly validating existing non-selector kind `output_contract` customization while recursively rejecting the closed reserved keys `required_outputs`, `direct_contract`, `direct_contract_id`, `assignment_contract_version`, `resolver_version`, and `contract_id` at queue-item root/payload/output_contract; done when sanctioned customization still claims, other unknown payload keys are ignored as resolver inputs, the merged contract reconstructs identically, and no actor/Markdown/result field can select a direct contract.
- [ ] 3.5 Implement DEW-004 and SNC-006 binding storage/projection: record the marker top-level in index/manifest/beacon, project the full strict contract to manifest/beacon/task/checklist, constrain each required result path-role once with draft-2020-12 `contains`/min/max plus path-to-role rules, prepopulate matching schema default/Result Starter pairs (or `[]` for empty contracts), expose no actor-fillable marker/contract fields, and reconstruct expected equality at load; done when structural projection tests and the candidate-result Zod shape accept every starter's declarations without claiming submit readiness before actor receipt/cache/source facts, index does not duplicate the contract, and projections do not become voters.
- [ ] 3.6 Implement AGO-007 and DEW-005 role compatibility so current-marker required outputs accept only exact canonical roles while genuinely marker-absent attempts retain the narrow legacy Wave1 `other -> canonical role` normalization; done when unknown/conflicting/partial markers fail closed and never select legacy.
- [ ] 3.7 Implement DEW-004 supplementary lineage projection for current Wave1 attempts with empty `required_outputs`; done when generated guidance permits eligible prior submitted `evidence_summary` source refs, does not require prior pair redeclaration, and preserves existing cache/source/provenance validation.

## 4. Target-Level Direct-Output Module

- [ ] 4.1 Implement DEW-005 and RWG-018's target-level module using Node 20 built-ins; done when adapters call one interface, regular files are opened once, `fstat` plus `4 MiB + 1` read use the same handle, shorter EOF is accepted, stable symlink/realpath/special/observed-oversize/read failures fail closed, and private reader helpers are not a second test/caller interface.
- [ ] 4.2 Implement DEW-005/RWG-018 fatal UTF-8 decoding and single-BOM policy inside the target-level module; done when both adapters receive roots produced from the same internal decode policy and invalid UTF-8 or multiple/embedded BOM content cannot produce divergent verdicts without adapters accessing bytes.
- [ ] 4.3 Implement RWG-018 `wave0.source-metadata-array.v1` inside the target-level module using top-level YAML array parsing plus `ReferenceMetadataArraySchema`; done when parse/schema prerequisites short-circuit dependent roots and source count remains absent from the interface.
- [ ] 4.4 Implement RWG-018 `wave1.evidence-summary.v1` and `wave1.question-list.v1` inside the target-level module; done when existing tolerant heading semantics are preserved, every required section is non-empty, and URL/return-map/provenance/quality judgment is absent.
- [ ] 4.5 Implement DEW-005/RWG-018 neutral bounded roots with stable code, contract ID, coordinate, expected/observed summary, and `root_class: semantic_content|contract_integrity`; done when missing target and parse/schema/semantic absence map to semantic_content, unsafe/read/oversize/invalid-UTF8 map to contract_integrity, the module cannot emit mechanical/repair_scope/recommended_action/primary_root_code, raw/decoded bytes and parser exceptions are not returned, and independent roots avoid prerequisite cascades.

## 5. Candidate Checkpoints And Lifecycle

- [ ] 5.1 Implement DEW-005/009 and SNC-006 candidate contract reconstruction before content reads in dry-submit, timeout-preflight, first normal submit, and eligible first late-submit; done when snapshot hash/version/Topic/manifest/beacon drift fails before success mutation with exact repair coordinates.
- [ ] 5.2 Implement DEW-005/014's work-unit adapter from verified required outputs to the target-level module and existing `violations[]`; done when the adapter cannot access/reparse bytes, combines neutral roots with candidate/receipt/output/source/cache/Engine facts, assigns every root exactly one provenance-based scope from the shared matrix, and returns exact surface, `write_to`, rerun, and one closed recommended_action without adding a CLI.
- [ ] 5.3 Implement DEW-005 and SNC-006 fresh-snapshot semantics for every predictive or first-acceptance invocation; done when formal submit and first late-submit reject invalid bytes written after an earlier dry-submit/timeout-preflight PASS and never consume cached bytes or mtimes as authorization.
- [ ] 5.4 Preserve DEW-005/013 replay semantics for same-result duplicate submit, already-accepted late-submit replay, and `recover-declaration`; done when they verify recorded hashes/durable postconditions without live direct reacceptance, new acceptance timestamps, artifact hashes, or changed queue/ledger identity.
- [ ] 5.5 Preserve DEW-005/013 mutation ordering and failure atomicity; done when direct failures occur before queue/index/status/result/receipt/cache/transaction/ledger success mutation and only existing diagnostic trace/log side effects remain permitted.
- [ ] 5.6 Implement DEW-014/015 root-to-action projection with one Engine-owned closed candidate schema for recommended_action plus primary_root_code and explicit timeout mapping; done when integrity-over-semantic-over-mechanical plus work_done choose and validate one action, submit pairs with null code, rejection pairs with the first stable code in the winning scope, formal rejection and timeout reuse the same owner, timeout returns the exact projection when evaluated or null otherwise and maps return_to_actor to actor-owned repair/fail_and_replace to block, semantic closure names `operate-work-unit fail --reason semantic_contract:<primary_root_code>` plus fresh-ID enqueue without automatic retry, the projection is non-persistent, and abandon is not a competing semantic route.

## 6. Wave Adapter Reuse And Simplification

- [ ] 6.1 Implement RWG-018 Wave0 adaptation to the target-level module and remove inline source YAML existence/read/direct interpretation; done when one read root maps to the existing earliest file rule, masks schema symptoms, and inspect/formal Gate retain source count and all phase-wide checks without a second YAML/schema verdict path.
- [ ] 6.2 Implement RWG-018 Wave1 adaptation to the target-level module and remove duplicate admitted-target existence/read plus inline Key Findings/question-section interpretations; done when one read root maps to the existing earliest file rule and inspect/formal Gate preserve tolerant presentation and all Wave-only URL/return-map/depth/provenance/phase rules.
- [ ] 6.3 Audit RWG-018 and DEW-005 for one-truth-path net simplification; done when repository search finds one target-level module implementation for each admitted direct fact, candidate and Wave adapters contain only target selection/projection concerns, and no generic linter, registry, dynamic import, path map, or artifact-hash authority was introduced.

## 7. Agent-Facing Contract And Real-Agent Canary

- [ ] 7.1 Implement DEW-004/005/014 and SNC-006 generated `task.md` projection in `work-unit-envelope.mjs`; done when actors see exact required path/role/direct-contract facts, verify writes before `work_done`, and Phase Agents are told to repair/rerun the same dry-submit or replace semantic work without duplicating artifact field/heading inventories.
- [ ] 7.2 Update AGQ-013, DEW-005/014, SNC-006, and WAI-007 only in `phase-wave1.md`, `shared-subagent-protocol.md`, `shared-schemas.md`, and `COMMANDS.md`; done when primary/supplementary cards author assignment_mode, mode-absent unclaimed cards point to the exact narrow repair command without inference, semantic replacement preserves mode/receipts under a fresh queue ID, dry-submit's predictive role is accurate, formal submit remains the delegated success owner, Wave remains phase-wide authority, and every sentence maps to the design whitelist.
- [ ] 7.3 Add DEW-014/015 and SNC-006 `experiments_playbook/exp_evidence-extraction/case-164-heavy-direct-output-candidate-contract.md`, update the suite README and register the exact path once in `PLAYBOOK_MANIFEST.md`; done when V2 frontmatter declares setup-only Topic/queue prerequisites ending before claim, independent `subject_execution: real_agent`, `external_calls: real`, one Subject Agent across three bounded turns, two required real child-actor turns, durable Subject/child/dry-submit/hash evidence roles, trace authority, a narrow non-quality claim, and honest native `NOT_RUN`, with fixtures forbidden from writing actor result/receipt/output/source/cache/ledger/trace.
- [ ] 7.4 Execute DEW-014/015 and SNC-006 case-164 through canonical Agent Experiment Autorun; done only when turn 1 retains the real first child's searched/fetched source/cache, controlled semantic fault, work_done receipt and native dry-submit JSON; the harness hashes first canonical outputs before turn 2; turn 2 consumes that rejection, runs non-retry fail plus fresh-ID same-primary enqueue/claim and pauses with unchanged first hashes; turn 3 invokes the independently searching/fetching/caching second child and submits only its valid replacement. A deterministic postcheck SHALL derive required `trace_jsonl` checks from native JSON/transcripts/refs/hashes without inventing dry-submit or lifecycle events; unavailable Subject/child/search/fetch execution produces incomplete `NOT_RUN`, never PASS.

## 8. Focused And Full Verification

- [ ] 8.1 Verify DEW-004/005/014, AGQ-013, SNC-006, and RWG-018 with `node --test tests/engine/helpers/direct-output-contract.test.mjs tests/schema/contracts/work-unit.test.mjs tests/engine/work-unit-lifecycle.test.mjs tests/engine/work-unit-submit.test.mjs`; done when resolver, schema, reader, evaluator, projection, compatibility, lifecycle, and mutation-boundary tests pass.
- [ ] 8.2 Verify AGQ-013, DEW-005/013/015, AGO-007, SNC-006, and WAI-007 with `node --test tests/integration/cli/operate-queue-validation.test.mjs tests/integration/cli/operate-work-unit.test.mjs`; done when real CLI enqueue/unclaimed-mode-repair/claim/dry-submit/timeout/submit/late/replay/recovery paths pass over temporary bundles.
- [ ] 8.3 Verify RWG-018 with `node --test tests/integration/cli/check-gate-wave0-complete.test.mjs tests/integration/cli/check-gate-wave1-complete.test.mjs`; done when shared direct roots and unchanged Wave-only rules pass in both inspect and formal Gate paths.
- [ ] 8.4 Verify DEW-004/005/014, SNC-006, and RWG-018 Markdown/package integration with `node --test tests/integration/md/work-unit-direct-output-guidance.test.mjs` and `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`; done when generated/static guidance parity and the shipped workflow package pass.
- [ ] 8.5 Run the complete AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007 regression surface with `npm test`; done when all repository tests pass or every pre-existing unrelated failure is recorded with a reproducible focused command and excluded from the ten verification claims.
- [ ] 8.6 Audit AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018 against non-goals; done when the diff has no dependency, TypeScript/Python, generic linter/registry, queue-authored direct/resolver contract ID, artifact hash, migration, Phase-owned candidate rule, hidden retry, auto repair, or hand-written runtime authority.

## 9. Release, Evidence, And Governance

- [ ] 9.1 Update `CHANGELOG.md` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007 as `v0.38`; done when the newest concise entry describes assignment-bound candidate direct contracts, narrow unclaimed assignment-mode repair, and shared Wave evaluation without claiming byte immutability or universal real-Agent compliance.
- [ ] 9.2 Synchronize the `DPT_FRAMEWORK/RUN.md` version banner/current-release metadata to `v0.38` for DEW-005 and SNC-006; done when it matches the newest CHANGELOG entry and contains no unrelated functional documentation expansion.
- [ ] 9.3 Complete `implementation-evidence.md` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007 across all ten verification-plan claims; done when each claim maps to executed native authority, deterministic evidence is separated from real-Agent evidence, case-164 retains its Subject/child/dry-submit/hash evidence and exact `PASS`/`NOT_RUN`, and residual hardlink/race/snapshot-only risks remain visible.
- [ ] 9.4 Run the production Markdown whitelist audit for AGQ-013, DEW-014, SNC-006, and RWG-018; done when `git diff --name-only` shows Agent-facing behavior Markdown only from the four-file whitelist, every hunk maps to the approved sections, other phase/role docs are unchanged, and `rg` finds no new `lint-agent-output`, `--schema`, `PATH_FORMAT_MAP`, `all Agent outputs`, or `all Markdown` product language.
- [ ] 9.5 Run `node openspec/governance/check-verification-routing.mjs --change reuse-delegated-output-contracts-at-submit --mode assets` for DEW-005/014/015, SNC-006, and RWG-018; done when all selected JS assets and the DEW-014/015/SNC-006 case-164 playbook exist in canonical boundaries, case-164 validates, and its manifest identity occurs exactly once without claiming RWG Agent-flow proof.
- [ ] 9.6 Run `node openspec/governance/check-project-reqs.mjs` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007; done with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired violations and no unnecessary new requirement ID.
- [ ] 9.7 Run `node openspec/governance/check-project-specs.mjs` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007; done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader violations.
- [ ] 9.8 Run `openspec validate reuse-delegated-output-contracts-at-submit --strict` and `git diff --check` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, RWG-018, and WAI-007; done when the complete change validates, the diff has no whitespace errors, and final review confirms current attempts never fall back to legacy or move phase-wide authority into submit.
