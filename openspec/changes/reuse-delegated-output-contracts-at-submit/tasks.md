## 1. Pre-Target Evidence Gates

- [ ] 1.1 Create change-root `implementation-evidence.md` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018; done when it records every pre-target command, native artifact reference, result boundary, `PASS`/`FAIL`/`NOT_RUN`, and residual risk without treating console summaries as verdict evidence.
- [ ] 1.2 Run `node openspec/governance/check-verification-routing.mjs --change reuse-delegated-output-contracts-at-submit --mode plan` for DEW-004/005/009/013/014/015, SNC-006, and RWG-018 before any target edit; done when all nine claims validate against the selected unit, integration, and Agent-flow routes.
- [ ] 1.3 Baseline DEW-005 and RWG-018 through the current production claim -> actor surfaces -> dry-submit -> formal submit -> Wave inspect path in a fresh disposable bundle; done when evidence shows whether a deterministically malformed exact required output can pass current submit and fail the later shared Wave fact, with no hand-written ledger, receipt, trace, or claimed PASS.
- [ ] 1.4 Spike the DEW-005/RWG-018 Node 20 reader policy without editing production or test targets; done when `implementation-evidence.md` records lexical escape, stable symlink/realpath escape, regular-file/special-file, same-handle `fstat + bounded read`, oversize/growth, UTF-8/BOM, hardlink, and concurrent-replacement observations on supported platforms.
- [ ] 1.5 Audit current legitimate Wave0/Wave1 artifact sizes and file types for DEW-005 and RWG-018; done when native sample paths/counts/maxima justify the 4 MiB regular-file cap or the design/spec/tasks are revised before target edits.
- [ ] 1.6 Run a current-version real Subject Agent/Sub-agent baseline for DEW-014, SNC-006, and RWG-018 through an existing canonical real-Agent path when available; done when native output/receipt/dry-submit/submit/Wave timing is retained, or an honest native `NOT_RUN` and capability boundary is recorded without fixture substitution or an Agent-frequency claim.
- [ ] 1.7 Review the preventive invariant for AGQ-013, DEW-005/014, SNC-006, and RWG-018 against tasks 1.3-1.6; done when implementation is explicitly authorized to continue, or target edits stop and proposal/design/specs/tasks are updated because the evidence shows version skew only, post-submit Phase ownership, ambiguous assignments, infeasible safe reads, or a false invariant.

## 2. Failing Focused Verification

- [ ] 2.1 Add failing DEW-004/005/014 and AGQ-013 resolver/evaluator/reader cases to `tests/engine/helpers/direct-output-contract.test.mjs`; done when supported assignment forms, selector rejection, unsafe/duplicate/conflicting paths, 4 MiB reads, UTF-8/BOM, symlink/realpath/special files, tolerant Wave facts, root short-circuiting, and repair scopes each fail before implementation.
- [ ] 2.2 Add failing DEW-004/005/009 and SNC-006 schema/projection cases to `tests/schema/contracts/work-unit.test.mjs` and focused envelope tests; done when current marker/closed IDs/unique tuples/exact equality/genuine legacy absence/partial-marker rejection and task/result-schema parity are covered.
- [ ] 2.3 Add failing DEW-005/013/015, AGO-007, and SNC-006 lifecycle cases to `tests/engine/work-unit-submit.test.mjs` and `tests/engine/work-unit-lifecycle.test.mjs`; done when fresh reads, post-dry-submit drift, legacy/current exact roles, supplementary reuse, replay, late-submit, recover-declaration, manifest/beacon drift, and zero-mutation rejection are observable.
- [ ] 2.4 Add failing DEW-005/015 and SNC-006 CLI integration cases to `tests/integration/cli/operate-work-unit.test.mjs`; done when claim/dry-submit/submit/late-submit/timeout-preflight/replay return stable root coordinates and preserve their specified mutation boundaries over temporary bundles.
- [ ] 2.5 Add failing RWG-018 adapter-parity cases to `tests/integration/cli/check-gate-wave0-complete.test.mjs` and `tests/integration/cli/check-gate-wave1-complete.test.mjs`; done when direct root codes/coordinates match candidate validation while `source_url_present`, count, provenance, queue, return-map, depth, and phase rules remain Wave-only.
- [ ] 2.6 Add failing DEW-004/014, SNC-006, and RWG-018 guidance coverage in `tests/integration/md/work-unit-direct-output-guidance.test.mjs`; done when generated task plus the three whitelisted production Markdown owners expose the new checkpoint boundary and all non-whitelisted phase/role docs remain free of duplicated direct-contract inventories.

## 3. Assignment Contract And Projection

- [ ] 3.1 Implement AGQ-013 primary/supplementary producer semantics so primary `topic_deepening` emits the exact paired `file:` receipts and explicit supplementary work emits an empty receipt set; done when current producers never encode a direct-contract ID and no suffix/prose/`writes_to` inference remains.
- [ ] 3.2 Implement DEW-004 and SNC-006 closed assignment constants and strict Zod schemas for `assignment_contract_version`, `required_outputs[]`, canonical roles, and the three direct contract IDs; done when `.refine()`/`.superRefine()` rejects unknown IDs, partial markers, unsafe/duplicate normalized paths, and conflicting path-role-contract tuples.
- [ ] 3.3 Implement DEW-004's Engine-owned resolver from registered kind, hash-bound queue snapshot, canonical recorded Topic UID/current slug, canonical `file:` receipts, and the index marker; done when the four assignment-matrix rows resolve exactly and unsupported or partial assignments fail before work-ID allocation or envelope/queue mutation.
- [ ] 3.4 Implement DEW-004/009 selector integrity by rejecting queue/payload `output_contract`, `direct_contract`, `required_outputs`, `assignment_contract_version`, and equivalent overrides for new claims; done when sanctioned producers still claim and no actor/Markdown/result field can select a contract.
- [ ] 3.5 Implement DEW-004 and SNC-006 binding storage/projection: record only the version marker in index, project the full strict contract to manifest/beacon/task/checklist/result schema, and reconstruct expected equality at load; done when index does not duplicate the contract and generated surfaces expose exact path-role-direct-contract facts without becoming voters.
- [ ] 3.6 Implement AGO-007 and DEW-005 role compatibility so current-marker required outputs accept only exact canonical roles while genuinely marker-absent attempts retain the narrow legacy Wave1 `other -> canonical role` normalization; done when unknown/conflicting/partial markers fail closed and never select legacy.
- [ ] 3.7 Implement DEW-004 supplementary lineage projection for current Wave1 attempts with empty `required_outputs`; done when generated guidance permits eligible prior submitted `evidence_summary` source refs, does not require prior pair redeclaration, and preserves existing cache/source/provenance validation.

## 4. Shared Reader And Direct Evaluator

- [ ] 4.1 Implement DEW-005 and RWG-018's bounded bundle reader using Node 20 built-ins; done when concrete bundle-relative regular files are opened once, `fstat` and capped read use the same handle, stable symlink/realpath/special/oversize/read failures fail closed, and diagnostics do not overclaim hardlink or race safety.
- [ ] 4.2 Implement DEW-005/RWG-018 fatal UTF-8 decoding and single-BOM policy inside the shared module; done when both adapters consume identical decoded bytes and invalid UTF-8 or multiple/embedded BOM content cannot produce divergent verdicts.
- [ ] 4.3 Implement RWG-018 `wave0.source-metadata-array.v1` through the neutral evaluator using top-level YAML array parsing plus `ReferenceMetadataArraySchema`; done when parse/schema prerequisites short-circuit dependent roots and source count remains absent from the interface.
- [ ] 4.4 Implement RWG-018 `wave1.evidence-summary.v1` and `wave1.question-list.v1` through the neutral evaluator; done when existing tolerant heading semantics are preserved, every required section is non-empty, and URL/return-map/provenance/quality judgment is absent.
- [ ] 4.5 Implement DEW-005/014 neutral bounded roots with stable code, contract ID, coordinate, expected/observed summary, and closed repair scope; done when parser exceptions/full artifact bytes are not leaked and independent roots can coexist without prerequisite cascades.

## 5. Candidate Checkpoints And Lifecycle

- [ ] 5.1 Implement DEW-005/009 and SNC-006 candidate contract reconstruction before content reads in dry-submit, timeout-preflight, first normal submit, and eligible first late-submit; done when snapshot hash/version/Topic/manifest/beacon drift fails before success mutation with exact repair coordinates.
- [ ] 5.2 Implement DEW-005's work-unit adapter from verified required outputs to fresh reader/evaluator results and existing `violations[]`; done when each root names the assigned surface, legal `write_to`, repair scope, and same dry-submit rerun command without adding a CLI.
- [ ] 5.3 Implement DEW-005 and SNC-006 fresh-snapshot semantics for every predictive or first-acceptance invocation; done when formal submit and first late-submit reject invalid bytes written after an earlier dry-submit/timeout-preflight PASS and never consume cached bytes or mtimes as authorization.
- [ ] 5.4 Preserve DEW-005/013 replay semantics for same-result duplicate submit, already-accepted late-submit replay, and `recover-declaration`; done when they verify recorded hashes/durable postconditions without live direct reacceptance, new acceptance timestamps, artifact hashes, or changed queue/ledger identity.
- [ ] 5.5 Preserve DEW-005/013 mutation ordering and failure atomicity; done when direct failures occur before queue/index/status/result/receipt/cache/transaction/ledger success mutation and only existing diagnostic trace/log side effects remain permitted.
- [ ] 5.6 Implement DEW-014/015 root-to-action diagnostics for mechanical, semantic-content, and contract-integrity failures; done when same-candidate mechanical repair is legal, post-`work_done` semantic failure names terminalize -> explicit enqueue -> new claim, and `fail`/`abandon` do not falsely advertise automatic requeue.

## 6. Wave Adapter Reuse And Simplification

- [ ] 6.1 Implement RWG-018 Wave0 adaptation to the shared reader/evaluator and remove the inline source YAML direct interpretation; done when inspect/formal Gate retain existing finding/hint shape, source count and all phase-wide checks, with no second YAML/schema verdict path.
- [ ] 6.2 Implement RWG-018 Wave1 adaptation to the shared reader/evaluator and remove inline Key Findings/question-section interpretations; done when inspect/formal Gate preserve tolerant presentation and all Wave-only URL/return-map/depth/provenance/phase rules.
- [ ] 6.3 Audit RWG-018 and DEW-005 for one-truth-path net simplification; done when repository search finds one implementation for each admitted direct fact, candidate and Wave adapters contain only target/projection concerns, and no generic linter, registry, dynamic import, path map, or artifact-hash authority was introduced.

## 7. Agent-Facing Contract And Real-Agent Canary

- [ ] 7.1 Implement DEW-004/005/014 and SNC-006 generated `task.md` projection in `work-unit-envelope.mjs`; done when actors see exact required path/role/direct-contract facts, verify writes before `work_done`, and Phase Agents are told to repair/rerun the same dry-submit or replace semantic work without duplicating artifact field/heading inventories.
- [ ] 7.2 Update DEW-005/014 and SNC-006 only in `DPT_FRAMEWORK/workflows/nodes/shared/shared-subagent-protocol.md`, `DPT_FRAMEWORK/workflows/nodes/shared/shared-schemas.md`, and `DPT_FRAMEWORK/COMMANDS.md`; done when dry-submit's predictive direct-shape role is accurate, formal submit remains the delegated success owner, Wave remains phase-wide authority, and every sentence maps to the design whitelist.
- [ ] 7.3 Add DEW-014/015, SNC-006, and RWG-018 `experiments_playbook/exp_evidence-extraction/case-164-heavy-direct-output-candidate-contract.md`, update the suite README and register the exact path once in `PLAYBOOK_MANIFEST.md`; done when V2 frontmatter declares setup-only real Sub-agent behavior, trace authority, a narrow non-quality claim, and honest native `NOT_RUN` without fixture substitution.
- [ ] 7.4 Execute DEW-014/015 and SNC-006 case-164 through canonical Agent Experiment Autorun with real Sub-agents; done only when native trace PASS proves first semantic rejection, no Phase-Agent content substitution, explicit terminalization/enqueue/new work ID, and second-actor acceptance, while `NOT_RUN` remains recorded as incomplete evidence rather than PASS.

## 8. Focused And Full Verification

- [ ] 8.1 Verify DEW-004/005/014, AGQ-013, SNC-006, and RWG-018 with `node --test tests/engine/helpers/direct-output-contract.test.mjs tests/schema/contracts/work-unit.test.mjs tests/engine/work-unit-lifecycle.test.mjs tests/engine/work-unit-submit.test.mjs`; done when resolver, schema, reader, evaluator, projection, compatibility, lifecycle, and mutation-boundary tests pass.
- [ ] 8.2 Verify DEW-005/013/015, AGO-007, and SNC-006 with `node --test tests/integration/cli/operate-work-unit.test.mjs`; done when real CLI claim/dry-submit/timeout/submit/late/replay/recovery paths pass over temporary bundles.
- [ ] 8.3 Verify RWG-018 with `node --test tests/integration/cli/check-gate-wave0-complete.test.mjs tests/integration/cli/check-gate-wave1-complete.test.mjs`; done when shared direct roots and unchanged Wave-only rules pass in both inspect and formal Gate paths.
- [ ] 8.4 Verify DEW-004/005/014, SNC-006, and RWG-018 Markdown/package integration with `node --test tests/integration/md/work-unit-direct-output-guidance.test.mjs` and `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`; done when generated/static guidance parity and the shipped workflow package pass.
- [ ] 8.5 Run the complete AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018 regression surface with `npm test`; done when all repository tests pass or every pre-existing unrelated failure is recorded with a reproducible focused command and excluded from the nine verification claims.
- [ ] 8.6 Audit AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018 against non-goals; done when the diff has no dependency, TypeScript/Python, generic linter/registry, queue-authored ID, artifact hash, migration, Phase-owned candidate rule, hidden retry, auto repair, or hand-written runtime authority.

## 9. Release, Evidence, And Governance

- [ ] 9.1 Update `CHANGELOG.md` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018 as `v0.38`; done when the newest concise entry describes assignment-bound candidate direct contracts and shared Wave evaluation without claiming byte immutability or universal real-Agent compliance.
- [ ] 9.2 Synchronize the `DPT_FRAMEWORK/RUN.md` version banner/current-release metadata to `v0.38` for DEW-005 and SNC-006; done when it matches the newest CHANGELOG entry and contains no unrelated functional documentation expansion.
- [ ] 9.3 Complete `implementation-evidence.md` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018 across all nine verification-plan claims; done when each claim maps to executed native authority, deterministic evidence is separated from real-Agent evidence, case-164 `PASS`/`NOT_RUN` is stated exactly, and residual hardlink/race/snapshot-only risks remain visible.
- [ ] 9.4 Run the production Markdown whitelist audit for DEW-014, SNC-006, and RWG-018; done when `git diff --name-only` shows Agent-facing behavior Markdown only from the three-file whitelist, every hunk maps to the approved sections, phase/role docs are unchanged, and `rg` finds no new `lint-agent-output`, `--schema`, `PATH_FORMAT_MAP`, `all Agent outputs`, or `all Markdown` product language.
- [ ] 9.5 Run `node openspec/governance/check-verification-routing.mjs --change reuse-delegated-output-contracts-at-submit --mode assets` for DEW-005/014/015, SNC-006, and RWG-018; done when all selected test/playbook assets exist in canonical boundaries, case-164 validates, and its manifest identity occurs exactly once.
- [ ] 9.6 Run `node openspec/governance/check-project-reqs.mjs` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018; done with 0 duplicate, 0 orphan, 0 unregistered, and 0 reusedRetired violations and no unnecessary new requirement ID.
- [ ] 9.7 Run `node openspec/governance/check-project-specs.mjs` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018; done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements, and 0 missingReqHeader violations.
- [ ] 9.8 Run `openspec validate reuse-delegated-output-contracts-at-submit --strict` and `git diff --check` for AGQ-013, AGO-007, DEW-004/005/009/013/014/015, SNC-006, and RWG-018; done when the complete change validates, the diff has no whitespace errors, and final review confirms current attempts never fall back to legacy or move phase-wide authority into submit.
