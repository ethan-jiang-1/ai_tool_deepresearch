## 1. Apply Entry And Planning Evidence

- [ ] 1.1 Complete the change-scoped plan review (`openspec-feedback:plan-review`) before the first target edit. Verify that WAI-004, RWG-002/003, and RWP-002 retain submitted evidence, Wave verdict, and Phase-flow ownership respectively; record any finding as an ordinary unchecked repair task with an owner and observable done condition.
- [ ] 1.2 @impl WAI-004, RWG-002, RWG-003, RWP-002: Run `node openspec/governance/check-verification-routing.mjs --change add-traceable-topic-focus-coverage --mode plan`, `openspec validate add-traceable-topic-focus-coverage --strict`, and `node openspec/governance/check-capability-discovery.mjs --change add-traceable-topic-focus-coverage` before target edits. Done when the planning artifacts and declared routes pass without target-code changes.
- [ ] 1.3 Run `$polish-openspec-change add-traceable-topic-focus-coverage` after all planning artifacts exist and resolve every fact-determined finding it reports. Done only when its final outcome is exactly `ready for apply`; ordinary validation commands do not substitute for that outcome.

## 2. Focus Coverage Direct-Fact Contract

- [ ] 2.1 @impl WAI-004: Extend the existing Wave1 depth-review parser/evaluator to accept an optional strict `focus_coverage` block with UID/current-round binding, unique commitments, mutually exclusive covered submitted-work refs or visible limited commitments, and the exact covered/partial/blocked outcome matrix. Done when absence preserves the no-focus baseline and no copied source/cache/profile/queue field becomes authority.
- [ ] 2.2 @impl WAI-004, RWG-002: Bind covered commitments only to current-round, reviewed, hash-valid submitted Wave1 rows for the same canonical Topic; require their paired index record to carry explicit `rerun_count` equal to the current profile, including explicit `0` for an initial run, and reject historical, legacy-unbound, unknown, mismatched, or unreviewed refs as direct roots. Done when the checker reports the earliest binding root and never derives coverage from HITL prose, filenames, or historical artifacts.
- [ ] 2.3 @impl RWG-002, RWG-003: Wire the focus-coverage evaluator into the existing Wave1 definition/inspect/formal-Gate path. Done when malformed/repairable coverage remains under the existing non-degradation-eligible depth-contract rule and masks any limit result; a valid partial/blocked outcome alone emits the definition-owned `focus_coverage_limit` required-floor rule, so the unchanged existing degradation predicate can partition it from all non-eligible roots; covered adds no P2 failure and no route, status, trace event, or controller is added.

## 3. Phase Guidance And Routed Verification

- [ ] 3.1 @impl RWP-002: Update `DEEP_RESEARCH_HARNESS/workflows/nodes/phases/phase-wave1.md` to derive minimal commitments from accepted current focus context, use only existing Wave1 queue/work-unit/submit operations for repairable work, and record a limitation only at an existing external/user-decision/missing-contract boundary. Done when it adds no HITL, queue kind, direct-search path, auto-rerun, or history relabelling.
- [ ] 3.2 @impl WAI-004, RWG-002, RWG-003: Add focused `unit` tests under `tests/` for focus-coverage structure, round/Topic/ref binding, outcome matrix, root masking, and the narrow existing eligibility predicate for a valid `focus_coverage_limit` finding. Done when they prove Engine contract only and use no fabricated trace or submitted evidence.
- [ ] 3.3 @impl WAI-004, RWG-002, RWG-003: Add `integration` tests under `tests/integration/` using real-shaped submitted authority plus depth-review files. Done when covered, partial, blocked, missing/no-focus, and repairable-invalid cases have exact clean/degraded/failed boundaries; valid partial/blocked appear as `focus_coverage_limit` while invalid coverage remains ineligible, without Agent semantic claims.
- [ ] 3.4 @impl WAI-004, RWG-002, RWG-003, RWP-002: Add a `deterministic_e2e` chain under `tests/e2e/` proving a rerun's historical submitted backing cannot close a current focus commitment, while a current submitted increment can. Done when the chain consumes normal Gate feedback and reaches no hand-written state or alternate route.
- [ ] 3.5 @impl RWP-002: Add or update a real `agent_flow_e2e` Playbook under `experiments_playbook/` for an accepted focus that proceeds through real incremental Wave1 work to native coverage outcome or honest limitation. Done when the independent Subject Agent owns semantic work and unavailable runtime finalizes only `NOT_RUN`, never fixture PASS.

## 4. Release, Evidence, And Closeout

- [ ] 4.1 @impl RWG-002, RWG-003, WAI-004, RWP-002: Update `CHANGELOG.md` and `DEEP_RESEARCH_HARNESS/RUN.md` to `v0.80`. Done when both give a concise behavior summary without presenting fixture or Agent-flow observations as broader proof.
- [ ] 4.2 @impl WAI-004, RWG-002, RWG-003, RWP-002: Record selected command results, retained native Agent-flow outcome/NOT_RUN coordinates, and residual risk in change-root implementation evidence. Run `node openspec/governance/check-verification-routing.mjs --change add-traceable-topic-focus-coverage --mode assets` after assets exist. Done when deterministic and Agent-behavior boundaries remain separate.
- [ ] 4.3 Sync accepted delta behavior to the three main specs through the supported spec-sync entry, then re-read and compare each synchronized requirement with its delta. Done when all existing requirements are preserved and main specs contain no delta headers or unregistered IDs.
- [ ] 4.4 Complete the change-scoped closeout review (`openspec-feedback:closeout-review`) after actual diff and selected evidence are available. Done when every finding is resolved or captured as an ordinary unchecked repair task; the marker is not treated as semantic proof or test success.
- [ ] 4.5 Run strict change validation, capability discovery, verification-routing asset validation, `node openspec/governance/check-project-reqs.mjs`, `node openspec/governance/check-project-specs.mjs`, and `git diff --check`. Done when every command passes before the governed archive transition.
