## 1. Verification Baseline

- [ ] 1.1 Baseline `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007` by running `node openspec/governance/check-verification-routing.mjs --change centralize-seed-topic-authoring-contracts --mode plan` before target edits; done when the six deterministic claims resolve to their canonical unit, integration, and deterministic E2E owners and the case-318 Agent-flow claim resolves to its registered playbook/trace owner.
- [ ] 1.2 Add failing focused `CTS-003` cases to `tests/engine/helpers/canonical-topic-state.test.mjs`; done when direction candidate schema, action/count mapping, duplicate UID targeting, canonical section replacement, and HITL1/layout restrictions are each covered without a live workspace claim.
- [ ] 1.3 Add failing `STM-001` parity cases to `tests/engine/helpers/canonical-topic-state.test.mjs`; done when the test extracts ordered appendix H2 headings, owning headings, and accepted one-time tokens from renderer output and the shared seed contract, while ignoring prose, YAML key order, and whitespace.
- [ ] 1.4 Replace the narrow existing direction tests with `tests/engine/helpers/rerun-direction.test.mjs` for `RTI-007`; done when matching/stale/future/legacy-unbound/invalid, add/supplement, heading suffix, optional bullet/bold labels, unknown extension, duplicate section/field, invalid action/count, empty required values, and semantic non-judgment are failing-before-change fixtures.
- [ ] 1.5 Add `tests/integration/cli/operate-topic-state-direction.test.mjs` for `CTS-003`; done when temporary bundles prove sanctioned rerun add/update/direction-only publication, HITL1 rejection, exact prepared recovery bytes, layout preservation, active-work blocking, and byte-identical `unchanged` behavior.
- [ ] 1.6 Add `tests/integration/md/seed-topic-authoring-contract.test.mjs` for `STM-001` and `RRM-003`; done when it proves both shared-node identities, focused phase `requires` reachability, static role cue parity, generated work-unit cue parity, and production workflow-package validity. Its duplicate-template assertion SHALL count the shared return-map contract's ordered multi-line five-label example plus section/token/ref-boundary signature, while permitting one-line five-field cues and authority disclaimers elsewhere; it SHALL not become a generic Markdown linter.
- [ ] 1.7 Extend `tests/integration/cli/check-gate-rerun-ready.test.mjs` for `RTI-007`; done when temporary bundles prove plan-bound seed scope, focused seed binding, prerequisite masking, exact current/future structural roots, future count-owner feedback, stale/legacy compatibility, and orphan-file exclusion.
- [ ] 1.8 Extend `tests/e2e/rerun-round-continuity.test.mjs` for `CTS-003` and `RTI-007`; done when fixture-labeled simulated Agent action input drives atomic add/update/direction-only candidates, accepted-workspace recovery, old-matching-direction non-bypass, profile-count synchronization, layout-only preservation, and downstream current-action classification through production deterministic paths without claiming real Agent judgment.

## 2. Focused Shared Authoring Contracts

- [ ] 2.1 Implement `STM-001` by adding `DPT_FRAMEWORK/workflows/nodes/shared/shared-seed-topic-authoring.md`; done when the valid shared node owns the complete initialization skeleton, appendix headings/tokens/responsibilities, direction fragment, Source-of-Record boundaries, and no-rationale/no-direction rule.
- [ ] 2.2 Implement `STM-001` reachability by adding `shared/shared-seed-topic-authoring` only to the actual loaded `requires` chains for `phase-seed-topics` and `phase-rerun`; done when both decision points reference it and Wave phases do not load the unrelated initialization/direction context.
- [ ] 2.3 Implement `RRM-003` by adding `DPT_FRAMEWORK/workflows/nodes/shared/shared-return-map-authoring.md`; done when it owns Wave-to-section responsibility, token lifecycle, five-field entry shape, concrete-first ref hierarchy, and same-inspect repair guidance.
- [ ] 2.4 Implement `RRM-003` reachability by adding `shared/shared-return-map-authoring` to `phase-seed-topics` and `phase-wave0/1/2` `requires`, while adding only static references to `subagent-dpt-source-intake`, `subagent-dpt-evidence-extractor`, and `subagent-dpt-topic-scout`; done when the real workflow loader reaches the shared contract at each content/backfill decision point and generated work units remain self-contained.
- [ ] 2.5 Implement `STM-001`, `RRM-003`, and `RTI-007` net simplification by replacing complete generic seed/direction/return-map examples in phase and overlapping shared-schema prose with focused references while retaining phase-specific authority, command order, Wave producer rules, and repair instructions; done when static coverage finds one complete definition per shared concern.
- [ ] 2.6 Implement `RRM-003` generated guidance by keeping work-unit `task.md` and spawn guidance self-contained with only the concise five-field cue and authority disclaimer; done when static parity aligns their field/boundary set with the shared return-map contract without runtime Markdown parsing.
- [ ] 2.7 Implement `STM-001` renderer parity by making the existing `renderNewSeedBody()` ordered arrays/markers the sole runtime structural manifest and aligning the shared Markdown mirror; done when initial and rerun-added seeds emit the accepted appendix headings/tokens and no third registry, second renderer, or runtime template parser exists.

## 3. Atomic Topic-State Direction Publication

- [ ] 3.1 Implement `CTS-003` direction candidate schemas in the existing topic-state input owner: nonnegative `rerun_count`, closed `add|supplement` action, and four non-empty guidance fields; done when Engine validates structure without generating or judging semantics.
- [ ] 3.2 Implement `CTS-003` action mapping by extending the existing ordered action list so sanctioned rerun `add_topic` requires `action: add`, `update_intent` requires `action: supplement`, and new `set_rerun_direction` accepts exact `topic_uid + direction` with `action: supplement`; done when HITL1 cannot carry direction, layout cannot invent it, and one UID cannot be targeted twice.
- [ ] 3.3 Implement `CTS-003` canonical direction rendering in the existing topic-state helper; done when add appends one section to the canonical skeleton, update/direction-only replace exactly one section, existing non-direction enrichment remains byte-preserved, and round-trip parsing agrees.
- [ ] 3.4 Implement `CTS-003` prepared/recover publication by staging plan, every explicitly touched current seed, and direction bytes in the existing hash-bound workspace; done when validation precedes prepared publication and recovery commits the exact accepted candidate bytes without widening the file set.
- [ ] 3.5 Implement `CTS-003` no-op/layout preservation; done when direction-only retains an unchanged plan-byte contract, `unchanged` requires registry and complete seed bytes to match, and layout-only operations preserve direction bytes.
- [ ] 3.6 Preserve `CTS-003` authorization, quiescence, path, commit-order, and recovery boundaries for the new action shape; done when normal/post-final rerun witnesses authorize it, active work blocks it, HITL1/arbitrary maintenance cannot invoke it, and no new CLI, workspace, state, or affected-topic receipt is introduced.
- [ ] 3.7 Update `RTI-007` phase-rerun retained-input guidance to form add/update/direction-only candidates from recorded rationale, run existing topic-state apply/recover, then increment profile count through its existing owner; done when post-commit direct seed writing, all-seed skip scans, and old-matching short-circuits are removed.
- [ ] 3.8 Implement `RTI-007` case-318 Agent-flow compatibility in `experiments_playbook/exp_wfn_rerun/case-318-heavy-rerun-direction-recovery.md` and `experiments_env/shared/run-iterative-interaction-subject.mjs`; done when the first real Subject Agent turn retains a direction-only candidate and invokes existing `operate-topic-state apply`, the adapter records that sanctioned apply from durable Subject evidence plus the resulting future direction/profile facts, and the second turn preserves the direction bytes, performs only the existing profile-count/Gate/handoff path, and never directly edits the seed direction or Engine authority.

## 4. Shared Direction Evaluation And Rerun Readiness

- [ ] 4.1 Implement `RTI-007` in focused `DPT_FRAMEWORK/engine/helpers/rerun-direction.mjs`, extracting the current resolver from the Wave evaluator and migrating/removing `tests/engine/helpers/wave-contract-evaluators-direction.test.mjs`; done when it collects all sections/fields, normalizes tolerated presentation, retains extensions, returns the five accepted states plus structural roots, and performs no I/O, persistence, routing, or semantic judgment.
- [ ] 4.2 Implement `CTS-003`/`RTI-007` helper ownership by making topic-state canonical rendering/round-trip assertion, Wave classification, and rerun-ready import that focused helper directly; done when topic-state does not depend on the broad Wave evaluator and no second direction parser remains.
- [ ] 4.3 Implement `RTI-007` downstream compatibility by migrating Wave classification and `checkRerunAddFullSynthesis()` to the shared result; done when matching and accepted legacy-unbound actions preserve compatibility, stale/future/invalid do not activate current actions, absence supplies no action, and the local action regex is deleted.
- [ ] 4.4 Implement `RTI-007` readiness ownership by adding `rerun_direction_structure` to the existing rerun-ready Gate definition and dispatch; done when no new Gate/CLI/state is created and the check uses `buildCanonicalTopicRegistryFact()` plus focused `evaluateCanonicalSeedBindings()` rather than a seed-directory scan or full topic-state inspection.
- [ ] 4.5 Implement `RTI-007` root-first masking and repair coordinates; done when profile/rationale/plan/seed-binding roots mask direction symptoms, duplicate/field roots precede dependent findings, and each finding names exact seed/section/field or profile count owner, legal `agent_action`, and the same Gate rerun command.
- [ ] 4.6 Implement `RTI-007` future/count synchronization by keeping a complete `future == profile + 1` direction failed at the existing profile-count owner, rejecting larger jumps, and repairing malformed future structure before count synchronization; done when only a structurally complete matching direction can pass this subcheck and stale/legacy content is not migration-blocked.

## 5. Focused Verification And Compatibility

- [ ] 5.1 Run `node --test tests/engine/helpers/canonical-topic-state.test.mjs tests/engine/helpers/rerun-direction.test.mjs`; done when `STM-001`, `CTS-003`, and focused `RTI-007` unit contracts pass.
- [ ] 5.2 Verify `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007` with `node --test tests/integration/cli/operate-topic-state-direction.test.mjs tests/integration/md/seed-topic-authoring-contract.test.mjs tests/integration/cli/check-gate-rerun-ready.test.mjs tests/integration/cli/rerun-round-continuity.test.mjs`; done when atomic topic-state publication, shared reachability/parity, and existing rerun-ready/continuity behavior pass over temporary bundles.
- [ ] 5.3 Verify `CTS-003` and `RTI-007` with `node --test tests/e2e/rerun-round-continuity.test.mjs`; done when labeled candidate input, atomic publication/recovery, count synchronization, and downstream classification pass through production deterministic code.
- [ ] 5.4 Verify `STM-001` and `RRM-003` package integration with `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs` and applicable workflow/Markdown regressions; done when the shipped package accepts both shared nodes and every focused `requires` edge.
- [ ] 5.5 Run the full `STM-001`/`CTS-003`/`RRM-003`/`RTI-007` regression surface with `npm test`; done when all repository `node:test` suites pass, or every pre-existing unrelated failure is recorded with a reproducible focused command and shown outside the six JS-led verification claims.
- [ ] 5.6 Audit `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007` against non-goals; done when the diff contains no heading rename, token reinjection, historical-bundle migration, semantic scoring, affected-topic state, template engine, generic linter, new CLI/Gate/trace/controller, auto repair, second renderer, or duplicate projection verdict.
- [ ] 5.7 Verify `RTI-007` by running `node DPT_FRAMEWORK/cli/validate-playbook.mjs experiments_playbook/exp_wfn_rerun/case-318-heavy-rerun-direction-recovery.md` and executing that case through the canonical Agent Experiment Autorun with a real authenticated Subject Agent; done when its native trace PASS proves the retained direction-only input/production apply, future-count crash window, byte-preserving recovery, same rerun-ready Gate, and Seed Topics handoff. `NOT_RUN`, a missing Subject Agent, fixture output, or Playbook-Agent substitution leaves this selected Agent-flow claim incomplete.

## 6. Release, Evidence, And Governance

- [ ] 6.1 Document `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007` in `CHANGELOG.md` and the `DPT_FRAMEWORK/RUN.md` release banner/summary as `v0.36`; done when both describe shared authoring and structural direction readiness without claiming real Agent semantic compliance.
- [ ] 6.2 Record `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007` commands/results in change-root `apply-evidence.md`; done when every verification-plan claim maps to executed proof, simulated Agent candidate input is distinguished from real Agent behavior, and case-318's native trace/health evidence is cited only for its narrow Agent-flow claim.
- [ ] 6.3 Update `_backlog/plans/seed-topic-projection-contract-repair.md` with `STM-001`/`CTS-003`/`RRM-003`/`RTI-007` Change B archive/version/commit evidence only after implementation verification; done when BUG-093/094 closure is factual and their moves remain deferred until archive succeeds.
- [ ] 6.4 Run `node openspec/governance/check-verification-routing.mjs --change centralize-seed-topic-authoring-contracts --mode assets` for `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007`; done when all seven declared proof assets exist in their canonical test-class boundaries.
- [ ] 6.5 Run `node openspec/governance/check-project-reqs.mjs` and `node openspec/governance/check-project-specs.mjs`; done with zero governance violations and traceability for `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007` without a new requirement ID.
- [ ] 6.6 Run `openspec validate centralize-seed-topic-authoring-contracts --strict` for `STM-001`, `CTS-003`, `RRM-003`, and `RTI-007`; done when the complete change validates with no error.
- [ ] 6.7 Review the final `STM-001`/`CTS-003`/`RRM-003`/`RTI-007` diff and closure boundary; done when apply evidence covers BUG-093/094 and the archive handoff moves both bugs plus the umbrella plan only after spec sync/archive succeeds.
