## 1. Apply Readiness And Evidence Baseline

- [ ] 1.1 Reconcile the selected Change B worktree boundary, current OpenSpec status, and P5 measurement record; explicitly preserve `.agents/skills/**` and `.claude/skills/**` as no-edit assets. (GCO-008)
- [ ] 1.2 Read current `openspec/operations/change-feedback-loop.md` and obtain `openspec instructions apply --change prune-and-automate-project-guidance --json`; retain configuration-delivered operation guidance as an Apply boundary. (GCO-008)
- [ ] 1.3 Complete the selected-change plan review (`openspec-feedback:plan-review`) after reading the proposal, delta, design, verification plan, and semantic-closure record; turn every actionable finding into an ordinary unchecked task with owner, smallest repair, and observable done condition. (GCO-008)
- [ ] 1.4 Run `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change prune-and-automate-project-guidance --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change prune-and-automate-project-guidance --mode plan`; repair and rerun every reported coordinate before target edits. (GCO-008)

## 2. Ownership And Reader-Boundary Review

- [ ] 2.1 Record a change-scoped before measurement for Charter, Context, control map, config, root/Harness entry adapters, and each candidate model/operation document; store the measurement and current semantic-duplication map in selected-change evidence. (GCO-008)
- [ ] 2.2 Apply the constitutional admission review to every Charter section; retain only the universal project core and record the canonical owner for every moved, compressed, or deleted meaning. (GCO-008)
- [ ] 2.3 Review root `CONTEXT.md`, `openspec/README.md`, `openspec/config.yaml`, and the execution-model canon as one ownership set; choose a single full owner for every repeated definition and leave compressed trigger pointers elsewhere. (GCO-008)
- [ ] 2.4 Review every current model and operation document for a bounded reader question, normal reasoning stop point, trigger, ordered legal steps, completion criterion, and authority limit; record retain/narrow/rename/delete decisions before editing any document. (GCO-008)
- [ ] 2.5 Reassess the semantic-precision, simple-reliable-control, and helper-oriented responsibility arguments against the concrete ownership map; record the reader question, direct Source of Record, net simplification, and Agent/Engine boundary in selected-change evidence. (GCO-008)

## 3. Apply The Guidance Simplification

- [ ] 3.1 Implement GCO-008 by narrowing `openspec/constitution/project-charter.md` to the approved mandatory core without changing accepted capability behavior or runtime authority. (GCO-008)
- [ ] 3.2 Implement GCO-008 by turning `openspec/README.md` into a demand-driven role/trigger router and preserving only role navigation needed to select the next authoritative surface. (GCO-008)
- [ ] 3.3 Implement GCO-008 by compressing root `CONTEXT.md` and the relevant `openspec/config.yaml` context/rules to their canonical-owner pointers while preserving all required Charter -> Context and `openspec instructions` operation routes. (GCO-008)
- [ ] 3.4 Apply only the evidence-backed retain/narrow/rename/delete decisions to model and operation documents; repair current links and do not rename or delete a document without its recorded replacement owner. (GCO-008)
- [ ] 3.5 Re-read all affected root and Harness entry surfaces after simplification; preserve their accepted routes, paired synchronization, research-entry behavior, and the exclusion of `.agents/skills/**` and `.claude/skills/**` edits. (GCO-008)

## 4. Deterministic Topology Coverage

- [ ] 4.1 Add or extend one focused `tests/integration/md/` topology contract for canonical current guidance paths, role frontmatter/authority coordinates, selected current internal Markdown links, duplicate mirrors, and bounded live old-path references. Its assertions must name the owned repair surface and must not judge prose quality. (GCO-008)
- [ ] 4.2 Update only the existing constitution, context-route, feedback-finalizer, verification-knowledge, and experiment-terminology regressions whose owned current-path assertions changed; do not duplicate selected-entry or lifecycle behavior contracts. (GCO-008)
- [ ] 4.3 Run the selected integration regressions, including the topology contract, evolution-direction governance, agent-context routing, research-entry routing, feedback-finalizer conformance, verification knowledge surfaces, and experiment terminology; record command results in selected-change evidence. (GCO-008)

## 5. Review, Sync, And Governed Archive

- [ ] 5.1 Record before/after entry measurements, deleted or compressed duplicate meanings, retained owner pointers, and residual risks; confirm that a smaller entry did not remove any accepted route. (GCO-008)
- [ ] 5.2 Run strict OpenSpec validation and all selected governance checks: capability discovery, taxonomy, requirement traceability, verification-routing assets mode, semantic-closure assets mode, and main-spec structure; repair and rerun reported coordinates. (GCO-008)
- [ ] 5.3 Review the selected semantic-closure record against the actual diff, then review the selected-change actual diff, artifacts, ownership evidence, and verification results; record every actionable closeout finding as an ordinary unchecked task. (GCO-008)
- [ ] 5.4 Complete the fresh no-finding closeout review (`openspec-feedback:closeout-review`) only after all ordinary closeout tasks are complete. (GCO-008)
- [ ] 5.5 Synchronize the approved GCO-008 delta to the main spec through the supported Agent-owned route, re-compare delta/main semantics, and rerun affected regressions and governance checks. (GCO-008)
- [ ] 5.6 Run `node openspec/governance/check-project-reqs.mjs --mode archive --change prune-and-automate-project-guidance` and `node openspec/governance/check-project-specs.mjs`; both must PASS before archive. (GCO-008)
- [ ] 5.7 Invoke `node openspec/governance/finalize-change-archive.mjs --change prune-and-automate-project-guidance` as the sole final archive transition; record its canonical archive coordinate and update the progressive tracker for P7 closure. (GCO-008)
