## 1. Apply Baseline And Focused Red Coverage

- [ ] 1.1 @impl SCO-002, PRP-002, PRP-005: Before any target edit, create change-root `apply-evidence.md`, record the scoped baseline, run `node openspec/governance/check-verification-routing.mjs --change make-pre-wave-readiness-feedback-direct --mode plan`, and run `openspec validate make-pre-wave-readiness-feedback-direct --strict`; done when the plan names all four proof classes, the active delta is valid, and the baseline distinguishes existing external behavior from deterministic contract evidence.
- [ ] 1.2 @impl SCO-002: Extend `tests/schema/contracts/profile.test.mjs` with failing candidate-count/ordinal cases for legacy absence, valid unavailable zero-count/no-URL and positive-count/final-URL branches, available positive counts only, cross-field contradictions, and forbidden unprobed metadata; done when the tests exercise `ProfileSchema` directly and do not invent a query/history surface.
- [ ] 1.3 @impl PRP-002, PRP-005: Extend `tests/integration/md/phase-hitl1-research-access.test.mjs` with failing assertions for the explicit recorded-decision -> existing status sync -> apply order, one search, ordered three-candidate bound, same-URL native/fallback sequence, no-legal-path stop, honest no-fetch versus attempted-native unavailable facts, one final observation, and evidence/history exclusion; done when the test is a Markdown/control-surface contract only and makes no network or Agent-behavior claim.
- [ ] 1.4 @impl STM-001: Add failing focused tests for the pure seed authoring evaluator over already-read declared seed bytes/path and expected canonical binding; done when malformed/frontmatter-binding failures name the declared repair surface, assert deterministic local facts, and perform no I/O, queue, or bundle mutation.
- [ ] 1.5 @impl AGQ-002, AGQ-009: Add a failing integration test for non-delegated `seed_topic_materialize` completion; done when malformed/frontmatter-binding failures, including a legacy queue lacking `bundle_name`, assert the declared-file `repair_kind`/`missing_fact`/`write_to`/same-checkpoint `rerun`; and ambiguous/mismatched producer declarations assert an owner-routed `missing_contract` result without a guessed file repair. Both branches SHALL preserve queue authority bytes and avoid terminalization, promotion/refill, render, or `queue_completed`. The test SHALL allow non-terminal attempt/receipt diagnostics and SHALL not claim whole-trace byte stability.

## 2. Bounded HITL1 Access Observation

- [ ] 2.1 @impl SCO-002: Extend `DPT_FRAMEWORK/schema/contracts/profile.mjs` with the optional internally consistent `eligible_candidate_count` / `final_candidate_ordinal` shape and no other observation state; done when old profiles remain readable, current available metadata requires a positive count/ordinal and final URL, valid unavailable no-candidate metadata remains possible, a positive-count unavailable no-legal-path branch retains its final URL, invalid combinations reject, and task 1.2 passes.
- [ ] 2.2 @impl PRP-002, PRP-005: Update `DPT_FRAMEWORK/workflows/nodes/phases/phase-hitl1.md` and its focused checklist/test surface to expose the existing pre-Gate status synchronization and the one-search/at-most-three-eligible-candidate serial probe; done when each candidate retains native-first plus one independently permitted exact same-URL fallback, first real content stops, permission/no-path stops at the current candidate, current writers record only final observation/count/ordinal facts, and task 1.3 passes.
- [ ] 2.3 @impl PRP-002, PRP-005: Audit the HITL1 production diff against the accepted observation boundary; done when no search client, browser abstraction, query/URL/attempt history, second fallback tier, retry controller, new HITL, status bypass, Gate exception, probe evidence, or user command handoff was introduced.

## 3. Direct Status-To-Apply Route

- [ ] 3.1 @impl PRP-002, CTS-003, CTS-004: Add `tests/integration/cli/operate-topic-state-hitl1-readiness.test.mjs` over a temporary bundle; done when it proves the existing `advance-status --to hitl1_recorded` establishes the legal window before canonical apply, stale/missing synchronization rejects without plan/seed/status/trace mutation, and no force/context/manual-status route is added.
- [ ] 3.2 @impl PRP-002, CTS-003, CTS-004: Verify the updated HITL1 control surface consumes the existing status command output before it creates/applies canonical topic intent, then runs the existing style and HITL1 Gate sequence; done when task 3.1 passes through real production CLIs and the implementation contains no state-machine, authorization, transition-table, or Gate-rule change.

## 4. Shared Seed Authoring Evaluation

- [ ] 4.1 @impl STM-001, STM-002, STM-003: Extract one pure seed authoring evaluator from the existing `seed-topics-ready` Gate path, using already-read declared seed bytes/path, the existing frontmatter parser, and canonical UID/id/slug/title/must-answer/scope/dependency binding; done when it returns deterministic local facts without I/O/mutation, does not judge body semantics, and `tests/engine/helpers/seed-topic-authoring-evaluator.test.mjs` passes.
- [ ] 4.2 @impl AGQ-002, AGQ-004, AGQ-009, STM-001: Wire a thin declared-path reader plus the evaluator into non-delegated `operate-queue complete` for `seed_topic_materialize` after receipt checking and before every terminal queue mutation; admit exactly one matching `writes_to` / `required_receipts` / `completion_receipt` `file:` declaration and `payload.topic_slug` canonical binding, then defer any legacy `bundle_name` normalization until a successful completion. Done when an ambiguous declaration returns owner-routed `missing_contract` without guessing a repair file, an evaluator failure returns `repair_kind: agent_action`, direct `missing_fact`, declared-file `write_to`, and same-checkpoint `rerun` without saving/touching queue bytes, rendering, or appending `queue_completed`, and repair followed by the same completion command succeeds.
- [ ] 4.3 @impl AGQ-002, AGQ-009, STM-001, STM-002, STM-003: Make the final seed-topics Gate import the same pure evaluator while retaining its phase-wide registry set comparison, queue-drain, trace, routing, and verdict ownership; done when integration coverage proves parser/evaluator reuse rather than a second parser, generic linter, directory scan for a card declaration, or new public command.
- [ ] 4.4 @impl AGQ-002, AGQ-009, STM-001: Implement `tests/integration/cli/operate-queue-seed-authoring.test.mjs` for malformed YAML/frontmatter, canonical-binding failures, byte-stable queue rejection, same-command repair, and final-Gate parity; done when all cases use real temporary bundles and production queue/Gate commands without treating body semantic quality as Engine input.

## 5. Routed End-To-End Proof

- [ ] 5.1 @impl SCO-002, PRP-002, PRP-005, AGQ-009, STM-001: Add `tests/e2e/pre-wave-readiness.test.mjs`; done when a fresh temporary bundle with explicitly labeled simulated HITL/Agent input traverses the production status/apply/style/profile/Gate/seed-queue/Gate chain, exposes producer-local failures, and does not claim search, fetch, page semantics, host permission, or real Agent behavior.
- [ ] 5.2 @impl SCO-002, PRP-002, PRP-005: Update case-115's production-facing playbook and Subject adapter for the three-candidate contract, current metadata, one final profile observation, existing Gate/handoff-or-unavailable ownership, evidence exclusion, and provider-scoped `NOT_RUN` boundary; done when it retains exactly one independent Subject identity and makes no fixture or cross-runtime claim.
- [ ] 5.3 @impl SCO-002, PRP-002, PRP-005: Update the existing case-115 observer and its focused integration coverage to consume only allowed public Subject/direct bundle facts for the bounded candidate sequence; done when a first-candidate outcome can satisfy the general claim, missing or contradictory required public facts become the existing `NOT_RUN` path, unobserved later-candidate branches are not reported as proof, and the observer neither fetches, judges page semantics, nor authors profile/Gate success.
- [ ] 5.4 @impl PRP-002, PRP-005: Validate case-115 with the canonical playbook validator and execute it once through the canonical Agent Experiment Autorun when an authenticated Subject runtime is available; done when `apply-evidence.md` records native PASS/FAIL/NOT_RUN with durable evidence references and never substitutes fixture, Playbook-Agent, or cross-runtime evidence for the selected Agent-flow claim.
- [ ] 5.5 @impl SCO-002, PRP-002, PRP-005, AGQ-009, STM-001: Run the selected unit, integration, deterministic E2E, workflow-package, and relevant pre-research/queue regression commands; done when their results are recorded by claim in `apply-evidence.md`, and any unrelated pre-existing failure has an exact focused reproduction outside this change's proof claims.

## 6. Release, Governance, And Archive Readiness

- [ ] 6.1 @impl SCO-002, PRP-002, PRP-005: Add the proposal-declared `v0.45` entry to `CHANGELOG.md`; done when it concisely describes direct pre-Wave readiness, bounded access observation, and seed authoring evaluation without claiming unobserved external behavior.
- [ ] 6.2 @impl SCO-002, PRP-002, PRP-005: Synchronize the `DPT_FRAMEWORK/RUN.md` version banner/current-release summary with the `v0.45` CHANGELOG entry; done when it matches the release statement and does not imply that Change 2 or Change 3 BUGs are fixed.
- [ ] 6.3 @impl SCO-002, PRP-002, PRP-005, AGQ-002, AGQ-009, STM-001: Complete `apply-evidence.md` and the non-goal review; done when every verification-plan claim has an authority-bound PASS/FAIL/NOT_RUN record, deterministic and Agent/external proof remain distinct, and the final scope contains no new dependency, lifecycle state, public CLI, Gate, controller, generic linter, retry tree, manual authority route, or Wave producer/Gate-feedback change.
- [ ] 6.4 @impl SCO-002, PRP-002, PRP-005: Run `node openspec/governance/check-verification-routing.mjs --change make-pre-wave-readiness-feedback-direct --mode assets`; done when every selected test and case-115 asset exists in its canonical boundary and route identities still match their declared proof scope.
- [ ] 6.5 @impl SCO-002, PRP-002, PRP-005, AGQ-002, AGQ-009: Sync the accepted delta specs through the repository's OpenSpec spec-sync/archive workflow and validate the resulting main-spec projection; done when SCO-002, PRP-002, PRP-005, AGQ-002, and AGQ-009 are the only changed requirements and no unnecessary requirement ID or capability is introduced.
- [ ] 6.6 @impl SCO-002, PRP-002, PRP-005: Run `node openspec/governance/check-project-reqs.mjs` and `node openspec/governance/check-project-specs.mjs`; done with zero duplicate, orphan, unregistered, reused-retired, delta-header-in-main, missing-purpose, missing-requirements, and missing-req-header violations.
- [ ] 6.7 @impl SCO-002, PRP-002, PRP-005: Run `openspec validate make-pre-wave-readiness-feedback-direct --strict` and a whitespace check scoped to this change's owned target paths/artifacts; done when the change is apply/archive-ready without treating unrelated worktree edits as this change's failure.
