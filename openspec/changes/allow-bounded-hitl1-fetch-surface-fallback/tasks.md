## 1. Pre-Target Verification And Red Evidence

- [ ] 1.1 @impl PRP-002, PRP-005: Create change-root `implementation-evidence.md`, record the pre-apply worktree baseline, then run `node openspec/governance/check-verification-routing.mjs --change allow-bounded-hitl1-fetch-surface-fallback --mode plan` before target edits; done when all four claims validate with `unit`/`deterministic_e2e` explicitly not applicable and no deterministic asset claims native fetch, curl or Agent behavior.
- [ ] 1.2 @impl PRP-002, PRP-005: Replace the one-fetch expectations in `tests/integration/md/phase-hitl1-research-access.test.mjs` with red target assertions for one search, only its first actual HTTP(S) result when eligible with no second-result substitution, native-first, one same-URL `curl --fail --silent --show-error --location --max-time 15 --proto '=http,https' --proto-redir '=http,https' -- "<same-url>"` fallback, native-success short circuit, host permission/URL safety boundary, existing observation/evidence boundary and Agent-owned mechanics; done when current production Markdown fails only the newly modified contract and the test adds no network call or parser-like wording dependency.
- [ ] 1.3 @impl PRP-002, PRP-005: Add or update one focused integration test for existing case-115, its current manifest registration and Subject 115 runner boundary; done when the current one-fetch wording fails the new assertions, the test requires a retained-transcript fallback witness contract, and it does not add another playbook, Subject identity or deterministic fallback proof.

## 2. Production HITL1 Contract

- [ ] 2.1 @impl PRP-002, PRP-005: Rewrite `phase-hitl1.md` §3d and its payload/stop/anti-cheating surfaces to implement one neutral search, only its first actual HTTP(S) result when eligible, native real-content success short circuit and at most one exact hardened same-URL `curl` fallback from task 1.2; done when HTTP errors are non-success, requests/redirects remain HTTP(S)-only, localhost/loopback/literal private or link-local targets are excluded, success/failure write one existing `research_access` observation, fallback success records `fetch_surface: curl`, exhausted failure preserves choices and the same Gate, independently existing permission keeps mechanics with Agent, and no policy widening, second URL/tier or user command handoff is authorized.
- [ ] 2.2 @impl PRP-002, PRP-005: Audit the production diff against the one-truth-path boundary; done when `ProfileSchema`, `hitl1-recorded` Gate/CLI, transitions, status, trace, Wave/work-unit guidance and evidence/provenance/receipt/ledger owners are unchanged, with no `fallback_used`, surface list, attempt history, fetch controller, retry tree, new HITL or offline-report path.

## 3. Real-Agent Verification Surfaces

- [ ] 3.1 @impl PRP-002, PRP-005: Update case-115 and Subject 115 runner text from “at most one fetch” to the new production bounded sequence while preserving one independent Subject, setup-only bundle, real external calls, one final profile observation, real Gate/handoff or honest unavailable branch, evidence exclusion and trace-owned completion; done when case-115 remains the single general probe canary, keeps its existing manifest identity and explicitly does not claim the curl branch ran when native fetch succeeds.
- [ ] 3.2 @impl PRP-002, PRP-005: Extend case-115's retained Subject transcript/result and direct profile/Gate/handoff/evidence-surface facts with an optional fallback witness; done when complete same-URL native absence/block/unavailable/failure, no-more-than-one curl attempt, real returned page content, `fetch_surface: curl`, real Gate pass/Setup handoff and no user command handoff/evidence leakage facts append one passed playbook-owned `hitl1-native-to-curl-fallback` check to bundle-root `rb_trace.jsonl` without inspecting private reasoning or executing fallback on the Subject's behalf.
- [ ] 3.3 @impl PRP-002, PRP-005: Keep case-115's general verdict independent from its optional fallback witness; done when existing general checks remain required, the branch check is emitted only from complete direct facts and is not universally required, the Playbook Agent/runner neither writes Subject profile/page bytes nor fabricates tool events, native success cannot count as fallback PASS, absence of the branch check becomes `NOT_RUN` only for the fallback verification claim in `implementation-evidence.md`, and Supervisor health/audit/cleanup remain post-completion owners.

## 4. Focused And Real Verification

- [ ] 4.1 @impl PRP-002, PRP-005: Run `node --test tests/integration/md/phase-hitl1-research-access.test.mjs tests/integration/md/hitl1-fetch-fallback-agent-flow.test.mjs` and `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`; done when production guidance, existing case-115/runner wiring and the shipped workflow package pass without external calls.
- [ ] 4.2 @impl PRP-002, PRP-005: Execute updated case-115 once through canonical Agent Experiment Autorun when an authenticated Subject runtime is available; done when `implementation-evidence.md` records the general claim's PASS/FAIL/NOT_RUN and durable evidence refs, then separately records fallback PASS only if the retained transcript proves the exact native-failure-to-curl-success branch.
- [ ] 4.3 @impl PRP-002, PRP-005: Classify the case-115 fallback witness independently from its bundle-root `hitl1-native-to-curl-fallback` trace check; done when the exact observed branch produces PASS, the branch check's absence produces honest `NOT_RUN` with residual Agent-behavior risk explicit, actual observed contradictory behavior produces FAIL, and no second playbook, rerun solely to chase the condition or fixture substitution is used.
- [ ] 4.4 @impl PRP-002, PRP-005: Run the relevant pre-research/HITL1 regression surface and `npm test`; done when all suites pass or every unrelated pre-existing failure is recorded with an exact focused reproduction and excluded from this Change's four verification claims.

## 5. Release, Governance And Closure

- [ ] 5.1 @impl VEM-001, VEM-002, VEM-004: Add the proposal-declared `v0.39` entry to root `CHANGELOG.md`; done when one or two concise bullets describe bounded same-URL HITL1 fallback and Agent-owned mechanics without file lists, requirement IDs, delegated-path claims or universal external-access claims.
- [ ] 5.2 @impl VEM-003, VEM-004: Synchronize the `DPT_FRAMEWORK/RUN.md` banner and current-release summary to `v0.39`; done when it matches the latest CHANGELOG entry and does not imply BUG-096's delegated Wave path is fixed.
- [ ] 5.3 @impl PRP-002, PRP-005: Complete `implementation-evidence.md` and audit non-goals; done when each verification-plan claim maps to its actual authority, PASS/FAIL/NOT_RUN and residual risk are explicit, and the final diff adds no dependency, Python, schema/state/Gate/transition change, hidden retry, second URL/tier, user co-runner or fabricated runtime evidence.
- [ ] 5.4 @impl PRP-002, PRP-005: Run `node openspec/governance/check-verification-routing.mjs --change allow-bounded-hitl1-fetch-surface-fallback --mode assets`; done when all selected integration and Agent-flow assets exist at canonical paths and each proof claim retains its declared actor, runtime, external-call and verdict boundary.
- [ ] 5.5 @impl PRP-002, PRP-005: Run `node openspec/governance/check-project-reqs.mjs`; done with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired violations, reusing PRP-002/PRP-005 without an unnecessary new requirement ID.
- [ ] 5.6 @impl PRP-002, PRP-005: Run `node openspec/governance/check-project-specs.mjs`; done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [ ] 5.7 @impl PRP-002, PRP-005, VEM-002, VEM-003: Run `openspec validate allow-bounded-hitl1-fetch-surface-fallback --strict` and `git diff --check`; done when the complete Change validates, v0.39 projections agree, and final review records only BUG-096's HITL1/main-Agent path fixed while leaving delegated closure to `deliver-work-unit-role-contracts-to-actors`.
