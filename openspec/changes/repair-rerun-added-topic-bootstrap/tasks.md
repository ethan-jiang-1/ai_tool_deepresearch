## 1. 先锁定全部十个 Gate 的 contract 与九个 BUG 基线

- [ ] 1.1 为 GSK-003、GSK-011 自动枚举 active `gate-*.definition.json` 与 `check-gate-*.mjs`，建立十对 bijection regression，明确包含 `rerun-ready`；增加Gate-definition production semantic-read no-bypass regression。Done：missing/extra definition、wrapper或registry drift会失败；loader、consistency、post-final、hygiene、audit均走shared parser；新增direct semantic read会被source scan发现；测试不得维护另一份手写Gate count、reader inventory或shape validator。
- [ ] 1.2 为 GSK-001、GSK-002、GSK-004、GSK-005、GSK-011 建立 all-Gate output matrix：十个 Gate pass 均返回兼容 `{check,routing,inspect,advice}` + `hints: []`；每个 Gate 至少一个 representative rule failure 返回完整 `rule_id/missing_fact/write_to/rerun`；existing durable failure diagnostic保留相同`hints[]`。Done：任一 wrapper/diagnostic漏hint、空/unresolved coordinate、非 same-Gate rerun或duplicate trace writer都会失败。
- [ ] 1.3 为 GSK-001、GSK-011 建立definition-reader parity regression：同一invalid definition在loader、consistency、post-final、hygiene和audit给出同一schema coordinate；post-final raw-byte hash不改变；新增direct semantic read触发no-bypass failure。Done：raw semantic parse旁路、第二shape validator或手写reader inventory都会失败。
- [ ] 1.4 为 GSK-002、GSK-004、GSK-008、GSK-011、IOC-001、IOC-003 覆盖所有 formal/inspect failure classes：invalid invocation、definition missing/unparseable、node/gate binding、handoff/status preflight、canonical topic-state/gate-specific prerequisite、rule evaluation、routing/config 与 durable handoff trace failure。Done：每类失败由检测它的existing helper直接返回统一finding；Wave inspect missing bundle/config也输出JSON+hints并保持exit 2；wrapper hand-build、prose translation或central preflight-root catalog会被检测。
- [ ] 1.5 为 GSK-008、GSK-011 增加 root/masking regression：parent authority/schema/identity failure 只产生 parent hint，dependent symptoms 被 mask；independent roots可并列；fatigue/degraded advice不替代 direct hint。Done：flat failure wall或多条竞争 repair strategy 会失败。
- [ ] 1.6 为 GSK-001、GSK-002、GSK-011、CHI-001、CHI-003 增加 repair ownership/consumption regression：`blocking_basis`只接受closed enum且presentation preference不能伪装成`accepted_invariant`；Agent-owned artifact指向exact field/path；受控 `{topic}` 等模板解析为current exact coordinate；Engine-owned authority只指existing legal operation或`missing_contract`；`failure_message`不得与metadata冲突；Controller执行authorized repair并same-check retry。Done：unknown/abused basis、unresolved placeholder、target/error-string inference、manual edit、矛盾message或把普通命令推给用户都会失败。
- [ ] 1.7 为 IOC-001、IOC-003、RWG-002、RWG-005、RWG-018 增加 inspect/formal parity：同 bytes/shared rule 复用existing `wave-contract-findings` finding，`rule_id/missing_fact/write_to`相同，各自 `rerun` 指向实际checkpoint；inspect保持 no routing、no write、exit `0/1/2`。Done：平行failure object、prose-derived blocking adapter、第二validator或inspect副作用会失败。
- [ ] 1.8 为 CTS-003 增加 new-seed regression：rerun `add_topic`、HITL1 add 与 `migrate_legacy seed_binding:new` 生成同一完整 skeleton、exact五个wave token、无 `scope_role` 伪正文；existing enrichment/body在update/layout后保留。Done：准确暴露BUG-081，normal add无退化。
- [ ] 1.9 为 RWP-001、RWP-014、WPG-001、WPG-002 增加Wave0增量回归：historical submitted coverage + new-topic valid submit pass；historical coverage + new orphan `source.yaml` fail；无rerun Gate exception。Done：覆盖BUG-082并证明new Topic走normal provenance path。
- [ ] 1.10 为 AGQ-002、AGQ-014 增加claim regression：delegated `operate-queue claim`、empty window、missing observation、unnecessary fallback返回不同root；b000历史后b001合法claim成功。Done：覆盖BUG-083；allocator若本来正确，测试保留且不安排重写。
- [ ] 1.11 为 DEW-004、DEW-009、DEW-013、DEW-017 增加helper regression：beacon/task/spawn/CLI examples共享canonical absolute root；bundle-relative refs只join一次；wrong nested root的inspect/dry-submit/submit零写；formal submit prepare与dry-submit在final rejection/transaction前不写queue-load trace/log、canonical cache、normalized result/receipt或transaction artifacts；starter/schema/actor constants一致；dry-submit返回independent roots。Done：覆盖BUG-084且strict actor/receipt/provenance不放宽。
- [ ] 1.12 为 CTS-007、REF-002、REF-003、REF-007、RWG-002、RWG-005、RWG-018、RWG-019、FIO-006、FIO-007 增加reference regression：UID-only/legacy pass；dual conflict一次；invalid index parent只报parent；valid table缺row只报该row；inspect/Gate/observability共享binding。Done：覆盖BUG-085且不要求historical mass rewrite。
- [ ] 1.13 为 EEX-001、EEX-002、EEX-003、RWG-002、RWG-005、RWG-018、RTI-001、RTI-003 增加count/format regression：short Core Content、few Key Facts、harmless heading/order/list差异不改变count；bare URL/Markdown link均接受；missing semantic section只由format报；Wave2 ledger六sections重排/equivalent heading pass、missing section fail once；filesystem-only仍不count。Done：覆盖BUG-086并证明Wave1 quantity与Wave2 ordered-section presentation blocker退役。
- [ ] 1.14 为 WAI-005、RWG-002、RWG-005、RWG-018 增加depth regression：minimal review只含reviewed refs + judgments即可；source/cache/novelty/floor从submitted rows/Wave0/profile派生；legacy copied fields不override；filesystem-only cache fail；missing reviewed ref短路。Done：覆盖BUG-087且无第二authority。
- [ ] 1.15 为 AGO-003、AGO-005、DEW-005、DEW-010、DEW-011、WPG-001、WPG-002、WPG-008 增加declaration recovery regression：prepare/dry-submit side-effect-free且不产生final ledger row/hash；locked transaction reload/revalidate、apply canonicalization后生成shared timestamp；normal row由direct owners复现recorded hash；late index只保存minimal context；reconstruction facts不直接满足Gate；hash-identical recovery/idempotency通过；legacy missing proof、malformed/conflict/hash mismatch/unsubmitted零mutation。Done：覆盖BUG-088且无prepare mutation/precomputed commit hash/witness/shadow ledger/new completion path。
- [ ] 1.16 为 SNC-006 增加source-ref lineage regression：current output pass；prior ledger→index/manifest/queue topic binding解析same UID/wave/kind且role在valid `prior_submitted_output_roles[]`中pass；Wave1 prior `evidence_summary` pass；invalid/duplicate role contract、path-derived topic、unresolved binding、prior `question_list/reference/other`、wrong kind/wave、filesystem-only、unsubmitted、cross-topic、ambiguous fail。Done：覆盖BUG-089并断言exact claim index/JSON pointer/same dry-submit rerun。
- [ ] 1.17 为 RWP-002、WAI-005、WAI-008 增加Markdown contract regression：`phase-wave1`实际 `requires` shared reference template；depth-review示例不要求ledger copies；Gate hint后Agent-owned flow明确执行authorized repair并rerun same checkpoint。Done：仅mention文件名、恢复duplicate shape或把普通命令推给用户时失败。

## 2. 建立 Gate rule metadata、shared root 与防漂移审计

- [ ] 2.1 实现 GSK-001、GSK-011：在 `DPT_FRAMEWORK/schema/contracts/gate-definition.mjs` 建立一个非deprecated的Zod Gate-definition contract，校验existing id/check/failure_message、`target`或check-specific `targets/fields/sources` authority descriptor、closed `blocking_basis` enum、`repair.owner`、受控placeholder和owner/write-surface跨字段约束。Done：四条existing target-less rules通过且不伪造target；missing descriptor、unknown/abused basis、unknown placeholder、owner/surface contradiction和manual Engine-authority edit fail并点名gate/rule；schema不含check dispatch/verdict逻辑。
- [ ] 2.2 实现 GSK-001、GSK-011：把`loadGateDefinition()`/safe loader、`consistency-validator.mjs`、`post-final-recovery.mjs` rerun guard、`validate-work-unit-hygiene.mjs`与definition tests/GSK-011 audit迁移到2.1 parser。Done：production semantic read无raw `JSON.parse`旁路；post-final继续hash原始bytes但rule/operator/value来自parsed definition；1.1/1.3 PASS。
- [ ] 2.3 实现 GSK-001、GSK-004、GSK-011：审阅 `instantiation-complete` 的全部rules并补blocking/repair metadata与producer/authority/checker/test closure，同时修正与metadata冲突的`failure_message`。Done：17条rule逐条通过static audit，无手改status/trace/ledger/index/receipt/hash advice。
- [ ] 2.4 实现 GSK-001、GSK-004、GSK-011：审阅 `hitl1-recorded` 与 `seed-topics-ready` rules，区分user decision、Agent artifact和Engine operation ownership，并清理矛盾compatibility message。Done：两份definition逐条通过static audit，HITL decision不被伪装成机械写文件。
- [ ] 2.5 实现 GSK-001、GSK-004、GSK-011：审阅 `setup-ready` 的全部rules，补blocking/repair closure并保留schema/status/basename direct authority。Done：21条rule逐条通过static audit，重复parent症状由masking处理，无“restore/edit status”类非法message。
- [ ] 2.6 实现 GSK-001、GSK-004、GSK-011：审阅 `wave0-complete` 的artifact、floor、trace、cache和work-unit provenance rules。Done：14条rule逐条通过static audit，orphan/provenance/trace owner不指向手写ledger或trace，dynamic Topic coordinate可解析。
- [ ] 2.7 实现 GSK-001、GSK-004、GSK-011、RWG-002、RWG-005：审阅 `wave1-complete` rules，删除retired quantity blocker并为artifact/depth/reference/provenance roots补closure。Done：active rules逐条通过static audit，presentation-only basis为零，legacy message不建议非法surface。
- [ ] 2.8 实现 GSK-001、GSK-004、GSK-011、RWG-018：审阅 `wave2-complete` rules，区分required semantic structure、structured finding authority与presentation；保留`ledger_fixed_sections` identity但移除ordered regex authority。Done：19条rule逐条通过static audit，六section set evaluator tolerant，parent finding/index roots短路dependent hints，trace/index message与legal owner一致。
- [ ] 2.9 实现 GSK-001、GSK-004、GSK-011：审阅 `hitl2-recorded`、`readiness-passed` 与 `rerun-ready` rules，明确user decision、trace/status和bundle integrity repair ownership。Done：三份definition逐条通过static audit，无ordinary command被推给用户，无删除/手改trace/status advice。
- [ ] 2.10 实现 GSK-003、GSK-011：扩展active rule/failed-exit closure audit，从schema-parsed definitions与wrappers派生ten-pair bijection，验证known dispatch、closed blocking basis、repair ownership、placeholder resolution、`failure_message` non-contradiction、shared/formal partition、hint projection和test closure；加入production definition-read、helper-owned finding、wrapper no-hand-build/no-central-catalog scans。Done：未来新增Gate/rule/direct semantic read/helper-external root translation会失败，不维护平行reader或preflight-root名单。
- [ ] 2.11 实现 GSK-002、GSK-003、GSK-004、GSK-011：清理accepted/runtime inventory drift，更新static assertions与registry摘要，把8/9 Gate改为derived ten active pairs并包含`rerun-ready`。Done：不新增requirement ID，code/delta/registry不再维护矛盾数量。
- [ ] 2.12 实现 GSK-002、GSK-004、GSK-008：泛化existing `wave-contract-findings.mjs` 的`makeContractFinding/buildContractEvaluation/projectInspectContract`，在现有shape上补closed `blocking_basis`、observed fact、repair owner、resolved write coordinate、mask relation和checkpoint context；`failed_rule_ids`改从blocking `rule_id`投影而非diagnostic `id`；限制`findingsFromCheckResult()`只处理advisory/diagnostic-only prose。Done：formal Gate与inspect可从同一finding投影`failed_rule_ids/hints/inspect/advice`；同rule多实例在hint coordinate中区分；无prose-derived blocking authority、平行failure object、persistent state或第二verdict。
- [ ] 2.13 实现 GSK-002、GSK-004、GSK-005：扩展shared result/finding builder生成top-level `hints[]` 与checkpoint-specific exact rerun；解析受控dynamic `write_to`；让existing Gate diagnostic serializer原样保存result hints。Done：known bundle/current node使用absolute bundle root + exact current node；per-topic path无placeholder；wrapper write ownership不变，无new finalizer/trace writer。
- [ ] 2.14 实现 GSK-002、GSK-004、GSK-011：让`parseGateCliArgs()`、`tryLoadGateDefinition()`、node/gate binding、handoff/status、routing和durable trace owners在检测direct failure时返回2.12 structured finding及compatibility prose。Done：invocation missing arg只报告known args/required template；wrapper不查central root map、不从helper reason/advice反推metadata；1.4 PASS。
- [ ] 2.15 实现 GSK-002、GSK-004、GSK-008：删除或降级`gateMessagePriority()/prioritizeMessages()`，使structured classification/masking/stable IDs决定primary roots；Gate attempt trend只比较`failed_rule_ids`。Done：`inspect[]`/`failure_message`改写不改变hint顺序或trend；legacy diagnostic无IDs时不参与比较，current result成为first comparable sample，不退回prose comparison。
- [ ] 2.16 实现 GSK-002、GSK-004、GSK-011：迁移 `instantiation-complete` 与 `setup-ready` wrappers消费helper/rule findings并project result。Done：所有failed exits含hint，existing exit/routing/audit behavior保持，wrapper无root metadata。
- [ ] 2.17 实现 GSK-002、GSK-004、GSK-008、GSK-011：迁移 `hitl1-recorded` 与 `seed-topics-ready` wrappers消费helper/rule findings。Done：topic-state parent root mask dependent rules，wrapper无hand-built opaque result/root metadata。
- [ ] 2.18 实现 GSK-002、GSK-004、GSK-008、GSK-011、RWG-018：迁移 `wave0-complete` wrapper消费shared/formal findings与durable handoff trace finding。Done：Gate/inspect shared coordinates一致，fatigue不覆盖direct hints。
- [ ] 2.19 实现 GSK-002、GSK-004、GSK-008、GSK-011、RWG-018：迁移 `wave1-complete` wrapper消费shared/formal findings与durable handoff trace finding。Done：Gate/inspect shared coordinates一致，missing declaration/index parent不产生failure wall。
- [ ] 2.20 实现 GSK-002、GSK-004、GSK-008、GSK-011、RWG-018：迁移 `wave2-complete` wrapper消费shared/formal findings与finding/index parent masking。Done：Gate/inspect shared coordinates一致，formal-only lifecycle authority不进入inspect。
- [ ] 2.21 实现 GSK-002、GSK-004、GSK-008、GSK-011：迁移 `hitl2-recorded`、`readiness-passed` 与 `rerun-ready` wrappers消费binding/handoff/status/rule/routing findings。Done：三份wrapper无hand-built failed result/root catalog，branch-sensitive routing保持accepted behavior。
- [ ] 2.22 实现 IOC-001、IOC-003、RWG-002、RWG-005、RWG-018：让Wave0/Wave1/Wave2 pure evaluators及会阻塞当前inspect的return-map helpers直接返回2.12 existing finding shape，inspect/formal分别投影自己的exact rerun；formal-only lifecycle roots使用同一shape但不复制进inspect。Done：1.7 PASS，blocking result不从`inspect/advice` prose恢复，inspect无formal副作用。
- [ ] 2.23 实现 IOC-001、IOC-003、GSK-002、GSK-004：迁移三个Wave inspect CLI的missing bundle、definition parse/load和其他invocation/config exits到统一JSON finding projection。Done：pass为`hints: []`；exit 2 failure含stable coordinate；stderr-only usage和hand-built inspect/advice-only error object被删除；unknown bundle只显示required placeholder。

## 3. 删除无blocking正当性的quality-control与级联症状

- [ ] 3.1 实现 EEX-001：把 `isCountable()` 收窄为accepted status + parseable source URL，删除Core Content长度、Key Facts数量、homepage/path、Jaccard/self-reference等heuristics与旧threshold常量。Done：1.13 PASS，unparseable/missing URL仍fail clearly。
- [ ] 3.2 实现 EEX-002、EEX-003：让countReferences/ref-count只使用authority-selected reference + narrow predicate，不重复format/backing/index/provenance checks。Done：per-topic exact scope、filesystem orphan和Agent count cases PASS。
- [ ] 3.3 实现 REF-002、WAI-008：shared reference parser保持required non-empty semantic sections strict，但宽容heading case/level/spacing/order/list presentation；`source_url_present`接受bare URL或Markdown link，Key Facts quantity/prose richness只advisory。Done：missing section一个format root，presentation-only不block。
- [ ] 3.4 实现 RWG-002、RWG-005、RTI-001、RTI-003：从blocking Wave1 definition删除`key_facts_min_lines`，同步退役rerun/reference fixed-order与fixed-quantity blocking wording，并移除/降级checker dispatch、GSK-011 inventory、degradation allowlist、producer wording和blocking fixtures。Done：normal与rerun共用同一tolerant semantic contract；无hidden shadow rule；quantity不进入`failed_rule_ids/hints[]`。
- [ ] 3.5 实现 RWG-018：把Wave2 `ledger_fixed_sections` checker从ordered regex改为tolerant six-section set/non-empty evaluator，保留stable rule id并删除ordered wording/fixtures。Done：1.13 PASS，order/case/heading level不block，missing section一个root，无shadow regex。
- [ ] 3.6 实现 WAI-005、RWG-002、RWG-005：重构`wave-depth-contracts.mjs`，从`reviewed_work_unit_refs[]`指向的hash-valid submitted rows派生source/cache/novelty/observed floor，从Wave0/profile取baseline/required floor。Done：1.14 PASS。
- [ ] 3.7 实现 WAI-005：把legacy `wave0_source_urls/source_claims/new_source_urls/new_source_floor`降为optional compatibility/advisory projection，不能override Engine-derived truth。Done：minimal和legacy drift cases PASS。
- [ ] 3.8 实现 REF-002、CTS-007、RWG-002、RWG-005、RWG-019：在existing topic-layout resolver上增加thin reference binding adapter，统一UID/legacy/dual handling。Done：无second identity map，1.12 binding cases PASS。
- [ ] 3.9 实现 REF-003、RWG-005、RWG-018：reference index先验证eight-column parent，再运行row/source-layer checks；parent invalid时mask row cascade。Done：1.12 index cases PASS，无generic dependency engine。
- [ ] 3.10 实现 FIO-006、FIO-007、IOC-001、IOC-003：file observability及Wave0/Wave2 inspect删除local raw-field parser，消费shared binding/root result并保持read-only。Done：1.7/1.12 PASS。

## 4. 接回normal Topic pipeline与helper surface

- [ ] 4.1 实现 CTS-003：重构existing canonical seed renderer/merge，new projection生成完整shared skeleton与accepted tokens，existing seed保留enrichment/body。Done：1.8 PASS，topic-state仍只触碰plan+explicit seed workspace。
- [ ] 4.2 实现 RWP-001、RWP-014：更新`phase-wave0.md`的direct-fact classification、normal task-card path、historical reuse、supplement、orphan处理和correct work-unit claim/submit。Done：1.9和Markdown tests PASS，无Wave0 Gate exception。
- [ ] 4.3 实现 AGQ-002：delegated queue-claim与empty返回distinct stable roots和repair triplet，queue bytes不变。Done：1.10 queue cases PASS，existing compatible fields不删除。
- [ ] 4.4 实现 AGQ-014：只在1.10证明真实allocator缺陷时最小修复next-batch claim；完善missing observation/unnecessary fallback repair coordinates。Done：合法b001 claim PASS，fail-closed cases零authority mutation。
- [ ] 4.5 实现 DEW-004、DEW-009：统一work-unit envelope的canonical absolute root，修正existing-index read prerequisite-before-create和shared beacon binding。Done：wrong nested root无directory/transaction/trace/log/rejection side effect。
- [ ] 4.6 实现 DEW-004、SNC-006：从manifest/output contract/cache policy生成task内Result JSON Starter与checklist，不预写`result.json`；kind contract增加并校验unique/subset `source_claims.prior_submitted_output_roles[]`，Wave1默认exact `['evidence_summary']`。Done：starter/task/schema/submit共享exact field与allowed prior roles，invalid contract fail early，不包含rejected keys或implicit compatibility。
- [ ] 4.7 实现 DEW-013、DEW-017：dry-submit independent roots带repair triplet，dependent validation short-circuit；formal repairable rejection唯一rerun指向same-candidate dry-submit。Done：1.11与all-root tests PASS，formal submit仍唯一normal success。
- [ ] 4.8 实现 SNC-006：构建由hash-valid bundle ledger row绑定existing index/manifest/queue topic payload再经canonical resolver派生的in-memory submitted-output index；prior source_ref要求exact path、same canonical Topic UID、same wave/kind和contract-declared role。Done：1.16 PASS，不从path/queue id猜Topic，无persistent index/hardcoded compatibility，当前cache refs仍由current result声明。

## 5. 增加唯一必要的explicit declaration recovery

- [ ] 5.1 实现 AGO-003、AGO-005、DEW-010、DEW-011：拆分side-effect-free candidate validation plan与commit declaration construction；prepare/dry-submit使用read-only queue/cache/result/receipt evaluation，不生成final row/hash或写canonicalization/trace/log/transaction state；normal/late transaction取得lock后reload/revalidate mutable facts、apply canonicalization，生成一个submission timestamp并构造ledger row/hash，一致用于ledger `declared_at`、index `terminal_at`、status `updated_at`、queue `completed_at`。Done：final rejection recorder仍可写existing diagnostics；commit不复用transaction外ledger hash，normal row从direct owners重建后hash等于recorded index/status hash，1.11/1.15 PASS。
- [ ] 5.2 实现 AGO-003、AGO-005、DEW-010：为late-submit扩展existing index schema，只保存non-empty accept reason、prior `timed_out` status和superseded retry IDs；禁止复制ledger row、result/output/source/cache/receipt/actor facts。Done：minimal-context schema/static/transaction tests PASS，Gate不读取它作coverage。
- [ ] 5.3 实现 AGO-003、DEW-005、DEW-010：在existing `operate-work-unit.mjs` owner增加`recover-declaration`；只对already-submitted missing row从direct owners重建并要求existing recorded hash equality。Done：不accept result、不complete queue、不改index/status hash、不生成new provenance。
- [ ] 5.4 实现 AGO-003、AGO-005、DEW-010：让recovery audit只写existing transaction/trace/log，restored row保持original schema/hash；exact row已存在时idempotent unchanged。Done：recovery metadata不进入ledger hash或第二row schema。
- [ ] 5.5 实现 AGO-003、DEW-010：legacy pre-unified-timestamp/pre-context eligibility只有在remaining direct facts + original submit/transaction evidence唯一复现existing recorded hash时成立。Done：任一proof缺失、冲突或hash mismatch零mutation；对确实already-submitted但不可复现的row只返回`missing_contract`，不建议外部备份/manual row/new attempt等未接受路径。
- [ ] 5.6 实现 WPG-001、WPG-002、WPG-008、RWG-018：Gate/inspect识别submitted index + missing ledger row为`submitted_declaration_missing`，诊断recovery eligibility并mask output/cache/count/bypass symptoms；reconstruction facts不直接coverage。Done：1.15与all-root tests PASS，`write_to`指Engine operation而非JSONL edit。

## 6. 对齐Agent-facing控制面与mechanical responsibility

- [ ] 6.1 实现 RWP-002、WAI-008：把`shared/shared-reference-template`加入`phase-wave1`实际`requires`；materialization wording移除固定facts数量与presentation blocker。Done：1.17 PASS，shared template与parser contract一致。
- [ ] 6.2 实现 RWP-002、WAI-005：更新Wave1 depth-review minimum shape与repair wording，只要求reviewed refs + non-derivable judgments；Engine-derived facts不让Agent重抄。Done：phase/example/static tests PASS。
- [ ] 6.3 实现 DEW-009、DEW-013、DEW-017、RWP-001、RWP-014：更新`shared-subagent-protocol.md`及existing actor decision playbook，使用canonical root、beacon只读、starter -> real work -> dry-submit -> repair -> formal submit。Done：无new interaction mode/permission/path owner。
- [ ] 6.4 实现 SNC-006、WAI-005：更新supplementary guidance，允许exact prior same-topic/same-wave/same-kind、contract-authorized-role submitted evidence path，不要求重复声明或覆盖旧evidence；新cache仍current submit。Done：1.16 Markdown/kind-contract/schema/validator agreement PASS。
- [ ] 6.5 实现 GSK-002、GSK-004、CHI-001、CHI-003：更新 instantiation/HITL1/setup/seed-topics controllers优先消费`hints[]`，Agent执行合法mechanical action。Done：user只承担HITL语义决定，ordinary commands不被推给用户。
- [ ] 6.6 实现 GSK-002、GSK-004、RWG-018、CHI-001、CHI-003：更新 Wave0/Wave1/Wave2 controllers优先消费root-first `hints[]`并rerun named checkpoint。Done：Agent不依赖opaque advice，不新增repair controller或second flow。
- [ ] 6.7 实现 GSK-002、GSK-004、CHI-001、CHI-003：更新 HITL2/readiness/rerun controllers消费Gate hints，并只在new semantics/risk/permission、external或missing-contract时最小升级。Done：human-directed不创造permission，Final/delivery lifecycle不变。
- [ ] 6.8 实现 DEW-005、WPG-001、WPG-008、RWP-014：保留anti-fabrication wording，禁止direct artifact追认、post-hoc receipt/result/provenance、hand-written ledger/hash/status/trace和reconstruction-facts-as-coverage。Done：hygiene tests能检测退化。

## 7. Regression与真实controlled evidence

- [ ] 7.1 验证 GSK-001、GSK-002、GSK-003、GSK-004、GSK-008、GSK-011 及 CTS/AGQ/DEW/SNC/EEX/WAI/REF/RWG/WPG/FIO/IOC affected requirements：运行all-Gate、definition-reader parity、structured-root/string-independence、inspect error-exit与九BUG targeted tests。Done：1.x全部PASS，无skip、无production bundle mutation。
- [ ] 7.2 验证 GSK-002、RWG-005、RWG-018、DEW-005、WPG-002：运行相关完整`tests/engine/`、Gate CLI、queue/work-unit CLI integration、Markdown contract、Wave0/Wave1/Wave2 Gate、file-observability suites。Done：全部PASS；unrelated pre-existing failure记录exact command/output，不误勾。
- [ ] 7.3 验证 RWP-001、RWP-014、AGQ-002、DEW-004：更新existing rerun `action:add` heavy canary的historical fixture、真实add两个Topic和normal Wave0 enqueue/probe/claim/submit步骤。Done：fixture只提供historical prerequisite，不产出new Topic research result。
- [ ] 7.4 验证 RWP-002、SNC-006、WAI-005、WAI-008：扩展canary的Wave1 supplementary prior-output引用、minimal depth review、shared template materialization与inspect/Gate步骤。Done：new Topic evidence来自real actor，Phase projection只使用submitted backing。
- [ ] 7.5 验证 AGO-003、AGO-005、DEW-010、WPG-001：加入明确fault injection，先真实submit，再删除一个bundle ledger row，观察`submitted_declaration_missing`并只用`recover-declaration`恢复。Done：不手写row/hash/result/receipt；reconstruction facts未直接满足Gate；恢复后normal Gate path通过。
- [ ] 7.6 验证 GSK-002、GSK-004、GSK-011、CHI-001、CHI-003：在canary保留至少一个可修复Gate failure，断言Agent仅根据`missing_fact/write_to/rerun`找到authorized surface、执行repair并重跑same Gate。Done：Agent不读Engine source、不猜内部lineage、不要求用户代跑命令。
- [ ] 7.7 验证 CTS-003、RWP-001、AGQ-002、DEW-004：由coding Agent从clean repo执行heavy canary through Wave0。Done：real actor/search/fetch产生trace-backed Wave0 PASS；beacon unchanged、无nested bundle；NOT_RUN/fixture smoke不算PASS。
- [ ] 7.8 验证 RWP-002、SNC-006、WAI-005、AGO-003、WPG-001：继续同一clean canary through Wave1与declaration recovery。Done：real actor/search/fetch产生trace-backed PASS；historical refs无mass rewrite；FAIL保留现场。

## 8. Governance、release与最终复读

- [ ] 8.1 为 GSK-002、GSK-003、GSK-004、EEX-001 更新所有affected code/Markdown的`@impl`或frontmatter `req`标注与registry摘要；不分配新ID。Done：active delta、registry、implementation引用一致，Gate count无drift。
- [ ] 8.2 为 GSK-002、RTI-003、AGO-003 更新`CHANGELOG.md`到v0.28，简述rerun-added Topic normal pipeline、all-Gate lineage hints、blocking rule净简化、hash-identical declaration recovery和supplementary source lineage。Done：不overclaim real-Agent evidence。
- [ ] 8.3 为 GSK-002、RWP-001 同步`DPT_FRAMEWORK/RUN.md` v0.28横幅与CHANGELOG最新条目。Done：版本一致。
- [ ] 8.4 验证本 Change 全部 requirement IDs：运行`node openspec/governance/check-project-reqs.mjs`。Done：0 duplicate / 0 orphan / 0 unregistered / 0 reusedRetired。
- [ ] 8.5 验证 GSK-001、RTI-001、AGO-003 等全部delta capability结构：运行`node openspec/governance/check-project-specs.mjs`。Done：0 deltaHeaderInMain / 0 missingPurpose / 0 missingRequirements / 0 missingReqHeader。
- [ ] 8.6 验证 GSK-001、GSK-011 及本 Change 全部requirements：运行`openspec validate repair-rerun-added-topic-bootstrap --type change --strict --no-interactive --json`与`openspec status --change repair-rerun-added-topic-bootstrap --json`。Done：artifacts valid且implementation tasks全部有真实evidence；是否进入apply/archive仍由用户决定。
- [ ] 8.7 验证 GSK-011、RWP-001、AGO-003、WPG-001：运行`git diff --check`并复读proposal/design/specs/tasks。Done：九个rerun-only BUG均有requirement、implementation task、focused regression与controlled evidence；new Topic走normal pipeline；十个formal Gate与三个Wave inspect的全部failure exits均有root-first hint；所有production definition readers共享one parser；existing finding被泛化且无string-priority/prose-trend authority；无persistent lineage graph、shadow ledger、new Gate/controller/lifecycle/success authority。
