## 0. Apply Gate

- [ ] 0.1 `openspec-feedback:plan-review` - Review proposal, design, `verification-plan.yaml`, and `semantic-closure.yaml` before the first target edit. Confirm the three failures are expectation/fixture drift against their cited Sources of Record, that `not_applicable` remains honest, and that no production behavior change is hidden. Done when every actionable finding is an ordinary unchecked task with an authoritative owner, smallest repair, and independently observable done condition.
- [ ] 0.2 @impl VER-001 Run `node openspec/governance/check-project-reqs.mjs --mode plan`, `node openspec/governance/check-verification-routing.mjs --change restore-deterministic-e2e-regression-baseline --mode plan`, and `node openspec/governance/check-semantic-closure.mjs --change restore-deterministic-e2e-regression-baseline --mode plan` before target edits. Done when all commands exit zero and their structural PASS results are not described as native test evidence.
- [ ] 0.3 Run mandatory planning polish after proposal completion: complete whole-change coherence and a distinct risk-led review of fixture dependency closure, release-coordinate source selection, and C5-versus-formal-Gate separation. Record readiness in `polish.md` and repair any planning finding before Apply; polish is not target-edit permission or runtime proof.

## 1. Native Finalizer Fixture

- [ ] 1.1 @impl CHF-003, CHF-004 Move the one-call finalizer proof to `tests/integration/governance/change-feedback-loop-archive.test.mjs`, then update its isolated project fixture to supply the finalizer; all six invoked governance checkers; their local contracts; a valid registry/main spec/catalog/semantic-fact catalog; and valid skip-specs, discovery, routing, and closure change records. Assert the ordered archived `checks` list is exactly `openspec_status`, `artifacts`, `tasks`, `strict_validation`, `requirement_governance`, `main_spec_governance`, `capability_taxonomy`, `capability_discovery`, `verification_routing`, `semantic_closure`, `native_archive`. Done when its production-finalizer subprocess returns that native outcome and complete sequence with no mocked finalizer result, omitted check, artificial delta spec, or false temporary-bundle runtime claim.

## 2. Current Contract Assertions

- [ ] 2.1 @impl FRE-001, WDC-001, WDC-004, WDC-005 Update `tests/e2e/deep-research-harness-migration.test.mjs` to derive and compare both `RUN.md` current-release declarations, find the matching top-level `CHANGELOG.md` section with a non-empty release entry, and retain the no-Harness-local-changelog assertion rather than pinning `v0.74`. Done when the existing temporary-bundle isolation assertions and current release-coordinate assertions pass without changing a production release file.
- [ ] 2.2 @impl RES-001, RES-002, REI-006, POF-001, POF-002, POF-003, RRD-002, RRD-008 Update `tests/e2e/post-final-rerun-lineage-continuity.test.mjs` to preserve C5's event-bound compatibility assertion, assert the formal stale-style Gate root, run the existing style writer, and rerun the same Gate to PASS. Done when the test proves both boundaries without a test-only freshness exception.

## 3. Verification And Archive

- [ ] 3.1 Run each selected native test asset directly, then run the repository test command. Done when the selected integration and deterministic-E2E tests plus the full suite pass with native `node:test` exit evidence.
- [ ] 3.2 `openspec-feedback:closeout-review` - Review the change-scoped actual diff, selected native test results, verification plan, and semantic-closure record. Done when no production behavior/spec/version edit or unresolved fixture/expectation finding remains; otherwise record ordinary pending repair tasks and leave this marker open.
- [ ] 3.3 @impl VER-001 Run `openspec validate restore-deterministic-e2e-regression-baseline --strict`, `node openspec/governance/check-project-reqs.mjs --mode archive --change restore-deterministic-e2e-regression-baseline`, `node openspec/governance/check-project-specs.mjs`, `node openspec/governance/check-verification-routing.mjs --change restore-deterministic-e2e-regression-baseline --mode assets`, and `node openspec/governance/check-semantic-closure.mjs --change restore-deterministic-e2e-regression-baseline --mode assets`. Done when every command exits zero and structural checks are not reported as test execution proof.
- [ ] 3.4 After all earlier tasks and any review-created repair tasks are complete, mark this archive-transition task complete immediately before invoking `node openspec/governance/finalize-change-archive.mjs --change restore-deterministic-e2e-regression-baseline`. If it returns non-zero, restore this task to unchecked and repair its named root. Done when the governed finalizer reports native archive success.
