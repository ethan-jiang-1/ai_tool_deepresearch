## 1. Apply Admission And Contract Surfaces

- [ ] 1.1 Run `node openspec/governance/check-verification-routing.mjs --change fix-seed-topic-projection-materialization --mode plan` before target edits; resolve any route/claim failure in `verification-plan.yaml`. Done when the plan-mode verdict passes. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)
- [ ] 1.2 Create the apply target manifest recording added control surfaces (slot map with heading/card descriptors and declared legacy aliases, packet schema, one pure readiness evaluator) and deleted/merged surfaces (two complete templates, manual token replacement, duplicate projection interpretation). Done when it maps each surface to its direct Source of Record and no target code has been edited before 1.1 passes. (CTS-004, STM-001, RRM-003, RRM-007, RWP-016)
- [ ] 1.3 Reconcile requirement traceability for the modified existing IDs and add only any genuinely required new registry entries; do not allocate an ID for an implementation helper. Done when spec deltas, code `@impl` annotations, tasks and registry ownership agree. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)

## 2. Canonical Slot And Writer

- [ ] 2.1 Implement the frozen Appendix Slot Map beside the canonical topic-state renderer and make renderer/locator use it. Done when each canonical heading, declared legacy heading base plus bounded suffix policy, initial token, Wave owner, source identity kind, merge rule and `回填卡` descriptor has one executable definition. (STM-001, RRM-002, RRM-003)
- [ ] 2.2 Extend `TopicApplyPlanSchema` with strict `wave_projection/apply_seed_projection` packet schemas and exact Wave lifecycle authorization. Done when unsupported raw Markdown/path/heading/patch input, a Wave0/1 work-id/ordinal mismatch, a Wave2 `entry_id`/source-W2F mismatch or legacy-round/wrong-topic finding, and non-current/historical/post-final callers are rejected before workspace creation. (CTS-004, RRM-002)
- [ ] 2.3 Implement authority-bound slot validation and atomic packet materialization through the existing topic-state workspace/recover path. Done when Wave0/1 submitted identities and current-round Wave2 W2F identities mapped to the packet topic can only write owned slots, Wave1 is all-or-nothing, Wave2 preserves Wave1 questions, replay is idempotent, a legal packet retains every `回填卡` while upgrading only its targeted declared legacy heading(s), and its transaction stages only the selected seed rather than `rb_plan.md`. (CTS-004, RRM-002)
- [ ] 2.4 Add post-write parser/readiness assertion and exact legacy-layout behavior. Done when interrupted writes recover through the existing exact command, a recognized legacy target changes only inside an otherwise legal targeted packet transaction, a selected missing target produces one `seed_projection_layout_missing` root, and a selected repeated target produces one `seed_projection_layout_ambiguous` root without inferred migration; those roots are writer-admission-only and never arise merely from read/inspect. (CTS-004, STM-001, RRM-007)

## 3. Shared Readiness And Gate Truth

- [ ] 3.1 Extract one pure projection-readiness evaluator that consumes canonical registry/seed binding, current submitted rows or current-round Wave2 finding facts mapped to each topic, executable slots and parsed entries. Done when it root-short-circuits unusable parents before dependent omissions, distinguishes valid deferred entries from generic prose, recognizes a validated packet-created Wave2 `entry_id` separately from the historical W2F-ref fallback, treats a token as blocking only where current direct-authority demand exists, and has no packet-selected-target/cardinality input. (RRM-002, RRM-007)
- [ ] 3.2 Refactor `inspectSeedTopicReturnMaps()` and all Wave inspect CLIs to consume the shared evaluator. Done when a demanded current token, wrong family/entry identity, invalid/missing identity, generic submitted prose and invalid concrete-first refs return one exact actionable root through the invoking inspect; a no-demand token does not require a synthetic entry, a Wave2 deferred entry may retain `refs: none` while its valid `entry_id` establishes identity, and repeated accepted headings retain the existing family-union read behavior. (RRM-007)
- [ ] 3.3 Integrate the same evaluator's structured findings directly into Wave0/1/2 formal contract evaluation and degradation eligibility; do not invoke an inspect CLI from a gate. Per invocation, load one normalized registry fact (and one Wave2 finding-index fact) and pass it to both the existing Wave contract evaluator and projection evaluator. Retire Wave1 `no_stale_mechanisms_token` / `no_stale_trends_token` / `no_stale_pending_questions_token` and Wave2 `backfill_judgment_token_absent` / `backfill_questions_token_absent` definition rules. Done when structural projection roots block handoff even at fatigue threshold, Wave0 token failure is covered by the same evaluator, and no second token/entry validator or independent authority read remains. (RRM-007)

## 4. Agent-Facing Template And Wave Loop

- [ ] 4.1 Create `shared-seed-topic-template.md`, migrate complete skeleton/slot/entry/packet/repair/rerun guidance into it, and retire the old full templates to short compatibility pointers or removal. Done when exactly one complete template remains, it states projection is not evidence authority, and its five precise canonical headings each retain a visible read-only `回填卡` with writer, authority, entry shape, legal action and prohibitions. (STM-001, RRM-003)
- [ ] 4.2 Update phase `requires`, shared schema guidance and `operate-topic-state` playbook to point at the one template and existing writer seam. Done when seed-topics, rerun and all three Wave phases load the template directly and no path asks an Agent to inspect helpers or hand-edit a seed. (STM-001, RRM-003, RWP-016)
- [ ] 4.3 Rewrite Wave0/1/2 closeout guidance to authority -> retained packet -> apply -> same inspect -> completion -> formal gate. Done when generic `WaveN submitted` prose and manual token replacement are prohibited and missing writer/authority feedback names the direct owner boundary. (RWP-016, RRM-002, RRM-007)

## 5. Deterministic Regression Evidence

- [ ] 5.1 Add focused unit coverage for slot-map/template/card parity, strict packet fields, owner/source binding, Wave2 `entry_id`/current-round source identity equality and target-topic binding with historical W2F-ref fallback, first token consumption, no-demand token behavior, idempotent correction, deferred disposition, cards never parsed as entries, targeted legacy-heading upgrade with suffix preservation, missing/ambiguous target rejection, no plan staging, repeated-heading read union, no partial Wave1 write and crash/recover behavior. Done when `tests/engine/helpers/seed-topic-projection.test.mjs` passes. (CTS-004, STM-001, RRM-002, RRM-003)
- [ ] 5.2 Add CLI integration coverage for each legal/illegal Wave window, including exact predecessor `current_gate` -> `next_gate` pairs; current/legacy/orphan seed boundaries; current work/W2F identity roots including legacy-round and wrong-topic finding rejection; legal targeted alias upgrade versus no read-time migration; partial/mixed legacy layouts; repeated-heading inspect/gate read compatibility versus writer-only ambiguous-target rejection; and inspect/formal-gate shared projection verdict. Update the existing gate-definition and Wave1/Wave2 gate CLI regressions so a residual demanded token yields only the evaluator's root, not a retired pattern-rule duplicate. Done when `tests/integration/cli/operate-topic-state-projection.test.mjs`, `tests/schema/gate-definition.test.mjs`, and the affected Wave1/Wave2 gate CLI regressions pass without test-local authority matching. (CTS-004, RRM-007)
- [ ] 5.3 Add a deterministic disposable-bundle Wave0 -> Wave1 -> Wave2 chain. Done when `tests/e2e/seed-topic-projection-materialization.test.mjs` proves completion ordering, valid projections, negative demanded-token/generic-prose paths, no-demand token non-fabrication, and degradation-ineligible structural failure through production CLIs. (CTS-004, RRM-002, RRM-007, RWP-016)
- [ ] 5.4 Update static Markdown/package tests for one complete template, direct phase loading, exact heading/card placement and prohibited duplicate/manual backfill guidance. Done when template parity and workflow-package validation pass. (STM-001, RRM-003, RWP-016)

## 6. Native Agent-Flow Evidence

- [ ] 6.1 Create and register `experiments_playbook/exp_seed-topic-projection/case-227-heavy-seed-topic-projection-materialization.md`. Done when it passes playbook/frontmatter/manifest routing validation and declares a real Phase Agent, a fresh disposable bundle, setup-only preconditions, native inspect/trace verdicts and conditional cleanup. (RRM-003, RWP-016)
- [ ] 6.2 Execute the registered case with the real required Agent runtime and record its native completion evidence. Done when the case proves only template/packet/writer/same-inspect usability; unavailable runtime is recorded as native NOT_RUN and is not replaced by fixtures or scripted output. (RRM-003, RWP-016)

## 7. Release And Governance

- [ ] 7.1 Update `CHANGELOG.md` for v0.53 and synchronize the `DPT_FRAMEWORK/RUN.md` version banner. Done when both name the same released version and concise projection-materialization behavior. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)
- [ ] 7.2 Run focused tests, relevant existing Wave/topic-state regressions, and full applicable JS suites; record native Agent-flow result separately. Done when failures are resolved or documented as pre-existing with evidence. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)
- [ ] 7.3 Run `node openspec/governance/check-verification-routing.mjs --change fix-seed-topic-projection-materialization --mode assets`. Done when every selected asset exists at its required route and the checker passes. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)
- [ ] 7.4 Run `node openspec/governance/check-project-reqs.mjs`. Done when it reports 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired IDs. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)
- [ ] 7.5 Run `node openspec/governance/check-project-specs.mjs`. Done when it reports 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader findings. (CTS-004, STM-001, RRM-002, RRM-003, RRM-007, RWP-016)
