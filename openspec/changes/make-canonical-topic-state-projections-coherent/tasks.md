## 1. Apply Baseline And Contract Fixtures

- [x] 1.1 Run `node openspec/governance/check-verification-routing.mjs --change make-canonical-topic-state-projections-coherent --mode plan` before any target edit, then read the selected test assets and current direct-owner surfaces. Done when the apply log identifies the contribution writer, reader, packet writer, style writer, and both Gate consumers without introducing another owner. (DEW-005, CTS-004, RRM-004, RRM-007, RES-002, PRG-002, REI-003)
- [x] 1.2 Add focused red regressions for BUG-151, BUG-152, BUG-154, BUG-157, and BUG-176 in the selected unit/integration assets. Done when each regression reaches a real production helper or CLI boundary and fails for its direct root rather than a test-local reimplementation. (DEW-005, CTS-004, RRM-004, RRM-007, RES-002, STM-001, STM-002)

## 2. Submit-Bound Wave0 Contributions

- [x] 2.1 Implement the narrow Engine-derived `source_contribution` declaration for successful current-version `wave0_source_intake` first acceptance, including strict schema/ledger hashing and semantic ordered-array digest helpers. Done when the declaration contains only target, direct-contract ID, validated length, and digest, is never caller supplied, and non-Wave0 outputs do not receive it. (DEW-005)
- [x] 2.2 Preserve recorded contribution declarations in duplicate submit and late-submit replay, keep an already-present declaration recovery idempotent, and retain only exact hash-proven legacy no-contribution reconstruction. Done when no historical operation re-evaluates mutable source bytes or creates a new contribution acceptance, and a missing row whose hash requires `source_contribution` returns one missing-contract/no-legal-recovery boundary without ledger mutation. (DEW-005)
- [x] 2.3 Extend focused submit and projection unit tests for first acceptance, duplicate/replay retention, exact legacy reconstruction, semantic-digest presentation tolerance, non-Wave0 exclusion, and lost-contribution failure. Done when a production submit followed by source mutation plus deletion of its current contribution row cannot recover or append provenance, while each permitted path asserts the hash-bound declaration through production submit/read paths. (DEW-005, RRM-007)

## 3. Contribution-Aware Wave0 Reading

- [x] 3.1 Replace historical Wave0 candidate expansion with one ordered contribution reader grouped by canonical topic and exact direct target. Done when legal append intervals own disjoint global ordinals, duplicate URLs remain distinct, and the reader does not rediscover all historical ownership from the live array. (RRM-007)
- [x] 3.2 Implement direct parent roots and masking for prefix drift, shortened/reordered prefixes, unsubmitted suffixes, non-monotonic declarations, and any mixed/multi-row same-target legacy group; retain bounded singleton-legacy readability. Done when no dependent candidate omission is emitted below an unusable contribution parent and the singleton fallback does not invent a contribution boundary. (RRM-007)
- [x] 3.3 Wire the same reader into Wave0 packet identity admission, inspect, and formal Wave0 Gate composition. Done when writer, inspect, and Gate accept the same exact contribution-owned coordinate and Wave1/Wave2 behavior is unchanged. (CTS-004, RRM-007)
- [x] 3.4 Extend the Wave0 projection and Gate integration tests for legal nineteen-to-twenty append ownership, legacy ambiguity, source-prefix drift, and current direct-output compatibility. Done when assertions inspect structured production findings rather than source-file timestamp/order guesses. (RRM-007)

## 4. Projection Transaction And Navigation

- [x] 4.1 Extract selected-slot entry parsing/rendering into a dependency-neutral pure projection-entry contract helper, then refactor upsert so replacement preserves block separators and the staged selected family is independently parseable before publication. Done when replay is idempotent, neighboring entries remain separate, cards/tokens are preserved, malformed selected neighbors reject before workspace publication, and no return-map/canonical-topic-state import cycle is introduced. (CTS-004)
- [x] 4.2 Put the normalized concrete-navigation evaluator in that same dependency-neutral helper and share it between Projection Packet admission and return-map readiness. Done when evidence-bearing packets require an existing safe flat `reference/*.md` target, accepted deferred dispositions remain legal, bounded basename near matches are diagnostic only, and neither caller owns a second classification. (CTS-004, RRM-004)
- [x] 4.3 Wire packet preflight, staged postcondition, inspect, and formal Wave Gates to their shared parser/navigation results without recursive inspect or duplicate validators. Done when a writer-successful packet satisfies the reader path, parent roots mask dependent omissions, and retired Wave1/Wave2 token rules cannot emit a second result beside the shared projection evaluator. (CTS-004, RRM-004, RRM-007)
- [x] 4.4 Extend canonical-topic-state, return-map, and production packet/inspect tests for adjacent-entry corruption, exact identity upsert, missing/near-match reference feedback, deferred navigation, selected-family scope, and one-root current-demand token handling. Done when no test accepts raw Markdown patching as a substitute for the CLI transaction or permits a Gate-specific token validator to duplicate the shared evaluator. (CTS-004, RRM-004, RRM-007)

## 5. Seed Body Boundary And Guidance

- [x] 5.1 Render stable `seed-initialization` start/end markers and an Engine-owned appendix boundary from the executable seed descriptor; update template parity descriptors and Wave0 card wording for contribution-owned ordinals. Done when a new seed has one editable initialization region, ordered cards/tokens, and no competing source-authority claim. (STM-001)
- [x] 5.2 Extend `seed-topics-ready` current-marker checks while preserving legacy body readability. Done when a template ghost below the initialization boundary returns one structural root, but a legacy duplicate body does not trigger migration or infer authority. (STM-002)
- [x] 5.3 Align seed, Wave0, and command-playbook guidance with contribution-reader feedback and bounded initialization edits. Done when guidance tells the Agent to use the existing writer/inspect loop and never to infer history, hand-edit a seed, or fabricate ledger provenance. (STM-001, RWP-001, RWP-014)
- [x] 5.4 Extend template/parity, seed-enrichment, seed Gate, and Wave0 guidance integration tests, including the Seed projection document contract. Done when tests cover the current-marker ghost, legacy compatibility, exact contribution ordinal wording, the one editable initialization boundary, and no packet-protocol duplication in templates. (STM-001, STM-002, RWP-001, RWP-014)

## 6. Style Feedback Loop

- [x] 6.1 Return a structured `style_projection` handoff only after a committed registry-length change, with selected profile, committed count, existing CLI command, and same checkpoint; keep topic-state free of profile writes. Done when add/safe-remove expose the handoff and projection/enrichment/rename/reorder do not. (CTS-004, RES-007, RES-008)
- [x] 6.2 Add one side-effect-free current-style freshness evaluator based on the existing pure style computation, and use it from HITL1-recorded and rerun-ready only after their existing prerequisites. Done when stale/absent/partial/wrong-profile params produce one masked-or-direct root with the existing writer and same-Gate rerun, without a second bundle reader, profile writer, or Gate-specific comparison. (RES-002, PRG-002, REI-003)
- [x] 6.3 Align HITL1 and rerun Phase guidance with `profile selection -> topic-state commit -> returned style handoff -> same Gate`; preserve C5's accepted event-bound style/count behavior. Done when rerun consumes the handoff's exact command before count increment, no-length-change guidance does not parse a profile or launch an unconditional style CLI, no guidance asks the user to perform the mechanical refresh, and neither Gate is presented as a style writer. (RES-002, PRP-002, REI-003)
- [x] 6.4 Extend topic-state handoff, style computation/apply, HITL1/rerun Gate, and HITL1/rerun Markdown integration tests for fresh pass, stale/absent root, prerequisite masking, profile preservation, length-bound handoff, same-check repair feedback, and legal pre-count-increment C5 continuation. Done when CLI assertions use production JSON/findings and Markdown assertions cover only the declared command/ownership loop. (RES-002, RES-007, RES-008, PRG-002, PRP-002, REI-003)

## 7. Evidence, Release, And Governance

- [x] 7.1 Run every selected unit and integration asset in `verification-plan.yaml`. Done when their native `node:test` outcomes prove only deterministic contracts declared in the plan.
- [x] 7.2 Run one real existing/disposable bundle replay for a legal Wave0 supplement plus one controlled contribution-prefix failure. Done when recorded evidence shows disjoint ordinal ownership, one direct failure root, and no claim of Agent behavior or fabricated runtime trace.
- [x] 7.3 Update `CHANGELOG.md` for `v0.62` and synchronize the `DPT_FRAMEWORK/RUN.md` version banner/release note. Done when both describe canonical projection coherence without claiming a new controller, repair service, or evidence authority. (DEW-005, CTS-004, RRM-004, RRM-007, RES-002, STM-001)
- [x] 7.4 Record the C2 disposition and verification coordinates in BUG-151, BUG-152, BUG-154, BUG-157, and BUG-176. Done when each card points to the implemented direct owner and any intentionally preserved legacy boundary.
- [x] 7.5 Run `node openspec/governance/check-verification-routing.mjs --change make-canonical-topic-state-projections-coherent --mode assets`, `openspec validate make-canonical-topic-state-projections-coherent --strict`, `node openspec/governance/check-project-reqs.mjs`, `node openspec/governance/check-project-specs.mjs`, and `git diff --check`. Done when every applicable check passes with no unrelated file modified to force a pass.
