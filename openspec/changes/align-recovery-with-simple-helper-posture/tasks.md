## 1. 统一演进指导

- [ ] 1.1 支持 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003 的 simplicity direction：将 `guidelines/simple-reliable-control.md` 的完整 canonical 内容迁移到 `guidelines/evolution-simple-reliable-control.md`，保留 `guideline_id: simple-reliable-control`，使用带引号的 `title: "Evolution Direction: Simple Reliable Control"`，更新 role/scope/defers_to/siblings，并按 charter-grade skeleton 保留 Purpose、Standing/Precedence、Core Direction、simplicity-only disciplines、Gradual Convergence、focused `Simplicity Admission Test` 与 Boundary；done condition 是只拥有 simplicity/net-simplification 轴，保留 direct authority、state ownership、short-circuit、same-check repair、fail-closed、testability、真实执行证据与 compatibility retirement，不新增 `family` / `axis` metadata，也不包含 helper responsibility。
- [ ] 1.2 支持 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003 的 helper direction：新建 `guidelines/evolution-helper-oriented-agent.md`，使用 `guideline_id: helper-oriented-agent`、带引号的 `title: "Evolution Direction: Helper-Oriented Agent"`、`authority: guidance`，并在 `defers_to` 中显式包含 `guidelines/project-charter.md`；按同一 charter-grade skeleton 覆盖 autonomous/human-directed authority、HITL/out-of-band placement、human decision vs Agent execution、escalation、no-command-runner-transfer、行动边界上的 no-fabrication、Gradual Convergence、focused `Helper Direction Review` 与 Boundary；done condition 是使用足够的 MUST/MUST NOT 约束未来 design，明确 Agent 不得为“帮助”而手写 authority/evidence 或假造权限，但不定义 runtime state/CLI/permission/persona/memory/helper subsystem，也不复制 simplicity disciplines。
- [ ] 1.3 支持 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003 的 Charter/index 地位：更新 `guidelines/project-charter.md` 与 `guidelines/README.md`，在 Charter 后建立并列 `Evolution Directions` 阅读层，reading order/precedence/decision routes/guidance map 分别指向两个 canonical 文件，并明确 architecture/change/recovery/helper review 默认 paired-load 两个 `evolution-*` 文件；Charter checklist 保留 AGENTS/config/spec/runtime precedence、Agent/Markdown/Engine ownership、future-vs-current truth、OpenSpec routing 等 authority/layer checks，把 complexity 与 action-responsibility 问题分别路由到两个 focused review；done condition 是两份文件满足 stable direction、normative guidance、explicit precedence、actionable routing、gradual convergence、owned boundary 六项宪章级指导契约，而不成为新的 Source of Record。
- [ ] 1.4 支持 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003 的 active 路径迁移：逐项核对 design 的 Active migration manifest，更新 Charter/index、7 个 mechanism/support guideline 的 sibling/prose/related links，以及 human-override、breakpoint-recovery 两个 active plan 的 design-principle links；两个 active plan 同时引用 simplicity/helper directions；done condition 是所有 active navigation/reference links 指向正确 canonical `evolution-*` 文件，且不修改 closed plan 或 `openspec/changes/archive/`。
- [ ] 1.5 支持 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003 的历史可读性：将旧 `guidelines/simple-reliable-control.md` 改为不带 frontmatter 的短普通 Markdown compatibility notice，只说明 canonical 文件已迁移并链接 `evolution-simple-reliable-control.md`；正文不含 MUST/MUST NOT 或独立原则，也不进入 README reading order、Guidance Map 或 Evolution Directions；done condition 是 notice 不拥有 `guideline_id` / authority / status metadata，closed plan 与两个 archived OpenSpec links 仍可沿旧路径找到 canonical guidance。
- [ ] 1.6 支持 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003 的 source coverage：把 human-override plan、breakpoint-recovery plan 与 BUG-079 提炼出的 authority context、helper responsibility、`materialize-before-work`、`canonical-or-blocked` 和 gradual convergence 分别路由到正确 direction、capability delta 或 proposal/design；done condition 是两个 evolution 文件不复制 backlog 路径或 incident 细节，不把来源标记为 runtime-fixed。

## 2. 对齐 Capability Contracts

- [ ] 2.1 实现 ACS-001：最小修改 `DPT_FRAMEWORK/COMMANDS.md` 的现有 `Audience And Interaction Contract`，加入 Agent-owned ordinary execution、human-directed decision source without command-runner transfer、human request 不自行扩展 host permission/Engine capability、repairable blocker/no-legal-path 行为，以及 autonomous/human-directed authority、HITL/out-of-band placement 的区分；done condition 是不声称 arbitrary override/reentry 已存在，不新增 command、state、checkpoint 或 runtime mutation path。
- [ ] 2.2 实现 ACS-003：扩展现有 `tests/engine/command-contract-docs.test.mjs` 对顶层 `COMMANDS.md` 的 marker assertions，验证 ACS-001 新增的 Agent-owned mechanical execution、human-directed decision-source semantics、no-permission/no-capability-by-request boundary、autonomous/human-directed authority distinction 和 HITL/out-of-band placement distinction；done condition 是不要求每个 playbook 复制顶层 audience statement，并且复用现有 test surface，不新增 validator、phrase class、prose-quality regex 或 test harness。
- [ ] 2.3 对齐 CDP-001：核对 `hitl-ux`、profile enum、transition chain 和 `phase-hitl2.md` 主体已满足 delta 中的 five-enum contract；修正 Stage Goal 的 `rerun from instantiation` 与 On Gate Fail 中不存在的 blocking `trace_event_present` failure；done condition 是 `repair` 表示 Agent 就地修复并 rerun HITL2 gate，`rerun` 表示 deterministic handoff 到 `phase-rerun`，`hitl2_recorded` 只作为 diagnostic event 保留，不存在旧 enum/restart 或虚假 gate-failure guidance，且不修改 Engine logic。
- [ ] 2.4 对齐 CDG-001、CDG-003：核对 `gate-hitl2-recorded.definition.json` 与 `check-gate-hitl2-recorded.mjs` 已满足 delta，并扩展 `tests/integration/cli/check-gate-hitl2-recorded.test.mjs` 的现有 non-deterministic decision case，明确断言 `repair` 时 `check.passed: true`、exit `0`、`routing.kind: no_transition`、`check.next: null`，不得默认 readiness；done condition 是 definition 接受 five-enum、没有 blocking `hitl2_recorded` trace rule、CLI 仍写 `gate_attempt`，且只有 `proceed_to_readiness` / `rerun` 产生 deterministic handoff，不修改 gate runtime。
- [ ] 2.5 对齐 SCO-001、SCO-007：保持 `DPT_FRAMEWORK/schema/enums.mjs` 不变，修正 `tests/schema/enums.test.mjs` 的 CurrentGate/HITL2 test 名称与 value lists，使其分别覆盖当前 11 个 gate values 和 6 个 HITL2 schema values（`not_started` + 5 recorded decisions），并在现有 `check-gate-hitl2-recorded` regression 中明确断言 `not_started` 被 gate enum 拒绝；done condition 是 schema、既有 SCO-001 contract、SCO-007 delta、SHC wording、gate enum 与 test 描述一致，不把 sentinel 当 actionable decision。
- [ ] 2.6 对齐 SHC-001、SHC-002：按 `ProfileSchema` 全量校正 `shared-profile.md`，补 `research_access`、`research_style_params`、`rerun_count`、`rationale`，删除不存在的 nested HITL1/user_feedback contract，把 `user_decision` 初值改为 `not_started`，并使 `repair` 与 `rerun -> phase-rerun` 分离；更新 `shared-gate-rules.md` 的 `hitl2-recorded` row，使其只投影当前 definition rules、shared handoff preflight 与 CLI-authored `gate_attempt`，不再列 blocking `trace_event_present` 或旧 status rule；done condition 是 shared prose 与 schema/CDP/CDG/definition/CLI 一致，仍明确自身是 generated summary，且不把 diagnostic `hitl2_recorded` event 写成 verdict authority。
- [ ] 2.7 对齐 CDE-001、CDE-002、CDE-003、CDE-005：按 `guidelines/command-experiments.md` 更新 `exp_wff_delivery/case-131`、`case-132`、`case-134`、`case-135` 的结构与 fixtures/steps，复用现有 `new-disposable-bundle.mjs` 和薄 Node.js fixture block，使用真实 predecessor `gate_attempt` + route-bound `load_complete`、正确 source-gate status window 和现行 gate output；done condition 是不手搓 template copy、不新增 runner/helper subsystem、不预置目标 gate 为已通过、不手写被测 gate result，repair case 回同一 gate，最终 verdict 来自真实 bundle/trace。
- [ ] 2.8 对齐 CDE-004：按 `guidelines/command-experiments.md` 重写 `exp_wff_delivery/case-133-standard-hitl2-rerun.md` 的旧 Agent-level routing proof，复用现有 shared infrastructure，验证 real HITL2 gate 输出 `check.next: phases/phase-rerun.md`、CLI-authored matching `gate_attempt`、chain 同时含 `passed`/`rerun` 两条 deterministic edge、`enter-phase`/`advance-status`/rerun preflight 接受该 route；done condition 是不新增第二 runner/helper，active experiment prose 不再声称 chain 只到 readiness、Agent 覆盖 `check.next` 或 restart from instantiation。
- [ ] 2.9 对齐 REI-003、REI-004、REI-005、CPT-003、CPT-004、WNC-010 的 light consumers：核对并按需更新 G14 `case-140` 至 `case-142` 与 G24 `case-301` 至 `case-305`，使 HITL2/rerun cases 使用 real gate-selected target、route-bound `enter-phase` witness、source-gate `advance-status` 和正确 incoming status window；done condition 是不得在被测 gate 前手写 `current_gate` 为目标 gate、不得手写被测 `gate_attempt`，indeterminate/max-count negative cases 仍 fail closed，且不新增 helper/runner。
- [ ] 2.10 对齐 AGT-010、CPT-003、CPT-004、WNC-010 的 full-chain consumers：更新 G5 `case-51`/`case-52`，使 happy/repair walker 消费 real gate output、route-bound entry witness、source-gate status sync，并正确处理 terminal/no-transition output；done condition 是不再逐 phase 预写目标 gate 为已通过，不用 `check.next` 非空作为所有 gate pass 的统一条件，且保留真实 fail→repair→rerun evidence。
- [ ] 2.11 对齐 REI-002、REI-003、REI-004、PLR-001、PLR-003 的 two-round/index consumer：更新 G24 `case-306` 的 two-round rerun status/handoff truth，并同步 `experiments_playbook/RUN_EXPS.md` 中 G5/G13/G14/G24 的 case 描述；done condition 是 runner current list 与 case truth 一致，two-round verdict 仍明确自身证明边界，且不新增批量等价 runner。
- [ ] 2.12 同步 requirement registry：更新 `openspec/governance/req-registry.yaml` 中 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005 的稳定描述；done condition 是不分配、退役或复用任何 requirement ID。
- [ ] 2.13 验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005 及既有 REI/CPT/WNC/AGT/PLR consumer obligations 的 cross-capability boundary：逐项核对 design matrix 中 modified、reviewed/unchanged 与 reviewed/deferred 理由；done condition 是 guideline prose 未暗中改变 capability，schema sentinel/action 边界清楚，shared projections 未成为第二 authority，controlled E2E 只证明当前 accepted behavior，且没有重复近似 requirement。

## 3. Version Management

- [ ] 3.1 实现 VEM-002、VEM-004：在 repo-root `CHANGELOG.md` 新增 `v0.21` 简洁条目，概括 Evolution Directions、Agent-owned helper execution、HITL2 spec/projection 与 controlled-E2E reconciliation；done condition 是不声称 override/reentry/persistence runtime 已实现。
- [ ] 3.2 实现 VEM-003：将 `DPT_FRAMEWORK/RUN.md` 版本横幅和 Current Release 同步到 `v0.21`，release bullets 与 CHANGELOG 最新条目一致。

## 4. 范围与回归验证

- [ ] 4.1 回归验证 ACS-001、ACS-003：运行 `node --test tests/engine/command-contract-docs.test.mjs` 必须 PASS，且 failure message 能指出缺失的 stable marker。
- [ ] 4.2 reconciliation 验证 SCO-001、SCO-007、CDP-001、CDG-001、CDG-003、SHC-001、SHC-002：运行 `node --test tests/schema/enums.test.mjs tests/schema/profile.test.mjs tests/engine/transition-chain.test.mjs tests/integration/cli/check-gate-hitl2-recorded.test.mjs tests/integration/md/no-phase-bypass-advice.test.mjs` 必须 PASS；确认 11-value CurrentGate、6-value HITL2 schema / 5-action gate boundary、`rerun -> phase-rerun`、`repair` pass 且 `next:null`、缺少 `hitl2_recorded` event 不阻塞 gate，并用 focused text audit 确认 active phase/shared guidance 不再包含旧 HITL2 contract。
- [ ] 4.3 controlled E2E 验证 CDE-001 至 CDE-005：按 `guidelines/command-experiments.md` 逐步真实执行 `exp_wff_delivery/case-131` 至 `case-135`，每个 case 必须从自己的 disposable bundle/trace 得出 PASS 并在 PASS 后 cleanup；done condition 是不得用批量等价脚本、静态阅读或手写 result 替代 playbook execution，FAIL 时保留现场并停止归档。
- [ ] 4.4 adjacent light E2E 验证 REI-003、REI-004、REI-005、CPT-003、CPT-004、WNC-010：按原 Markdown 逐步执行 G14 `case-140` 至 `case-142` 与 G24 `case-301` 至 `case-305`；done condition 是每个 case 使用自己的真实 bundle/trace 裁决并 PASS，negative cases 保持 expected fail-closed 语义，PASS 后 cleanup，且不得用聚合 runner 替代逐 case execution。
- [ ] 4.5 adjacent standard E2E 验证 AGT-010、PLR-001、PLR-003、REI-002、REI-003、REI-004：逐步执行 G5 `case-51`/`case-52` 与 G24 `case-306`；done condition 是 full-chain、repair 与 two-round rerun 各自从真实 runtime evidence 得出 PASS，FAIL 时保留现场并停止归档。
- [ ] 4.6 version 验证 VEM-002、VEM-003、VEM-004：运行 `node --test tests/engine/version-management.test.mjs` 必须 PASS，且 `CHANGELOG.md` 最新版本与 `DPT_FRAMEWORK/RUN.md` 横幅均为 `v0.21`。
- [ ] 4.7 范围验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005、VEM-002、VEM-003、VEM-004 及既有 REI/CPT/WNC/AGT/PLR consumer obligations：检查 implementation diff，只允许两个 canonical `guidelines/evolution-*.md`、旧路径 notice、Project Charter、Guidelines Index、受影响 active guideline/plan references、`DPT_FRAMEWORK/COMMANDS.md`、`phase-hitl2.md`、`shared-profile.md`、`shared-gate-rules.md`、G5 `case-51`/`case-52`、G13 `case-131` 至 `case-135`、G14 `case-140` 至 `case-142`、G24 `case-301` 至 `case-306`、`experiments_playbook/RUN_EXPS.md`、`tests/schema/enums.test.mjs`、`tests/engine/command-contract-docs.test.mjs`、`tests/integration/cli/check-gate-hitl2-recorded.test.mjs`、requirement registry、`CHANGELOG.md`、`DPT_FRAMEWORK/RUN.md` 与本 change artifacts；不得修改 closed plan、OpenSpec archive、runtime schema/state/CLI/gate definition/transition/event/capability ID，也不得关闭两个 source plans 或 BUG-079。
- [ ] 4.8 路径验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003：先以 design 的 Active migration manifest 逐项核对，再用 `rg --pcre2 '(?<!evolution-)simple-reliable-control\\.md'` 区分旧 basename 与 canonical 新文件名；done condition 是 active navigation/sibling/design-principle links 全部使用 `evolution-simple-reliable-control.md`，旧 basename 只出现在 compatibility notice、closed/archive historical text 和本 change 的 migration-contract 说明中；historical files 未修改且旧路径可达；两个 canonical `evolution-*` 文件在文件列表、README 与 Charter 中稳定相邻。
- [ ] 4.9 一致性验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005：逐项核对 proposal Source Coverage、cross-capability matrix、13 个 modified requirements、既有 consumer obligations、两个 direction ownership、design dependency bands、delta scenarios 与 tasks，确保 current apply、spec/projection/experiment reconciliation 和 follow-up runtime obligations 没有互相冒充、重复或遗漏。
- [ ] 4.10 OpenSpec 验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005：运行 `openspec validate align-recovery-with-simple-helper-posture --strict` 必须 PASS。
- [ ] 4.11 Requirement governance 验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005：运行 `node openspec/governance/check-project-reqs.mjs` 必须 PASS（0 duplicate / 0 orphan / 0 unregistered / 0 reusedRetired）。
- [ ] 4.12 Main-spec governance 验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005：运行 `node openspec/governance/check-project-specs.mjs` 必须 PASS（0 deltaHeaderInMain / 0 missingPurpose / 0 missingRequirements / 0 missingReqHeader）。
- [ ] 4.13 文档与补丁卫生验证 ACS-001、ACS-003、CDP-001、CDG-001、CDG-003、SCO-007、SHC-001、SHC-002、CDE-001 至 CDE-005、VEM-002、VEM-003、VEM-004：运行 `git diff --check` 必须 PASS，并确认两个 canonical guideline 使用现有 frontmatter vocabulary、title 冒号已正确引用、simplicity/helper `guideline_id` 唯一、compatibility notice 无 frontmatter、repo 未新增 `family` / `axis` metadata taxonomy。
