## 1. Pre-Target Verification And Red Evidence

- [ ] 1.1 @impl DEW-009, SNC-007, RWP-015: Create change-root `implementation-evidence.md`, record the pre-apply commit/worktree baseline, and run `node openspec/governance/check-verification-routing.mjs --change deliver-work-unit-role-contracts-to-actors --mode plan` before target edits; done when all five claims validate, deterministic E2E is explicitly not applicable, and deterministic assets do not claim actor/search/fetch behavior.
- [ ] 1.2 @impl DEW-009: Add red `tests/engine/work-unit-contract-delivery.test.mjs` for closed registered role-key derivation, contained shipped role/shared refs, missing/unknown/mismatch fail-closed behavior, direct-contract descriptor/evaluator ID parity and immutable bounded descriptor output; done when current code fails because neither projection owner exists, without constructing an envelope or adding a second heading list in the test.
- [ ] 1.3 @impl DEW-009, RWP-015: Add red `tests/integration/engine/work-unit-role-contract-delivery.test.mjs` over real primary/supplementary Wave1 claim envelopes; done when current generated task/spawn fails canonical role/shared absolute-ref and contract-owned minimum authoring assertions, while existing identity/path/assignment/result-schema bindings, supplementary no-paired-rewrite and zero partial publication on invalid lineage remain explicit.
- [ ] 1.4 @impl SNC-007, RWP-015: Add red `tests/integration/md/subagent-fetch-contract-delivery.test.mjs` for one shared per-URL chain, role/shared ref delivery, exact bounded curl/permission/exhaustion/batching/cache/source/receipt boundaries and absence of Python, wget, role-local duplicate chains, automatic retry and user command handoff; done when current duplicated/missing/Python guidance fails only those target assertions without external calls.

## 2. Contract-Owned Projections And Envelope Delivery

- [ ] 2.1 @impl DEW-009: Deepen the existing direct-output contract module so each closed ID owns its evaluator and one bounded immutable actor descriptor; done when the descriptor names only contract purpose/minimum structure, unknown/missing descriptors fail closed, fresh-byte evaluation and root semantics are unchanged, and no envelope/test/role file owns an independent heading inventory.
- [ ] 2.2 @impl DEW-009: Implement pure canonical role-guidance projection from existing registered work-unit role keys; done when repo-relative role ref, contained absolute regular-file path and explicit shared refs derive without queue/actor input, unknown/mismatch/missing/escape fails with one nearest owner-repair action, and no registry, persisted role field, snapshot or hash is added.
- [ ] 2.3 @impl DEW-009, RWP-015: Wire validated role/shared projections and direct descriptors into `work-unit-envelope.mjs` task/spawn generation before publication; done when primary Wave1 actor sees exact role/read paths plus paired authoring semantics before work, supplementary empty-required-output remains unchanged, task remains a projection rather than acceptance voter, and invalid lineage leaves no partial work-unit envelope or runtime mutation.
- [ ] 2.4 @impl DEW-009: Audit assignment compatibility; done when index/manifest/beacon/result schema/version, queue snapshot/resolver, dry-submit/formal submit, receipt/provenance/ledger/Gate/transition owners and historical claimed envelopes are unchanged, with no actor-selectable ref/contract, migration, second submit or parallel success path.

## 3. Shared Agent-Facing Fetch Contract

- [ ] 3.1 @impl SNC-007: Add one canonical shared page-fetch guidance surface for relevant delegated roles; done when each URL uses available built-in/browser then Node `fetch` and at most one exact hardened same-URL curl under independent host permission, real content is required, multi-URL batching stays separate, and failure returns bounded cache/source/receipt facts or the smallest external boundary without another tier/controller/retry.
- [ ] 3.2 @impl SNC-007, RWP-015: Update active source-intake, evidence-extractor, topic-scout, claim-verifier and source-diagnostic role/protocol surfaces to reference the shared fetch owner and generated canonical role delivery; done when role-specific search/output obligations remain, duplicate fetch chains and Python fallbacks are removed, Phase Agent does not hand-author actor semantics, and user/Agent/Engine responsibility matches the two evolution guidelines.
- [ ] 3.3 @impl DEW-009, SNC-007, RWP-015: Run the unit, envelope and Markdown red suites plus `node DPT_FRAMEWORK/cli/validate-work-unit-hygiene.mjs` and `node DPT_FRAMEWORK/cli/validate-workflow-package.mjs`; done when all deterministic delivery/hygiene/package checks pass with no network or Agent-behavior claim.

## 4. Real Actor Verification And Regression

- [ ] 4.1 @impl DEW-009, SNC-007, RWP-015: Strengthen existing manifest-registered case-221 and its existing observer/runner surfaces without adding a case or Subject identity; done when each real dpt-evidence-extractor starts from generated task-directed role/shared/authoring refs, retained public actor evidence and output hashes bind actor-owned first-return bytes before any Phase repair, dry-submit/formal submit/ledger/Gate remain native owners, and missing required public facts use the existing NOT_RUN protocol.
- [ ] 4.2 @impl DEW-009, SNC-007, RWP-015: Keep case-221 general first-return compliance and optional delegated fallback checks independent; done when actor-first paired direct PASS cannot be created by Phase/Playbook edits after work_done, native success/no fallback leaves the optional check absent, contradictory order/URL/command/count/facts append failed, exact complete native-failure-to-curl-success may append pass, and Supervisor health/audit/cleanup remain post-completion owners.
- [ ] 4.3 @impl DEW-009, SNC-007, RWP-015: Execute updated case-221 once through canonical Agent Experiment Autorun when authenticated Playbook/actor/search/fetch runtimes are available; done when `implementation-evidence.md` records native case and general claim PASS/FAIL/NOT_RUN from durable completion/trace/evidence refs, with no fixture substitution, parent-authored output or rerun solely to chase a branch.
- [ ] 4.4 @impl SNC-007: Classify the optional delegated native-to-curl witness only from its bundle-root trace check; done when pass yields claim PASS, failure yields FAIL, absence yields honest claim-level NOT_RUN without relabeling case/general outcome, and Claude/Codex/page-semantic residuals remain explicit.
- [ ] 4.5 @impl DEW-009, SNC-007, RWP-015: Run focused work-unit/direct-output/role/phase regression and `npm test`; done when relevant suites pass and every unrelated pre-existing failure has an exact focused reproduction recorded outside these five claims.

## 5. Release, Bug Status, Governance And Closure

- [ ] 5.1 @impl DEW-009, SNC-007, RWP-015: Add proposal-declared `v0.40` to root `CHANGELOG.md`; done when one or two concise bullets describe actor contract delivery/shared fetch guidance without file lists, requirement IDs, universal first-pass or external-access claims.
- [ ] 5.2 @impl DEW-009, SNC-007, RWP-015: Synchronize `DPT_FRAMEWORK/RUN.md` banner/current-release to `v0.40`; done when it matches CHANGELOG and does not claim BUG-097 or unobserved runtime behavior fixed.
- [ ] 5.3 @impl DEW-009, SNC-007, RWP-015: Complete `implementation-evidence.md` and audit non-goals/control surfaces; done when every claim maps to its native PASS/FAIL/NOT_RUN authority, added/removed surfaces are explicit, and final diff has no dependency, Python, persisted schema/state/role marker, second validator/submit path, actor selector, fetch controller/retry tree, user co-runner or fabricated evidence.
- [ ] 5.4 @impl DEW-009, SNC-007, RWP-015: Update BUG-098 and BUG-096 status only from native evidence; done when BUG-098 moves to fixed only if the general real-actor first-return claim passes, BUG-096 moves only if this delegated fallback claim passes in addition to archived HITL1 closure, otherwise each remains active with exact NOT_RUN/FAIL residual, and BUG-097 remains open.
- [ ] 5.5 @impl DEW-009, SNC-007, RWP-015: Run `node openspec/governance/check-verification-routing.mjs --change deliver-work-unit-role-contracts-to-actors --mode assets`; done when all selected assets exist at canonical paths and actor/runtime/external-call/verdict boundaries remain declared.
- [ ] 5.6 @impl DEW-009, SNC-007, RWP-015: Run `node openspec/governance/check-project-reqs.mjs`; done with 0 duplicate, 0 orphan, 0 unregistered and 0 reusedRetired violations while reusing the existing requirement IDs.
- [ ] 5.7 @impl DEW-009, SNC-007, RWP-015: Run `node openspec/governance/check-project-specs.mjs`; done with 0 deltaHeaderInMain, 0 missingPurpose, 0 missingRequirements and 0 missingReqHeader violations.
- [ ] 5.8 @impl DEW-009, SNC-007, RWP-015: Run `openspec validate deliver-work-unit-role-contracts-to-actors --strict` and `git diff --check`; done when the complete Change validates, v0.40 projections agree, archive wording does not overclaim NOT_RUN branches, and final review confirms one contract owner plus one existing repair/submit path.
