# Capability Catalog

This catalog is a navigation projection for Coding Agents. It answers which
existing behavior contract is worth inspecting before a change is proposed;
the linked `spec.md` remains the authoritative behavior contract. Read the
catalog to form a candidate set, inspect the cited main specs, then record the
reuse or new-capability decision in the change proposal.

> 本 catalog 列出的 capability 行即当前 **accepted** capabilities；历史或废弃 capability 不在此列出——历史 change 记录在 `openspec/changes/archive/`，废弃 requirement ID 保留在 `openspec/governance/req-registry.yaml` 的 `[DEPRECATED]` 组。Catalog 只用于导航，main spec 仍是 behavior authority。

## Agent

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| agent/agent-command-surface | Agent-facing command responsibility, legal repair, HITL direction, and Engine authority boundaries. | agent commands, permission, HITL, repair | Entry documentation and command responsibility; not runtime-state authority. | capability:agent/agent-context-routing; capability:governance/guidance-constitution | Agent chooses and executes legal commands; Markdown explains the handoff. | Engine retains deterministic command and state authority. |
| agent/agent-context-routing | Root context routes Agents to the Charter, relevant authority surfaces, and on-demand ADRs. | context routing, Charter, terminology, ADR | Repository instruction discovery; not a behavior or runtime source of record. | capability:agent/agent-command-surface; capability:governance/guidance-constitution | Agent reads the required context; Markdown provides the route. | No deterministic owner; Node does not interpret context semantics. |
| agent/agent-output-declaration | Declared work-unit output, cache-trail, and submitted-ledger contract. | output declaration, ledger, cache trails, submit | Agent-declared artifacts become trusted only through submitted work-unit validation. | capability:agent/work-unit-submission; capability:bundle/file-observability | Agent declares output roles and sources; Markdown carries task guidance. | Engine validates declarations and consumes the submitted ledger. |
| agent/agent-testing | Agent-assisted command-experiment playbooks and evidence expectations. | agent testing, command experiments, playbooks, trace | Experiment guidance and coverage; not a conventional test-runner contract. | capability:workflow/playbook-runner; capability:verification/verification-routing | Agent runs and evaluates playbooks; Markdown is the runner-facing procedure. | Engine supplies real trace and deterministic verdict inputs. |
| agent/agentic-queue | Structured queue state, task-card production, claim, completion, and refill behavior. | queue, task cards, claim, completion, refill | Queue runtime semantics; task cards are projections rather than state authority. | capability:agent/delegated-work-units; capability:engine/gate-state-machine | Agent reads task cards and acts on feedback; Markdown projects queue work. | Engine owns queue state, receipts, and transitions. |
| agent/cmd-subagent-environment | Project command playbook for configuring a real sub-agent environment. | subagent environment, command playbook, setup | Host setup guidance; distinct from the production work-unit runtime contract. | capability:agent/subagent-dispatch; capability:agent/subagent-directory-contract | Agent follows setup guidance; Markdown presents the playbook. | Engine does not own host-environment semantics. |
| agent/delegated-work-units | Production delegated-work assignment surface: work-unit identity, envelope binding, claim profile, task projection, and actor policy. | work units, delegation, identity, envelope, claim profile, actor policy, briefing | Assignment & briefing surface; submit/validation moves to work-unit-submission, preflight to work-unit-preflight, correction to work-unit-correction. | capability:agent/subagent-dispatch; capability:agent/work-unit-submission; capability:agent/work-unit-preflight; capability:agent/work-unit-correction | Agent performs bounded delegated work and submits results; Markdown exposes the envelope. | Engine allocates identity, binds claims, and records receipts. |
| agent/hitl-ux | HITL conversational alignment, review, Chinese-first prompts, and semantic-progress guidance. | HITL, user alignment, review, prompts | Human interaction guidance; not a lifecycle-state machine or command authority. | capability:agent/agent-command-surface; capability:workflow/silent-wave-execution | Agent conducts the conversation; Markdown carries prompts and context. | Engine does not judge human semantic decisions. |
| agent/local-deepseek-claude-launcher | Repo-owned local launcher and supervised Claude-compatible invocation contract. | launcher, provider config, supervisor, autorun | Host launcher configuration; separate from experiment runtime and provider credentials. | capability:verification/experiment-agent-autorun; capability:agent/agent-command-surface | Agent follows documented launch responsibilities; Markdown documents invocation. | Node validates configuration and manages the direct child lifecycle. |
| agent/queue-input-validation | Queue input, bundle identity, topic-registry, and suspicious-argument validation. | queue validation, topic registry, bundle identity, CLI safety | Admission validation around queue operations; not semantic topic assessment. | capability:agent/agentic-queue; capability:research/canonical-topic-state | Agent repairs actionable validation feedback; Markdown explains legal inputs. | Engine validates arguments, registry bindings, and projections. |
| agent/subagent-directory-contract | Work-unit directory as the production delegated runtime envelope and containment boundary. | work-unit directory, containment, delegated artifacts | Filesystem envelope for delegated work; distinct from orchestration and provenance policy. | capability:agent/delegated-work-units; capability:workflow/workflow-directory-contract | Agent uses the assigned envelope; Markdown describes the directory contract. | Engine enforces work-unit path and containment rules. |
| agent/subagent-dispatch | Work-unit claim, dispatchable prompt, beacon metadata, and nonce-bound dispatch. | subagent dispatch, claim, beacon, nonce | Dispatch boundary between queue demand and submit; not generic agent scheduling. | capability:agent/delegated-work-units; capability:agent/subagent-node-contract | Agent reads the dispatched brief and returns through the stated path. | Engine creates claims, beacons, and nonce-bound trace links. |
| agent/subagent-node-contract | Node-level sub-agent handoff, task/result, and execution-surface contract. | subagent node, handoff, task, result | Agent-facing node closure; separate from directory mechanics and queue state. | capability:agent/subagent-dispatch; capability:workflow/workflow-node-contract | Agent reads and fulfills node instructions; Markdown is the handoff surface. | Engine validates bounded deterministic node inputs and outputs. |
| agent/subagent-runtime-logging | Runtime logging contract for sub-agent execution and correlation. | subagent logging, runtime logs, correlation | Observability of delegated activity; not work completion authority. | capability:engine/logger; capability:agent/delegated-work-units | Agent emits required contextual logs; Markdown exposes logging expectations. | Engine writes and validates deterministic logging surfaces. |
| agent/work-unit-correction | Correction of failed or drifted delegated work: fail-closed terminalization, audited late-submit recovery, supersession lineage, transaction journal recovery. | correction, terminalization, late-submit, supersession, transaction recovery | Correction surface; submit/validation moves to work-unit-submission, preflight to work-unit-preflight. | capability:agent/delegated-work-units; capability:agent/work-unit-submission | Agent repairs or terminalizes through audited correction paths; Markdown exposes correction guidance. | Engine audits supersession lineage and settles transaction journals. |
| agent/work-unit-preflight | Side-effect-free prediction before delegated work commits: dry-submit mirroring submit semantics, strict provenance projection, timeout preflight recommendation. | preflight, dry-submit, provenance projection, timeout preflight | Prediction surface; never records completion or mutates ledgers. | capability:agent/delegated-work-units; capability:agent/work-unit-submission | Agent runs preflight before committing; Markdown exposes predicted outcome. | Engine renders read-only preflight verdicts and candidate projections. |
| agent/work-unit-provenance-gate | Provenance gate for submitted work-unit contributions and downstream admission. | provenance gate, submitted work, admission, evidence | Gate admission for delegated contributions; does not assess research quality. | capability:agent/work-unit-submission; capability:engine/gate-state-machine | Agent repairs missing provenance evidence; Markdown returns gate feedback. | Engine evaluates submitted provenance facts and gate verdicts. |
| agent/work-unit-submission | Authoritative recording of delegated-work completion: submit transaction, canonicalization, submitted-ledger coverage, durable postconditions, drift detection. | submission, submit authority, canonicalization, ledger, hashes | Completion-authority surface; preflight prediction moves to work-unit-preflight, correction to work-unit-correction. | capability:agent/delegated-work-units; capability:agent/work-unit-preflight; capability:agent/work-unit-correction | Agent submits accepted results; Markdown exposes completion contracts. | Engine validates submits, canonicalizes, and writes ledger receipts. |

## Engine

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| engine/check-inspect-feedback | Check, inspect, and advice feedback vocabulary and deterministic feedback boundaries. | check, inspect, advice, feedback | Returns actionable facts to an Agent; does not select or perform semantic repairs. | capability:agent/agent-command-surface; capability:engine/framework-engine | Agent interprets feedback and chooses legal follow-up; Markdown carries the response. | Engine emits deterministic verdicts and diagnostics. |
| engine/cli-exit-code-conventions | CLI exit-code conventions for success, check failure, and operational error. | CLI, exit codes, failure, conventions | Process-result contract shared by command surfaces; not human-facing semantic guidance. | capability:engine/cli-inspect-output-conventions; capability:engine/framework-engine | Agent reads exit outcomes and repairs valid failures; Markdown documents commands. | Engine returns consistent process exit statuses. |
| engine/cli-inspect-output-conventions | Structured inspect output and bounded diagnostic conventions for CLIs. | inspect output, diagnostics, CLI, JSON | Diagnostic presentation complements exit status; it is not a second state authority. | capability:engine/check-inspect-feedback; capability:engine/cli-exit-code-conventions | Agent uses diagnostics to reason about repair; Markdown presents the next step. | Engine formats reliable inspect diagnostics. |
| engine/cli-phase-transition | Deterministic phase-transition lookup and phase-specific loading. | phase transition, transition table, loader | Exact phase dispatch; does not orchestrate the Agent Flow. | capability:engine/transition-table; capability:workflow/workflow-node-contract | Agent follows the selected phase guidance; Markdown owns phase instructions. | Engine resolves legal deterministic transition targets. |
| engine/framework-engine | Framework Engine boundary for schemas, checks, receipts, state transitions, and trace. | framework engine, schemas, checkpoints, receipts | Deterministic trust root; excludes search, writing, and multi-stage Agent orchestration. | capability:engine/schema-core; capability:engine/trace-writer | Agent supplies judgment and reads feedback; Markdown controls multi-stage flow. | Engine owns deterministic contracts and checkpoint execution. |
| engine/gate-fork-router | Deterministic router from gate results to explicit fork branches. | gate router, branches, fork, repair | Branch classification only; Agent Flow remains in Markdown and Agent action. | capability:engine/gate-state-machine | Agent follows the returned branch guidance; Markdown describes branch work. | Engine maps gate facts to declared branches. |
| engine/gate-skeleton | Shared gate skeleton for rule execution, results, and bounded diagnostics. | gate skeleton, rules, diagnostics, verdict | Generic deterministic gate foundation; no research-quality judgment. | capability:engine/gate-state-machine; capability:engine/check-inspect-feedback | Agent consumes failure diagnostics; Markdown supplies repair context. | Engine evaluates skeleton rules and emits verdict structure. |
| engine/gate-state-machine | Gate-state transition semantics and legal terminal or repair outcomes. | gate state machine, transitions, repair, terminal | Deterministic gate lifecycle; not the overall Agent workflow controller. | capability:engine/gate-fork-router; capability:workflow/repair-loop | Agent performs the required repair action; Markdown presents the loop. | Engine owns gate states and legal deterministic transitions. |
| engine/logger | Structured logging and trace-adjacent logger behavior. | logger, structured logs, diagnostics | Logging implementation; separate from trace authority and semantic conclusions. | capability:engine/trace-writer; capability:agent/subagent-runtime-logging | Agent supplies contextual logging inputs; Markdown documents observability work. | Engine writes structured log records. |
| engine/logging-conventions | Repository logging conventions for diagnostic clarity and safe output. | logging conventions, diagnostics, output | Conventions guide log consumers; they do not replace trace contracts. | capability:engine/logger; capability:engine/cli-inspect-output-conventions | Agent follows readable diagnostic conventions; Markdown documents them. | Engine preserves established logging format and boundaries. |
| engine/runtime-reentry-debuggability | Runtime reentry and debug inspection contract for recoverable operations. | reentry, debugging, recovery, inspect | Diagnostics for repeated invocation; does not create new lifecycle authority. | capability:bundle/artifact-persistence-recovery; capability:engine/check-inspect-feedback | Agent uses inspection results for repair; Markdown carries recovery guidance. | Engine exposes deterministic reentry diagnostics. |
| engine/schema-core | Core runtime schemas, parsing, and validation primitives. | schemas, validation, parsing, runtime state | Structural contract layer; no semantic research or workflow judgment. | capability:engine/framework-engine; capability:agent/agentic-queue | Agent supplies valid shaped content and repairs feedback; Markdown names required fields. | Engine parses and validates structured state. |
| engine/trace-writer | Canonical trace writing and durable trace-event recording. | trace, events, provenance, durable records | Trace truth for deterministic execution; distinct from human progress summaries. | capability:engine/framework-engine; capability:agent/delegated-work-units | Agent reads trace feedback and performs factual work; Markdown links trace context. | Engine writes canonical trace events. |
| engine/transition-table | Explicit phase transition table and legal next-phase mapping. | transition table, phases, state machine | Transition declaration consumed by phase dispatch; not Markdown workflow content. | capability:engine/cli-phase-transition; capability:engine/gate-state-machine | Agent follows the legal next phase; Markdown contains the phase controller. | Engine resolves declared transition coordinates. |

## Bundle

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| bundle/artifact-persistence-recovery | Crash-safe persistence, atomic publication, sweep, and recovery for content-bearing bundle artifacts. | artifact persistence, recovery, atomic publish, sweep | Durable artifact mechanics; submitted-backing admission stays a separate evidence boundary. | capability:research/final-delivery-backing; capability:engine/runtime-reentry-debuggability | Agent follows recovery diagnostics and writes content; Markdown explains recovery action. | Engine stages, publishes, sweeps, and validates durable artifacts. |
| bundle/bundle-data-isolation | Isolation contract for concurrent runtime bundles and their control, evidence, artifacts, and trace. | bundle isolation, runtime state, concurrent runs | Per-run containment; not a generic repository workspace convention. | capability:workflow/workflow-directory-contract; capability:bundle/run-entry | Agent selects the active bundle and observes isolation rules; Markdown names the root. | Engine validates bundle-local data boundaries. |
| bundle/bundle-map | Passive `BUNDLE_MAP.md` navigation contract for the contents of a runtime bundle. | bundle map, navigation, runtime bundle, BUNDLE_MAP | Human and Agent map only; not a lifecycle node, playbook, or authority. | capability:bundle/bundle-data-isolation; capability:bundle/run-entry | Agent reads the map to orient work; Markdown is the map surface. | No deterministic owner; Node does not infer lifecycle from the map. |
| bundle/cache-raw-web-content | Standardized raw web-content cache hierarchy and source-traceability layout. | raw cache, web content, source metadata, provenance | Raw retrieval cache; distinct from curated evidence and final artifacts. | capability:research/research-access-adapter; capability:research/evidence-extraction | Agent reads cached material and records source context; Markdown guides retrieval work. | Engine enforces deterministic cache-layout and metadata conventions. |
| bundle/cmd-bundle-instantiation | Bundle-instantiation command playbook, templates, and validation/inspection contract. | bundle instantiation, templates, validate bundle, inspect bundle | Initial bundle setup command surface; not later lifecycle orchestration. | capability:bundle/run-entry; capability:verification/integration-tests | Agent runs the documented instantiation workflow; Markdown is the command playbook. | Engine creates and validates deterministic bundle structure. |
| bundle/file-observability | Work-unit-aware inspection of planned, unplanned, submitted, and cache-trail files. | file observability, work units, inspect, cache trails | Diagnostic observation only; submitted ledgers remain artifact authority. | capability:agent/agent-output-declaration; capability:agent/work-unit-submission | Agent reads inspect feedback and decides repair; Markdown presents diagnostics. | Engine inventories files and produces deterministic diagnostics. |
| bundle/reference-flat-format | Flat `reference/` naming, rich-source files, index, and human-navigation convention. | reference format, evidence files, index, source naming | Reference organization; separate from raw cache and evidence-quality assessment. | capability:bundle/cache-raw-web-content; capability:research/evidence-extraction | Agent creates and reads navigable reference material; Markdown explains conventions. | Engine checks deterministic reference structure where specified. |
| bundle/run-entry | `DEEP_RESEARCH_HARNESS/RUN.md` current-entry contract, shortcut boundary, and aligned Agent files. | RUN.md, harness entry, shortcuts, agent route | Reusable Harness entry; current route is separate from optional human history, mutable runtime state, and a per-run playbook. | capability:agent/agent-context-routing; capability:workflow/workflow-directory-contract | Agent follows the current entry route; Markdown contains Harness instructions. | Engine validates deterministic entry consistency where specified. |
| bundle/run-scoped-tmp-artifacts | Run-scoped script intermediate artifacts live in bundle `_tmp/`, never the system temp directory; staging helper and hardcoded `/tmp/` write scan. | tmp artifacts, staging, _tmp, intermediate products | Bundle-scoped staging convention; distinct from raw web cache (`_cache/`) and durable artifacts. | capability:bundle/cmd-bundle-instantiation; capability:bundle/bundle-data-isolation | Agent writes script intermediates to bundle `_tmp/`; Markdown documents the location contract. | Engine scaffolds `_tmp/`, resolves staging paths, and scans for hardcoded system-tmp writes. |

## Research

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| research/canonical-topic-state | Canonical topic identity, progress projection, and atomic topic-state mutation workspace. | topic registry, progress, inspect, apply, recover | Topic identity source of record; progress is projected rather than persisted as authority. | capability:agent/queue-input-validation; capability:agent/agentic-queue | Agent supplies legal topic intent and reads projections; Markdown carries topic workflow context. | Engine owns atomic state mutation and deterministic progress projection. |
| research/content-delivery-experiments | Playbooks for HITL2, readiness, and final content-delivery lifecycle experiments. | delivery experiments, HITL2, readiness, final | Experiment acceptance surface for delivery phases; verdict comes from real trace, not playbook prose. | capability:research/content-delivery-gate-implementation; capability:agent/agent-testing | Agent runs and interprets experiments; Markdown contains playbooks. | Engine provides actual trace and deterministic gate evidence. |
| research/content-delivery-gate-implementation | Real deterministic gate definitions and CLI behavior for HITL2, readiness, and final delivery phases. | delivery gates, HITL2, readiness, final | Deterministic gate implementation; does not judge research quality or final prose quality. | capability:research/content-delivery-phase-content; capability:engine/gate-state-machine | Agent repairs declared gate failures; Markdown presents gate feedback. | Engine evaluates delivery gate rules and state facts. |
| research/content-delivery-phase-content | Complete Agent-readable bodies for HITL2, readiness, and final phase nodes. | phase content, HITL2, readiness, final, Markdown | Markdown phase controller; authoritative runtime state remains outside phase prose. | capability:research/content-delivery-gate-implementation; capability:workflow/workflow-node-contract | Agent follows and updates phase work; Markdown owns the phase instructions. | Engine validates declared phase and gate contracts. |
| research/evidence-extraction | Engine-side evidence extraction, reference counting, quality signals, and ledger cache-trail validation. | evidence extraction, references, cache trails, ledger | Deterministic evidence metadata checks; not final research synthesis or source judgment. | capability:bundle/cache-raw-web-content; capability:agent/agent-output-declaration | Agent selects evidence and interprets results; Markdown guides extraction work. | Engine extracts structural evidence facts and validates cache trails. |
| research/final-delivery-backing | Reader-visible Final Markdown declaration connecting selected findings to authenticated submitted backing. | final backing, findings, evidence, submitted artifacts | Delivery backing link; does not make prose quality an Engine verdict. | capability:bundle/artifact-persistence-recovery; capability:agent/work-unit-provenance-gate | Agent writes findings and chooses supported claims; Markdown presents the declaration. | Engine authenticates submitted backing and durable artifact facts. |
| research/plan-hostfile-sections | `rb_plan.md` host-file template, readable projections, and setup-ready body checks. | rb_plan, host file, setup, projections | Human-readable planning surface; structured runtime state remains authoritative. | capability:research/canonical-topic-state; capability:research/user-research-controls | Agent maintains plan guidance and user inputs; Markdown is the host file. | Engine checks required structure and generates progress presentation. |
| research/post-final-recovery | Narrow post-Final recovery that records a legal HITL2 rerun handoff with lineage. | post-final recovery, rerun, lineage, HITL2 | Exact recovery after legal Final delivery; does not widen lifecycle authority. | capability:workflow/rerun-incremental-node; capability:engine/runtime-reentry-debuggability | Agent supplies rerun rationale and follows the recovery handoff; Markdown presents it. | Engine records lineage-bound recovery events and validates conditions. |
| research/pre-research-experiments | Runner-facing playbooks for pre-research foundation phases, repair, and review. | pre-research experiments, HITL1, repair loop, trace | Experiment acceptance surface; deterministic verdicts use real trace JSONL. | capability:research/pre-research-gate-implementation; capability:workflow/playbook-runner | Agent runs the playbooks and reviews outcomes; Markdown is the experiment surface. | Engine supplies gate execution and trace facts. |
| research/pre-research-gate-implementation | Deterministic gate rules for instantiation, HITL1, and setup readiness. | pre-research gates, instantiation, HITL1, setup | Gate checks existing files and schemas; does not evaluate research quality. | capability:research/pre-research-phase-content; capability:engine/gate-state-machine | Agent repairs explicit gate failures; Markdown provides the operational phase context. | Engine evaluates deterministic pre-research rules. |
| research/pre-research-phase-content | Complete Agent-readable bodies for instantiation, HITL1, and setup phase nodes. | phase content, instantiation, HITL1, setup | Markdown workflow content; it does not replace runtime schema or gate authority. | capability:research/pre-research-gate-implementation; capability:workflow/workflow-node-contract | Agent performs phase work and user alignment; Markdown controls the phase flow. | Engine validates structured facts and gate transitions. |
| research/research-access-adapter | Single explicit research-access adapter for truthful search and same-URL page-fetch capability. | research access, search, web fetch, host permissions | Access and host-capability boundary; does not fabricate retrieval success or evidence quality. | capability:bundle/cache-raw-web-content; capability:research/evidence-extraction | Agent chooses and truthfully uses available research access; Markdown describes the boundary. | Engine records deterministic adapter configuration and diagnostics where defined. |
| research/research-return-map | Agent-readable navigation from evidence to claims, affected questions, status, and next hops. | return map, evidence, claims, navigation, next hop | Interpretation map over existing authority; it does not mutate status or establish evidence truth. | capability:research/evidence-extraction; capability:research/final-delivery-backing | Agent interprets evidence and updates narrative navigation; Markdown is the return map. | Deterministic inspection verifies projection readiness (RRM-006/007); Node does not judge claim interpretation. |
| research/research-styles | Research style profiles controlling depth, breadth, resolved parameters, and numeric floors. | research styles, depth, breadth, profiles, thresholds | Parameter profile and gates; qualitative research quality stays with the Agent. | capability:research/canonical-topic-state; capability:research/research-wave-gate-implementation | Agent chooses strategy and qualitative tradeoffs; Markdown communicates style intent. | Engine resolves profiles and enforces declared numeric floors. |
| research/research-wave-experiments | Runner-facing experiments for the wave0, wave1, and wave2 research lifecycle. | research waves, experiments, wave0, wave1, wave2 | Experiment acceptance surface; real trace is the deterministic proof source. | capability:research/research-wave-gate-implementation; capability:workflow/playbook-runner | Agent runs, observes, and interprets wave experiments; Markdown contains playbooks. | Engine supplies real trace and gate outcomes. |
| research/research-wave-gate-implementation | Deterministic gate rules for wave0, wave1, and wave2 completion. | wave gates, wave0, wave1, wave2, deterministic checks | Structural and provenance gates only; no semantic judgment of research quality. | capability:research/research-wave-phase-content; capability:engine/gate-state-machine | Agent repairs stated structural deficiencies; Markdown returns gate feedback. | Engine evaluates wave-completion rules and receipts. |
| research/research-wave-phase-content | Complete Agent-readable bodies for the wave0, wave1, and wave2 phase nodes. | wave phases, phase content, wave0, wave1, wave2 | Markdown agent-flow controller; structured state and gates retain authority. | capability:research/research-wave-gate-implementation; capability:workflow/workflow-node-contract | Agent executes research and synthesis tasks; Markdown controls phase guidance. | Engine checks declared schemas, transitions, and gates. |
| research/seed-topic-materialization | Seed-topic materialization phase and deterministic readiness gate before wave0. | seed topics, materialization, setup, readiness gate | Structural pre-wave boundary; semantic topic quality remains a HITL1 and Agent concern. | capability:research/canonical-topic-state; capability:research/pre-research-gate-implementation | Agent materializes and reviews topic content; Markdown provides phase instructions. | Engine checks materialization structure, consistency, and count floors. |
| research/user-research-controls | Optional user research controls captured once in `rb_plan.md` to guide Agent judgment. | user controls, rb_plan, research preferences, guidance | Agent guidance only; it preserves Engine, Gate, and runtime-authority boundaries. | capability:research/plan-hostfile-sections; capability:agent/hitl-ux | Agent interprets user preferences; Markdown captures them in the plan. | No deterministic owner; Node does not turn preferences into authority. |
| research/wave0-artifacts-directory | `artifacts/wave0/` layout for thin wave0 source-list artifacts. | wave0 artifacts, source list, directory layout | Wave-organized artifact location; distinct from references and later synthesis. | capability:research/research-wave-phase-content; capability:workflow/workflow-directory-contract | Agent writes and reads wave0 source lists; Markdown explains artifact intent. | Engine validates deterministic artifact coordinates where specified. |
| research/wave1-intake | Queue-driven topic deepening, evidence-summary production, and seed-topic backfill. | wave1, deepening, queue, evidence summary, backfill | Topic-specific research flow; raw search noise stays outside Phase Agent context. | capability:agent/agentic-queue; capability:research/research-wave-phase-content | Agent performs deepening and backfill reasoning; Markdown carries task-card context. | Engine manages queue, artifacts, and declared structural feedback. |
| research/wave2-synthesis | Queue-driven cross-topic finding triage, targeted search, and synthesis artifacts. | wave2, synthesis, cross-topic, findings, ledger | Cross-topic synthesis flow; Engine checks boundaries but does not author conclusions. | capability:agent/agentic-queue; capability:research/final-delivery-backing | Agent classifies findings and writes synthesis; Markdown controls the iterative process. | Engine validates defined artifact, trace, and gate facts. |

## Verification

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| verification/experiment-agent-autorun | Deterministic Autorun Supervisor and real Playbook Agent execution for bounded command experiments. | autorun, supervisor, command experiments, lifecycle | Experiment execution contract; not a general production-agent runtime or approval bypass. | capability:agent/local-deepseek-claude-launcher; capability:workflow/playbook-runner | Agent executes bounded playbook work; Markdown defines the experiment procedure. | Engine supervises selection, lifecycle, health, audit, and cleanup facts. |
| verification/experiment-observability | Post-run health reports, gate diagnostics, trace inspection, and runner-report protocol. | experiment observability, health, diagnostics, trace | Post-run observation only; does not rerun gates or redefine verdict semantics. | capability:engine/logger; capability:engine/trace-writer | Agent reads health output and reports observations; Markdown carries the runner report. | Engine captures diagnostics and reads existing runtime facts. |
| verification/experiment-ref-integrity | Experiment suite guarding Agent/Engine provenance integrity and real delegated completion paths. | reference integrity, provenance, delegation, trace | Integrity coverage for existing production boundaries; it is not a second runtime implementation. | capability:agent/work-unit-submission; capability:agent/agent-output-declaration | Agent runs evidence-bearing experiment cases; Markdown defines experiment scenarios. | Engine verifies provenance, ledger, trace, and completion facts. |
| verification/experiment-run-strategy | Recomputable, explainable selection projection for registered Agent Experiment cases. | experiment strategy, selection, budget, cases | Planning projection for experiment batches; preserves case identity and unknown facts. | capability:agent/agent-testing; capability:verification/verification-routing | Agent uses the projection to choose legal experiment work; Markdown presents options. | Engine computes reproducible selection facts and constraints. |
| verification/experiment-shared-infra | Shared `experiments_env/shared/` utilities for experiment setup and execution. | experiment infrastructure, shared utilities, experiments_env | Experiment-only utility location; never a production run-bundle dependency. | capability:agent/agent-testing; capability:workflow/playbook-runner | Agent uses documented shared utilities during experiments; Markdown scopes their use. | Engine may run deterministic experiment utilities, not production runtime state. |
| verification/integration-tests | Integration-test contract for bundle validation and inspection using real file I/O. | integration tests, validate bundle, inspect bundle, fixtures | Repository test behavior; separate from Agent-run experiment playbooks. | capability:bundle/cmd-bundle-instantiation; capability:verification/test-fixtures | Agent chooses relevant test execution and interprets failures; Markdown documents intent. | Node executes deterministic integration assertions. |
| verification/test-fixtures | Minimal Harness test fixtures isolated from the real framework directory. | test fixtures, isolation, schema, templates | Test-only inputs; never mutable production or run-bundle truth. | capability:verification/integration-tests; capability:workflow/workflow-directory-contract | Agent selects fixtures for tests; Markdown describes fixture scope. | Node creates and validates deterministic fixture inputs. |
| verification/verification-routing | Canonical verification taxonomy, claim routing, execution profiles, and verdict boundaries. | verification routing, claims, tests, execution profiles | Chooses appropriate proof surface; does not manufacture runtime or Agent evidence. | capability:agent/agent-testing; capability:governance/change-feedback-loop | Agent selects and runs the applicable verification route; Markdown records the plan. | Engine validates routable deterministic claims and asset structure. |

## Governance

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| governance/change-feedback-loop | Repository-level OpenSpec feedback lifecycle and deterministic archive prerequisites. | feedback lifecycle, review, finalizer, archive | Change review and closeout loop; not a replacement for main-spec behavior authority. | capability:governance/requirement-traceability; capability:verification/verification-routing | Agent performs review and records actionable findings; Markdown carries lifecycle tasks. | Node finalizer checks mechanical prerequisites before archive. |
| governance/guidance-constitution | Implementation-neutral constitutional guidance and durable design-review hierarchy. | guidance, constitution, Charter, design review | Guiding navigation layer; cannot override accepted behavior or runtime truth. | capability:agent/agent-context-routing; capability:governance/change-feedback-loop | Agent applies guidance to design judgment; Markdown publishes the navigation hierarchy. | No deterministic owner; Node does not treat guidance as behavior authority. |
| governance/requirement-traceability | Self-documenting, verifiable requirement registry, capability boundaries, and discovery discipline. | requirements, registry, capability discovery, catalog | Governance traceability; catalog remains navigation and main specs retain behavior authority. | capability:governance/change-feedback-loop; capability:verification/verification-routing | Agent discovers candidates and records rationale; Markdown carries registry and proposal protocol. | Node validates IDs, paths, catalog structure, and discovery-record shape. |
| governance/semantic-fact-closure | Bounded semantic-fact vocabulary, per-change closure records, and structural lifecycle checks. | semantic facts, closure, resolver, consumers, catalog | Change-governance declaration and structural route; not a runtime schema, Gate, or semantic-completeness verdict. | capability:governance/change-feedback-loop; capability:verification/verification-routing; execution-surface:openspec/governance/check-semantic-closure.mjs | Agent determines applicability and reviews resolver/consumer completeness; Markdown carries the record and review prompts. | Node validates strict record, catalog, route, and asset facts without inferring runtime semantics. |
| governance/version-management | Optional non-authoritative root changelog history and the absence of internal release choreography. | changelog, history, non-authoritative, entry boundary | Human historical navigation; not a runtime lifecycle, package/Git release, compatibility selector, or deployment controller. | capability:bundle/run-entry; capability:governance/guidance-constitution | Agent may add concise history; Markdown keeps current entry routing independent of it. | No Engine/Node version, release, compatibility, or runtime authority is introduced. |

## Workflow

| Capability path | Purpose | Keywords | Boundaries / neighbors | Related entries | Agent/Markdown owns | Engine/Node owns |
| --- | --- | --- | --- | --- | --- | --- |
| workflow/conditional-nodes | Deterministic branch classification, state transform, and repair checkpoint expression. | conditional nodes, branches, state transform, repair | Deterministic branch model; it is not Agent-facing workflow-node execution. | capability:engine/gate-fork-router | Agent follows the selected branch; Markdown carries branch-specific work. | Engine classifies declared conditions and transforms state. |
| workflow/dynamic-node-loading | Explicit `fileRef` loading of Agent-readable Markdown node closure. | dynamic nodes, fileRef, Markdown loading, node closure | Loads one declared node; does not precompile a DAG or own Agent Flow. | capability:workflow/workflow-node-contract; capability:engine/cli-phase-transition | Agent reads the loaded node and performs its work; Markdown is the node content. | Engine resolves explicit deterministic file references. |
| workflow/playbook-runner | Agent-facing `experiments_playbook/RUN.md` runner and report protocol for playbooks. | playbook runner, experiments, RUN.md, reports | Agent-run experiment workflow; explicitly not a traditional test runner. | capability:agent/agent-testing; capability:verification/experiment-agent-autorun | Agent reads instructions, runs commands, evaluates trace, and reports; Markdown is the runner. | Engine supplies command outputs and trace facts, not orchestration judgment. |
| workflow/repair-loop | Deterministic gate-failure repair loopback, re-evaluation, and terminal outcomes. | repair loop, gate failure, re-evaluation, terminal | Deterministic checkpoint loop; it does not perform or judge the repair itself. | capability:engine/gate-state-machine | Agent decides and performs semantic repair; Markdown presents repair steps. | Engine re-evaluates gates and returns explicit outcomes. |
| workflow/rerun-incremental-node | `phase-rerun` node translating an HITL2 rerun decision into incremental rerun context. | rerun, incremental node, HITL2, profile | Rerun-preparation phase only; downstream research remains in existing wave capabilities. | capability:research/post-final-recovery; capability:workflow/workflow-node-contract | Agent explains and prepares rerun intent; Markdown controls the rerun phase. | Engine validates declared profile and transition facts. |
| workflow/rerun-topic-integration | Rerun topic integration, reference format, re-synthesis, traceability, and cache-trail contracts. | rerun topics, integration, re-synthesis, reference, cache trail | Topic-addition integration; separate from initial wave1 and wave2 execution. | capability:research/wave1-intake; capability:research/wave2-synthesis | Agent performs research and integration judgment; Markdown guides rerun work. | Engine checks declared reference, trace, and cache-trail facts. |
| workflow/shared-node-content | Complete Agent-readable content for shared workflow nodes and generated summaries. | shared nodes, Markdown content, generated summaries | Guidance and projections only; never deterministic rule authority. | capability:workflow/workflow-node-contract; capability:research/pre-research-phase-content | Agent uses shared guidance; Markdown owns the readable content. | Engine may generate summaries but retains state and rule authority elsewhere. |
| workflow/silent-wave-execution | Autonomous Agent behavior during silent wave execution, degradation, trace, and HITL2 surfacing. | silent waves, autonomy, degradation, escalation, HITL2 | Agent responsibility convention; it does not create lifecycle state or Engine control. | capability:agent/hitl-ux; capability:research/research-wave-phase-content | Agent executes autonomous research and records degradations; Markdown states the posture. | Engine records deterministic trace and gate facts only. |
| workflow/workflow-directory-contract | Runtime artifact directories, naming, bundle-root coordinate vocabulary, and containment rules. | workflow directories, bundle root, runtime paths, artifacts | Coordinate contract for mutable runtime truth; Harness assets remain reusable and read-only. | capability:bundle/bundle-data-isolation; capability:agent/subagent-directory-contract | Agent resolves bare runtime paths against the active bundle; Markdown names coordinates. | Engine validates path and containment contracts. |
| workflow/workflow-node-contract | Workflow-node metadata, phase manifest, and required node skeleton output contract. | workflow nodes, metadata, phase manifest, skeleton | Node structural contract; complete phase content stays in its domain capabilities. | capability:workflow/dynamic-node-loading; capability:workflow/shared-node-content | Agent reads node metadata and follows Markdown body guidance. | Engine validates node metadata, manifest, and required structure. |
