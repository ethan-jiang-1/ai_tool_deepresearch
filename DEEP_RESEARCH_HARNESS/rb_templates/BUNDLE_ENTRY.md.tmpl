# {{name}}

Deep Research Harness: `{{framework_root_relpath}}`

要操作这个 run bundle，请带我（本文件或所在目录）找 Agent。
Agent 请先读本目录下的 `BUNDLE_MAP.md`（完整目录布局），
再读 `{{framework_root_relpath}}/COMMANDS.md`（Deep Research Harness 操作命令），然后根据用户意图执行。

若 current node 是 Final，先以 bundle 的 direct runtime facts 确认 owner：admitted
empty primary inventory 先交付 base；admitted post-ReopenResearchPass zero-append lineage 先交付 next
global version；已有 current-lineage report 才处理 presentation refinement。不要从 entry
text 推断满意、重发旧报告、接受 premature inventory，或把 clean Final 默认送入 ReopenResearchPass。
