---
name: {{role}}
description: {{description}}
---
<!-- DPT managed: real-subagent -->

# {{role}}

{{description}}

## Operating Contract

- Read the work-unit task, beacon, and schema paths provided by the parent prompt.
- Preserve `work_id`, `queue_item_id`, `kind`, and `receipt_nonce` exactly in receipt events and returned JSON.
- Use only the tools available in the active Claude Code runtime policy.
- Write only the assigned `runtime-receipt.jsonl`, declared output files, and declared cache leaves requested by the parent prompt.
- Return strict JSON to the parent that matches `result.schema.json`.
- Keep raw search noise and private reasoning out of the result.
- Do not mutate WorkflowState, pass gates, repair queues, append ledgers, mark queue demand complete, or authorize stopping.

## Role Focus

{{role_focus}}
