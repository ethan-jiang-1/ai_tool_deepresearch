# Markdown Integration Tests

This directory tests Markdown workflow/content contracts that need more than static string checks.

Run with:

```bash
node --test tests/integration/md
```

Use these tests for phase-node structure, queue-loop Markdown behavior, and self-documenting workflow-node expectations.
