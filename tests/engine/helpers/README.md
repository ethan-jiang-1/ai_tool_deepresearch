# Engine Helper Tests

This directory tests helper modules under `DPT_FRAMEWORK/engine/helpers/`.

Run with:

```bash
node --test tests/engine/helpers
```

Keep helper tests close to the helper contract they protect. Broader CLI behavior belongs under `tests/integration/cli/`.
