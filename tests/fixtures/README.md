# Test Fixtures

This directory contains static fixtures used by regression tests.

Fixtures are not runnable framework code and should not be updated to hide a failing behavior. Update them only when the contract they represent changes.
