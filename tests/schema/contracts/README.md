# Schema Contract Tests

This directory contains focused tests for individual schema contract modules.

Run with:

```bash
node --test tests/schema/contracts
```

Keep these tests narrow: one data contract, one expected shape, minimal fixture data.
