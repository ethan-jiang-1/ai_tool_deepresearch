# Schema Tests

Schema tests protect data contracts used by the framework engine and CLIs.

Run with:

```bash
node --test tests/schema
```

Use `contracts/` for focused schema contract files and this directory for higher-level schema behavior.
