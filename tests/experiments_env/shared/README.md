# Shared Experiments Environment Tests

This directory tests shared experiment-environment utility modules.

Run with:

```bash
node --test tests/experiments_env/shared
```

Keep these tests utility-focused. Agent-driven playbook behavior belongs in `experiments_playbook/`.
